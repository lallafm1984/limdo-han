# LimDo 개발 지침

## 프로젝트 목적

LimDo는 다섯 살 아이를 위한 개인용 로컬 우선 Android 학습 앱이다. 장기 목표는 한글을 단계적으로 익히게 하는 것이며, 특히 손글씨, 획순, 획 방향, 아이 친화적 상호작용을 중요하게 다룬다.

`LOOP_GOAL.md`에서 명시적으로 요청하지 않은 전체 앱 기능을 한꺼번에 구현하지 않는다.

## 기록 언어 규칙

- 상태, 이력, 목표, QA, 자동화 계약, 작업자 지시문을 포함한 프로젝트 Markdown 문서는 한글로 작성한다.
- 파일명, 명령어, 코드 식별자, Android API명, 상태 토큰처럼 정확성이 필요한 기술 표기는 원문을 유지할 수 있다.
- `.loop/history.md`는 2026-08-24에 기존 영문 기록을 한글 연대기로 한 번 정리했다. 이후에는 한글로만 덧붙이고 기존 기록을 수정하거나 삭제하지 않는다.
- CLI 작업자의 최종 보고와 Codex 앱 주기 보고도 한글로 작성한다.

## 대상 아이 특성

- 대상 아이는 한국어 듣기는 가능하지만 아직 한글 UI 문장을 읽지 못한다.
- 지시, 방향, 결과, 초기화, 사용할 수 없는 동작을 글자만으로 알려주지 않는다.
- 보호자와 접근성 서비스를 위한 짧은 한글 문구는 유지하되, 아이의 이해 가능성은 시범, 모양, 위치, 아이콘, 움직임 또는 검증된 음성으로 입증한다.
- 음성은 재생과 실패 대체 동작을 구현하고 검증한 뒤에만 이해 근거로 인정한다.

## 기술 기준

- Android, Kotlin, Jetpack Compose
- 향후 쓰기 엔진은 Custom Canvas 사용
- 최소 SDK 26
- 기준 에뮬레이터 화면: 가로 2340 × 세로 1080
- 로컬 우선 구조
- 명시적 요청 없이는 서버, 로그인, 광고, 분석, 민감 권한을 추가하지 않는다.

## 필수 루프 절차

매 반복마다 다음 순서를 지킨다.

1. `LOOP_GOAL.md`를 읽는다.
2. `.loop/state.md`를 읽는다.
3. `.loop/history.md`의 최근 기록을 읽는다.
4. 현재 구현과 검증 근거를 확인한다.
5. 충족되지 않은 조건 중 우선순위가 가장 높은 하나를 고른다.
6. 검증 가능한 구체적 가설 하나를 세운다.
7. 합리적인 최소 변경 하나를 적용한다.
8. 새 검증을 실행한다.
9. 이전 반복과 결과를 비교한다.
10. 결과를 한글로 `.loop/history.md`에 덧붙인다.
11. `.loop/state.md`를 한글로 갱신한다.
12. 성공 또는 중지 조건까지 계속한다.

## Codex CLI 자동화

현재 프로젝트는 새 세션 CLI 단계다. 작업 시작 또는 재개 전에 `CLI_AUTOMATION.md`와 `.loop/queue.md`를 읽는다.

- `.loop/queue.md`의 활성 루프 하나만 작업한다.
- 중단된 루프는 `.loop/state.md`에서 재개하며 기억만으로 처음부터 다시 시작하지 않는다.
- `.loop/history.md`는 한글 덧붙이기 전용 기록으로 유지하고 새 검증을 모두 남긴다.
- `codex exec` 작업자는 정확히 한 번의 루프 반복만 수행하고 종료한다. 다음 새 세션은 `scripts/run-cli-loop.sh`만 시작한다.
- 작업자는 `codex exec resume`, `codex exec fork` 또는 다른 중첩 Codex 프로세스를 실행하지 않는다.
- Codex 앱에서 받은 최신 사용자 수정 지시는 `.loop/user-directives.md`에 한글로 기록하고 다음 새 세션이 다른 목표보다 먼저 읽는다.
- 루프 성공 조건을 모두 통과하면 정확한 2340 × 1080에서 새 `아이 대리 QA`를 수행하고, 글을 읽지 못하지만 한국어를 들을 수 있는 다섯 살 아이 관점의 불편을 `.loop/qa-findings.md`에 기록한다. 이를 실제 아이 관찰로 표현하지 않는다.
- 새 QA 근거에서 우선순위가 가장 높은 미해결 불편 하나만 다음 번호 루프로 만들고 `LOOP_GOAL.md`, 큐, 상태를 갱신한다. 같은 작업자는 다음 루프를 구현하지 않고 종료하며 감독자가 새 세션을 시작한다.
- 제품 불편을 새로 찾지 못하면 최신 사용자 지시와 장기 한글 학습 목표에서 아직 구현하지 않은 제품 작업 중 가장 우선순위가 높은 하나를 선택한다. 실제로 서로 다른 사용자 흐름이나 위험이 아니면 QA 탐색 루프를 새로 만들지 않는다.
- 루프 완료와 다음 루프 준비를 함께 로컬 체크포인트 커밋 하나로 만든 뒤 `git push origin HEAD`로 현재 브랜치를 일반 push한다.
- 사용자는 검증된 루프 완료 커밋의 위 일반 push를 승인했다. force push, tag, release, pull request, 배포, 외부 메시지와 그 밖의 원격 상태 변경은 별도 명시적 승인 없이는 하지 않는다.
- 사람 검토 관문은 사용하지 않는다. 자동 전환은 새 화면·동작 근거가 있는 `아이 대리 QA` 또는 구체적인 미검증 흐름에서만 허용한다.
- `docs/1차-목표-작업-기획서.md`는 1차 목표의 지속 계약이다. 현재 활성 루프가 끝난 뒤에는 문서의 미충족 항목 중 가장 앞선 제품 작업 또는 가장 높은 결함 하나를 다음 루프로 만들고, 세 메뉴·`가·나·다·라·마·바·사·아·자·차·카·타·파·하` 14글자 선택·디자인·연출·QA·아이 대리 시뮬레이션 관문을 모두 통과할 때까지 자동 전환을 계속한다.
- 문서에서 말하는 `디자인 팀`, `QA 팀`은 서로 다른 검증 책임을 맡는 자동 역할이다. 실제 사람이 참여하지 않았다면 사람 팀의 검토나 승인을 받았다고 표현하지 않는다.
- 1차 목표가 모두 통과하면 큐를 `없음`으로 바꾸고 `IDLE`로 종료한다. 사용자의 새 지시 없이 2차 목표를 시작하지 않는다.

## 2차 그래픽·시스템 지속 계약

사용자는 2차 목표의 그래픽·시스템 구현 시작을 승인했다. `docs/2차-목표-제품-기획서.md`와 `docs/그래픽-시스템-루프-계약.md`에서 아직 충족되지 않은 가장 앞선 제품 작업 또는 가장 높은 실제 화면 결함 하나를 다음 루프로 이어 간다.

- 모든 루프는 `.loop/state.md`에 `단계`, `시각 변경`, `자산 필요 판정`을 구체적 근거와 함께 기록한다.
- 시각 변경이면 자동 그래픽 디자인 역할이 현재 production 화면과 기존 자산을 먼저 검토한다. 실제 사람이 참여하지 않았다면 사람 그래픽 디자이너의 검토·승인으로 표현하지 않는다.
- 전용 bitmap이 필요하면 `imagegen` 스킬의 Codex 내장 이미지 생성기로 자체 생성하고 project production 리소스와 실제 소비 코드에 연결한다. 기존 자산이 충분하면 새로 만들지 않고 재사용 근거를 기록한다.
- 생성 파일만으로 완료하지 않는다. 자산 자동 검사, 최종 APK 포함, 동일 상태의 변경 전·후 2340 × 1080 PNG·hierarchy·focus와 자동 그래픽 디자인·자동 QA·아이 대리 QA를 모두 확인한다.
- 하나라도 실패하거나 새 P0·P1 또는 진행 방해 P2가 있으면 가장 중요한 결함 하나만 최소 재생성 또는 표시 조정하고 새 APK·새 화면으로 다시 판정한다.
- 그래픽·시스템 구현이 끝나면 별도 `최종 전체 플레이` 루프에서 새 최종 APK로 모든 고유 화면·중요 상태를 실제 사용자 입력으로 방문하고 전 화면을 새로 캡처한다. 화면 누락 0건과 종합 판정 통과 전에는 큐를 `없음` 또는 `IDLE`로 바꾸지 않는다.
- `scripts/check-visual-loop.sh`와 `scripts/check-full-play-evidence.sh`를 우회하거나 문구만 맞춰 허위 통과시키지 않는다.

## 작업 가치 관문

- 모든 활성 `LOOP_GOAL.md`는 `제품 변경`, `결함 수정`, `새 사용자 위험`, `사용자 지시` 중 하나로 분류하고 사용자 가치, 이전 루프와 다른 새로운 근거, 중복 방지 종료 경계를 명시한다.
- 허용되는 다음 작업은 아이가 쓰고 배우는 제품을 실제로 바꾸는 작업, 관찰 근거가 있는 결함 수정, 기존과 실질적으로 다른 사용자 흐름·위험 검증, 최신 사용자 지시의 직접 반영뿐이다.
- 같은 화면·callback·동작을 유지한 채 숫자만 늘린 반복, `N`회 통과 뒤 근거 없이 `N+1`회를 만드는 검증, 제품 코드나 판정에 영향을 주지 않는 캡처 재생산을 금지한다.
- 반복 내구성 QA는 시작 전에 근거 있는 상한과 실패 판정을 고정한다. 그 상한을 통과하면 완료하며 다음 루프에서 횟수를 한 단계 늘리지 않는다.
- 새 불편과 서로 다른 미검증 위험이 없으면 미완료 제품 지시를 진행한다. 그것도 없으면 큐를 `없음`으로 만들고 `IDLE`로 종료하며 작업을 꾸며 내지 않는다.
- `scripts/check-work-value.sh`와 세션 직전 `scripts/check-automation.sh` 검사를 우회하거나 통과만을 위해 문구를 허위로 작성하지 않는다.
- 같은 사용자 지적 또는 같은 근본 원인이 두 번 나타나면 `docs/회귀-규칙.md`에 원인과 지켜야 할 규칙의 이유를 남기고, 기계가 측정할 수 있는 항목은 테스트나 자동화 검사로 고정한다.

## 검증 규칙

코드 확인만으로 완료를 주장하지 않는다. Android 변경은 보통 macOS/Linux에서 `./scripts/verify.sh`, Windows에서 `scripts/verify.ps1`로 새 단위 테스트, lint, debug build 결과를 확보해야 한다.

시각 Android 루프는 `alarmquest-qa` 에뮬레이터의 세로 기기 해상도 1080 × 2340을 사용하고, 앱이 요구하는 가로 2340 × 세로 1080 상태에서 검증한다.

에뮬레이터를 장시간 켜 둔 상태의 QA를 금지한다. 각 시각·입력 QA 시작 전에 `adb -s emulator-5554 shell cat /proc/uptime`의 첫 값을 확인한다. 값이 7,200초 이상이면 새 입력·캡처를 진행하지 않고 `adb -s emulator-5554 emu kill`로 완전히 종료해 serial이 사라진 것을 확인한 뒤 `alarmquest-qa`를 cold boot한다. 부팅 완료 후 물리 1080 × 2340, `user_rotation=1`, LimDo 2340 × 1080, package·focus를 다시 확인해야 한다. 재시작 전 7,200초 이후에 수집한 화면은 완료 근거로 사용하지 않는다. 정확한 종료·재실행 절차는 `QA_CHECKLIST.md`와 `docs/루프-엔지니어링-적용.md`를 따른다.

시각 또는 입력 루프는 `QA_CHECKLIST.md`를 읽고 충족한다. `자동`, `에뮬레이터`, `아이 대리 점검`, `실제 아이 관찰` 근거를 구분하며 성인이나 에뮬레이터 점검을 다섯 살 아이 사용성의 실제 증거로 표현하지 않는다.

손글씨 입력 영역은 카드 외곽이 아니라 실제 그리기 내부를 측정한다. 2340 × 1080에서 최소 1872 × 648 px이고 화면에서 가장 큰 아이 상호작용 영역이어야 한다. 입력 영역만 넓고 실제 따라 쓰는 글자 길이 작게 남는 경우를 통과로 처리하지 않는다.

글자 geometry는 교육용 한글 글자꼴의 고유 비율과 모양을 보존한다. Canvas 너비와 높이에 맞추려고 x축과 y축을 서로 다른 배율로 늘이지 않으며, 글자별 정규화 template을 균일 배율로 확대한다. 현재 `ㄱ`뿐 아니라 이후 `가` 같은 조합 글자도 같은 정사각형 em 기준 안에서 자모 배치와 획 비율을 유지해야 한다. 실제 표시 경로와 입력 판정 경로는 같은 template과 배율을 사용한다.

## 변경 원칙

- 반복마다 초점이 분명한 변경 하나를 우선한다.
- 사용자 또는 이전 작업자의 관련 없는 변경을 보존한다.
- 구체적인 필요가 없으면 의존성을 추가하지 않는다.
- 근거 없이 Gradle 또는 SDK 버전을 바꾸지 않는다.
- 통과만을 위해 테스트나 lint를 끄지 않는다.
- `git reset --hard`를 포함한 파괴적 Git 명령을 사용하지 않는다.

## 중지 조건

다음 중 하나면 멈추고 한글로 보고한다.

- 현재 루프 성공 조건 통과 뒤 자동 QA로 다음 루프를 만들 수 없다.
- 같은 근본 원인이 세 반복 연속 실패했다.
- 15회 반복에 도달했다.
- 파괴적 작업이 필요하다.
- 목표에서 추론할 수 없는 큰 구조 결정이 필요하다.
- 필수 Android 도구를 사용할 수 없다.
- 계속하려면 원격 변경 또는 현재 범위를 넘는 권한이 필요하다.

## 완료 보고

반복 횟수, 변경 파일, 검증 명령과 결과, 남은 위험, 권장 다음 루프를 한글로 보고한다.
