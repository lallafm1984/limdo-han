# 루프 목표 139 — 네 번째 기본 자음 묶음 확장

## 작업 가치 관문

분류: 사용자 지시
사용자 가치: `ㄱ → ㄴ → ㄷ → ㄹ → ㅁ → ㅂ → ㅅ → ㅇ → ㅈ`을 익힌 아이가 공식 기본 자모 순서의 다음 자음을 같은 큰 쓰기 경험으로 계속 배울 수 있다.
새로운 근거: 루프 138의 새 2340 × 1080 아이 대리 QA에서 `ㅅ·ㅇ·ㅈ`은 새 미해결 불편 없이 통과했고, 최신 사용자 지시의 자음 단계를 이어 가려면 기본 자모 순서의 다음 `ㅊ → ㅋ → ㅌ`이 제품에 필요하다.
중복 방지: 이미 통과한 13개 글자를 다시 구현하거나 전체 자음·다음 모음·받침을 한꺼번에 추가하지 않는다. 다음 좁은 묶음 `ㅊ → ㅋ → ㅌ`만 조사·구현·검증한다.

## 목표

`ㅊ → ㅋ → ㅌ`을 교육용 획 template, 획순·방향 판정, 현재 획 동적 안내, 한국어 음성, 획수 자동차 보상과 다음 전환에 연결한다. 기준 화면은 정확한 2340 × 1080이다.

## 성공 조건

1. `ㅊ·ㅋ·ㅌ`의 획 구분과 방향은 신뢰할 수 있는 한글 교육 근거를 먼저 확인하고 공식 근거와 LimDo 제품 template 결정을 문서에서 구분한다.
2. 기본 자음 순서는 기존 9개 자음 다음 `ㅊ → ㅋ → ㅌ`로 이어지며 다른 글자를 추가하지 않는다.
3. 각 글자는 같은 정사각형 em 안에서 고유 모양과 비율을 보존하고 보이는 guide와 입력 판정이 같은 template을 사용한다.
4. 현재 획의 시작점·끝점·움직이는 방향과 짧은 한국어 음성이 글자별 획수 전체를 안내한다.
5. 정상 입력은 획순과 첫 방향을 판정하고 자동차가 선언된 획수만큼 이동하며, 차량은 화면 안에서 글자와 24 dp 이상 떨어진다.
6. 기존 13개 글자의 geometry·판정·음성·보상·다음 흐름을 회귀시키지 않는다.
7. 새 단위 테스트, `./scripts/verify.sh`, `git diff --check`를 통과하고 정확한 2340 × 1080에서 세 글자의 초기·정상 입력·성공 보상·다음 전환을 검증한다.
8. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

공식 또는 교육 기관 자료에서 `ㅊ·ㅋ·ㅌ`의 획 구분·방향 근거를 확인한 뒤 기존 lesson 계약에 추가하는 최소 구현을 시작한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비한다. 새 불편이 없으면 다음 미완료 제품 단계 하나만 준비하고, 완료 변경을 커밋 하나로 만든 뒤 `git push origin HEAD`로 일반 push한다.
