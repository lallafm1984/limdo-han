# 루프 목표 168 — 음성 제거·몰입형 화면·`ㅈ·ㅊ` 직선 자형 교정

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 다섯 살 아이가 불쾌한 합성 음성이나 시스템 바의 가림 없이 넓은 쓰기판에서 반듯한 `ㅈ·ㅊ` 자형을 보고 따라 쓰게 한다.
새로운 근거: 사용자가 실제 휴대폰 플레이에서 음성이 듣기 불편하고, 모바일 상·하단 시스템 바가 화면을 가리며, `ㅈ·ㅊ`의 여러 중간 굴곡이 꾸불꾸불해 보인다고 새로 지적했다.
중복 방지: TTS 실행 경로와 다시 듣기 UI가 제거되고, 시스템 바가 실행·포커스 복귀 때 숨겨지며, `ㅈ·ㅊ·자·차`가 불필요한 중간 꺾임 없는 공통 production geometry와 실제 2340 × 1080 화면 검증을 통과하면 같은 원인의 좌표 미세조정 반복을 만들지 않는다.

## 목표

1. Android TTS 초기화·자동 재생·오답/정답 재생과 `다시 듣기` 조작을 제품 실행 경로에서 제거한다. 현재 획 점선과 무음 동적 시범은 유지한다.
2. 상태 표시줄과 내비게이션 표시줄을 앱 실행 및 포커스 복귀 때 몰입형으로 숨기고, 가장자리 스와이프 때만 일시적으로 나타나게 한다.
3. `ㅈ·ㅊ`과 조합 글자 `자·차`의 몸체에서 여러 중간 좌표가 만드는 굴곡을 제거한다. 윗가로 뒤 중앙 접합부와 왼쪽 아래를 단순 연결하고, 오른쪽 획도 접합부에서 끝점으로 반듯하게 연결한다.

## 성공 조건

1. 앱 코드에 `TextToSpeech` 실행 구현이 없고, 메뉴 진입·쓰기 진입·오답·정답·다음 글자에서 음성이 재생되지 않으며 `다시 듣기` 버튼도 화면과 접근성 트리에 없다.
2. `WindowInsetsCompat.Type.systemBars()`를 실행·포커스 복귀에서 숨기고 `BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE`를 사용한다. 실제 SM-S931N 캡처가 상·하단 또는 회전 뒤 좌우 시스템 바에 가리지 않은 정확한 2340 × 1080 앱 화면이다.
3. `ㅈ·자` 몸체 첫 획은 윗가로 시작·끝, 중앙 접합부, 왼쪽 아래 끝의 네 점이고 오른쪽 획은 접합부·오른쪽 아래 끝의 두 점이다. `ㅊ·차`도 짧은 윗가로 뒤 같은 몸체 계약을 공유하며 불필요한 중간 꺾임이 없다.
4. 표시·점선·동적 시범·입력 판정이 변경한 production geometry 하나를 공유하고, 정방향 입력은 통과하며 역방향은 거부한다.
5. `./scripts/verify.sh`, `git diff --check`, 실제 SM-S931N 설치·실행·포커스·치명 오류 검사와 대표 `ㅈ·ㅊ` 화면 직접 비교가 통과한다.
6. 자동·실제 휴대폰·아이 대리 QA와 실제 아이 관찰 여부를 분리 기록한다.

## 완료 정의

모든 성공 조건을 통과하면 루프 168을 완료하고 새 아이 대리 QA에서 서로 다른 미해결 제품 결함이 있을 때만 다음 루프를 준비한다. 새 결함과 미완료 1차 목표가 없으면 큐를 `없음`, 상태를 `IDLE`로 바꾸고 완료 체크포인트를 `git push origin HEAD`로 일반 push한다.
