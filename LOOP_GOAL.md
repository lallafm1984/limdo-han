# 루프 목표 117 — 첫 조합 글자 `가`의 실사용 geometry 계약

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 아이가 단일 자음 `ㄱ` 다음 단계인 조합 글자 `가`를 실제 비율과 획순으로 배울 수 있는 기반을 만든다.
새로운 근거: 현재 `gaFixture`는 화면에 노출하지 않는 테스트 fixture일 뿐이므로, 제품 코드가 사용할 `가` geometry와 3획 계약을 새 자동 검증으로 확정한다.
중복 방지: production `ga` API와 계약 테스트가 통과하면 이 geometry 기반 작업은 완료하며 같은 callback 재생 횟수나 QA 회차를 늘리지 않는다.

## 목표

현재 테스트 전용 `gaFixture`를 첫 조합 글자 수업에서 직접 사용할 수 있는 production geometry로 전환한다. 정사각형 em 안에서 `ㄱ`과 `ㅏ`의 상대 위치, 3획 순서, 균일 배율을 하나의 진실로 고정한다.

## 성공 조건

1. `WritingCanvasGeometry`에 제품 코드가 호출할 명확한 `ga(width, height)` API가 있고 테스트 전용 `gaFixture` 명칭과 노출을 제거한다.
2. `가`는 정사각형 em 안에서 왼쪽 `ㄱ` 1획, 오른쪽 `ㅏ` 세로 1획, 오른쪽 짧은 가로 1획의 총 3획 순서를 유지한다.
3. `ㄱ`과 `ㅏ`의 상대 위치·간격·길이는 정규화 template 하나에서 파생하며 Canvas 너비와 높이에 서로 다른 배율을 적용하지 않는다.
4. 1962 × 775와 서로 다른 비율의 양수 Canvas에서 동일한 정규화 자모 배치와 균일 배율을 새 단위 테스트로 입증한다.
5. 잘못된 Canvas 크기는 기존 geometry와 같은 방식으로 거부하고, 기존 `ㄱ` geometry·동적 안내·판정 테스트를 회귀시키지 않는다.
6. `./scripts/verify.sh`, `./scripts/check-automation.sh`, `git diff --check`를 통과한다.
7. 이번 루프는 아직 화면에 `가`를 표시했다고 주장하지 않는다. 정확한 2340 × 1080 시각 검증은 다음 표시 연결 루프에서 수행하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

먼저 현재 `gaFixture` 사용처와 정규화 template을 확인한 뒤, production `ga` API와 필요한 교육용 3획·균일 배율 회귀 테스트만 최소 변경으로 구현한다.

## 완료 정의

모든 조건을 통과하면 다음 제품 단계인 `가` 수업 표시 연결을 작업 가치 관문이 있는 다음 루프로 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
