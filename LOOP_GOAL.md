# 루프 목표 007 — 초기 음성 재생 중 다시 듣기 상태 QA

## 목표

정확한 2340 × 1080 cold launch에서 자동 초기 안내가 시작·재생·완료될 때 다시 듣기 버튼의 비활성·활성 시각 상태가 callback과 일치하고, 글을 읽지 못하는 아이에게도 현재 누를 수 있는지를 새 연속 근거로 판정한다.

이 루프는 먼저 QA 탐색을 수행하고, 구체적 문제가 입증된 경우에만 현재 화면 범위에서 최소 수정한다. 새 글자·수업·서버·계정·분석·권한·의존성을 추가하지 않는다.

## 성공 조건

1. `alarmquest-qa`의 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`에서 새 debug APK를 cold launch한다.
2. 음성 `Initializing`, `Ready`, `Playing`, 완료 전환의 시각 버튼, UI hierarchy, `LimDoSpeech` callback을 시간 순으로 새로 수집한다.
3. 재생 중 비활성과 완료 후 활성이 음소거·스피커 아이콘, 명도, clickable semantics로 일치하며 문구만에 의존하지 않는다.
4. 상태 전환 중에도 쓰기 위치·시작점·방향·지우기는 이해할 수 있고 안전하게 유지된다.
5. 문제가 입증되어 코드를 바꾸면 `./scripts/verify.sh`의 자동화 계약, 단위 테스트, lint, debug build가 통과한다.
6. 아이 대리 QA에서 상태별 버튼을 문장을 읽지 않고 구분할 수 있는지 판정하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

cold launch 직후부터 초기 음성 완료 후까지 짧은 간격의 화면·hierarchy·callback을 수집해 상태 전환을 먼저 판정한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 로컬 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다. push가 실패하면 정확한 오류를 기록하고 force push하지 않는다.
