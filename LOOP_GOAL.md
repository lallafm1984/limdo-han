# 루프 목표 128 — 현재 획만 강조하는 방향 안내

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 글을 읽지 못하는 아이가 여러 화살표 중 하나를 추측하지 않고, 지금 시작하거나 이어 쓸 획의 방향 하나에 집중할 수 있다.
새로운 근거: 루프 127 최종 새 APK의 정확한 2340 × 1080 `captures/loop127/iteration2/final-proxy-qa/01-idle-after-clear.png`에서 빈 `가`에 1획 오른쪽·아래쪽, 2획 아래쪽, 3획 오른쪽의 네 화살표가 모두 같은 강도로 동시에 보인다.
중복 방지: 입력 전·입력 중 현재 획의 다음 방향만 강하게 보이고 완료한 획과 미래 획의 정적 화살표가 사라지거나 명확히 약해지는 새 연속 화면 근거를 얻으면 종료한다. 동일 안내를 추가 재생하거나 이미 통과한 음성 상태를 다시 수집하지 않는다.

## 목표

`가` 방향 안내에서 현재 획의 다음 방향만 시각적으로 우선하고, 완료·미래 획의 화살표가 아이의 주의를 경쟁하지 않게 한다.

## 성공 조건

1. 입력 전에는 첫 획 시작과 첫 진행 방향만 강하게 보이며 2·3획 정적 화살표는 보이지 않거나 명확히 약하다.
2. 입력 중에는 현재 손가락보다 앞선 다음 방향 하나만 활성화하고, 완료한 획 안내는 사라지며 다음 획은 해당 획 차례에 활성화된다.
3. production `ga` geometry·획순·동적 시범·입력 판정의 단일 진실을 유지하고 별도 좌표를 중복하지 않는다.
4. 정상 3획·재시도·지우기·음성·3칸 보상·다음 차량 흐름을 회귀시키지 않는다.
5. 정확한 2340 × 1080에서 입력 전→1획 진행·완료→2획→3획의 새 연속 화면·hierarchy를 확인한다.
6. `./scripts/verify.sh`, `./scripts/check-automation.sh`, `git diff --check`를 통과한다.
7. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

`WritingCanvas`의 정적·동적 화살표가 현재 `completedStrokes`와 입력 진행 상태를 어떻게 함께 그리는지 조사하고, 현재 획 하나만 강조하는 최소 가설을 세운다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비하고, 검증된 완료·준비 변경을 커밋 하나로 만든 뒤 `git push origin HEAD`로 일반 push한다.
