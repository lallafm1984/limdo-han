# 루프 목표 222 — `ㄴ + ㅣ → 니` 오른쪽 세로 모음 확장

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 다섯 살 아이가 `느`의 아래쪽 가로 모음과 달리 `니`의 오른쪽 세로 모음을 비교하며 `ㄴ`과 `ㅣ`의 좌·우 조립을 익힌다.
새로운 근거: 루프 221은 `ㄴ + ㅡ → 느`의 위·아래 평행 가로 모음만 다루었으며, `ㅣ`를 초성 오른쪽에 조립하는 흐름은 아직 구현하지 않았다.
중복 방지: 이번 루프는 `ㄴ + ㅣ → 니` 한 조립 흐름과 production `니` 쓰기 연결만 다룬다. `ㄷ`와 다음 교육 단계는 함께 늘리지 않는다.

## 목표

`ㄴ`을 왼쪽 칸과 `ㅣ`를 오른쪽 칸에 놓아 `니`를 완성하고 production `니` 쓰기 lesson으로 연결한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. 조립 진입에서 `느`와 `니`를 모양으로 구분해 `니`를 선택하고 홈·뒤로 복귀가 동작한다.
3. `ㄴ`과 `ㅣ` 조각, 초성 왼쪽 칸과 모음 오른쪽 칸, 조립 전·선택·완성 상태가 글을 읽지 않아도 구분된다.
4. 잘못된 칸·순서·빈 칸은 완성으로 처리하지 않고 처벌 없이 다시 안내한다.
5. 완성 `니`와 production `니` 표시·안내·판정이 같은 정사각형 em·균일 배율·자모 상대 위치를 사용한다.
6. 완성 뒤 production `니` WritingCanvas 1962 × 954 px로 진입하고 네 그림 조작·정방향 입력·성공·다음·홈 회귀가 없다.
7. 정확한 2340 × 1080 변경 전·후 PNG·hierarchy·focus·APK SHA와 시작·오순서·완성·쓰기·성공 근거에서 잘림·겹침·가림이 0건이다.
8. 새 P0·P1·진행 방해 P2가 0건이고 자동 그래픽 디자인·자동 QA·에뮬레이터·아이 대리 QA가 통과한다.

## 완료 정의

`ㄴ + ㅣ → 니` 한 조립·쓰기 연결만 새 근거로 통과하면 완료한다. `ㄷ`와 다음 교육 단계는 별도 루프로 나눈다. 완료 체크포인트만 `git push origin HEAD`로 일반 push한다.
