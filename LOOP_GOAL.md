# 루프 목표 167 — 점선 획 안내와 `ㅅ·ㅈ·ㅊ` 교육용 자형 재교정

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 글을 읽지 못하는 다섯 살 아이가 가려짐이나 색 혼동 없이 점선과 움직임을 보고 정확한 위치·방향·획순으로 따라 쓰게 한다.
새로운 근거: 사용자가 실제 플레이에서 움직이는 안내 오브젝트와 획 안내선의 불일치, 큰 오브젝트의 쓰기판 가림, 흰 원 테두리의 배경 혼동, `ㅅ·ㅈ·ㅊ` 자형·쓰기 오류를 새로 보고했다.
중복 방지: 점선 중심선과 동적 안내가 같은 production geometry를 사용하고 `ㅅ·ㅈ·ㅊ·사·자·차`의 표시·시범·판정 계약 및 대표 실화면을 한 번 통과하면 같은 원인의 크기 조정이나 캡처 반복 루프를 만들지 않는다.

## 목표

1. 현재 써야 하는 한 획의 실제 판정 중심선만 동일한 크기의 작은 원을 촘촘하고 일정한 간격으로 배치해 표시한다. 완료했거나 아직 쓰지 않을 획의 점은 숨긴다.
2. 움직이는 안내 오브젝트는 같은 production 경로 중심 위에서만 이동하도록 하고, 글자나 쓰기판을 가리지 않는 크기로 줄인다.
3. 시작·끝·움직임 표식의 흰 테두리를 배경과 구분되는 색으로 바꾼다.
4. 첨부한 자음 획순 기준대로 `ㅅ·ㅈ·ㅊ`과 조합 글자 `사·자·차`의 자형·획순·방향·판정을 함께 교정한다.

## 성공 조건

1. 점선 중심선의 모든 원은 지름과 간격이 일정하고 production geometry 위에 있다. 화면에는 현재 획의 원만 나타나며 표시 경로와 판정 경로가 일치한다.
2. 동적 안내 중심은 현재 획 위에 있으며 안내 오브젝트와 시작·끝 표식은 안내 획 굵기의 40% 이내로 쓰기 배경을 과도하게 가리지 않는다.
3. 시작·끝·동적 표식은 흰색 외곽선을 사용하지 않고 노란 쓰기판·점선·아이 입력선과 명확히 구분된다.
4. 첨부 형태처럼 `ㅅ` 1획은 위 중앙에서 아래로 내려오다가 부드럽게 왼쪽 아래로 휘고, 2획은 1획의 중앙 접합부에서 오른쪽 아래로 뻗는다. `ㅈ` 1획은 긴 윗가로를 왼쪽에서 오른쪽으로 쓴 뒤 끊지 않고 중앙 접합부를 지나 왼쪽 아래로 부드럽게 휘며, 2획은 그 중앙 접합부에서 오른쪽 아래로 뻗는다. `ㅊ`은 짧은 윗가로 1획, 긴 가로 뒤 중앙 접합부를 지나 왼쪽 아래로 휘는 2획, 같은 접합부에서 오른쪽 아래로 뻗는 3획이다. `사·자·차`도 같은 초성 계약을 공유한다.
5. 정방향은 통과하고 역방향·잘못된 획 순서는 거부하는 자동 회귀 검사가 통과한다.
6. `./scripts/verify.sh`, `git diff --check`, 정확한 2340 × 1080 에뮬레이터에서 대표 여섯 글자의 초기·시범·입력·성공 화면을 첨부 `ㅅ·ㅈ·ㅊ` 기준 이미지와 같은 상태로 직접 비교하고 아이 대리 QA가 통과한다.
7. 실제 아이 관찰 여부를 자동·에뮬레이터·아이 대리 근거와 분리 기록한다.

## 완료 정의

모든 성공 조건을 통과하면 루프 167을 완료하고 새 아이 대리 QA에서 서로 다른 미해결 제품 결함이 있을 때만 다음 루프를 준비한다. 새 결함과 미완료 1차 목표가 없으면 큐를 `없음`, 상태를 `IDLE`로 바꾸고 완료 체크포인트를 `git push origin HEAD`로 일반 push한다.
