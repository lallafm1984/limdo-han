# 루프 목표 010 — 부정확한 아이 한 획 입력 허용 범위 QA

## 목표

정확한 2340 × 1080에서 아이의 부정확한 접촉을 대리한 `ㄱ` 한 획 입력을 실행해, 시작·꼭지·도착 허용 범위와 경로 통로가 실제로 충분한지 판정한다.

이번 루프는 정확한 중심선 ADB 입력만 통과한 기존 근거를 보완하는 좁은 QA 탐색이다. 문제가 입증되기 전에 판정 알고리즘을 바꾸지 않고, 새 글자·수업·저장·점수·서버·계정·분석·권한·의존성을 추가하지 않는다.

## 성공 조건

1. `alarmquest-qa`의 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`에서 새 debug APK를 검증한다.
2. 현재 Canvas 1962 × 775 px와 정사각형 `ㄱ` geometry를 유지한다.
3. 정확한 중심선 입력과 함께, 시작·가로 획·꼭지·세로 획·도착 주변을 서로 다른 좌표로 부정확하게 지나는 대표 성공·재시도 입력을 최소 5개 실행한다.
4. 각 입력의 좌표, 판정 결과, 화면 표시, 음성 callback을 기록해 어느 편차까지 자연스러운 한 획으로 받아들이는지 판정한다.
5. 글을 읽지 않는 아이 관점에서 유사한 입력이 일관되게 성공·재시도로 보이고 들리는지 아이 대리 QA한다.
6. 문제가 입증되면 가장 작은 판정 geometry 보정과 단위 테스트만 적용하고, 문제가 없으면 제품 코드를 바꾸지 않는다.
7. 코드를 바꾸면 `./scripts/verify.sh`의 자동화 계약, 단위 테스트, Android lint, debug build와 `git diff --check`가 통과한다.
8. `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

현재 debug APK에서 정확한 중심선 주입과 비교할 부정확한 대표 좌표 세트를 먼저 정의하고 제품 코드 변경 없이 재현한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 로컬 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다. push가 실패하면 정확한 오류를 기록하고 force push하지 않는다.
