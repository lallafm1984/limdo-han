# 루프 목표 154 — `가나다` 다음 첫 기본 모음 `ㄱ` 행

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 자음·모음·`가나다`를 익힌 아이가 한 번에 초성과 모음을 모두 바꾸지 않고, 이미 배운 `ㄱ`을 고정한 채 기본 모음 하나씩만 바꾸며 받침 없는 글자를 단계적으로 읽고 쓸 수 있다.
새로운 근거: 사용자가 `가나다` 다음에는 받침 없는 글자에서 모음만 교육 순서대로 바꾸는 흐름을 직접 요청했다. 현재 curriculum은 `가·나·다` 뒤에 `라·마·바…`로 초성을 바꾸므로 이 지시와 `docs/한글-교육-순서-근거.md`의 첫 기본 모음 행 계약을 아직 구현하지 않았다.
중복 방지: 이번 루프는 첫 `ㄱ` 행 `가 → 갸 → 거 → 겨 → 고 → 교 → 구 → 규 → 그 → 기`만 구현·검증한다. `ㄴ·ㄷ` 행, 복잡한 모음 `ㅐ`, 받침 추가, 교육 단계 선택 메뉴는 한꺼번에 구현하지 않고 다음 별도 제품 루프로 남긴다.

## 목표

기존 `가나다` 글자의 짜임 수업 뒤에 초성 `ㄱ`을 고정하고 기본 모음 `ㅏ → ㅑ → ㅓ → ㅕ → ㅗ → ㅛ → ㅜ → ㅠ → ㅡ → ㅣ`만 바꾸는 첫 받침 없는 글자 행을 연결한다. 글을 읽지 못하는 다섯 살 아이가 같은 초성과 달라지는 모음을 큰 글자 길, 동적 획 안내와 검증된 한국어 음성으로 구분할 수 있어야 한다.

## 성공 조건

1. 교육 단계는 `자음 → 모음 → 글자의 짜임 → 받침 없는 글자` 순서를 유지한다.
2. `가 → 나 → 다` 뒤의 첫 받침 없는 글자 행은 `가 → 갸 → 거 → 겨 → 고 → 교 → 구 → 규 → 그 → 기` 순서를 명시한다. 이미 학습한 `가`를 행의 기준 글자로 재사용하되 curriculum에서 불필요한 중복 입력으로 만들지 않는다.
3. 새 글자는 기존 기본 자모 template을 조합해 초성 `ㄱ`은 고정하고 중성만 바뀐다.
4. 세로 모음 조합과 가로 모음 조합은 각각의 `좌우`·`상하` 음절 짜임을 사용하며 x축·y축을 서로 다른 비율로 찌그러뜨리지 않는다.
5. 표시·동적 획 안내·입력 판정은 같은 정사각형 em template과 균일 배율을 사용한다.
6. 각 lesson의 획수·방향 metadata·한국어 음성 안내·자동차 이동 칸 수가 실제 template 획과 일치한다.
7. 기본 자모를 이미 배웠더라도 새 조합 글자의 첫 진입에서 음성·움직임으로 과제를 이해할 수 있고 글자 문장을 읽어야만 진행하지 않는다.
8. `KoreanCurriculumTest`가 순서, 단계, 조합 geometry, 획수·방향, 정상 입력과 전체 순환을 고정한다.
9. `./scripts/verify.sh`, `git diff --check`가 통과한다.
10. 정확한 2340 × 1080에서 대표 세로 모음 `갸·거`, 가로 모음 `고·구`, 마지막 `기`의 초기·입력·성공과 행 순환을 새 PNG·hierarchy로 확인한다.
11. WritingCanvas 실제 내부가 `[189,63][2151,1017]` = 1962 × 954 px 이상을 유지하고 화면에서 가장 큰 아이 상호작용 영역이다.
12. 좌우 세로 조작 열, 시작·끝 표시, 동적 안내, 자동차 보상과 오른쪽 다음 활성 상태가 새 글자 길을 가리지 않는다.
13. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 루프 경계

이 루프가 통과하면 최신 사용자 지시의 교육 단계·글자 선택 메뉴를 다음 제품 루프로 준비한다. 메뉴는 자음·모음·글자의 짜임·받침 없는 글자·받침 있는 글자를 교육 순서대로 큰 단계 카드에서 고르고, 글을 읽지 못하는 아이가 모양·아이콘·위치·움직임·검증된 한국어 음성으로 선택할 수 있게 한다. `ㄴ·ㄷ` 기본 모음 행은 메뉴와 별개로 우선순위를 비교해 하나만 다음 루프로 선택한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 서로 다른 미검증 위험 또는 제품 불편 하나만 기록한다. 새 불편이 없으면 최신 미완료 사용자 지시인 교육 단계 선택 메뉴를 다음 루프로 준비한다. 완료와 다음 루프 준비를 체크포인트 커밋 하나로 만들고 `git push origin HEAD`로 일반 push한다. force push·tag·release·배포는 하지 않는다.
