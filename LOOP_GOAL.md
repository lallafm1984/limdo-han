# 루프 목표 171 — 기본 모음 노출·순환·획순 교정

## 작업 가치 관문

분류: 사용자 지시
사용자 가치: 다섯 살 아이가 기본 모음을 정해진 순서로 만나고, 화면에 보이는 점선과 같은 획순·방향으로 처음부터 올바르게 익히게 한다.
새로운 근거: 사용자 첨부 모음 순서표와 production 코드를 대조한 결과 기본 메뉴에 `ㅐ`가 끼어 있었고, `ㅓ·ㅕ·ㅗ·ㅛ`의 획순 또는 방향이 참고표와 반대로 정의돼 있었다.
중복 방지: 기본 모음 메뉴와 다음 순환을 `ㅏ·ㅑ·ㅓ·ㅕ·ㅗ·ㅛ·ㅜ·ㅠ·ㅡ·ㅣ`로 고정하고 10개 전체의 획순·방향 oracle과 정확한 화면을 통과하면 같은 순서 재감사를 반복하지 않는다.

## 목표

1. 기본 모음 메뉴와 성공 뒤 다음 순환을 `ㅏ·ㅑ·ㅓ·ㅕ·ㅗ·ㅛ·ㅜ·ㅠ·ㅡ·ㅣ` 순서로 고정하고 `ㅐ`는 현재 기본 10모음 메뉴에서 제외한다.
2. `ㅓ·ㅕ`는 짧은 가로를 왼쪽에서 오른쪽으로 먼저 쓴 뒤 세로를 위에서 아래로 쓰게 한다.
3. `ㅗ·ㅛ`는 짧은 세로를 위에서 아래로 먼저 쓴 뒤 긴 가로를 왼쪽에서 오른쪽으로 쓰게 한다.
4. 표시·현재 획 점선·동적 표식·시작끝 표식·입력 판정이 교정된 production geometry 하나를 공유하게 한다.

## 성공 조건

1. 모음 선택과 다음 순환이 정확히 기본 10모음 순서이며 `ㅣ` 다음은 `ㅏ`다.
2. 기본 10모음의 획수·순서·시작점·방향이 사용자 첨부 모음 순서표와 일치한다.
3. 각 정방향 production 획은 성공하고 역방향과 다른 획 선행은 성공하지 않는다.
4. `./scripts/verify.sh`, `git diff --check`, `emulator-5554` focus·치명 오류 검사와 정확한 2340 × 1080 모음 선택·대표 교정 화면 직접 비교가 통과한다.
5. 자동·에뮬레이터·아이 대리 QA와 실제 아이 관찰 여부를 분리 기록한다.

## 완료 정의

모든 성공 조건을 통과하면 루프 171을 완료한다. 새 아이 대리 QA에서 서로 다른 미해결 제품 결함이 없으면 큐를 `없음`, 상태를 `IDLE`로 바꾸고 완료 체크포인트를 `git push origin HEAD`로 일반 push한다.
