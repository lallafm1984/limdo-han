# 루프 목표 051 — 입력 전 첫 획 동적 시범

## 목표

정확한 2340 × 1080의 입력 전 상태에서 `ㄱ` 첫 획의 시작→가로→꼭지→세로→끝 순서를 움직임만으로 보여 주는 반복 시범을 추가한다. 정사각형 학습판과 기존 입력·판정·음성은 유지하며 입력 중 안내와 자동차 자산·보상은 이번 루프에 구현하지 않는다.

## 성공 조건

1. 입력 전 큰 안내 표식이 `ㄱ` 중심선을 시작→가로→꼭지→세로→끝 순서로 반복 이동한다.
2. 안내는 정사각형 학습판·guide·시작점·끝점을 가리지 않고 터치 입력을 가로채지 않는다.
3. 입력이 시작되면 입력 전 시범이 중단되어 아이 획과 모순되지 않는다.
4. 초기화 뒤 시범이 복원되고 재시도·성공·다시 듣기·음성 상태와 충돌하지 않는다.
5. 정확한 2340 × 1080 연속 프레임과 단위 테스트로 이동 순서·위치·상태 전환을 새로 검증한다.
6. `./scripts/verify.sh`와 `git diff --check`가 통과하고 새 아이 대리 QA 및 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

현재 정적 방향 표식과 상태 모델을 확인하고, 입력 전 첫 획 시범만 추가하는 최소 애니메이션 변경을 적용한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 입력 중 다음 방향 안내를 별도 루프로 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
