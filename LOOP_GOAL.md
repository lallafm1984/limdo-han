# 루프 목표 180 — 참고 이미지형 `가` 초성 곡선 내림 교정

## 작업 가치 관문

분류: 사용자 지시
사용자 가치: 아이가 `가`를 따라 쓸 때 첨부 참고 이미지처럼 초성 `ㄱ`이 꺾인 뒤 처음에는 거의 아래로 내려오다가 왼쪽으로 완만하게 휘는 모양을 보고 그대로 연습할 수 있다.
새로운 근거: 최신 첨부 이미지의 초성은 단순 직선 대각선이 아니라 위 꺾임에서 수직에 가깝게 출발해 아래로 갈수록 왼쪽으로 휘는 곡선이다. 직전 0.26 em 직선안은 방향은 맞지만 형태가 다르며, 기존 수직형 첫 획은 `갸·거·겨·기`에도 공유돼 `가` 전용 분리가 계속 필요하다.
중복 방지: 첨부 이미지에서 요청한 `가` 초성 곡선 중심선만 교정하고 표시·동적 안내·입력 판정의 공통 geometry 일치를 검증한다. 독립 `ㄱ`, `갸·거·겨·기`, 받침 글자와 다른 자모의 자형·획순·판정, 네 조작과 성공·재시도 연출은 바꾸지 않는다.

## 목표

1. `가` 초성 `ㄱ`의 윗가로는 기존처럼 0.05→0.48 em로 오른쪽 진행을 유지한다.
2. 꺾임 `(0.48, 0.12)`에서 시작해 제어점 `(0.48, 0.46)`·`(0.27, 0.75)`를 지나 끝점 `(0.10, 0.88)`에 닿는 12구간 cubic Bézier 표본으로 참고 이미지의 완만한 왼쪽 휨을 만든다.
3. `가` 전용 초성 template을 기존 수직형 `ㄱ`과 분리해 독립 `ㄱ` 및 `갸·거·겨·기`는 기존 직각을 유지한다.
4. 점선 중심, 시작·끝 표식, 동적 방향 표식과 production 입력 판정이 모두 같은 새 `가` 곡선 geometry를 사용한다.
5. 새 곡선을 정방향으로 따라 쓴 입력은 성공하고, 기존 수직 끝점을 따르거나 역방향·잘못된 획순으로 쓴 입력은 성공하지 않게 한다.

## 성공 조건

1. `./scripts/verify.sh`와 `git diff --check`가 통과한다.
2. 자동 검사에서 `가` 첫 획의 곡선 표본은 꺾임 뒤 x가 단조 감소하고 y가 단조 증가하며, 처음에는 거의 수직으로 내려오고 끝점은 꺾임보다 0.38 em 왼쪽·0.76 em 아래에 있다.
3. 독립 `ㄱ`과 `갸·거·겨·기` 첫 획은 꺾임점·끝점 x가 계속 같아 직각을 유지한다.
4. `가`의 입력 방향 안내와 전체 동적 시범은 곡선의 각 표본 구간에서 같은 접선 단위 방향 벡터를 사용한다.
5. 새 production `가` 세 획이 순서대로 성공하며, 이전 수직형 첫 획·정확한 역방향·잘못된 획순은 성공하지 않는다.
6. 정확한 2340 × 1080 에뮬레이터 화면에서 `가`의 현재 획 점선과 큰 흰 글자 길이 참고 이미지처럼 꺾임 뒤 완만하게 왼쪽으로 휘고, 쓰기판 1962 × 954 px와 네 상시 활성 버튼이 보존된다.
7. 참고 이미지와 같은 상태의 최종 앱 캡처를 한 비교 입력으로 대조한 `design-qa.md`가 P0·P1·P2 없이 `final result: passed`이고, 같은 최종 APK에서 곡선 `가` 첫 획 입력과 다음 획 안내 전환을 확인한다.
8. 자동·에뮬레이터·아이 대리 QA와 실제 아이 관찰 여부를 분리 기록한다.

## 완료 정의

`가` 전용 곡선 geometry와 표시·안내·판정 일치, 참고 이미지 디자인 QA, 요청하지 않은 `ㄱ` 계열 보존이 자동 검사와 정확한 2340 × 1080 에뮬레이터 동작·아이 대리 QA에서 통과하면 루프 180을 완료한다. 최신 debug APK를 연결 가능한 실제 `SM-S931N`에 무선 덮어쓰기 설치하고, 기기가 연결되지 않으면 그 전달 제한을 사실대로 기록한다. 활성 큐를 `없음`으로 전환하고 완료 체크포인트를 만든 뒤 `git push origin HEAD`로 일반 push한다.
