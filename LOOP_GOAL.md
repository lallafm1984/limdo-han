# 루프 목표 055 — `strokeCount` 기반 자동차 이동 보상

## 목표

현재 `ㄱ` lesson data에 실제 교육용 `strokeCount=1`을 명시하고, 정답 한 번에 활성 자동차가 정확히 한 칸 이동하는 짧고 분명한 보상을 보여 준다. 실패·지우기·다시 듣기에는 이동하지 않으며 같은 성공 callback 중복으로 두 번 움직이지 않는다.

## 성공 조건

1. 현재 `ㄱ` lesson data에 `strokeCount=1`이 명시되고 보상 이동량의 단일 진실로 사용된다.
2. 정상 성공 한 번에 활성 자동차가 정확히 한 칸만 순차 이동하고 이동 완료 전 새 입력으로 상태가 꼬이지 않는다.
3. 실패·지우기·다시 듣기는 0칸이며 같은 성공 callback의 중복은 추가 이동을 만들지 않는다.
4. 성공 `★ ✓`와 음성 시작 뒤 이동이 연결되고 짧고 분명한 시작·중간·완료 상태가 있다.
5. 2340 × 1080에서 이동 경로와 차량이 학습판·guide·시작점·끝점·동적 안내·아이 획을 가리지 않고 입력을 가로채지 않는다.
6. 자동 상태·중복 회귀, `./scripts/verify.sh`, `git diff --check`를 통과한다.
7. 새 연속 화면·hierarchy와 아이 대리 QA, `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

현재 lesson data와 성공 상태 전환을 확인하고 `strokeCount=1` 및 한 칸 이동 상태만 다루는 최소 변경을 적용한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
