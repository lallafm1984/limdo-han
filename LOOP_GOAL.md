# 루프 목표 133 — 3칸 자동차 보상의 글자 가림 방지

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 아이가 `가` 3획을 완성한 직후에도 자동차에 가려지 않은 전체 글자와 자기 획을 보며 성공 결과를 연결할 수 있다.
새로운 근거: 루프 132 새 APK의 정확한 2340 × 1080 `captures/loop132/iteration1/strengthened-arrowhead/05-after-success.png`에서 3칸 이동한 경찰차 `[477,62][813,288]`가 production `가` 1획의 왼쪽 위를 가린다. 이전 `ㄱ` 1칸 성공의 `[225,62][561,288]`에서는 발생하지 않던 다획 보상 경로의 새 결함이다.
중복 방지: 자동차 3칸 이동 수·순서·자산과 글자 geometry는 유지하고, 보상 경로와 production 글자 bounds의 안전 여백만 보정한다. 같은 흐름의 반복 횟수나 캡처 수를 늘리지 않는다.

## 목표

`strokeCount=3`의 자동차 보상이 세 칸을 모두 보이면서도 production `가` 글자와 아이 획을 가리지 않는 안전 경로를 사용한다.

## 성공 조건

1. 경찰차의 시작과 1·2·3칸 각 위치가 production `가` guide·아이 획과 겹치지 않고 최소 24 dp 안전 여백을 유지한다.
2. 경로는 현재 글자 geometry·자동차 크기·화면 크기에서 파생하고 `가` 전용 절대 화면 좌표를 추가하지 않는다.
3. 성공 후 자동차는 정확히 3칸을 순서대로 이동하고, 실패·지우기·다시 듣기는 자동차를 이동시키지 않는다.
4. `ㄱ` 1칸 보상과 9종 자동차 순환·다음 차량 흐름을 회귀시키지 않는다.
5. 정확한 2340 × 1080에서 성공 전·1칸·2칸·3칸·다음 차량 화면과 hierarchy를 확인한다.
6. `./scripts/verify.sh`, `./scripts/check-automation.sh`, `git diff --check`를 통과한다.
7. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

현재 자동차 보상 배치와 production `가` bounds를 같은 좌표계에서 측정해, 3칸 이동을 유지하면서 가림을 피하는 최소 경로 보정 가설을 세운다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비하고, 검증된 완료·준비 변경을 커밋 하나로 만든 뒤 `git push origin HEAD`로 일반 push한다.
