# 루프 목표 208 — 가로 모음 조립 첫 번째 좁은 묶음

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 다섯 살 아이가 세로 모음은 초성 오른쪽에 놓지만 가로 모음 `ㅗ`는 초성 아래에 놓아 `고`가 되는 새 글자 구조를 조립과 쓰기로 연결할 수 있다.
새로운 근거: 루프 205~207은 `ㅐ`·`ㅓ`·`ㅕ` 세로 모음을 초성 오른쪽 칸에 놓는 구조만 구현했으며, 초성 아래의 가로 모음 칸·조립 모양·production `고` 연결은 실질적으로 다른 미구현 교육 흐름이다.
중복 방지: 이번 루프는 `ㄱ + ㅗ → 고` 한 조립 화면과 쓰기 연결만 다룬다. 다른 가로 모음·초성·받침을 함께 늘리지 않으며 통과 뒤 다음 교육 묶음은 새 루프로 분리한다.

## 목표

루프 207의 검증된 복수 목표 선택·조립 상호작과 production token을 재사용하되, `ㄱ`을 윗칸과 `ㅗ`를 아랫칸에 놓아 `고`를 완성하고 기존 `고` 쓰기 lesson으로 연결한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. 기존 조립 진입에서 `가`·`거`·`겨`·`고`를 글자 모양으로 구분해 선택하고 홈·뒤로 복귀가 동작한다.
3. `ㄱ`과 `ㅗ` 조각, 초성 윗칸과 가로 모음 아랫칸, 조립 전·선택·완성 상태가 글을 읽지 않아도 구분된다.
4. 잘못된 칸·순서·빈 칸은 완성으로 처리하지 않고 처벌 없이 다시 안내한다.
5. 완성 `고`와 production `고` 표시·안내·판정이 같은 정사각형 em·균일 배율·자모 상대 위치를 사용한다.
6. 완성 뒤 기존 `고` WritingCanvas 1962 × 954 px로 진입하고 네 그림 조작·정방향 입력·성공·다음·홈 회귀가 없다.
7. 정확한 2340 × 1080 변경 전·후 PNG·hierarchy·focus·APK SHA와 시작·오순서·완성·쓰기 근거에서 잘림·겹침·가림이 0건이다.
8. 새 P0·P1·진행 방해 P2가 0건이고 자동 그래픽 디자인·자동 QA·에뮬레이터·아이 대리 QA가 통과한다.

## 완료 정의

`ㄱ + ㅗ → 고` 한 조립 흐름과 기존 `고` 쓰기 연결만 새 자동·실화면 근거로 통과하면 완료한다. 다른 모음·초성·받침은 별도 단일 루프로 나누며 완료 체크포인트만 `git push origin HEAD`로 일반 push한다.
