# 루프 목표 185 — 한 lesson의 쓰기 전 보호자 녹음

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 보호자가 선택한 lesson에 아이가 쓰기 전에 들을 짧은 목소리를 기기 안에 직접 녹음하고 확인·교체·삭제할 수 있다.
새로운 근거: 루프 184에서 보호자 전용 진입과 38개 lesson 목록을 만들었지만 lesson별 녹음 동작·파일·권한·상태 화면은 아직 production에 없다.
중복 방지: 이번 루프는 대표 lesson 하나의 `쓰기 전` 녹음 시작·정지·미리 듣기·다시 녹음·삭제와 app-private no-backup 저장까지만 구현한다. `정답 후` 녹음, 모든 lesson 확장, 아이 화면 재생, 자동 다음과 학습 목록 편집은 후속 루프로 분리한다.

## 목표

1. 보호자 목록에서 대표 lesson 하나를 열어 `쓰기 전` 녹음 없음·녹음 중·완료 상태를 확인한다.
2. 녹음 버튼을 누른 시점에만 `RECORD_AUDIO`를 요청하고 최대 8초에 안전하게 정지한다.
3. 완료 파일은 `noBackupFilesDir/guardian_voice/`의 lesson·START 고유 경로에 원자적으로 저장한다.
4. 미리 듣기·정지·다시 녹음·삭제가 다른 lesson과 아이 학습 흐름을 바꾸지 않는다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. 마이크 권한은 보호자 화면의 녹음 버튼에서만 요청되고 거부·취소 뒤에도 보호자 목록과 아이 무음 학습이 계속된다.
3. 녹음은 최대 8초, 임시 파일→완료 파일 원자적 교체, `noBackupFilesDir` 저장이며 인터넷·외부 저장 권한은 0건이다.
4. 녹음 없음·녹음 중·완료·미리 듣기·다시 녹음·삭제 상태와 callback이 자동 검사와 실제 기기에서 일치한다.
5. 화면 이탈·background·권한 취소에서 recorder/player가 중단·해제되고 손상 파일은 무음 상태로 돌아간다.
6. 정확한 2340 × 1080 보호자 녹음 화면 PNG·hierarchy·focus에서 주요 동작이 최소 64 × 64 dp이고 상태가 색 하나에 의존하지 않으며 잘림·겹침이 없다.
7. `.loop/visual-evidence.md`에 package·APK SHA, 자동 그래픽 디자인·자동 QA·아이 대리 QA 통과와 새 P0·P1·진행 방해 P2 0건을 기록한다.

## 완료 정의

대표 lesson 하나의 `쓰기 전` 녹음 lifecycle과 no-backup 파일 격리가 자동·실기기·화면 근거로 통과하면 루프 185를 완료한다. 다음에는 같은 저장 계약을 현재 lesson 전체에 확장하거나 `정답 후` 녹음을 가장 앞선 미구현 단일 제품 루프로 준비한다.
완료 체크포인트만 `git push origin HEAD`로 일반 push하며 실패한 반복은 커밋하거나 push하지 않는다.
