# 루프 목표 057 — 성공 보상 중 Activity 재생성 QA 탐색

## 목표

성공 자동차 이동의 시작·이동 중·완료 각 시점에서 Activity 재생성이 발생해도 활성 차량, 이동 예약, 보상 offset, 입력 잠금과 다음 입력 준비가 일관되게 유지되는지 좁게 검증하고, 실제 실패가 확인될 때만 최소 상태 보정을 적용한다.

## 성공 조건

1. `START`·`MOVING`·`COMPLETE` 각 시점의 Activity 재생성 전후 활성 차량 종류가 동일하다.
2. 재생성 때문에 자동차가 중복 이동하거나 다음 차량이 조기 순환하지 않는다.
3. 완료 뒤 지우기는 예측 가능한 다음 차량으로 한 번만 바꾸고 시작 위치로 복원한다.
4. 이동 완료 전 재생성·지우기는 현재 차량을 유지하고 다음 정상 성공을 정확히 한 번 재무장한다.
5. 성공 음성·`★ ✓`, `strokeCount=1`의 126 px 한 칸 이동, 이동 중 입력 잠금과 비가림·비상호작용이 유지된다.
6. 자동 상태 회귀, `./scripts/verify.sh`, `git diff --check`를 통과한다.
7. 정확한 2340 × 1080 새 연속 화면·hierarchy와 아이 대리 QA, `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

제품 코드를 먼저 바꾸지 않고 성공 이동의 세 단계에서 Activity 재생성을 각각 유도해 차량 종류·bounds·callback·입력 상태를 수집한다. 실패가 재현될 때만 원인에 맞는 최소 상태 보정을 한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
