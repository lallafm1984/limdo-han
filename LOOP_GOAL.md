# 루프 목표 205 — 자음+모음 조립 첫 좁은 묶음

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 다섯 살 아이가 자음과 모음의 위치 관계를 글 설명 없이 직접 조립해 `가`가 되는 구조를 이해하고 곧바로 같은 글자를 쓰기로 연결할 수 있다.
새로운 근거: 루프 204까지는 이미 완성된 글자의 접근성·쓰기 흐름을 검증했지만, 자음과 모음을 네모 칸에 놓아 음절을 만드는 조립 상호작용은 기존 메뉴·쓰기·접근성과 실질적으로 다른 미구현 M4 사용자 흐름이다.
중복 방지: 이번 루프는 `ㄱ + ㅏ → 가` 한 조립 화면과 쓰기 연결만 다룬다. 다른 모음·초성·받침·교육 범위를 함께 늘리지 않으며 이 첫 흐름이 통과하면 다음 좁은 묶음은 새 루프로 분리한다.

## 목표

기존 production geometry와 디자인 token을 사용해 `ㄱ`과 세로 모음 `ㅏ`를 네모 칸의 올바른 좌우 위치에 놓아 `가`를 완성하고, 완성 결과에서 기존 `가` 쓰기 lesson으로 진입하는 첫 조립 흐름을 만든다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. 홈 또는 `가나다` 선택 흐름에서 첫 조립 화면으로 실제 사용자 입력으로 진입하고 홈·뒤로 복귀가 동작한다.
3. `ㄱ`과 `ㅏ` 조각, 초성 왼쪽 칸과 세로 모음 오른쪽 칸, 조립 전·선택·완성 상태가 글을 읽지 않아도 모양·위치·움직임으로 구분된다.
4. 잘못된 칸·순서·빈 칸은 완성으로 처리하지 않고 처벌 없이 원상태 또는 다음 가능한 위치를 시각적으로 다시 안내한다.
5. 완성한 `가`의 조립 geometry와 기존 production `가` 표시·안내·판정 geometry가 같은 정사각형 em·균일 배율·자모 상대 위치 계약을 사용한다.
6. 완성 뒤 기존 `가` WritingCanvas로 진입하고 1962 × 954 px, 네 그림 조작, 정방향 입력·성공·다음·홈 회귀가 없다.
7. 정확한 2340 × 1080 변경 전·후 PNG·hierarchy·focus·APK SHA와 조립 시작·선택·완성·쓰기 연결 근거를 직접 읽어 잘림·겹침·가림 0건을 확인한다.
8. 새 P0·P1·진행 방해 P2가 0건이고 자동 그래픽 디자인·자동 QA·에뮬레이터·아이 대리 QA가 통과한다.

## 완료 정의

`ㄱ + ㅏ → 가`의 한 조립 흐름과 기존 `가` 쓰기 연결만 새 자동·실화면 근거로 통과하면 완료한다. 다른 모음·초성·받침 확장은 별도 단일 루프로 나누며, 모든 성공 조건과 새 아이 대리 QA가 통과한 완료 체크포인트만 `git push origin HEAD`로 일반 push한다.
