# 루프 목표 006 — 참고 화면 기반 어린이 쓰기 화면 재구성

## 목표

사용자가 제공한 `design/reference-kids-tracing-ui.png`의 장점을 LimDo의 `ㄱ` 따라 쓰기 화면에 적용한다. 글을 읽지 못하지만 한국어를 들을 수 있는 다섯 살 아이가 화면을 보자마자 큰 글자 길, 시작 위치, 진행 방향과 필수 조작을 이해하게 한다.

참고 화면처럼 밝고 따뜻한 노란 배경 위에 중앙의 아주 큰 흰색 `ㄱ` 쓰기 길을 배치하고, 독창적인 도마뱀 안내 캐릭터가 시작점과 진행 방향을 보여 주게 한다. 기존 정확한 손글씨 입력·판정, 로컬 한국어 음성, 다시 듣기와 지우기는 유지한다.

참고 화면의 상표, 로고, 정확한 캐릭터, 과일 그림이나 배치를 복제하지 않는다. 프로젝트용 원본 자산 `limdo_gecko_guide.png`, `limdo_sunny_flower_background.png`를 사용하고 Android 표준 아이콘을 필수 조작에 사용한다.

이 루프는 새 글자, 다음 수업, 저장, 점수, 보상, 서버, 계정, 분석, 광고, 민감 권한, 새 의존성을 추가하지 않는다.

## 성공 조건

1. 정확한 2340 × 1080에서 낮은 대비의 장식이 있는 따뜻한 노란 배경이 화면을 채우고 중앙 쓰기 과제를 방해하지 않는다.
2. 실제 손글씨 입력 영역은 중앙의 가장 큰 요소이며 최소 1170 × 540 px로 기존 최소 높이 378 px보다 넉넉하다.
3. `ㄱ` 목표 길은 노란 배경과 높은 대비의 굵은 흰색 획으로 보이고, 아이가 그린 선은 목표와 구분되는 진한 색으로 계속 보인다.
4. 독창적인 도마뱀 안내 캐릭터와 일치하는 시작점, 오른쪽·아래 방향 단서가 글을 읽지 않고도 한 흐름으로 연결된다.
5. 끝 위치는 캐릭터가 향하는 작은 목표 이미지 또는 명확한 도착 단서로 보이며 색 하나에만 의존하지 않는다.
6. 상단 설명문과 큰 상태 카드는 주 과제와 경쟁하지 않도록 제거하거나 시각적으로 축소한다. 보호자·접근성 서비스를 위한 짧은 한글 설명과 semantics는 유지한다.
7. 다시 듣기와 지우기는 알아보기 쉬운 표준 아이콘, 최소 64 × 64 dp, 12 dp 이상 간격과 분명한 활성·비활성 상태를 갖는다.
8. 아직 사용할 수 없는 다음 동작은 글을 읽지 않아도 약하게 보이거나 숨겨져 핵심 과제와 경쟁하지 않는다. 동작하지 않는 활성 버튼처럼 보이면 안 된다.
9. 초기, 재시도, 성공 상태는 각각 시작 캐릭터·방향 강조, 부드러운 재시도 시각 단서, 큰 성공 단서와 기존 한국어 음성으로 구분된다.
10. 시작점·방향·결과 단서와 손글씨 판정, 다시 듣기, 지우기, 음성 실패 대체 동작을 단위 테스트 또는 검증 가능한 순수 모델로 회귀 방지한다.
11. `./scripts/verify.sh`의 자동화 계약, 단위 테스트, Android lint, debug build가 통과하고 manifest·의존성에 새 권한, 서비스, 원격 기능이 없다.
12. 새 debug APK를 `alarmquest-qa`에 설치해 물리 1080 × 2340, 앱 2340 × 1080에서 cold launch하고 초기·재시도·성공·지우기·다시 듣기 화면과 동작을 새로 검증한다.
13. `design/reference-kids-tracing-ui.png`와 같은 상태의 새 구현 화면을 비교한 `design-qa.md`에서 의도적 LimDo 적용을 제외한 P0·P1·P2 시각 문제를 해결하고 `final result: passed`를 기록한다.
14. 아이 대리 QA에서 문장을 읽지 않고도 쓰기 위치, 시작점, 진행 방향, 성공·재시도, 다시 듣기와 지우기를 식별한다. 실제 아이가 사용하지 않았다면 `실제 아이 관찰: 실행 안 함`으로 기록한다.

## 검증 방법

- 구현 전 `./scripts/check-automation.sh`와 최신 `.loop/user-directives.md`를 확인한다.
- 코드 변경 뒤 `./scripts/verify.sh`와 `git diff --check`를 실행한다.
- 원본 PNG 자산의 크기, alpha, 선명도와 Android 표시 시 crop·늘어짐·배경 이음새를 확인한다.
- `alarmquest-qa`에서 앱 데이터를 지운 cold launch 초기 화면, 잘못된 시작 뒤 재시도, 올바른 `ㄱ` 뒤 성공, 다시 듣기와 지우기를 새로 캡처한다.
- 실제 입력 내부 bounds, 주요 터치 bounds와 간격, focus, app bounds 2340 × 1080, `ROTATION_90`을 측정한다.
- 참고 이미지와 구현 화면을 같은 가로 비율로 한 비교 이미지에 놓고 typography, spacing, color, asset quality, copy와 상태를 검토한다.
- 완료 직전 새 아이 대리 QA로 다음 불편 하나 또는 구체적인 미검증 흐름 하나를 `.loop/qa-findings.md`에 기록한다.

## 완료 정의

새 근거로 14개 조건이 모두 통과해야 완료다. 완료 직전 아이 대리 QA에서 가장 중요한 미해결 불편 하나를 다음 번호 루프로 만들고 `LOOP_GOAL.md`, `.loop/queue.md`, `.loop/state.md`를 갱신한다. 완료 루프와 다음 루프 준비를 로컬 체크포인트 커밋 하나로 만든 뒤 `git push origin HEAD`로 일반 push한다. push가 실패하면 정확한 오류를 기록하고 force push하지 않는다.
