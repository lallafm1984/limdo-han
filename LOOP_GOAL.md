# 루프 목표 145 — 두 번째 받침 없는 글자 확장

## 작업 가치 관문

분류: 사용자 지시
사용자 가치: 아이가 첫 받침 없는 글자 `라·마·바`를 익힌 뒤, 이미 배운 `ㅅ·ㅇ·ㅈ`과 `ㅏ`를 조합한 `사 → 아 → 자`를 따라 쓰며 같은 교육 단계에서 글자 범위를 넓힐 수 있다.
새로운 근거: 루프 144의 새 2340 × 1080 아이 대리 QA에서 `라·마·바`의 5·5·6획 안내·판정·보상과 30개 lesson 순환이 통과했고 새 제품 불편은 없었다. `OPEN_SYLLABLES`의 다음 미구현 좁은 묶음은 이미 배운 다음 자음 `ㅅ·ㅇ·ㅈ`과 `ㅏ`의 조합이다.
중복 방지: 이미 통과한 30개 글자를 재수집하지 않고 새 받침 없는 글자 `사 → 아 → 자`만 조사·구현·검증하며, 다른 조합이나 받침 글자를 함께 추가하지 않는다.

## 목표

`사 → 아 → 자`를 교육용 조합 template, 획순·방향 판정, 현재 획 동적 안내, 한국어 음성, 획수 자동차 보상과 다음 전환에 연결한다. 기준은 정확한 2340 × 1080이다.

## 성공 조건

1. 받침 없는 글자 단계와 `사·아·자`의 획 근거를 확인하고 공식 교육 흐름과 LimDo 제품 template 결정을 문서에서 구분한다.
2. 기존 `라 → 마 → 바` 다음 `사 → 아 → 자`로 이어지며 다른 글자를 추가하지 않는다.
3. 정사각형 em 안의 고유 모양·비율과 guide·판정의 단일 template을 유지한다.
4. 획별 시작·끝·움직이는 방향과 짧은 한국어 음성이 전체 획을 안내한다.
5. 정상 입력은 획순·첫 방향을 판정하고 자동차가 실제 획수만큼 이동하며 글자와 24 dp 이상 떨어진다.
6. 기존 30개 글자의 geometry·판정·음성·보상·다음 흐름을 회귀시키지 않는다.
7. 새 단위 테스트, `./scripts/verify.sh`, `git diff --check`와 정확한 2340 × 1080 초기·입력·보상·전환을 통과한다.
8. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

`사·아·자`의 획 근거를 확인한 뒤 기존 lesson 계약에 추가하는 최소 구현을 시작한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA의 최우선 미해결 불편 또는 다음 미완료 제품 단계 하나만 준비하고, 커밋 후 `git push origin HEAD`로 일반 push한다.
