# 루프 목표 173 — 코덱스 내부 생성 실패·다시 시도 아틀라스 적용

## 작업 가치 관문

분류: 사용자 지시
사용자 가치: 글을 읽지 못하는 다섯 살 아이가 실패를 성공과 즉시 구분하고, 겁먹지 않은 채 어디서 다시 시작할지 모양과 움직임으로 이해하게 한다.
새로운 근거: 루프 172에서 성공 별빛·색종이 투명 아틀라스는 통과했지만 실패 피드백은 상단 중앙의 시스템 문자 `↻`에 의존해 새 성공 그림체와 일치하지 않고 기기 글꼴에 따라 모양이 달라진다.
중복 방지: 이번 루프는 실패·다시 시도 자산과 연출만 적용한다. 성공 아틀라스·글자 geometry·판정·버튼은 보존하고, 통과 뒤 버튼 UI 개선을 별도 다음 루프로 준비한다.

## 목표

1. 외부 API 없이 코덱스 내부 이미지 생성기로 실패·다시 시도를 뜻하는 둥근 되돌림 화살표와 부드러운 재도전 반짝임을 실제 투명 배경으로 생성한다.
2. 실패 전용 RGBA 아틀라스의 셀 크기·슬롯·alpha·표시 크기를 한글 문서와 자동 검사로 고정한다.
3. 기존 `↻` 문자를 새 자산과 짧은 재도전 연출로 교체하고, 기존 쓰기판 흔들림·현재 시작점 재강조와 같은 시간축에서 한 번만 재생한다.
4. 자동 디자인 역할은 성공과 구분되는 형태·색·같은 그림체를, 연출 역할은 아이를 위협하지 않는 유한 움직임과 비가림을, QA 역할은 자동·에뮬레이터·아이 대리 관문을 독립 판정한다.

## 성공 조건

1. 최종 실패 아틀라스는 RGBA PNG이고 셀 네 모서리 alpha가 0이며 유효 픽셀이 셀 경계를 넘지 않는다.
2. 생성 prompt·원본·앱 파일·슬롯 좌표가 `docs/시각-아틀라스-루프-기획.md`에 한글로 기록된다.
3. 실패 자산은 주황 계열 되돌림 형태와 부드러운 반짝임으로 노란 성공 별빛·색종이와 글 없이 구분된다.
4. 실패 연출은 1.5초 안에 끝나며 글자 길·현재 시작점·홈·지우기·다음 조작을 가리지 않고 다음은 계속 비활성이다.
5. 기존 `↻` 문자는 production에서 제거되고 접근성 설명과 재시도 시작점 재강조는 유지된다.
6. `./scripts/verify.sh`, `git diff --check`, 정확한 2340 × 1080 에뮬레이터 실패 시작·중간·끝·재입력 화면, focus·치명 오류 검사가 통과한다.
7. 자동·에뮬레이터·아이 대리 QA와 실제 아이 관찰 여부를 분리 기록한다.

## 완료 정의

모든 성공 조건을 통과하면 루프 173을 완료하고 홈·지우기·다음 버튼 UI 아틀라스 개선을 다음 단일 루프로 준비한다. 완료 체크포인트는 `git push origin HEAD`로 일반 push한다.
