# 루프 목표 123 — `가` 3획 음성 안내 일치

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 한글을 읽지 못하지만 한국어를 듣는 아이가 다시 듣기만으로도 `가`의 세 획 순서와 방향을 예측할 수 있다.
새로운 근거: 루프 122의 2340 × 1080 `가` 3획 화면와 달리 `SpokenCue.INITIAL`은 `오른쪽, 아래`로 첫 획만 설명해 2·3획을 누락한다.
중복 방지: 초기·다시 듣기·재시도 음성이 같은 3획 순서를 말하고 시각 시범과 일치하면 종료한다. 재생 횟수를 늘리는 루프로 전환하지 않는다.

## 목표

production `가` 3획의 순서·방향과 초기·다시 듣기·재시도 한국어 음성을 일치시킨다.

## 성공 조건

1. `SpokenCue.INITIAL`은 1획 오른쪽→아래쪽, 2획 아래쪽, 3획 오른쪽을 짧고 구체적인 한국어로 안내한다.
2. 첫 자동 안내와 다시 듣기가 같은 production cue를 사용한다.
3. 잘못된 2·3획 재시도 안내는 현재 획에 맞는 방향을 말하고 시각 안내와 모순되지 않는다.
4. 음성 재생 실패 대체·재생·지우기 callback을 회귀시키지 않는다.
5. 정확한 2340 × 1080에서 음성 callback과 3획 시범의 일치를 새로 확인한다.
6. 루프 122의 3획 판정·3칸 보상·입력 잠금·지우기를 회귀시키지 않는다.
7. `./scripts/verify.sh`, `./scripts/check-automation.sh`, `git diff --check`를 통과한다.
8. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

`SpokenCueModel`과 재시도 callback의 획 정보 전달 경계를 조사하고 production `ga` 획 순서에 맞는 최소 음성 모델·테스트를 추가한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비하고, 검증된 완료·준비 변경을 커밋 하나로 만든 뒤 `git push origin HEAD`로 일반 push한다.
