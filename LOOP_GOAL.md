# 루프 목표 034 — 동일 표본 수의 구간별 밀집 분포 QA

## 목표

루프 033에서 MOVE 3·7·18개와 무관하게 유지된 155 px `SUCCESS`와 156 px `RETRY_DIRECTION` 경계가 같은 MOVE 표본 수와 비슷한 총 입력 시간에서 중간 표본이 균등·꼭지 밀집·끝점 밀집으로 배치될 때도 유지되는지 정확한 2340 × 1080에서 탐색한다. 문제 입증 전에 제품 로직을 바꾸지 않는다.

## 성공 조건

1. `alarmquest-qa`의 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`에서 새 debug APK를 검증한다.
2. Canvas 1962 × 775 px와 정사각형 `ㄱ` geometry를 유지한다.
3. 같은 시작·끝·되돌림 좌표, MOVE 표본 수와 비슷한 총 입력 시간에서 155 px와 156 px를 중간 표본의 균등·꼭지 밀집·끝점 밀집 분포로 각각 최소 1회 놓는다.
4. 각 입력의 전체 좌표·MOVE 표본 수·명령 간격·실측 시점, 결과·버튼 화면과 음성 callback 순서를 기록한다.
5. 중간 MOVE 표본의 구간별 밀집 분포와 무관하게 155 px는 `SUCCESS`, 156 px는 `RETRY_DIRECTION`인지 아이 대리 QA한다.
6. 문제가 입증되면 최소 보정과 단위 테스트만 적용하고, 아니면 제품 코드를 바꾸지 않는다.
7. 코드를 바꾸면 `./scripts/verify.sh`와 `git diff --check`가 통과한다.
8. `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

155 px와 156 px를 같은 MOVE 표본 수와 비슷한 총 입력 시간의 균등·꼭지 밀집·끝점 밀집 분포로 각각 재현한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
