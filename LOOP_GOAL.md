# 루프 목표 044 — 성공 callback 즉시 지우기 뒤 재입력 회복 QA

## 목표

정확한 2340 × 1080에서 정상 획의 `SUCCESS` 재생 시작 callback 직후 추가 지연 없이 지우기를 전달해 초기 상태로 수렴한 다음, 앱 데이터 재초기화나 추가 지우기 없이 정상 중심선 획을 다시 입력해 한 번의 `SUCCESS`로 회복하는지 먼저 확인한다. 문제가 입증될 때만 초기화 뒤 입력·음성 상태를 최소 보정한다.

## 성공 조건

1. 정확한 에뮬레이터에서 앱 데이터를 초기화하고 정상 중심선 입력의 `SUCCESS` 시작 callback 직후 추가 지연 없이 지우기를 한 번 전달해 초기 상태 수렴을 새로 확인한다.
2. 초기화 수렴 뒤 앱 데이터 재초기화·추가 지우기 없이 같은 정상 중심선 획을 다시 입력하고 전체 좌표·시점을 기록한다.
3. 두 번째 입력이 굵은 완료 획, `★ ✓`, 한 번의 `SUCCESS` 시작→완료 callback으로 끝나는지 확인한다.
4. 첫 취소된 `SUCCESS`의 늦은 완료 callback이 두 번째 입력의 음성·성공 화면을 덮거나 중복시키지 않는지 두 번째 성공 완료 뒤 3초 이상 확인한다.
5. Canvas 1962 × 775 px, 주요 버튼 373 × 168 px와 간격 37 px를 유지한다.
6. 제품 문제가 없으면 코드를 바꾸지 않고 QA 근거만 남긴다.
7. `./scripts/verify.sh`와 `git diff --check`가 통과하고 새 아이 대리 QA 및 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

앱 데이터 초기화와 초기 안내 완료 뒤 첫 정상 입력의 `SUCCESS` 시작 callback을 감시해 지연 없이 지우고, 초기 상태 수렴을 확인한 직후 두 번째 정상 중심선 입력을 연속 재현한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
