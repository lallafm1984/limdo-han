# 루프 목표 177 — 적용 음성 제거와 실제 휴대폰 재설치

## 작업 가치 관문

분류: 사용자 지시
사용자 가치: 마음에 들지 않는 안내 음성이 학습을 방해하지 않게 하고, 아이가 기존의 명확한 점선·시작점·동적 방향 표식만으로 계속 사용할 수 있게 한다.
새로운 근거: 루프 176에서 적용한 로컬 영웅 음성을 실제 휴대폰에서 들은 사용자가 제거와 새 무선 빌드를 직접 요청했다.
중복 방지: 루프 176에서 추가한 음성 자산·재생기·production 연결·전용 검사만 제거한다. 시각 아틀라스·글자 geometry·획 시범·입력 판정·버튼 동작은 변경하지 않는다.

## 목표

1. 루프 176에서 추가한 로컬 음성 파일 6개와 `LocalHeroVoice` 재생기·매핑을 제거한다.
2. 메뉴·쓰기 시작·재시도·성공·다음·홈 전환의 음성 호출을 production에서 제거한다.
3. 무음 동적 획 시범·현재 획 점선·시작·끝 표식·성공·실패 아틀라스와 입력 판정을 유지한다.
4. 새 debug APK를 실제 `SM-S931N`에 무선 덮어쓰기 설치하고 실행 상태를 확인한다.

## 성공 조건

1. `./scripts/verify.sh`와 `git diff --check`가 통과한다.
2. production과 최종 APK에 `HeroVoiceCue`, `LocalHeroVoice`, `LimDoVoice`, `MediaPlayer`, `hero_*.wav`가 남지 않는다.
3. 정확한 2340 × 1080에서 현재 획 점선·동적 방향 표식·성공·실패·버튼 상태가 기존과 동일하게 동작한다.
4. `SM-S931N`에 최신 APK가 `-r`로 설치되고 `com.example.limdo/.MainActivity`가 실행·포커스되며 치명 오류가 없다.
5. 실제 휴대폰에서 메뉴 진입·쓰기 시작·재시도 동안 음성 재생 로그가 없고 시각 시범과 입력이 계속 동작한다.
6. 자동·실제 휴대폰·아이 대리 QA와 실제 아이 관찰 여부를 분리 기록한다.

## 완료 정의

음성 자산·코드·검사를 완전히 제거하고 자동 검증과 실제 `SM-S931N` 무선 설치·무음 흐름·아이 대리 QA에서 새 P0·P1 결함이 없으면 루프 177을 완료한다. 활성 큐를 `없음`으로 전환한 뒤 완료 체크포인트를 만들고 `git push origin HEAD`로 일반 push한다.
