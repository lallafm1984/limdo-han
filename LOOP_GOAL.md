# 루프 목표 153 — 세 번째 받침 글자 묶음 `갓 → 강 → 갖`

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 아이가 이미 배운 `가` 짜임에 다음 기본 종성 `ㅅ·ㅇ·ㅈ`을 차례로 결합해 받침 있는 글자 학습을 실제로 확장한다.
새로운 근거: 루프 152의 새 2340 × 1080 아이 대리 QA에서 조사 결함 수정과 기존 입력 흐름이 통과했고 새 미해결 불편은 없었다. 최신 교육 단계 지시의 `받침 있는 글자`는 현재 종성 `ㄱ·ㄴ·ㄷ·ㄹ·ㅁ·ㅂ`까지만 production에 있어 다음 기본 종성 흐름 `ㅅ·ㅇ·ㅈ`은 아직 표시·안내·판정·음성·보상 근거가 없다.
중복 방지: 기존 45개 lesson이나 조사 선택을 다시 수정하지 않는다. 새 글자 `갓·강·갖`만 production에 추가하고 각 고유 종성 geometry와 전체 순환을 검증하면 완료한다. 동일 근거를 재수집하는 후속 루프를 만들지 않는다.

## 목표

받침 있는 글자 단계에 `갓 → 강 → 갖`을 좁은 한 묶음으로 추가한다.

## 성공 조건

1. production lesson 순서가 `갈 → 감 → 갑 → 갓 → 강 → 갖 → ㄱ`으로 이어진다.
2. `갓·강·갖`은 상단 `가`와 하단 `ㅅ·ㅇ·ㅈ`의 고유 모양을 정사각형 em 안에서 유지하며 표시·동적 안내·판정이 같은 template을 사용한다.
3. 각 lesson의 정상 획순·방향은 성공하고 반대 방향 또는 순서 오류는 거부된다.
4. 획별 짧은 한국어 안내, 글자별 성공 음성, 실제 `strokeCount`만큼의 자동차 보상과 중복 callback 방지가 기존 공통 계약에 연결된다.
5. 전체 lesson 회귀 단위 테스트, `./scripts/verify.sh`, `git diff --check`가 통과한다.
6. 정확한 2340 × 1080에서 새 세 글자의 초기·입력·성공·보상·`갖 → ㄱ` 전환, WritingCanvas 최소 1872 × 648 px와 좌우 조작 열 비가림을 확인한다.
7. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

기존 `가`와 자모 `ㅅ·ㅇ·ㅈ` template을 받침 글자 비율로 조합한 최소 production lesson·테스트를 추가한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비한다. 새 불편이 없으면 최신 사용자 지시와 교육 단계의 다음 미완료 제품 작업을 선택하며, 둘 다 없으면 큐를 `없음`으로 만들고 `IDLE`로 종료한다. 완료 커밋 뒤 `git push origin HEAD`로 일반 push한다.
