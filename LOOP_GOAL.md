# 루프 목표 190 — 현재 38개 lesson의 두 보호자 녹음 칸 연결

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 보호자가 아이에게 노출되는 모든 자음·모음·글자마다 `쓰기 전`과 `정답 후` 목소리를 독립적으로 녹음해 사용할 수 있게 한다.
새로운 근거: 루프 184~188은 대표 `ㄱ`의 두 event와 lifecycle만 검증했으며, M1 완료 관문이 요구하는 현재 노출 38개 lesson 전체 연결은 아직 구현되지 않았다.
중복 방지: 보호자 목록의 38개 lesson 두 칸과 안정적인 파일 mapping만 연결하고 아이 화면 재생·자동 다음·목록 편집은 M2·M3 후속 루프로 분리한다.

## 목표

1. 보호자 목록의 자음 14개·기본 모음 10개·`가~하` 14개 모두에서 `쓰기 전`·`정답 후` 녹음 상세에 진입할 수 있게 한다.
2. 각 칸을 안정적인 `lessonId`와 `START`·`SUCCESS` 파일에 독립 연결하고 다른 lesson·event 파일을 덮어쓰지 않게 한다.
3. 기존 권한 시점·8초 상한·원자 저장·손상 파일 무음 복귀·background 해제·no-backup 계약을 보존한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. 38개 lesson × 두 event의 경로가 중복·누락 없이 자동 검사되고 대표 첫·중간·마지막 lesson의 녹음·재생·삭제가 다른 파일을 보존한다.
3. 정확한 2340 × 1080 실제 기기에서 자음·모음·글자 대표 lesson의 두 칸 진입과 상태·focus·64 dp 동작·잘림 0건을 확인한다.
4. 인터넷·외부 저장·backup·아이 화면 마이크 요청은 0건이고 새 P0·P1·진행 방해 P2가 0건이다.

## 완료 정의

현재 노출 38개 lesson의 두 녹음 칸이 로컬 파일과 정확히 연결되고 자동·실제 기기·아이 대리 QA 회귀가 없을 때 완료한다. 다음에는 M2의 쓰기 전 보호자 녹음 1회 재생과 무음 대안을 준비한다.
완료 체크포인트만 `git push origin HEAD`로 일반 push하며 실패한 반복은 커밋하거나 push하지 않는다.
