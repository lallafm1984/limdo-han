# 루프 목표 091 — 스물일곱 번째 안내 완료 뒤 지우기·스물여덟 번째 다시 듣기 탐색

## 목표

루프 090의 복원·취소·스물일곱 번째 `INITIAL` 완료 흐름 뒤 다시 지우고, 스물여덟 번째 다시 듣기가 새 `INITIAL`을 정확히 한 번 시작·완료하며 후속 성공을 유지하는지 좁게 검증한다.

## 성공 조건

1. 루프 090의 saved-state·PID 교체·복원 취소·첫 재무장 취소·두~스물일곱 번째 `INITIAL` 완료 후 지우기·스물여덟 번째 다시 듣기 순서를 확정한다.
2. 복원 `INITIAL`은 0회, 첫 재무장은 시작 1회·완료 0회, 두~스물여덟 번째는 시작·완료 각 1회이고 느지막 callback이 없다.
3. 모든 지우기와 안내 전후 소방차는 `[99,62][435,288]`, 빈 획·무표식·입력 가능 상태를 유지한다.
4. 이어진 첫 정상 입력은 `SUCCESS` 시작·완료 각 1회와 소방차 126 px 한 칸 이동만 만든다.
5. 지우기·다시 듣기·비활성 다음·차량 비가림·비상호작과 음성 callback이 회귀하지 않는다.
6. 제품 변경이 있으면 자동 회귀를 추가하고 `./scripts/verify.sh`, `git diff --check`를 통과한다.
7. 정확한 2340 × 1080 화면·hierarchy·PID·saved-state·callback과 아이 대리 QA, `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

제품 코드를 먼저 바꾸지 않고 루프 090 흐름 뒤 지우기·스물여덟 번째 다시 듣기와 후속 정상 입력을 수집한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
