# 루프 목표 149 — 받침 없는 글자 `파 → 하` 확장

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 아이가 이미 익힌 `ㅍ·ㅎ`과 `ㅏ`를 `파·하`로 조합해 자모와 음절의 연결을 배운다.
새로운 근거: 루프 148 최종 아이 대리 QA에서 새 미해결 불편을 발견하지 못했다. 최신 사용자 지시의 교육부 흐름에서 기본 자음 중 아직 `ㅏ`와 조합하지 않은 `ㅍ·ㅎ`의 첫 받침 없는 글자가 남아 있다.
중복 방지: 루프 148의 레이아웃·버튼·캡처를 다시 수정하지 않는다. `파·하` 두 lesson의 production template·획순·판정·음성·보상·순환을 한 번 일치시켜 검증하면 완료하고 횟수만 늘려 재검증하지 않는다.

## 목표

기존 `타` 뒤에 `파 → 하`를 추가하고, 교정된 `ㅍ·ㅎ`의 고유 모양과 `ㅏ`의 상대 배치를 정사각형 em 안에서 보존한다.

## 성공 조건

1. production 교육 순서가 `… → 차 → 카 → 타 → 파 → 하 → ㄱ`로 이어진다.
2. `파·하`는 교정된 `ㅍ·ㅎ`와 `ㅏ`를 정사각형 em 안에 표준 상대 위치·비율로 배치한다.
3. 표시·입력 판정·획별 동적 안내가 같은 template과 균일 x/y 배율을 사용한다.
4. 정상 획순·방향은 성공하고 반대 방향·잘못된 시작·미완성 입력은 기존 판정 계약으로 구분된다.
5. 초기·획별·성공 한국어 음성과 자동차 보상 칸이 각 lesson의 `strokeCount`와 일치한다.
6. 기존 37개 lesson과 루프 148의 1962 × 954 px WritingCanvas·좌우 조작 열·음성 callback이 회귀하지 않는다.
7. 새 curriculum 회귀 테스트, `./scripts/verify.sh`, `git diff --check`와 정확한 2340 × 1080의 `파·하` 초기·입력·성공·` 하 → ㄱ` 전환 화면을 통과한다.
8. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

`KoreanCurriculum`의 조합 글자 template과 `ㅍ·ㅎ·ㅏ` geometry를 대조해 `파·하` 두 lesson을 추가하는 최소 제품 변경과 회귀 테스트를 구현한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비한다. 새 불편과 미완료 사용자 제품 지시가 없으면 큐를 `없음`으로 만들고 `IDLE`로 종료한다. 완료 커밋 뒤 `git push origin HEAD`로 일반 push한다.
