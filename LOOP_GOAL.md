# 루프 목표 165 — 기본 자음·모음 참고 자형·획순·방향 전체 감사

## 작업 가치 관문

분류: 새 사용자 위험
사용자 가치: 아이가 기본 자음·모음 전체를 참고 교육 자료와 같은 자형·획순·방향으로 보고 따라 쓴다.
새로운 근거: 루프 162~164는 사용자가 지적한 `ㄹ·ㅅ·ㅈ·ㅊ·ㅍ`과 조합 글자만 교정했으며, 최신 지시는 기본 자음 14개·기본 모음 10개 전체의 명시적 감사를 별도로 요구한다.
중복 방지: 24개의 명시적 참고 행렬과 production 계약을 한 번 대조하고, 발견된 가장 중요한 미일치 하나만 교정하거나 미일치 없음을 입증한 뒤 같은 행렬의 횟수만 늘려 다시 감사하지 않는다.

## 목표

자음 참고자료와 모음 참고자료를 기준으로 기본 자음 14개·기본 모음 10개의 표시·동적 안내·음성·입력 판정·획수 보상이 하나의 교육용 template·순서·방향 계약을 공유하는지 전체 감사한다.

## 성공 조건

1. `ㄱ·ㄴ·ㄷ·ㄹ·ㅁ·ㅂ·ㅅ·ㅇ·ㅈ·ㅊ·ㅋ·ㅌ·ㅍ·ㅎ`의 획 분리·순서·방향이 자음 참고자료와 일치한다.
2. `ㅏ·ㅑ·ㅓ·ㅕ·ㅗ·ㅛ·ㅜ·ㅠ·ㅡ·ㅣ`의 획 분리·순서·방향이 모음 참고자료와 일치한다.
3. 24개 lesson의 표시 geometry, `strokeDirections`, 초기 음성, production 정방향 수락·역방향/잘못된 순서 거부, 획수 보상이 명시적 참고 행렬과 일치한다.
4. 감사에서 불일치가 발견되면 아이 학습에 가장 큰 영향을 주는 하나만 최소 변경으로 교정하고 나머지는 구체적 근거와 함께 다음 루프로 준비한다. 불일치가 없으면 전체 관문 통과를 기록한다.
5. 대표 첫·중간·마지막 자모와 불일치 교정 자모를 정확한 2340 × 1080에서 직접 읽고 LimDo focus, WritingCanvas 1962 × 954 px, 정방향·거부·조작·장식 비가림을 통과한다.
6. `./scripts/verify.sh`, `git diff --check`, 새 아이 대리 QA가 통과하고 P0·P1 미해결 결함이 없으며 `실제 아이 관찰: 실행 안 함`을 분리 기록한다.

## 완료 정의

모든 성공 조건을 새 자동·에뮬레이터 근거로 통과한다. 미일치가 남으면 가장 중요한 하나를 다음 결함 루프로 준비하고, 모든 참고 계약이 통과하면 1차 목표 최종 전체 검증을 다음 루프로 준비한다. 루프 165 완료와 다음 루프 준비를 체크포인트 커밋 하나로 만들고 `git push origin HEAD`로 일반 push한다. 루프 165의 구현은 다음 새 세션이 수행한다.
