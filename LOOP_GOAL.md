# 루프 목표 124 — `가` 3획 음성·시각 시범 동기화

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 한글을 읽지 못하는 아이가 음성으로 듣는 현재 획과 화면에서 움직이는 현재 획을 같은 지시로 이해할 수 있다.
새로운 근거: 루프 123 새 APK에서 `INITIAL`은 약 13.37초지만 시각 시범은 3초 주기로 약 4.5회 반복되어 음성과 화살표 획이 엇갈린다.
중복 방지: 초기·다시 듣기의 1·2·3획 음성 구간에 해당 획 시범만 일치하고 완료·실패·지우기 복귀를 통과하면 종료한다. 재생 횟수를 늘리는 루프로 전환하지 않는다.

## 목표

production `가` 3획 음성 재생 구간과 동적 시각 시범의 현재 획을 일치시킨다.

## 성공 조건

1. 초기·다시 듣기 재생 중 1획 음성에는 1획 오른쪽→아래쪽 시범만 보인다.
2. 2획 음성에는 2획 아래쪽, 3획 음성에는 3획 오른쪽 시범만 보인다.
3. 재생 완료·실패·지우기·백그라운드 복원에서 시범과 음성이 엇갈리거나 중복되지 않는다.
4. 입력 중 현재 획 안내, 3획 판정, 3칸 보상, 입력 잠금, 지우기 callback을 회귀시키지 않는다.
5. 정확한 2340 × 1080에서 음성 callback과 획별 연속 프레임의 시간 일치를 새로 확인한다.
6. `./scripts/verify.sh`, `./scripts/check-automation.sh`, `git diff --check`를 통과한다.
7. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

`LocalKoreanSpeech`의 utterance 단위 callback과 `WritingCanvas` 시범 progress 경계를 조사하고, 3획을 각각 재생·시범하는 최소 상태 모델 가설을 세운다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비하고, 검증된 완료·준비 변경을 커밋 하나로 만든 뒤 `git push origin HEAD`로 일반 push한다.
