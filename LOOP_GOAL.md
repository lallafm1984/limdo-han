# 루프 목표 125 — 성공 뒤 아이가 이해할 수 있는 `다음` 진행

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 한글을 읽지 못하는 아이가 `가`를 완성한 뒤 글자 문구를 읽거나 성공한 획을 지우는 행동에 의존하지 않고, 활성화된 모양과 위치로 다음 쓰기를 시작할 수 있다.
새로운 근거: 루프 124 최종 새 APK의 정확한 2340 × 1080 성공 화면에서 `★  ✓`와 차량 3칸 보상 뒤에도 `다음`은 회색 `준비 중` 비활성으로 남고, 다음 입력과 차량 순환은 `지우기`에만 연결되어 있다.
중복 방지: 성공 완료 뒤 `다음` 한 번이 새 입력과 다음 차량을 준비하고, 실패·보상 이동 중에는 비활성이며 `지우기`는 현재 차량을 유지하는 초기화로 구분되면 종료한다. 같은 성공 화면을 횟수만 늘려 재검증하지 않는다.

## 목표

성공 뒤 `다음`을 아이가 알아볼 수 있는 활성 동작으로 전환하고, `지우기`와 다음 입력의 의미를 분리한다.

## 성공 조건

1. `다음`은 `SUCCESS`와 3칸 보상 이동 완료 뒤에만 활성화되고, 성공 전·실패·이동 중에는 글을 읽지 않아도 비활성임이 보인다.
2. 활성 `다음`은 최소 64 × 64 dp이고 한 번 누르면 입력·결과·보상 위치를 초기화하며 차량을 정확히 다음 순서로 바꾼다.
3. `지우기`는 성공·실패 여부와 관계없이 현재 차량을 유지하며 현재 쓰기만 되돌린다.
4. `다음`·`지우기`의 중복 callback과 이동 중 입력을 막고, 다시 듣기·3획 판정·3칸 보상을 회귀시키지 않는다.
5. 정확한 2340 × 1080에서 성공 전·이동 중·이동 완료·다음 입력 화면과 hierarchy를 새로 확인한다.
6. `./scripts/verify.sh`, `./scripts/check-automation.sh`, `git diff --check`를 통과한다.
7. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

현재 `ActionPlaceholder`와 `onClear`에 섞인 `VehicleCarouselState.prepareNextInput`을 조사하고, 성공 완료 뒤 활성 `다음`과 현재 차량 유지 `지우기`를 분리하는 최소 상태 전이 가설을 세운다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비하고, 검증된 완료·준비 변경을 커밋 하나로 만든 뒤 `git push origin HEAD`로 일반 push한다.
