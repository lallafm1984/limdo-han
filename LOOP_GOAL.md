# 루프 목표 155 — `자음·모음·가나다` 세 기본 메뉴와 학습 진입

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 글을 읽지 못하는 다섯 살 아이가 첫 화면에서 세 학습 종류를 큰 대표 글자·고유 아이콘·위치·음성으로 구분하고 원하는 수업에 직접 들어갈 수 있다.
새로운 근거: 루프 154까지 쓰기 수업은 순차 진행만 가능하며, 1차 기획서가 요구하는 `자음·모음·가나다` 세 독립 진입 경로는 아직 없다.
중복 방지: 이번 루프는 홈의 세 메뉴, 좁은 글자 선택, 쓰기 진입과 홈 복귀만 구현한다. 디자인 토큰 통일, 화려한 연출, 독립 QA 역할과 8개 아이 대리 시뮬레이션은 후속 루프로 남긴다.

## 목표

앱 시작 시 교육 순서대로 `자음·모음·가나다` 세 큰 메뉴를 보여 주고, 각 메뉴에서 현재 검증된 글자를 골라 기존 큰 쓰기 수업으로 진입하며 홈으로 안전하게 돌아올 수 있게 한다.

## 성공 조건

1. 앱 시작과 재실행의 안전한 기본 진입점은 세 메뉴 홈이다.
2. 세 메뉴가 `자음 → 모음 → 가나다` 순서의 서로 겹치지 않는 큰 카드이며 대표 글자·서로 다른 아이콘·위치와 짧은 한글 접근성 문구를 가진다.
3. 글을 읽지 않아도 카드 모양·아이콘·선택 상태와 검증된 한국어 음성으로 세 종류를 구분할 수 있고, 음성 실패 때도 시각 단서만으로 선택 가능하다.
4. 각 메뉴는 현재 curriculum의 올바른 글자 선택으로 연결되고 글자 선택은 해당 `LessonSpec`의 기존 쓰기 화면을 연다.
5. 쓰기 화면에서 홈 복귀가 항상 가능하고 입력 중·성공·재시도 상태에서 복귀해도 중복 보상이나 잘못된 lesson 상태가 남지 않는다.
6. 시스템 뒤로 가기는 쓰기 → 글자 선택 → 홈 순서로 이동하며 앱 재실행은 홈에서 시작한다.
7. 주요 터치 영역은 최소 64 × 64 dp, 인접 간격은 최소 12 dp이고 정확한 2340 × 1080에서 겹침·잘림이 없다.
8. 기존 WritingCanvas 1962 × 954 px 이상, 정사각형 em, 획순·방향·음성·판정·차량 보상이 회귀하지 않는다.
9. 메뉴 연결·선택·복귀 상태 테스트와 `./scripts/verify.sh`, `git diff --check`가 통과한다.
10. 정확한 2340 × 1080에서 홈, 자음·모음·가나다 선택, 각 대표 쓰기 진입과 홈 복귀를 새 PNG·hierarchy·focus로 확인한다.
11. 최종 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 루프 경계

통과하면 기획서 순서에 따라 홈·선택·쓰기·성공 화면의 디자인 토큰과 자동차 중심 시각 체계를 통일하는 별도 제품 루프를 준비한다.

## 완료 정의

모든 조건을 새 자동·에뮬레이터·화면 근거로 통과하고 새 아이 대리 QA를 기록한다. 발견한 가장 중요한 새 불편 하나가 있으면 다음 결함 루프로, 없으면 기획서의 다음 미충족 제품 단계로 전환한다. 완료와 다음 루프 준비를 체크포인트 커밋 하나로 만들고 `git push origin HEAD`로 일반 push한다.
