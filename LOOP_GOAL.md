# 루프 목표 169 — 기본 자음 광학 비율·자동차 제거·동적 표식 교정

## 작업 가치 관문

분류: 사용자 지시
사용자 가치: 다섯 살 아이가 기본 자음을 서로 같은 크기 체계의 글자로 인식하고, 학습 글자를 가리거나 주의를 빼앗는 자동차 없이 현재 획과 성공·다음 동작에 집중하게 한다.
새로운 근거: 사용자가 실제 휴대폰 플레이에서 `ㅈ·ㅊ`의 윗가로가 길고 아랫다리가 짧아 보이며, `ㄹ`의 아랫가로 끝이 윗가로와 맞지 않는다고 새로 지적했다. 새 2340 × 1080 캡처에서도 `ㄹ`의 윗부분이 아랫부분보다 오른쪽으로 75 px 이동해 보이고, `ㅈ·ㅊ`의 아랫부분이 윗가로보다 오른쪽에서 약 52 px 짧게 끝나는 현상을 확인했다.
중복 방지: 기본 자음이 같은 정사각형 em·굵기·광학 중심 계약을 공유하고, `ㄹ`과 `ㅈ·ㅊ·자·차`의 외곽이 정렬되며 production 화면과 보상 흐름에서 자동차가 모두 사라지면 같은 원인의 좌표·차량 제거 반복을 만들지 않는다.

## 목표

1. 교육부·국립국어원 자료가 정하는 교육 흐름과 자모 순서, 글꼴 기술의 정사각형 em·광학 중심 원칙, LimDo가 정해야 하는 수치 보정값을 분리해 한글 문서로 남긴다.
2. 기본 자음 14개가 같은 정사각형 em과 굵기를 쓰고 광학 중심·점유 크기가 공통 범위 안에 드는 자동 계약을 추가한다.
3. `ㄹ·라`의 윗가로와 아랫가로 중심점·보이는 외곽 끝을 좌우 모두 맞춘다.
4. `ㅈ·ㅊ·자·차`는 윗가로 양끝과 아랫다리 양끝을 같은 좌우 기준에 놓고 접합부를 높여 아랫다리가 짧아 보이지 않게 한다. 표시·점선·동적 시범·입력 판정은 같은 production geometry를 공유한다.
5. 홈·메뉴 전환·선택·쓰기·성공에서 자동차 표시와 차량 이동 보상 상태를 제거하고, 성공 즉시 `다음`을 활성화한다.
6. 원형 점선을 따라가는 동적 표식을 작은 단일 원과 내부 방향표로 단순화하고 시작·끝 표식과 겹치지 않게 한다.

## 성공 조건

1. 새 조사 문서가 공식 자료와 LimDo 설계 결정을 명확히 구분하고, 공식 자료가 특정 0~1 좌표를 규정한다고 과장하지 않는다.
2. 기본 자음 14개가 동일한 `emSize`와 `strokeWidth`를 사용하고, 정규화 중심과 점유 범위 자동 검사를 통과한다.
3. `ㄹ·라`는 윗가로 시작·오른쪽 꺾임과 아랫가로의 왼쪽 꺾임·끝점이 각각 같은 x축 기준을 사용하며, 둥근 선끝을 포함한 실제 화면 외곽도 맞는다.
4. `ㅈ·자`와 `ㅊ·차` 몸체의 아랫다리 끝 x좌표가 윗가로 좌우 끝과 각각 같고, 긴 가로 오른쪽 끝·중앙 접합부·왼쪽 아래 끝이 한 직선에 있으며 양쪽 다리 길이가 같으면서 기존 획순·정역방향 판정이 통과한다.
5. `./scripts/verify.sh`, `git diff --check`, `emulator-5554` 설치·실행·포커스·치명 오류 검사와 정확한 2340 × 1080 `ㄹ·ㅈ·ㅊ` 새 화면 직접 비교가 통과한다. 최신 사용자 지시에 따라 이번 루프의 화면 판정은 실제 휴대폰이 아니라 에뮬레이터에서 수행한다.
6. 자동·에뮬레이터·아이 대리 QA와 실제 아이 관찰 여부를 분리 기록한다.
7. production Kotlin·resource 접근성 문구와 정확한 2340 × 1080 홈·선택·쓰기·성공 화면에 자동차가 남지 않고, 성공 별빛·체크와 `다음` 흐름은 유지된다.
8. 동적 표식 지름은 안내 획 굵기의 28% 이하이고 현재 획의 8~92% 사이만 이동하며, 정확한 2340 × 1080 화면에서 점선 중심·진행 방향과 일치하고 고정 표식을 가리지 않는다.

## 완료 정의

모든 성공 조건을 통과하면 루프 169를 완료하고 새 아이 대리 QA에서 서로 다른 미해결 제품 결함이 있을 때만 다음 루프를 준비한다. 새 결함과 미완료 1차 목표가 없으면 큐를 `없음`, 상태를 `IDLE`로 바꾸고 완료 체크포인트를 `git push origin HEAD`로 일반 push한다.
