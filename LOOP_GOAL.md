# 루프 목표 178 — 쓰기 화면 네 조작 상시 활성과 전체 화면 판정 피드백

## 작업 가치 관문

분류: 사용자 지시
사용자 가치: 아이가 성공 여부와 관계없이 네 조작으로 자유롭게 탐색하고, 판정 결과를 작은 우측 표식이나 상실감을 주는 `X`가 아니라 쓰기 화면 전체에서 즉시 알아볼 수 있는 다정한 그림으로 이해한다.
새로운 근거: 현재 쓰기 화면에는 홈·지우기·다음만 있고 다음은 성공 전 회색 자물쇠로 비활성이다. 또한 성공은 글자 우측의 작은 별빛·체크, 재도전은 상단의 작은 되돌림 화살표라 멀리서 결과를 알아보기 어렵다. 사용자가 네 버튼 상시 활성과 전체 화면 성공 원형·다정한 재도전 이미지를 직접 요청했다.
중복 방지: 쓰기 화면의 가장자리 네 조작, 이전·다음 순환, 성공·재도전 피드백 표시만 바꾼다. 버튼 아틀라스 원본, 글자 geometry, 입력 판정과 무음 동적 시범은 보존한다.

## 목표

1. 쓰기 화면 왼쪽에 홈·다시쓰기, 오른쪽에 이전·다음 버튼을 각각 64 × 64 dp 이상으로 배치한다.
2. 이전·다음 버튼을 성공 여부와 관계없이 항상 활성화하고 회색 자물쇠·비활성 표현을 production에서 제거한다.
3. 이전·다음은 현재 메뉴의 글자 순서 안에서 앞·뒤로 순환하고 글자가 바뀔 때 입력·성공·실패 상태를 초기화한다.
4. 기존 다음 화살표를 좌우 반전해 이전으로 재사용하며 홈·다시쓰기·다음의 기존 그림체와 눌림 반응을 유지한다.
5. 성공 시 중앙이 투명한 큰 축하 원형 이미지를 쓰기판 높이에 가깝게 표시하고, 실패 시 `X`·슬픈 표정·경고색이 없는 미소 구름과 되돌림 화살표 이미지를 같은 규모로 표시한다.
6. 판정 이미지는 네 조작보다 아래에 그려 버튼을 가리거나 막지 않고, 다시쓰기·이전·다음·홈 또는 새 입력에서 즉시 해제한다.

## 성공 조건

1. `./scripts/verify.sh`와 `git diff --check`가 통과한다.
2. 자음·모음·가나다 각각에서 이전·다음이 메뉴 밖으로 나가지 않고 첫↔마지막을 포함해 양방향 순환한다.
3. production에 성공 여부를 확인하는 다음 차단, 비활성 `enabled`, 자물쇠 행 렌더링과 `사용 불가` 접근성 상태가 남지 않는다.
4. 정확한 2340 × 1080에서 네 버튼이 각각 64 × 64 dp 이상이고 같은 열의 간격이 12 dp 이상이며 글자 길·시작점을 가리지 않는다.
5. 두 생성 이미지는 실제 투명 알파를 가진 정사각형 RGBA PNG이고, 성공은 열린 원형·재도전은 긍정 표정과 되돌림 화살표로 서로 구분되며 `X`·슬픈 표정·글자가 없다.
6. 성공·실패 이미지는 쓰기판 높이의 80% 이상으로 표시되면서 네 버튼은 계속 보이고 누를 수 있다.
7. 초기·입력 중·실패·성공 상태 모두에서 이전·다음 전환과 다시쓰기·홈 callback이 동작하고 새 글자는 깨끗한 입력 상태로 시작한다.
8. 자동·에뮬레이터·실제 휴대폰·아이 대리 QA와 실제 아이 관찰 여부를 분리 기록한다.

## 완료 정의

네 조작의 상시 활성·양방향 순환·상태 초기화와 전체 화면 성공·재도전 이미지가 자동 검사와 정확한 2340 × 1080 화면·실제 동작·아이 대리 QA에서 통과하고 새 P0·P1 결함이 없으면 루프 178을 완료한다. 활성 큐를 `없음`으로 전환한 뒤 완료 체크포인트를 만들고 `git push origin HEAD`로 일반 push한다.
