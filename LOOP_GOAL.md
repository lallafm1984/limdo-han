# 루프 목표 122 — `가` 3획 보상 이동 일치

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 아이가 `가`를 세 획으로 완성하면 자동차가 실제 획수와 같은 세 칸을 순서대로 움직여 쓰기 노력과 결과를 일치시킨다.
새로운 근거: 루프 121의 정확한 2340 × 1080 `가` 3획 성공에서 경찰차는 126 px 한 칸만 이동했고, 현재 성공 callback은 `strokeCount = 1`인 `GieokLesson`을 사용한다.
중복 방지: production `가` lesson의 `strokeCount = 3`을 보상 상태에 연결하고 성공 1회에 3칸·실패/지우기/다시 듣기에 0칸·중복 callback에 추가 0칸을 확인하면 종료한다. 이동 횟수를 한 단계씩 늘리는 내구성 루프로 전환하지 않는다.

## 목표

production `가` lesson data의 실제 `strokeCount = 3`과 성공 보상을 연결해 세 번의 짧고 분명한 차량 이동을 보여 준다.

## 성공 조건

1. 현재 production `가` lesson은 `glyph = "가"`, `strokeCount = 3`을 단일 진실로 사용한다.
2. 정답 1회는 자동차를 정확히 3칸 이동시키고 각 칸의 이동을 순서대로 구분할 수 있다.
3. 실패·지우기·다시 듣기는 0칸이고 같은 성공 callback의 중복은 추가 이동을 만들지 않는다.
4. 이동 완료 전 새 입력을 막고 지우기 후에는 새 시도를 예측 가능하게 시작한다.
5. 정확한 2340 × 1080에서 `가` 성공의 3칸 순차 이동과 최종 위치를 연속 프레임으로 확인한다.
6. 루프 121의 3획 판정·입력 중 방향 안내·음성·지우기를 회귀시키지 않는다.
7. `./scripts/verify.sh`, `./scripts/check-automation.sh`, `git diff --check`를 통과한다.
8. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

`GieokLesson`이 production `가` 화면에 남은 경로와 `LessonRewardState` 애니메이션의 한 번에 3칸 건너뛰기를 조사하고, production lesson data와 칸별 이동을 분리해 검증하는 최소 수정을 한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비한다. 완료 루프와 준비된 다음 루프를 체크포인트 커밋 하나로 만들고 `git push origin HEAD`로 일반 push한다.
