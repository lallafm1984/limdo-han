# 루프 목표 056 — 성공 이동 중 활성 자동차 정체성 유지

## 목표

정답 보상의 시작·이동 중·완료까지 성공 직전의 활성 자동차를 같은 모습으로 유지하고, 다음 자동차 순환은 이동 완료 뒤 다음 입력 준비 시점에만 적용한다.

## 성공 조건

1. 성공 직전·이동 시작·중간·완료의 차량 종류가 동일하다.
2. 다음 차량은 이동 완료 뒤 지우기로 다음 입력을 준비할 때 예측 가능한 9종 순서의 다음 종류로 한 번만 바뀐다.
3. 실패·다시 듣기·중복 성공 callback은 차량 종류를 바꾸지 않고, 지우기는 성공 완료 전 순환시키지 않는다.
4. 기존 `strokeCount=1`의 126 px 한 칸 이동, 성공 음성·`★ ✓`, 이동 중 입력 잠금이 유지된다.
5. 2340 × 1080에서 차량이 학습판·guide·시작점·끝점·동적 안내·아이 획을 가리지 않고 입력을 가로채지 않는다.
6. 자동 상태·순환 회귀, `./scripts/verify.sh`, `git diff --check`를 통과한다.
7. 새 연속 화면·hierarchy와 아이 대리 QA, `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

성공 callback에서 차량 종류가 이동보다 먼저 바뀌는 현재 상태 전환을 확인하고, 이동 완료 뒤 다음 입력 준비 시점으로 순환을 미루는 최소 상태 변경만 적용한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
