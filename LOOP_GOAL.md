# 루프 목표 216 — `ㄴ + ㅕ → 녀` 셋째 초성 확장

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 다섯 살 아이가 검증된 `ㄴ` 초성에 짧은 획 두 개를 가진 `ㅕ`를 결합해 `너`와 `녀`의 모음 차이를 조립과 쓰기로 연결할 수 있다.
새로운 근거: 루프 215는 `ㄴ + ㅓ → 너` 한 짧은 획 모음만 다루었으며 같은 초성에 두 짧은 획을 조립하는 흐름은 아직 구현하지 않았다.
중복 방지: 이번 루프는 `ㄴ + ㅕ → 녀` 한 조립 흐름과 production `녀` 쓰기 연결만 다룬다. `ㄴ`의 다른 모음과 `ㄷ`는 함께 늘리지 않는다.

## 목표

검증된 조립·쓰기 token과 production geometry 규칙을 재사용하되, `ㄴ`을 왼칸과 `ㅕ`를 오른칸에 놓아 `녀`를 완성하고 production `녀` 쓰기 lesson으로 연결한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. 조립 진입에서 `너`와 `녀` 과제를 글자 모양으로 구분해 `녀` 조립을 선택하고 홈·뒤로 복귀가 동작한다.
3. `ㄴ`과 `ㅕ` 조각, 초성 왼칸과 세로 모음 오른칸, 조립 전·선택·완성 상태가 글을 읽지 않아도 구분된다.
4. 잘못된 칸·순서·빈 칸은 완성으로 처리하지 않고 처벌 없이 다시 안내한다.
5. 완성 `녀`와 production `녀` 표시·안내·판정이 같은 정사각형 em·균일 배율·자모 상대 위치를 사용한다.
6. 완성 뒤 production `녀` WritingCanvas 1962 × 954 px로 진입하고 네 그림 조작·정방향 입력·성공·다음·홈 회귀가 없다.
7. 정확한 2340 × 1080 변경 전·후 PNG·hierarchy·focus·APK SHA와 시작·오순서·완성·쓰기·성공 근거에서 잘림·겹침·가림이 0건이다.
8. 새 P0·P1·진행 방해 P2가 0건이고 자동 그래픽 디자인·자동 QA·에뮬레이터·아이 대리 QA가 통과한다.

## 완료 정의

`ㄴ + ㅕ → 녀` 한 모음 확장 조립 흐름과 production `녀` 쓰기 연결만 새 자동·실화면 근거로 통과하면 완료한다. `ㄴ`의 다른 모음과 `ㄷ`는 별도 단일 루프로 나누며 완료 체크포인트만 `git push origin HEAD`로 일반 push한다.
