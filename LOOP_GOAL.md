# 루프 목표 049 — 회복 성공 지우기 뒤 정상 재입력 QA

## 목표

정확한 2340 × 1080에서 앱 데이터를 한 번만 초기화하고, 정상 중심선 입력→`SUCCESS` 시작 callback 즉시 지우기를 3회 연속 반복한 뒤 네 번째 정상 입력을 성공 완료까지 유지한다. 이어 지우기를 한 번 눌러 초기 상태로 복원한 직후 다섯 번째 정상 입력이 이전 callback의 개입 없이 한 번의 성공 완료로 회복하는지 먼저 확인한다. 문제가 입증될 때만 완료 상태 초기화 뒤 재입력 전환을 최소 보정한다.

## 성공 조건

1. 정확한 에뮬레이터에서 앱 데이터를 한 번만 초기화하고 초기 안내 완료 뒤 정상 중심선을 다섯 번 입력한다.
2. 첫 세 입력의 `SUCCESS` 시작 callback 직후 추가 sleep 없이 지우기를 한 번씩 전달한다.
3. 네 번째 입력은 성공 음성 완료까지 유지하고 지우기를 정확히 한 번 눌러 초기 상태로 복원한다.
4. 추가 초기화나 지우기 없이 다섯 번째 정상 입력이 굵은 완료 획·`★ ✓`와 `SUCCESS` 시작→완료 callback 한 회로 끝난다.
5. 다섯 번째 성공 뒤 3초 이상 이전 callback이 늦게 나타나거나 성공 화면을 덮지 않는다.
6. Canvas 1962 × 775 px, 주요 버튼 373 × 168 px와 간격 37 px를 유지한다.
7. 제품 문제가 없으면 코드를 바꾸지 않고 QA 근거만 남긴다.
8. `./scripts/verify.sh`와 `git diff --check`가 통과하고 새 아이 대리 QA 및 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

세 번의 성공 callback 즉시 지우기와 네 번째 성공 완료·지우기를 같은 세션에서 재현한 직후 다섯 번째 정상 입력의 성공 회복을 확인한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
