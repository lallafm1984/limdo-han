# 루프 목표 050 — 정사각형 학습판과 geometry 실측

## 목표

정확한 2340 × 1080에서 전체 입력 Canvas는 기존 최소 크기를 유지하되, 아이가 보는 점선 중심축·학습판을 Canvas의 짧은 변 기준 정사각형로 중앙 정렬한다. `ㄱ` template, 보이는 guide, 입력 판정은 같은 정사각형 em과 균일 x/y 배율을 유지한다. 동적 안내·자동차 자산·보상은 이번 루프에 구현하지 않는다.

## 성공 조건

1. 2340 × 1080에서 WritingCanvas는 최소 1872 × 648 px이고 화면에서 가장 큰 아이 상호작용 영역을 유지한다.
2. 보이는 학습판의 가로·세로는 동일한 픽셀이며 WritingCanvas 안에 중앙 정렬된다.
3. `ㄱ` geometry는 정사각형 em에서 x/y 동일 배율로 표시되고 판정되며 기존 교육용 비율·중심선·획 굵기를 유지한다.
4. 정상 입력, 재시도, 성공, 지우기, 다시 듣기의 기존 동작과 callback을 깨뜨리지 않는다.
5. 단위 테스트와 정확한 2340 × 1080 초기·진행 중·재시도·성공 화면에서 정사각형 학습판, 균일 em, Canvas·글자 bounds를 새로 실측한다.
6. `./scripts/verify.sh`와 `git diff --check`가 통과하고 새 아이 대리 QA 및 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

현재 학습판·Canvas·`WritingCanvasGeometry`를 실측하고, 보이는 학습판만 정사각형으로 만드는 최소 layout·geometry 변경을 적용한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 최신 사용자 지시의 다음 순서인 동적 획 안내를 별도 루프로 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
