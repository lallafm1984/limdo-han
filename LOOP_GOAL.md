# 루프 목표 148 — 좌우 세로 조작 배치와 쓰기 공간 확대

## 작업 가치 관문

분류: 사용자 지시
사용자 가치: 글자를 처음 쓰는 아이가 화면 가운데의 더 높고 넓은 정사각형 쓰기 공간에 손을 크게 움직일 수 있고, 듣기·지우기·다음 조작은 좌우 가장자리에서 쉽게 찾는다.
새로운 근거: 2026-08-26 최신 사용자 지시가 하단 조작 UI의 비효율을 구체적으로 지적했다. 루프 147의 정확한 2340 × 1080 새 화면에서도 WritingCanvas는 1962 × 775 px인 반면, 하단 `ActionShelf`가 y=865~1033의 168 px 높이를 계속 차지해 세로 확장 여지가 남아 있음을 확인했다.
중복 방지: 이 루프는 루프 147에서 완료한 자형·획순·lesson 확장을 다시 수정하지 않는다. 하단 선반을 제거하고 좌우 세로 조작 열과 가운데 WritingCanvas의 실제 내부 크기·입력 좌표·보상 배치를 한 번 검증하면 완료한다.

## 목표

하단의 듣기·지우기·다음 조작을 화면 좌우 가장자리의 세로 열로 재배치한다. 가운데 WritingCanvas는 기존 1962 × 775 px보다 세로가 커지고, 교육용 정사각형 em·글자 비율·표시와 판정 경로 일치를 유지해야 한다.

## 성공 조건

1. 듣기와 지우기는 왼쪽 세로 열, 다음은 오른쪽 세로 열에 배치하며 하단 `ActionShelf`가 쓰기판 아래 공간을 차지하지 않는다.
2. 각 조작은 아이가 글을 읽지 못해도 아이콘·위치·색·활성 상태로 구분되고, 한글 접근성 설명과 기존 음성 상태 계약을 유지한다.
3. 정확한 2340 × 1080에서 실제 WritingCanvas 내부가 최소 1872 × 900 px이며 이전 1962 × 775 px보다 세로로 커지고 화면에서 가장 큰 아이 상호작용 영역이다.
4. 좌우 조작 열은 WritingCanvas·자동차·성공 표식·시스템 표시 영역과 겹치지 않고, 각 터치 대상은 최소 64 dp와 12 dp 간격을 유지한다.
5. 교육용 정사각형 em의 x·y 균일 배율, 글자 고유 비율, 표시 경로와 입력 판정 경로, 획별 동적 안내를 변경하거나 왜곡하지 않는다.
6. 빈 화면·입력 중·성공·음성 재생 중에서 듣기·지우기·다음의 활성 상태와 기존 callback이 유지된다.
7. 새 레이아웃 회귀 테스트, `./scripts/verify.sh`, `git diff --check`와 정확한 2340 × 1080 초기·입력·성공·다음 전환 화면을 통과한다.
8. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

현재 `LearningShell`의 좌표·여백·보상 geometry를 측정하고, 좌우 조작 열을 기존 화면 가장자리 여백 안에 배치하면서 가운데 WritingCanvas의 아래 여백을 제거하는 최소 레이아웃 변경을 구현한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비한다. 새 불편과 미완료 사용자 제품 지시가 없으면 큐를 `없음`으로 만들고 `IDLE`로 종료한다. 완료 커밋 뒤 `git push origin HEAD`로 일반 push한다.
