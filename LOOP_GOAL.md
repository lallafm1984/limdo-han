# 루프 목표 170 — `ㄱ·ㄴ·ㄷ` 보이는 외곽 크기 통일

## 작업 가치 관문

분류: 사용자 지시
사용자 가치: 다섯 살 아이가 기본 직각 자음을 같은 크기의 정사각형 글자 틀로 인식하고, 글자마다 다른 손 움직임 범위를 다시 추측하지 않게 한다.
새로운 근거: 정확한 2340 × 1080 화면과 production 좌표에서 `ㄱ` 중심선은 em의 94% × 94%, `ㄴ`은 76% × 80%, 비교 기준인 `ㄷ`은 76% × 76%임을 확인했다. 기존 광학 검사는 50~95%의 넓은 범위만 검사해 이 차이를 잡지 못했다.
중복 방지: `ㄱ·ㄴ·ㄷ`의 중심선과 둥근 cap을 포함한 보이는 외곽이 같은 76% 정사각형으로 고정되고 정확한 화면에서 통과하면 같은 원인의 임의 축소 반복을 만들지 않는다.

## 목표

1. standalone `ㄱ·ㄴ·ㄷ`의 중심선 외곽을 같은 76% × 76% 정사각형과 같은 중심에 놓는다.
2. 표시·현재 획 점선·동적 표식·시작끝 표식·입력 판정이 교정된 production geometry를 함께 사용하게 한다.
3. 정확한 2340 × 1080 에뮬레이터 화면에서 `ㄱ·ㄴ·ㄷ`의 보이는 외곽을 비교한다.

## 성공 조건

1. `ㄱ·ㄴ·ㄷ`의 중심선 가로·세로 길이는 모두 em의 76%이고 중심은 쓰기판 중앙과 일치한다.
2. 같은 안내 굵기와 둥근 cap을 포함한 세 글자의 보이는 가로·세로 외곽이 각각 정확히 같다.
3. 기존 획순·정방향 수락·역방향 거부와 기본 자음 14개 광학 범위 검사가 회귀하지 않는다.
4. `./scripts/verify.sh`, `git diff --check`, `emulator-5554` focus·치명 오류 검사와 정확한 2340 × 1080 전후 화면 직접 비교가 통과한다.
5. 자동·에뮬레이터·아이 대리 QA와 실제 아이 관찰 여부를 분리 기록한다.

## 완료 정의

모든 성공 조건을 통과하면 루프 170을 완료한다. 새 아이 대리 QA에서 서로 다른 미해결 제품 결함이 없으면 큐를 `없음`, 상태를 `IDLE`로 바꾸고 완료 체크포인트를 `git push origin HEAD`로 일반 push한다.
