# 루프 목표 194 — 성공 후 자동 다음 lesson 이동

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 아이가 정답을 완성한 뒤 멈춘 성공 화면에 남지 않고, 충분히 축하를 본 뒤 현재 메뉴의 다음 글자로 자연스럽게 이동한다.
새로운 근거: 사용자가 실제 휴대폰 확인에서 성공 후 화면이 멈춘 채 남는 결함을 직접 확인했다. 현재 제품 기획은 `정답 후` 녹음이 있으면 1회 재생 완료 뒤, 없거나 실패하면 기존 성공 연출의 최소 노출 뒤 자동 이동을 요구한다.
중복 방지: 성공 event의 자동 예약·1회 이동·수동 취소 규칙만 교정한다. 새 성공 이미지, `사` geometry, 보호자 목록 완료 화면, 다른 시각 연출은 이번 루프에서 바꾸지 않는다.

## 목표

1. 자유 쓰기에서 성공 후 최소 축하 노출 시간을 지킨 뒤 현재 메뉴의 다음 lesson으로 자동 이동한다.
2. `정답 후` 보호자 녹음이 있으면 성공 화면을 유지하며 녹음을 딱 1회 재생하고, 정상 종료·손상·재생 실패·timeout 뒤 다음으로 이동한다.
3. 자동 이동 대기 중 `홈·다시쓰기·이전·다음`을 누르면 예약과 재생을 취소하고 사용자 선택을 우선한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. 자동 검사가 녹음 없음·정상 종료·재생 실패·timeout, 중복 recomposition·foreground 복귀의 1회 예약, 네 수동 조작의 취소 우선을 고정한다.
3. `alarmquest-qa` 물리 1080 × 2340·가로 2340 × 1080·LimDo focus에서 녹음 없는 대표 성공→다음 lesson과 녹음 있는 대표 재생→다음 lesson을 실제 입력과 새 PNG·hierarchy·상태 로그로 입증한다.
4. 대기 중 네 수동 조작 각각이 자동 이동을 취소하고 중복 다음·중복 재생·멈춘 애니메이션이 0건임을 확인한다.
5. 성공 이미지·완성 글자·네 조작·쓰기판·입력 판정에 회귀가 없고 자동 그래픽 디자인·자동 QA·아이 대리 QA의 새 P0·P1·진행 방해 P2가 0건이다.

## 완료 정의

성공 후 재생·최소 노출·자동 다음·수동 취소가 자동 계약과 에뮬레이터 실제 흐름에서 모두 통과할 때 완료한다. 새 성공 이미지 교체는 후속 단일 시각 루프로 남긴다.
완료 관문을 모두 통과한 체크포인트만 `git push origin HEAD`로 일반 push한다.
