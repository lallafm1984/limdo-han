# 루프 목표 164 — `ㅍ`·`파` 교육용 4획 순서·판정 공유 교정

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 아이가 참고 교육 자료와 같은 순서로 `ㅍ`과 `파`를 보고 따라 쓰며 획수만큼 보상을 받는다.
새로운 근거: 2026-08-27 사용자 참고 자형은 `ㅍ`을 윗가로→왼쪽 세로 아래→오른쪽 세로 아래→아랫가로의 4획으로 요구하지만 현재 geometry는 윗가로→아랫가로→왼쪽 세로→오른쪽 세로 순서다.
중복 방지: `ㅍ`·`파`의 네 획을 공유 geometry·방향 계약으로 고정하고 정방향·역방향·잘못된 순서·정확한 화면을 통과한 뒤 같은 흐름을 횟수만 늘려 재검증하지 않는다.

## 목표

`ㅍ`과 `파`의 표시·동적 안내·음성·입력 판정·4칸 보상을 참고 자료의 4획 순서로 일치시킨다.

## 성공 조건

1. `ㅍ`은 윗가로, 왼쪽 세로 아래, 오른쪽 세로 아래, 아랫가로 순서의 4획이다.
2. 같은 네 획 geometry와 방향이 `파`의 초성에 공유되고 두 lesson의 음성·4획 보상이 일치한다.
3. 정방향 입력은 순서대로 수락되고 세로 역방향·아랫가로 선행은 성공으로 통과하지 않는다.
4. 정확한 2340 × 1080에서 `ㅍ`·`파`의 초기·획별·성공 화면을 직접 읽고 WritingCanvas 1962 × 954 px, LimDo focus, 조작·장식 비가림을 통과한다.
5. `./scripts/verify.sh`, `git diff --check`, 새 아이 대리 QA가 통과하고 P0·P1 미해결 결함이 없으며 `실제 아이 관찰: 실행 안 함`을 분리 기록한다.

## 완료 정의

모든 성공 조건을 새 자동·에뮬레이터 근거로 통과한 뒤 기본 자음 14개·기본 모음 10개의 참고 자형·획순·방향 전체 감사를 다음 루프로 준비한다. 루프 164 완료와 다음 루프 준비를 체크포인트 커밋 하나로 만들고 `git push origin HEAD`로 일반 push한다. 루프 164의 구현은 다음 새 세션이 수행한다.
