# 루프 목표 158 — 세 전체 경로의 독립 자동 QA 역할

## 작업 가치 관문

분류: 새 사용자 위험
사용자 가치: 자음·모음·가나다 세 경로가 각각 올바른 학습·재시도·성공·다음·홈 복귀로 끝나 아이가 잘못된 글자나 멈춘 상태에 남지 않는다.
새로운 근거: 루프 155~157은 기능·디자인·연출을 통과했지만 세 독립 경로의 성공·재시도·다음·홈·연속 조작·재실행을 자동 QA 역할로 분리 판정하지 않았다.
중복 방지: 통과한 연출 프레임을 횟수만 늘려 재수집하지 않고, 세 서로 다른 navigation·lesson 경로와 상태 오염 위험만 판정한다.

## 목표

자동 QA 역할로 자음·모음·가나다 세 전체 경로와 회귀를 자동·에뮬레이터·화면 근거로 독립 판정한다.

## 성공 조건

1. 세 경로가 각각 홈→선택→대표 쓰기→성공→다음→홈 복귀를 올바른 데이터로 완주한다.
2. 각 경로의 실패→재시도, 다시 듣기, 지우기, 성공 전·후 `다음` 상태가 일치한다.
3. 가나다 14글자의 중복·누락·순서와 lesson·획수·음성·보상·복귀 매핑이 자동 검사를 통과한다.
4. 빠른 연속 탭, 뒤로 가기, 입력 중 홈, 재실행에서 중복 보상·잘못된 lesson·멈춘 애니메이션·이전 입력 잔류가 없다.
5. WritingCanvas 1962 × 954 px, 정사각형 em·균일 배율, 획순·방향·판정·음성·보상에 회귀가 없다.
6. `./scripts/verify.sh`, `git diff --check`, 정확한 2340 × 1080 focus·hierarchy·화면 근거가 통과한다.
7. `자동 QA 역할: 통과/실패`를 분리 기록하고 P0·P1 0개임을 확인한다.
8. 최종 아이 대리 QA와 `실제 아이 관찰: 실행 안 함`을 남긴다.

## 다음 루프 경계

통과하면 1차 기획서의 여덟 시나리오를 판정하는 다섯 살 아이 대리 시뮬레이션 루프를 준비한다.

## 완료 정의

모든 조건을 새 근거로 통과하고 최종 아이 대리 QA를 남긴다. 완료와 다음 루프 준비를 체크포인트 커밋 하나로 만든 뒤 `git push origin HEAD`로 일반 push한다.
