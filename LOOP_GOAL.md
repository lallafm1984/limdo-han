# 루프 목표 200 — 보호자 목록 현재 위치·이어하기·처음부터

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 보호자가 아이가 다음에 시작할 항목을 확인하고 이어하거나 처음부터 안전하게 재시작한다.
새로운 근거: 루프 199는 목록 항목·순서·삭제를 완성했지만 현재 진행 index와 재실행 후 이어하기·처음부터 선택은 production model·UI에 없다.
중복 방지: 이번 루프는 현재 index 저장·표시·이어하기·처음부터까지만 구현한다. 아이 목록 실행·마지막 완료 화면·진도 요약은 후속 단일 루프로 분리한다.

## 목표

보호자 자유 학습 목록의 현재 index를 no-backup app-private 저장소에 원자 보존하고, 보호자 화면에서 현재 항목을 분명히 보며 `이어하기`와 `처음부터`를 명시적으로 선택한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. 현재 index는 빈 목록에서 안전하고, 항목 삭제·이동 뒤에도 유효한 범위로 보정되며 중복 ID가 아닌 저장 index를 유지한다.
3. 보호자 편집 화면에 현재 항목을 색 하나가 아닌 모양·문구·semantics로 구분하고 `이어하기`·`처음부터` 주요 동작은 192 px 이상이며 빈 목록에서 비활성이다.
4. 이어하기는 현재 index를 보존하고, 처음부터는 0으로 변경하며 강제 종료·재실행 뒤 각각 같은 상태가 복원된다.
5. 정확한 2340 × 1080·LimDo focus에서 빈·중복·중간·마지막 현재 위치의 잘림·겹침·잘못된 항목 표시·오동작이 0건이며 루프 199의 페이지·이동·삭제·녹음·추가 callback이 유지된다.
6. 기존 세 메뉴 자유 쓰기·네 조작·단일 녹음·성공 자동 다음에 회귀가 없고 자동 그래픽 디자인·자동 QA·아이 대리 QA에서 새 P0·P1·진행 방해 P2가 0건이다.

## 완료 정의

보호자 목록 현재 index의 안전한 보정·원자 저장·재실행 복원과 보호자용 이어하기·처음부터 선택이 자동·에뮬레이터·실화면 근거로 통과할 때 완료한다. 아이 목록 실행·마지막 완료 화면은 다음 제품 루프로 남기며 완료 체크포인트만 `git push origin HEAD`로 일반 push한다.
