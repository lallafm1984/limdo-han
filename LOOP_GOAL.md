# 루프 목표 026 — 끝점 도달 뒤 놓기 전 장시간 유지 QA

## 목표

정확한 2340 × 1080에서 정상 `ㄱ` 획이 끝점에 도달한 뒤 손가락을 놓기 전에 장시간 유지해도 진행 중 입력이 초기화되지 않고, 놓는 순간 결과·버튼·음성 상태가 일관되는지 탐색한다. 루프 025의 새 아이 대리 QA에서 정한 구체적인 미검증 흐름이며 문제 입증 전에 제품 로직을 바꾸지 않는다.

## 성공 조건

1. `alarmquest-qa`의 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`에서 새 debug APK를 검증한다.
2. Canvas 1962 × 775 px와 정사각형 `ㄱ` geometry를 유지한다.
3. 정상 `ㄱ` 획으로 끝점 `(1461,742)`에 도달한 뒤 서로 다른 유지 시간 최소 5개 동안 손가락을 계속 둔 다음 같은 끝점에서 놓는다.
4. 각 입력 좌표, 시작·유지·종료 시점, 결과·버튼 상태 화면과 음성 callback 순서를 기록한다.
5. 글을 읽지 않는 아이 관점에서 끝점에서 손가락을 잠시 멈춰도 정상 입력이 초기화되지 않고, 놓은 뒤 성공 결과가 일관되는지 아이 대리 QA한다.
6. 문제가 입증되면 가장 작은 보정과 단위 테스트만 적용하고, 문제가 없으면 제품 코드를 바꾸지 않는다.
7. 코드를 바꾸면 `./scripts/verify.sh`와 `git diff --check`가 통과한다.
8. `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

정상 중심선 획으로 끝점에 도달한 뒤 0.5·1·2·3·4초 유지하고 같은 위치에서 놓는 사례를 제품 변경 없이 재현한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 로컬 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
