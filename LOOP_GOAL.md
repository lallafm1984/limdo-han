# 루프 목표 053 — 경찰차 기본 안내와 동물 교체

## 목표

정확한 2340 × 1080의 현재 `ㄱ` 학습 화면에서 동물 안내 이미지를 오른쪽을 향한 경찰차 기본 안내 이미지로 교체한다. 기존 정사각형 학습판·동적 획 안내·입력·판정·음성은 유지하며 9종 순환과 `strokeCount` 보상 이동은 이번 루프에 구현하지 않는다.

## 성공 조건

1. 기본 안내 이미지가 `limdo_vehicle_police.png` 경찰차로 보이고 동물 이미지는 기본 화면에서 사라진다.
2. 경찰차는 오른쪽을 향한 둥근 유아용 2D 그림체, 투명 모서리, 글자·번호·브랜드·워터마크 없음 기준을 충족한다.
3. 경찰차가 정사각형 학습판, 흰 guide, 시작점, 끝점, 입력 전 시범, 입력 중 화살표와 아이 획을 가리지 않는다.
4. 초기·입력 중·재시도·성공·지우기·다시 듣기 상태에서 같은 기본 경찰차가 임의로 바뀌지 않고 기존 상호작용을 가로채지 않는다.
5. 정확한 2340 × 1080 연속 화면·hierarchy와 자산 알파 검사로 배치·가림 없음·상태 유지를 새로 검증한다.
6. `./scripts/verify.sh`와 `git diff --check`가 통과하고 새 아이 대리 QA 및 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

현재 동물 안내 이미지의 Compose 배치와 경찰차 PNG의 알파·방향을 확인하고, 기본 이미지 교체만 하는 최소 변경을 적용한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 9종 자동차의 예측 가능한 순환을 별도 루프로 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
