# 루프 목표 131 — 짧은 획의 동적 안내 겹침 완화

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 글을 읽지 못하는 아이가 짧은 3획에서도 움직이는 방향 안내와 시작·끝 표식을 각각 구분해 어디서 시작해 어느 쪽으로 끝내는지 이해할 수 있다.
새로운 근거: 루프 130 최종 새 APK의 정확한 2340 × 1080 `captures/loop130/iteration2/separated-current-end/03-after-second-stroke.png`에서 3획의 동적 청록 화살표와 흰 강조 링이 큰 초록 시작점 위를 덮고 주황 끝점 바로 옆까지 차지한다. 시작·끝 색 자체는 약 4.96 px 분리됐지만 방향 표식이 두 신호 사이에서 경쟁한다.
중복 방지: production 좌표·판정·현재 획 시작·끝 좌표와 이미 통과한 마커 분리를 바꾸지 않고, 입력 전 및 2획 완료 뒤 연속 프레임에서 방향성이 보이는 동적 안내가 시작·끝의 색 중심을 가리지 않으면 종료한다. 안내 재생 횟수를 늘리는 검증은 하지 않는다.

## 목표

짧은 production `가` 3획에서 동적 안내의 크기 또는 진행 범위를 현재 획 길이에 맞춰 시작·끝 표식을 가리지 않게 한다.

## 성공 조건

1. 3획의 동적 안내는 오른쪽 방향성을 유지하면서 초록 시작점과 주황 끝점의 색 중심을 가리지 않는다.
2. 동적 안내의 위치·크기는 production `ga` 현재 획 길이와 `completedStrokes`에서 파생하고 3획 전용 화면 좌표를 중복하지 않는다.
3. 입력 전 1획과 1획 완료 뒤 2획의 동적 안내 크기·방향을 회귀시키지 않는다.
4. 현재 획 시작·끝, 정상 3획·재시도·지우기·음성·3칸 보상·다음 차량 흐름을 회귀시키지 않는다.
5. 정확한 2340 × 1080에서 2획 완료 뒤 동적 안내의 연속 프레임과 3획 성공 화면·hierarchy를 확인한다.
6. `./scripts/verify.sh`, `./scripts/check-automation.sh`, `git diff --check`를 통과한다.
7. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

`WritingCanvas`의 동적 안내 원·링·화살표 크기와 `gaDemonstrationGuide`의 짧은 3획 진행 범위를 조사하고, production 획 길이에 비례해 가림을 줄이는 최소 가설을 세운다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비하고, 검증된 완료·준비 변경을 커밋 하나로 만든 뒤 `git push origin HEAD`로 일반 push한다.
