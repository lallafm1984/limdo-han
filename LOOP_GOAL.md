# 루프 목표 028 — 끝점 도달 뒤 큰 되돌림의 판정 경계 QA

## 목표

정확한 2340 × 1080에서 정상 `ㄱ` 획이 끝점에 도달한 뒤 손가락이 같은 세로 경로를 100 px보다 크게 되돌아가 놓일 때 성공과 재시도의 판정 경계가 거리 증가에 따라 일관되는지 탐색한다. 루프 027의 새 아이 대리 QA에서 정한 구체적인 미검증 흐름이며 문제 입증 전에 제품 로직을 바꾸지 않는다.

## 성공 조건

1. `alarmquest-qa`의 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`에서 새 debug APK를 검증한다.
2. Canvas 1962 × 775 px와 정사각형 `ㄱ` geometry를 유지한다.
3. 정상 `ㄱ` 획으로 끝점 `(1461,742)`에 도달한 뒤 100 px보다 큰 서로 다른 거리 최소 5개만큼 같은 세로 경로를 위로 되돌아가 놓는다.
4. 각 입력 좌표, 시작·끝점 도달·되돌림·종료 시점, 결과·버튼 상태 화면과 음성 callback 순서를 기록한다.
5. 글을 읽지 않는 아이 관점에서 큰 되돌림의 성공·재시도 경계가 예측 가능하고 일관되는지 아이 대리 QA한다.
6. 문제가 입증되면 가장 작은 보정과 단위 테스트만 적용하고, 문제가 없으면 제품 코드를 바꾸지 않는다.
7. 코드를 바꾸면 `./scripts/verify.sh`와 `git diff --check`가 통과한다.
8. `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

정상 중심선 획으로 끝점에 도달한 뒤 같은 세로 경로를 100 px보다 큰 서로 다른 거리 최소 5개만큼 위로 되돌아가 놓는 사례를 제품 변경 없이 재현한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 로컬 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
