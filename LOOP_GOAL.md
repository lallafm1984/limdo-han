# 루프 목표 189 — `카` 하단 기울기와 `하` 자모 분리

## 작업 가치 관문

분류: 사용자 지시
사용자 가치: 아이가 `카`의 초성 모양을 `가`와 일관되게 보고, `하`의 초성·중성을 붙은 한 덩어리로 잘못 배우지 않게 한다.
새로운 근거: 2026-08-28 최신 사용자 실화면 지시에서 `카` 하단 기울기와 `하`의 `ㅎ·ㅏ` 접촉이 새 자형 결함으로 확인됐다.
중복 방지: `카` 초성 중심선과 `하` 비접점 guide 간격만 교정하고 다른 글자·녹음 확장·M2 재생은 후속 루프로 분리한다.

## 목표

1. `카`의 초성 `ㅋ` 하단이 `가`의 초성 흐름과 비슷하게 자연스럽게 기울도록 표시·안내·판정 공통 geometry를 교정한다.
2. `하`의 초성 `ㅎ`과 중성 `ㅏ` 비접점 guide 외곽이 맞닿거나 겹치지 않도록 교육용 분리 간격을 확보한다.
3. 기존 정사각형 em·균일 배율·획순·방향·큰 쓰기판·무음 흐름을 보존한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. `카`와 `하`의 표시·현재 획 안내·동적 표식·입력 판정이 같은 production geometry를 사용하고 정방향 수락·역방향 거부가 자동 검사에 고정된다.
3. 정확한 2340 × 1080 실제 기기에서 `카`·`하` 초기·획별·성공 화면을 직접 비교해 `카` 기울기와 `하` 비접점 외곽 간격을 확인한다.
4. WritingCanvas 1962 × 954 px, 네 조작, focus와 다른 글자 회귀가 없고 새 P0·P1·진행 방해 P2가 0건이다.

## 완료 정의

두 자형 결함이 공통 geometry와 실제 화면에서 해결되고 전체 회귀가 없을 때 완료한다. 다음에는 M1의 현재 노출 38개 lesson 두 녹음 칸 연결을 다시 준비한다.
완료 체크포인트만 `git push origin HEAD`로 일반 push하며 실패한 반복은 커밋하거나 push하지 않는다.
