# 루프 목표 120 — `가` 3획 입력 판정 연결

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 아이가 화면의 `가`를 세 획으로 따라 썼을 때 실제 획순·방향·완성도로 성공과 다시 시도를 판정받게 한다.
새로운 근거: 루프 119의 정확한 2340 × 1080 연속 프레임에서 production `ga` 3획 표시·동적 안내는 일치했지만, 실제 pointer 입력은 여전히 `GieokTraceEvaluator`의 단일 `ㄱ` 경로로 판정된다.
중복 방지: production `ga` geometry의 세 획에 대한 정상·잘못된 획순·방향·가이드 이탈을 새 evaluator 테스트와 정확한 화면 입력으로 확인하면 이 판정 연결은 종료한다. 이 루프에서 새 글자나 수업 순환은 추가하지 않는다.

## 목표

`WritingCanvasGeometry.ga`의 세 획을 표시와 같은 균일 geometry로 판정하는 입력 상태와 evaluator를 연결한다.

## 성공 조건

1. 입력 판정은 production `ga(width, height)`의 세 획 좌표에서 파생되고 별도 표시 좌표를 중복하지 않는다.
2. `ㄱ` 가로→세로, `ㅏ` 위→아래, `ㅏ` 왼쪽→오른쪽의 세 획을 순서대로 쓴 입력만 `SUCCESS`가 된다.
3. 잘못된 획 시작·방향·순서·가이드 이탈·미완성이 각각 안전한 다시 시도로 판정되고 앱이 종료되지 않는다.
4. 정확한 2340 × 1080에서 세 번의 pointer 획이 표시 guide와 같은 경로에 그려지고, 성공·재시도·지우기 흐름을 확인한다.
5. 루프 119의 `가` 표시·동적 안내와 기존 보상·음성·초기화 테스트를 회귀시키지 않는다.
6. `./scripts/verify.sh`, `./scripts/check-automation.sh`, `git diff --check`를 통과한다.
7. 아이 대리 QA에서 3획 입력만 입증하고 실제 아이 사용성을 완료했다고 주장하지 않으며 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

`GieokTraceEvaluator`와 `TraceAttempt`의 단일 획 가정을 조사하고, production `ga` 3획을 순차적으로 받는 최소 상태·판정 계약을 테스트부터 추가한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비한다. 완료 루프와 준비된 다음 루프를 체크포인트 커밋 하나로 만들고 `git push origin HEAD`로 일반 push한다.
