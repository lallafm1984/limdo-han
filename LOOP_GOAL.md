# 루프 목표 063 — 복원 pending 취소 뒤 다시 듣기 재무장 탐색

## 목표

수동 다시 듣기 `INITIAL` pending을 새 PID로 복원한 뒤 TTS `Ready` 전 지우기로 취소하고, 준비 완료 뒤 다시 듣기를 한 번 누르면 새 `INITIAL`이 중복 없이 재생되며 빈 입력·소방차·후속 성공이 일관되는지 좁게 검증하고 실제 실패가 확인될 때만 최소 보정을 적용한다.

## 성공 조건

1. `INITIAL` pending saved-state와 PID 교체, TTS `Ready` 전 지우기, 준비 완료 뒤 다시 듣기 한 번의 순서를 확정한다.
2. 지우기 전후 늦은 복원 `INITIAL`은 0회이고, 다시 듣기 뒤 새 `INITIAL` 시작·완료만 각 1회다.
3. 다시 듣기 전후 소방차는 `[99,62][435,288]`, 빈 획·무표식·입력 가능 상태를 유지한다.
4. 이어진 첫 정상 입력은 `SUCCESS` 시작·완료 각 1회와 소방차 126 px 한 칸 이동만 만든다.
5. 지우기·다시 듣기·비활성 다음·차량 비가림·비상호작과 음성 callback이 회귀하지 않는다.
6. 제품 변경이 있으면 자동 상태 회귀를 추가하고 `./scripts/verify.sh`, `git diff --check`를 통과한다.
7. 정확한 2340 × 1080 화면·hierarchy·PID·saved-state·callback과 아이 대리 QA, `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

제품 코드를 먼저 바꾸지 않고 루프 062의 유효한 복원·조기 지우기 흐름 뒤 TTS `Ready`를 확인한 다음 다시 듣기 한 번과 후속 정상 입력을 수집한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
