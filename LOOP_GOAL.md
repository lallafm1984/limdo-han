# 루프 목표 156 — 홈·선택·쓰기 화면 디자인 토큰과 자동차 시각 체계 통일

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 글을 읽지 못하는 다섯 살 아이가 홈에서 고른 학습이 선택·쓰기 화면까지 같은 색·차량·카드 형태로 이어진다는 것을 보고 선택 결과와 현재 과제를 더 쉽게 이해할 수 있다.
새로운 근거: 루프 155에서 세 메뉴와 학습 진입 기능은 통과했지만, 1차 기획서가 요구하는 홈·선택·쓰기·성공 화면의 재사용 디자인 토큰과 하나의 자동차 시각 언어는 아직 독립적으로 구현·검증되지 않았다.
중복 방지: 이번 루프는 색상·모서리·그림자·간격·차량 연결을 공통 토큰으로 통일하는 제품 변경만 다룬다. 진입·선택·성공·재시도의 화려한 애니메이션, 독립 자동 QA 역할과 8개 아이 대리 시뮬레이션은 후속 루프로 남긴다.

## 목표

홈에서 선택한 `자음·모음·가나다`의 고유 색과 차량 단서가 해당 글자 선택 화면과 기존 쓰기 화면까지 이어지도록 공통 디자인 토큰을 만들고 적용한다. 홈·선택·쓰기 화면은 밝은 `자동차 한글 놀이터`의 같은 카드·외곽선·그림자·간격 규칙을 공유하되, 글자 길과 주요 조작을 가리지 않아야 한다.

## 성공 조건

1. 홈·자음 선택·모음 선택·가나다 선택·대표 쓰기 화면이 공통 색상·모서리·외곽선·그림자·간격 토큰을 코드에서 재사용한다.
2. 자음·모음·가나다의 고유 색과 차량이 홈에서 선택 화면, 대표 쓰기 화면까지 일관되게 이어져 글을 읽지 않아도 선택 결과를 구분할 수 있다.
3. 세 메뉴는 서로 다른 고유 색을 유지하면서도 밝은 자동차 놀이터의 굵고 둥근 2D 시각 언어를 공유한다.
4. 글자 카드의 기본·선택·비활성 상태가 색 하나가 아니라 외곽선·명도·모양 중 둘 이상으로 구분된다.
5. 장식·차량·그림자가 글자, 시작·끝 표시, 동적 안내, 홈·다시 듣기·지우기·다음 버튼을 가리지 않는다.
6. 정확한 2340 × 1080에서 홈·세 선택·대표 쓰기 화면에 겹침·잘림이 없고 주요 터치 영역 최소 64 × 64 dp, 인접 간격 최소 12 dp를 유지한다.
7. 기존 WritingCanvas 1962 × 954 px 이상, 정사각형 em, 획순·방향·음성·판정·차량 보상과 14글자 선택·복귀가 회귀하지 않는다.
8. 디자인 토큰과 메뉴별 시각 연결을 고정하는 자동 테스트, `./scripts/verify.sh`, `git diff --check`가 통과한다.
9. 정확한 2340 × 1080의 새 홈·자음·모음·가나다 선택·대표 쓰기 PNG·hierarchy·focus를 직접 읽고 색·간격·글자 모양·가림을 판정한다.
10. 자동 디자인 역할 판정과 최종 아이 대리 QA를 기록하고, 실제 아이가 참여하지 않았다면 `실제 아이 관찰: 실행 안 함`을 남긴다.

## 다음 루프 경계

통과하면 기획서 순서에 따라 진입·선택·학습 성공·재시도의 화려하지만 안전한 연출을 구현하는 별도 제품 루프를 준비한다.

## 완료 정의

모든 조건을 새 자동·에뮬레이터·화면 근거로 통과하고 새 아이 대리 QA를 기록한다. 발견한 가장 중요한 새 불편 하나가 있으면 다음 결함 루프로, 없으면 기획서의 다음 미충족 제품 단계로 전환한다. 완료와 다음 루프 준비를 체크포인트 커밋 하나로 만들고 `git push origin HEAD`로 일반 push한다.
