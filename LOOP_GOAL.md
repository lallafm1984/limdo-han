# 루프 목표 054 — 9종 자동차의 예측 가능한 순환

## 목표

경찰차→소방차→구급차→스포츠카→택시→버스→쓰레기차→화물트럭→지게차의 고정 순서를 제품 데이터로 두고 예측 가능하게 순환한다. 재시도·지우기·다시 듣기는 현재 차량을 바꾸지 않으며 `strokeCount` 이동 보상은 이번 루프에 구현하지 않는다.

## 성공 조건

1. 9종 차량이 명시된 고정 순서와 각 `drawable-nodpi` 자산으로 연결되고 첫 차량은 경찰차다.
2. 순환을 일으키는 기존 학습 이벤트 하나만 선택하고 이벤트당 한 단계만 진행한다.
3. 재시도·지우기·다시 듣기는 현재 차량을 바꾸지 않고 callback 중복으로 두 단계 이상 건너뛰지 않는다.
4. 9번 진행하면 다시 경찰차로 돌아오며 불필요한 재구성에도 현재 선택이 바뀌지 않는다.
5. 2340 × 1080에서 차량이 학습판·guide·시작점·끝점·동적 안내·아이 획을 가리지 않고 입력을 가로채지 않는다.
6. 9개 PNG의 RGBA·투명 모서리·방향·금지 요소와 고정 순서, `./scripts/verify.sh`, `git diff --check`를 통과한다.
7. 새 연속 화면·hierarchy와 아이 대리 QA, `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

현재 상태 전환에서 순환에 적합한 기존 이벤트를 하나로 정하고 순서·자산 연결·상태 유지만 다루는 최소 변경을 적용한다.

## 완료 정의

모든 조건을 통과하면 `strokeCount` 이동 보상을 별도 루프로 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
