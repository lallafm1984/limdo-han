# 루프 목표 179 — 실패 이미지 제거·부드러운 자동 재시도와 아이 판정 완화

## 작업 가치 관문

분류: 사용자 지시
사용자 가치: 아이가 틀렸을 때 실패 그림으로 상실감을 느끼지 않고, 짧고 부드러운 화면 반응 뒤 곧바로 다시 쓸 수 있다. 손이 아직 정교하지 않은 다섯 살 아이의 시작점·경로·끝점 오차도 더 넉넉히 받아준다.
새로운 근거: 루프 178에서 적용한 전체 화면 재도전 이미지는 최신 사용자 선호와 맞지 않는다. 모든 production 글자가 공유하는 `LessonTraceEvaluator`의 시작점 0.18 em·경로 0.16 em·끝점 0.20 em·가이드 이탈 25% 기준도 어린아이 입력에는 좁다.
중복 방지: 실패 피드백과 production 공통 판정 허용치만 바꾼다. 성공 축하 이미지, 글자 geometry·획순·정방향 계약, 홈·다시쓰기·이전·다음 상시 활성과 무음 동적 시범은 보존한다.

## 목표

1. 실패용 전체 화면 이미지·설명·production 참조를 제거한다.
2. 실패 시 화면 전체를 좌우 최대 8 dp로 짧게 흔들고 밝은 색을 최대 alpha 0.16으로 덧입히는 420 ms 이하의 부드러운 효과를 준다.
3. 효과가 끝나면 실패 획과 판정 상태를 자동으로 지우고 현재 글자의 첫 획 안내부터 다시 시작한다.
4. 모든 production 글자가 공유하는 판정 허용치를 시작점 0.22 em, 경로 0.20 em, 끝점 0.24 em, 가이드 이탈 35%로 완화한다.
5. 방향 판정은 목표 시작점이 아니라 아이가 실제로 손을 댄 지점부터 의미 있는 이동을 측정하고, 내적 -0.25 이하의 명확한 역방향만 거부해 초기 위치 보정 움직임을 오판하지 않게 한다.
6. 정확한 역방향, 잘못된 획순, 가이드에서 크게 벗어난 입력은 계속 성공하지 않게 한다.

## 성공 조건

1. `./scripts/verify.sh`와 `git diff --check`가 통과한다.
2. production·리소스·접근성 문구에 실패 이미지와 `retry_feedback_description`이 남지 않는다.
3. 실패 효과의 변위·밝기 값이 모든 진행률에서 유한하고 정해진 최대치를 넘지 않으며 마지막에는 0으로 돌아온다.
4. 실패 직후 420 ms 안에 입력이 자동으로 비워지고 현재 글자의 첫 획 점선·시작점·동적 안내가 다시 보인다.
5. 이전 기준에서는 실패하던 시작점 0.20 em·경로 0.19 em·끝점 0.22 em 수준의 대표 어린이 입력이 새 공통 판정에서 성공한다.
6. 모든 학습 글자의 정확한 획은 성공하고, 역방향·잘못된 획순과 대표 큰 이탈은 성공하지 않는다.
7. 정확한 2340 × 1080에서 실패 이미지 없이 화면 전체가 부드럽게 반응하며 네 조작은 효과 중과 자동 초기화 뒤에도 계속 보이고 눌린다.
8. 자동·에뮬레이터·실제 휴대폰·아이 대리 QA와 실제 아이 관찰 여부를 분리 기록한다.

## 완료 정의

실패 이미지 제거, 부드러운 전체 화면 효과, 자동 재시작과 production 공통 판정 완화가 자동 검사와 정확한 2340 × 1080 에뮬레이터 동작·아이 대리 QA에서 통과하고 새 P0·P1 결함이 없으면 루프 179를 완료한다. 최신 debug APK를 `SM-S931N`에 무선 덮어쓰기 설치한 뒤 활성 큐를 `없음`으로 전환하고 완료 체크포인트를 만들어 `git push origin HEAD`로 일반 push한다.
