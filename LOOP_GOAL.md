# 루프 목표 058 — 성공 보상 중 프로세스 종료·복원 QA 탐색

## 목표

성공 자동차 이동의 `MOVING`·`COMPLETE` 상태에서 시스템이 앱 프로세스를 종료한 뒤 저장된 Activity 상태로 복원해도 활성 차량, 이동 예약, 보상 offset, 완료 획, 음성 pending과 다음 입력 준비가 일관되게 유지되는지 좁게 검증하고, 실제 실패가 확인될 때만 최소 상태 보정을 적용한다.

## 성공 조건

1. `MOVING`·`COMPLETE` 저장 뒤 프로세스 PID가 실제로 바뀐 복원 전후 활성 차량 종류가 동일하다.
2. 프로세스 복원 때문에 자동차가 중복 이동하거나 다음 차량이 조기 순환하지 않는다.
3. `MOVING` 복원은 저장 offset에서 126 px 한 칸 완료로 수렴하고 입력 잠금을 유지한다.
4. `COMPLETE` 복원 뒤 지우기는 예측 가능한 다음 차량으로 한 번만 바꾸고 시작 위치로 복원한다.
5. 성공 음성 pending·`★ ✓`·완료 획과 차량 비가림·비상호작용이 일관되게 유지된다.
6. 자동 상태 회귀, `./scripts/verify.sh`, `git diff --check`를 통과한다.
7. 정확한 2340 × 1080 새 연속 화면·hierarchy와 아이 대리 QA, `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

제품 코드를 먼저 바꾸지 않고 `MOVING`·`COMPLETE`를 독립적으로 만든 뒤 프로세스 종료와 저장 상태 복원을 확정할 PID·saved-state 근거를 수집한다. 단순 강제 종료 뒤 새 시작을 복원으로 오인하지 않고, 실패가 재현될 때만 원인에 맞는 최소 상태 보정을 한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
