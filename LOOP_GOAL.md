# 루프 목표 059 — `COMPLETE` 복원 후 재입력의 추가 보상 이동

## 목표

성공 이동 `COMPLETE` 저장 상태를 새 PID로 복원한 뒤 지우기 전 정상 횟을 재입력했을 때 같은 차량이 추가 126 px 이동하는 현상을 독립 재현하고, 원인에 맞는 최소 보정으로 정답 한 번에 보상 한 칸만 유지한다.

## 성공 조건

1. `COMPLETE` 새 PID 복원 후 지우기 전 재입력이 추가 `SUCCESS`·추가 126 px 이동을 만드는지 1배와 지연 조건에서 구분한다.
2. 한 번 완료된 횟과 보상은 지우기로 다음 입력을 준비하기 전까지 포인터 재입력으로 변하지 않는다.
3. 정상 성공 이동, `MOVING` 입력 잠금, 지우기 후 소방차 시작 위치·다음 입력은 회귀하지 않는다.
4. 성공 음성·완료 횟·`★ ✓`·차량 비가림·비상호작이 일관되게 유지된다.
5. 자동 상태 회귀, `./scripts/verify.sh`, `git diff --check`를 통과한다.
6. 정확한 2340 × 1080 새 연속 화면·hierarchy·callback과 아이 대리 QA, `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

제품 코드를 먼저 바꾸지 않고 루프 058의 제외 탐색을 독립 재현한다. 복원 offset 중복 합산과 완료 후 포인터 수용을 로그·상태로 구분한 뒤 실제 실패에만 최소 보정을 적용한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
