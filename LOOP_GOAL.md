# 루프 목표 020 — 성공 음성 중 지우기 QA

## 목표

정확한 2340 × 1080에서 정상 `ㄱ` 획의 `SUCCESS` 음성이 재생 중일 때 지우기를 눌러 선·결과·음성 전환 일관성을 탐색한다. 루프 019의 새 아이 대리 QA에서 불편 없이 남은 미검증 흐름이며, 문제 입증 전에 제품 로직을 바꾸지 않는다.

## 성공 조건

1. `alarmquest-qa`의 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`에서 새 debug APK를 검증한다.
2. Canvas 1962 × 775 px와 정사각형 `ㄱ` geometry를 유지한다.
3. `SUCCESS` 재생 시작 뒤 서로 다른 시점 최소 5개에서 지우기를 누른다.
4. 각 입력 좌표, 지우기 시점, 선·결과 화면, 음성 callback 순서를 기록한다.
5. 글을 읽지 않는 아이 관점에서 선·결과가 일관되게 초기화되고 이전 성공 음성이 중지되는지 아이 대리 QA한다.
6. 문제가 입증되면 가장 작은 상태 전환 보정과 단위 테스트만 적용하고, 문제가 없으면 제품 코드를 바꾸지 않는다.
7. 코드를 바꾸면 `./scripts/verify.sh`와 `git diff --check`가 통과한다.
8. `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

정상 입력으로 `SUCCESS` 재생을 시작한 뒤 서로 다른 지우기 시점 최소 5개를 정의하고 제품 코드 변경 없이 재현한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 로컬 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
