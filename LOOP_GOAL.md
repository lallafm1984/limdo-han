# 루프 목표 226 — `ㄷ + ㅗ → 도` 셋째 초성 가로 모음 확장

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 다섯 살 아이가 오른쪽 세로 모음과 다른 초성 아래의 `ㅗ`를 모양으로 구분해 `도`를 조립한다.
새로운 근거: 루프 225는 `ㄷ + ㅕ → 뎌`만 연결했으며, 아래 가로 모음 `ㅗ`와 production `도` 쓰기 흐름은 아직 구현하지 않았다.
중복 방지: 이번 루프는 `ㄷ + ㅗ → 도` 한 조립 흐름과 production 쓰기 연결만 다룬다. 다음 모음은 함께 늘리지 않는다.

## 목표

`ㄷ`을 위쪽 칸과 `ㅗ`를 아래쪽 칸에 놓아 `도`를 완성하고 production `도` 쓰기 lesson으로 연결한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. `뎌`와 `도`를 모양으로 구분해 선택하고 홈·뒤로 복귀가 동작한다.
3. `ㄷ`·`ㅗ` 조각과 위·아래 칸, 조립 전·선택·완성 상태가 글을 읽지 않아도 구분된다.
4. 오순서·빈 칸은 완성으로 처리하지 않는다.
5. 조립·production 표시·안내·판정이 같은 정사각형 em·균일 배율·자모 배치를 쓴다.
6. WritingCanvas 1962 × 954 px·네 조작·정방향 입력·성공·다음·홈 회귀가 없다.
7. 정확한 2340 × 1080 전·후 PNG·hierarchy·focus·APK SHA와 핵심 상태에서 잘림·겹침·가림이 0건이다.
8. 새 P0·P1·진행 방해 P2가 0건이고 자동 그래픽 디자인·자동 QA·에뮬레이터·아이 대리 QA가 통과한다.

## 완료 정의

`ㄷ + ㅗ → 도` 한 연결만 통과하면 완료하고 다음 모음은 별도 루프로 나눈다.
완료 체크포인트만 `git push origin HEAD`로 일반 push한다.
