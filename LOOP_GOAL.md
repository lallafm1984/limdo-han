# 루프 목표 160 — 1차 목표 최종 전체 검증·완료 관문

## 작업 가치 관문

분류: 새 사용자 위험
사용자 가치: 세 메뉴와 14글자 전체 흐름이 하나의 1차 학습 경험으로 회귀 없이 이어진다.
새로운 근거: 루프 159는 아이 이해 시나리오 8개를 개별 통과했지만 기획서의 전체 관문과 14글자 흐름을 한 번의 최종 근거로 통합 판정하지 않았다.
중복 방지: 동일 흐름의 횟수를 늘리지 않고 1차 완료에 필요한 세 메뉴·14글자 행렬과 독립 역할 관문을 한 번만 통합 판정한다.

## 목표

`docs/1차-목표-작업-기획서.md`의 완료 관문을 새 자동·에뮬레이터·화면 근거로 전체 판정하고 1차 목표를 완료한다.

## 성공 조건

1. `자음`·`모음`·`가나다` 세 메뉴의 홈 선택→글자 선택→쓰기 진입→홈 복귀가 새 근거로 통과한다.
2. `가·나·다·라·마·바·사·아·자·차·카·타·파·하` 14글자의 선택·production 성공·횟수 보상·다음·홈 동작이 전체 행렬에서 통과한다.
3. 자형·획순·획 방향·표시·안내·판정이 공유 geometry를 유지하고 1962 × 954 px WritingCanvas와 좌우 조작이 회귀하지 않는다.
4. `자동 디자인 역할`, `자동 QA 역할`, `아이 대리 시뮬레이션: 8/8 통과`를 서로 분리해 판정한다.
5. P0 0개, P1 0개이고 새 아이 대리 QA에 미해결 제품 불편이 없다.
6. `실제 아이 관찰: 실행 안 함`을 자동 근거와 분리해 기록한다.
7. `./scripts/verify.sh`, `git diff --check`, LimDo focus, 정확한 2340 × 1080 새 화면 근거가 통과한다.
8. 모든 관문 통과 뒤 큐를 `없음`, 상태를 `IDLE`로 바꾸고 2차 목표를 시작하지 않는다.

## 완료 정의

모든 성공 조건을 새 근거로 통과한 뒤 최종 아이 대리 QA를 남기고, 1차 목표 완료·`IDLE` 전환을 하나의 체크포인트 커밋으로 만든 뒤 `git push origin HEAD`로 일반 push한다.
