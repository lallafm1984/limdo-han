# 루프 목표 047 — 3회 재순환 뒤 4회차 성공 완료 회복 QA

## 목표

정확한 2340 × 1080에서 앱 데이터를 한 번만 초기화하고, 정상 중심선 입력→`SUCCESS` 시작 callback 즉시 지우기를 같은 세션에서 3회 연속 반복한 뒤 네 번째 정상 입력을 지우지 않고 성공 완료까지 유지했을 때 새 성공 화면과 음성이 한 번만 정상 회복하는지 먼저 확인한다. 문제가 입증될 때만 반복 초기화 뒤 입력·음성 상태를 최소 보정한다.

## 성공 조건

1. 정확한 에뮬레이터에서 앱 데이터를 한 번만 초기화하고 초기 안내 완료 뒤 정상 중심선을 네 번 입력한다.
2. 첫 세 입력의 `SUCCESS` 시작 callback 직후 추가 sleep 없이 지우기를 한 번씩 전달하고, 중간에 앱 데이터 재초기화나 추가 지우기를 사용하지 않는다.
3. 네 번째 입력은 지우지 않고 굵은 완료 획·`★ ✓`가 나타나며 `SUCCESS` 시작→완료 callback이 정확히 한 번 이어지는지 확인한다.
4. 네 번째 성공 완료 뒤 3초 이상 이전 취소 callback이 뒤늦게 나타나거나 성공 화면을 덮지 않는지 확인한다.
5. Canvas 1962 × 775 px, 주요 버튼 373 × 168 px와 간격 37 px를 유지한다.
6. 제품 문제가 없으면 코드를 바꾸지 않고 QA 근거만 남긴다.
7. `./scripts/verify.sh`와 `git diff --check`가 통과하고 새 아이 대리 QA 및 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

앱 데이터 초기화와 초기 안내 완료 뒤 성공 callback 즉시 지우기를 3회 연속 재현하고, 네 번째 정상 입력을 성공 완료까지 유지한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
