# 루프 목표 132 — 짧은 획 화살촉 방향성 강화

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 글을 읽지 못하는 아이가 짧은 3획에서도 움직이는 표식을 단순한 점이 아니라 오른쪽을 가리키는 화살표로 즉시 구분할 수 있다.
새로운 근거: 루프 131 새 APK의 정확한 2340 × 1080 `captures/loop131/iteration1/short-third-guide/03-third-guide-frame-a.png`~`05-third-guide-frame-c.png`에서 축소된 안내가 시작·끝 색 중심은 가리지 않지만 화살촉 전체가 약 23 px 길이에 묶여 124 px 획과 지름 약 154 px 시작점에 비해 매우 작다.
중복 방지: 루프 131에서 통과한 3획 안내 중심의 25~75% 범위, 링 0.5배, production 좌표·판정을 유지한다. 화살촉의 방향 식별성만 별도 최소 크기로 보강하고 안내 재생 횟수나 캡처 횟수를 늘리지 않는다.

## 목표

짧은 production `가` 3획의 링과 이동 범위는 유지하면서 오른쪽 화살촉만 아이가 한눈에 구분할 수 있는 크기로 보강한다.

## 성공 조건

1. 3획 안내는 연속 프레임에서 오른쪽 화살촉이 분명하며 초록 시작점과 주황 끝점의 색 중심을 가리지 않는다.
2. 화살촉 최소 크기는 production 현재 획 길이·시각 배율에서 파생하고 3획 전용 화면 좌표를 추가하지 않는다.
3. 루프 131의 3획 25~75% 이동 범위와 0.5배 링, 1·2획 기존 크기·방향을 회귀시키지 않는다.
4. 정상 3획·재시도·지우기·음성·3칸 보상·다음 차량 흐름을 회귀시키지 않는다.
5. 정확한 2340 × 1080에서 2획 완료 뒤 연속 프레임, 3획 성공 화면과 hierarchy를 확인한다.
6. `./scripts/verify.sh`, `./scripts/check-automation.sh`, `git diff --check`를 통과한다.
7. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

`drawDirectionArrow`의 전체 배율과 화살촉 비율을 분리해, 짧은 획 링·중심 이동은 유지하면서 화살촉 방향만 최소 크기를 갖게 하는 최소 가설을 세운다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비하고, 검증된 완료·준비 변경을 커밋 하나로 만든 뒤 `git push origin HEAD`로 일반 push한다.
