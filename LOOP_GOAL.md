# 루프 목표 013 — `ㄱ` 세로획 corridor 경계 반복 흔들림 QA

## 목표

정확한 2340 × 1080에서 `ㄱ` 꼭지점을 정상 통과한 뒤 세로획 corridor 경계를 좌우로 짧게 여러 번 드나드는 한 획을 실행해 세로 진행과 횡방향 흔들림의 판정 일관성을 탐색한다.

이번 루프는 루프 012 아이 대리 QA에서 제품 불편 없이 남은 구체적인 미검증 흐름이다. 문제가 입증되기 전에 판정 알고리즘을 바꾸지 않고, 새 글자·수업·저장·점수·서버·계정·분석·권한·의존성을 추가하지 않는다.

## 성공 조건

1. `alarmquest-qa`의 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`에서 새 debug APK를 검증한다.
2. Canvas 1962 × 775 px와 정사각형 `ㄱ` geometry를 유지한다.
3. 정상 세로 진행과 비교해 세로획 corridor 경계를 서로 다른 진폭·횟수로 드나드는 대표 한 획을 최소 5개 실행한다.
4. 각 입력의 좌표, 경계 횡단 횟수, 판정 결과, 화면 표시, 음성 callback을 기록한다.
5. 글을 읽지 않는 아이 관점에서 유사한 입력이 일관되게 성공·재시도로 보이고 들리는지 아이 대리 QA한다.
6. 문제가 입증되면 가장 작은 세로획 geometry 보정과 단위 테스트만 적용하고, 문제가 없으면 제품 코드를 바꾸지 않는다.
7. 코드를 바꾸면 `./scripts/verify.sh`의 자동화 계약, 단위 테스트, Android lint, debug build와 `git diff --check`가 통과한다.
8. `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

정상 세로 진행과 비교할 세로획 corridor 경계 좌우 흔들림 좌표 세트를 먼저 정의하고 제품 코드 변경 없이 재현한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 로컬 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다. push가 실패하면 정확한 오류를 기록하고 force push하지 않는다.
