# 루프 목표 162 — `ㄹ`·`라` 교육용 3획 자형·획순·방향·판정 공유 교정

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 아이가 참고 교육 자료와 같은 3획 순서·방향으로 `ㄹ`과 `라`를 보고 따라 쓴다.
새로운 근거: 2026-08-27 사용자가 자음 참고 이미지와 함께 `ㄹ` 획순·방향 결함을 보고했고, 현재 template의 둘째 획은 참고 3획 계약과 다른 연결·방향을 사용한다.
중복 방지: `ㄹ`과 `라`의 공유 geometry를 하나의 계약으로 고정하고 정방향·역방향 판정과 정확한 화면을 통과한 뒤 같은 흐름을 횟수만 늘려 재검증하지 않는다.

## 목표

사용자 참고 자형의 3획을 `ㄹ`·`라`에 공유하고 표시·동적 안내·음성·입력 판정·획수 보상을 일치시킨다.

## 성공 조건

1. `ㄹ`의 첫 획은 위 가로를 왼쪽에서 오른쪽으로 쓴 뒤 오른쪽을 아래로 이어 쓴다.
2. 둘째 획은 가운데 가로를 왼쪽에서 오른쪽으로 독립해 쓴다.
3. 셋째 획은 왼쪽 아래로 내려간 뒤 아래 가로를 오른쪽으로 이어 쓴다.
4. `ㄹ`과 `라`의 표시·안내·판정이 같은 3획 template·방향 metadata를 공유하고 음성·획수 보상이 일치한다.
5. 정방향 3획은 순서대로 수락되고 가운데 가로의 역방향·잘못된 획 순서는 성공으로 통과하지 않는다.
6. 정확한 2340 × 1080에서 `ㄹ`·`라` 초기·획별·성공 화면의 자형·접점·방향을 직접 읽고 WritingCanvas 1962 × 954 px, LimDo focus, 조작·장식 비가림을 통과한다.
7. `./scripts/verify.sh`, `git diff --check`, 새 아이 대리 QA가 통과하고 P0·P1 미해결 결함이 없으며 `실제 아이 관찰: 실행 안 함`을 분리 기록한다.

## 완료 정의

모든 성공 조건을 새 자동·에뮬레이터 근거로 통과한 뒤 `ㅅ·ㅈ·ㅊ` 공유 사선·획순 결함을 루프 163으로 준비한다. 루프 162 완료와 다음 루프 준비를 체크포인트 커밋 하나로 만들고 `git push origin HEAD`로 일반 push한다. 루프 162의 구현은 다음 새 세션이 수행한다.
