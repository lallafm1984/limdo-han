# 루프 목표 141 — 다음 기본 모음 묶음 확장

## 작업 가치 관문

분류: 사용자 지시
사용자 가치: 기본 자음 단계를 마친 아이가 이미 배운 `ㅏ` 다음 공식 순서의 `ㅑ → ㅓ → ㅕ`를 같은 큰 쓰기 경험으로 이어 배울 수 있다.
새로운 근거: 루프 140의 새 2340 × 1080 아이 대리 QA에서 기본 자음 14개가 새 불편 없이 통과했고, 교육부 단계 계약의 다음 미완료 제품 단계는 모음 확장이다.
중복 방지: 이미 통과한 기본 자음과 `ㅏ`, 조합 글자, 뒤 모음을 함께 변경하지 않고 `ㅑ → ㅓ → ㅕ`만 조사·구현·검증한다.

## 목표

`ㅑ → ㅓ → ㅕ`를 교육용 획 template, 획순·방향 판정, 현재 획 동적 안내, 한국어 음성, 획수 자동차 보상과 다음 전환에 연결한다. 기준은 정확한 2340 × 1080이다.

## 성공 조건

1. `ㅑ·ㅓ·ㅕ`의 순서·획 근거를 확인하고 공식 근거와 LimDo 제품 template 결정을 문서에서 구분한다.
2. 기본 자음 다음 기존 `ㅏ → ㅑ → ㅓ → ㅕ`로 이어지며 다른 글자를 추가하지 않는다.
3. 정사각형 em 안의 고유 모양·비율과 guide·판정의 단일 template을 유지한다.
4. 획별 시작·끝·움직이는 방향과 짧은 한국어 음성이 전체 획을 안내한다.
5. 정상 입력은 획순·첫 방향을 판정하고 자동차가 실제 획수만큼 이동하며 글자와 24 dp 이상 떨어진다.
6. 기존 18개 글자의 geometry·판정·음성·보상·다음 흐름을 회귀시키지 않는다.
7. 새 단위 테스트, `./scripts/verify.sh`, `git diff --check`와 정확한 2340 × 1080 초기·입력·보상·전환을 통과한다.
8. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

`ㅑ·ㅓ·ㅕ`의 획 근거를 확인한 뒤 기존 lesson 계약에 추가하는 최소 구현을 시작한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA의 최우선 미해결 불편 또는 다음 미완료 제품 단계 하나만 준비하고, 커밋 후 `git push origin HEAD`로 일반 push한다.
