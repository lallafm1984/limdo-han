# 루프 목표 151 — 받침 있는 글자 `갈 → 감 → 갑` 두 번째 묶음

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 아이가 첫 받침 `ㄱ·ㄴ·ㄷ` 다음으로 이미 익힌 기본 자음 `ㄹ·ㅁ·ㅂ`을 종성에 결합해 받침 글자의 짜임을 단계적으로 넓힌다.
새로운 근거: 루프 150 최종 아이 대리 QA에서 `각·간·갇`의 첫 상하 짜임과 입력 흐름이 통과했고 새 미해결 불편은 없었다. 최신 사용자 지시의 다음 미구현 제품 단계는 받침 있는 글자이며 production에는 종성 `ㄹ·ㅁ·ㅂ` 조합이 없다.
중복 방지: 루프 150의 `각·간·갇` 화면이나 레이아웃을 다시 수정하지 않는다. 다음 좁은 묶음 `갈·감·갑`의 고유 종성 template·획순·판정·음성·보상·순환을 한 번 일치시켜 검증하면 완료하고, 나머지 받침이나 겹받침을 한꺼번에 추가하지 않는다.

## 목표

기존 `갇` 뒤에 `갈 → 감 → 갑`을 추가하고, 상단 `가` 아래에 종성 `ㄹ·ㅁ·ㅂ`을 정사각형 em의 고유 비율로 배치한다.

## 성공 조건

1. production 교육 순서가 `… → 각 → 간 → 갇 → 갈 → 감 → 갑 → ㄱ`로 이어지고 세 lesson의 stage가 `FINAL_CONSONANTS`이다.
2. `갈·감·갑`은 상단 초성 `ㄱ`·중성 `ㅏ`와 하단 종성 `ㄹ·ㅁ·ㅂ`의 상대 위치·고유 모양을 정사각형 em 안에서 보존하며 비균일 확대하지 않는다.
3. 표시·입력 판정·획별 동적 안내가 각 lesson의 같은 template과 균일 x/y 배율을 사용한다.
4. 정상 획순·방향은 성공하고 반대 방향·잘못된 시작·미완성 입력은 기존 판정 계약으로 구분된다.
5. 초기·획별·성공 한국어 음성과 자동차 보상 칸이 `갈·감·갑`의 `strokeCount`와 일치한다.
6. 기존 42개 lesson, 1962 × 954 px WritingCanvas, 좌우 조작 열과 음성 callback이 회귀하지 않는다.
7. 새 curriculum 회귀 테스트, `./scripts/verify.sh`, `git diff --check`와 정확한 2340 × 1080의 세 lesson 초기·입력·성공·`갑 → ㄱ` 전환 화면을 통과한다.
8. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

기존 `각·간·갇`의 상단 배치와 `ㄹ·ㅁ·ㅂ` production 자모 template을 대조해 `갈·감·갑`의 최소 lesson 계약과 회귀 테스트를 구현한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비한다. 새 불편과 미완료 사용자 제품 지시가 없으면 큐를 `없음`으로 만들고 `IDLE`로 종료한다. 완료 커밋 뒤 `git push origin HEAD`로 일반 push한다.
