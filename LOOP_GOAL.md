# 루프 목표 193 — `사` 초성 `ㅅ` 중심선 교정

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 처음 한글을 쓰는 아이가 꾸불거리거나 물결처럼 보이는 획이 아니라 단정하고 쉽게 따라 쓸 수 있는 교육용 `ㅅ`을 보게 한다.
새로운 근거: 사용자가 실제 앱의 `사`에서 초성 `ㅅ` 두 다리가 불필요하게 꾸불거린다고 직접 관찰했다. 현재 `saTemplate`의 `siotLegs`는 각 다리에 여러 중간점을 두어 굵은 Canvas 선에서 작은 방향 변화가 증폭될 수 있다.
중복 방지: `사`의 초성 `ㅅ` 중심선과 이를 고정하는 회귀 검사만 교정한다. 중성 `ㅏ`, 다른 글자, 입력 중 화살표 제거, 성공 후 자동 다음 이동은 이번 루프에서 바꾸지 않는다.

## 목표

1. `사`의 초성 `ㅅ` 두 다리를 불필요한 방향 반전과 물결 없이 매끈하고 단정한 교육용 중심선으로 표시한다.
2. 두 다리의 위 접합부와 아래 좌우 끝, 획순·방향을 보존하며 표시·점선·입력 판정이 같은 production geometry를 사용한다.
3. 같은 자형 결함의 재발 원인과 지켜야 할 규칙을 `docs/회귀-규칙.md`에 기록하고 측정 가능한 중심선 조건을 자동 검사로 고정한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. 자동 검사에서 `사` 초성 두 획의 시작·접합·끝, 진행 방향, 과도한 중간점·방향 반전 0건, 정방향 수락과 역방향·큰 이탈 거부가 고정된다.
3. `alarmquest-qa` 물리 1080 × 2340·가로 2340 × 1080·LimDo focus에서 변경 전·후 `사` 초기, 초성 각 획, 중성 획, 성공 PNG·hierarchy를 새로 수집한다.
4. 직접 시각 판정에서 `ㅅ` 두 다리가 한눈에 단정한 `ㅅ`으로 읽히고 꾸불거림·S자·갈고리·접합 틈·비정상 겹침·잘림이 0건이다.
5. 입력 전 시범과 입력 중 화살표 0개, 시작점·끝점·점선·아이 선·네 조작·재시도·성공 흐름을 유지하며 자동 그래픽 디자인·자동 QA·아이 대리 QA의 새 P0·P1·진행 방해 P2가 0건이다.

## 완료 정의

`사` 초성 `ㅅ`이 자동 geometry 계약과 에뮬레이터 실제 화면에서 단정한 교육용 자형으로 확인되고 공통 학습·판정 흐름에 회귀가 없을 때 완료한다. 완료 체크포인트만 `git push origin HEAD`로 일반 push하며 실패한 반복은 커밋하지 않는다.
