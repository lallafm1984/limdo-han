# 루프 목표 008 — 다시 듣기 빠른 반복 터치 QA

## 목표

정확한 2340 × 1080에서 초기 안내 완료 후 다시 듣기를 빠르게 반복 터치했을 때 음성 요청이 중첩되지 않고, 재생 중 비활 상태와 callback이 안전하게 유지되는지 새 근거로 판정한다.

이 루프는 먼저 QA 탐색을 수행하고, 구체적 문제가 입증된 경우에만 현재 화면 범위에서 최소 수정한다. 새 글자·수업·서버·계정·분석·권한·의존성을 추가하지 않는다.

## 성공 조건

1. `alarmquest-qa`의 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`에서 새 debug APK를 cold launch한다.
2. 초기 안내 완료 후 다시 듣기 bounds 안을 짧은 간격으로 여러 번 탭하고 입력 시각, UI hierarchy, `LimDoSpeech` callback을 시간 순으로 수집한다.
3. 하나의 안내만 재생되고 추가 탭은 재생 중 비활성으로 안전하게 무시되며, 완료 후 버튼이 다시 활성화된다.
4. 반복 터치 중에도 쓰기 위치·시작점·방향·지우기는 이해할 수 있고 안전하게 유지된다.
5. 문제가 입증되어 코드를 바꾸면 `./scripts/verify.sh`의 자동화 계약, 단위 테스트, lint, debug build가 통과한다.
6. 아이 대리 QA에서 반복 터치 중 누를 수 없음과 완료 후 다시 누를 수 있음을 문장을 읽지 않고 구분할 수 있는지 판정하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

제품 코드를 바꾸지 않고 완료 상태의 다시 듣기를 빠르게 반복 탭해 안전성을 먼저 판정한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 로컬 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다. push가 실패하면 정확한 오류를 기록하고 force push하지 않는다.
