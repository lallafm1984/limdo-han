# 루프 목표 127 — 음성 재생 중 다시 듣기 상태 표시

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 한글을 읽지 못하는 아이가 안내가 실제로 재생 중임을 소리 끊김이 아닌 분명한 스피커 상태로 이해하고, 완료 뒤 다시 듣기가 활성됨을 예측할 수 있다.
새로운 근거: 루프 126의 새 APK로 수집한 정확한 2340 × 1080 `captures/loop126/iteration1/next-initial-flow/04-next-initial-playing.png`에서 `INITIAL` callback과 획별 시범은 재생 중이지만, 다시 듣기는 빨간 금지선이 있는 `🔇`로 표시된다.
중복 방지: 재생 중·재생 가능·실제 사용 불가를 서로 다른 시각·접근성 상태로 구분하고, 재생 중 아이콘이 음소거나 고장으로 읽히지 않는 연속 화면 근거를 얻으면 종료한다. 음성 횟수를 늘리는 반복은 만들지 않는다.

## 목표

음성 안내 재생 중의 다시 듣기 버튼을 실제 소리 상태와 일치하는 아이 친화적 표시로 바꾼다.

## 성공 조건

1. `Playing`은 스피커에서 소리가 나오는 중임을 글 없이 보여 주고 `🔇`·빨간 금지선을 사용하지 않는다.
2. `Ready`/`Completed`는 기존 활성 다시 듣기로 복귀하고, `Initializing`/`Unavailable`/`Error`의 실제 사용 불가와 재생 중을 시각·접근성 설명에서 구분한다.
3. 재생 중 중복 탭은 계속 비활성이고, 지우기의 최신 요청 우선·완료 뒤 다시 듣기·음성 callback 계약을 회귀시키지 않는다.
4. `가` 3획·3칸 보상·다음 `INITIAL`·차량 순환을 회귀시키지 않는다.
5. 정확한 2340 × 1080에서 `INITIAL` 시작→재생 중 상태→완료→활성 복귀의 새 연속 화면·hierarchy·callback을 확인한다.
6. `./scripts/verify.sh`, `./scripts/check-automation.sh`, `git diff --check`를 통과한다.
7. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

`SpeechPlaybackState` 별 다시 듣기 표시를 조사하고 재생 중만 소리가 나오는 상태로 구분하는 최소 가설을 세운다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비하고, 검증된 완료·준비 변경을 커밋 하나로 만든 뒤 `git push origin HEAD`로 일반 push한다.
