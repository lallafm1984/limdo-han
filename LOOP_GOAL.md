# 루프 목표 040 — 성공 음성 재생 중 초기화 QA

## 목표

정확한 2340 × 1080에서 정상 성공의 `SUCCESS` 음성이 재생 중일 때 지우기 버튼을 한 번 누르면 이전 음성이 중단되고 완료 획·결과가 사라지며 초기 화면과 안내 상태가 모순 없이 복원되는지 먼저 확인한다. 문제가 입증될 때만 음성 취소와 초기화 순서를 최소 보정한다.

## 성공 조건

1. 정확한 에뮬레이터에서 정상 중심선 입력 직후 74.4 px 완료 획, `★ ✓`, `SUCCESS` 재생 시작을 새로 확인한다.
2. `SUCCESS` 재생 완료 전에 지우기 버튼을 한 번 눌러 이전 성공 음성이 중단되는지 callback으로 확인한다.
3. 지우기 직후 완료 획과 결과 표식이 사라지고 흰 guide 124 px, 초록 시작점, 두 방향 화살표, 주황 끝점과 초기 안내 상태가 복원되는지 확인한다.
4. 초기화 뒤 같은 정상 입력을 다시 수행해 새 `SUCCESS` callback이 한 번만 시작·완료되는지 확인한다.
5. Canvas 1962 × 775 px, 주요 버튼 373 × 168 px와 간격 37 px를 유지한다.
6. 제품 문제가 없으면 코드를 바꾸지 않고 QA 근거만 남긴다.
7. `./scripts/verify.sh`와 `git diff --check`가 통과하고 새 아이 대리 QA 및 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

정상 성공의 `SUCCESS` 재생 시작을 확인한 즉시 완료 전에 지우기 한 번과 재입력을 순서대로 수집해 화면과 callback을 비교한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
