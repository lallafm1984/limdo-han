# 루프 목표 195 — 홈 보호자 접근 아이콘 왼쪽 상단 재배치

## 작업 가치 관문

분류: 사용자 지시
사용자 가치: 아이의 세 학습 메뉴와 보호자 전용 진입이 시각적으로 경쟁하지 않고, 보호자는 최소 터치 영역을 유지한 별도 위치에서 접근할 수 있다.
새로운 근거: 2026-08-28 홈 실화면에서 보호자 접근 버튼이 화면 가운데 하단에 놓여 세 학습 카드와 같은 수준의 선택지처럼 보인다는 사용자 지시가 확인됐다.
중복 방지: 보호자 접근 버튼의 위치·시각 크기·터치 영역만 교정한다. 성공 후 자동 다음과 생성된 정답 이미지는 후속 독립 루프로 남긴다.

## 목표

홈 가운데 하단의 보호자 접근 버튼을 왼쪽 상단의 작은 시각 아이콘으로 옮기되, 2초 길게 누르기와 Android 최소 터치 영역·접근성 설명을 유지한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. 보호자 아이콘의 시각 실루엣은 세 학습 카드보다 작고 왼쪽 상단에 있으며, 터치 영역은 최소 192 × 192 px이다.
3. 정확한 2340 × 1080·LimDo focus에서 system bar·화면 외곽·제목·세 메뉴와 잘림·겹침이 0건이다.
4. 2초 미만 접촉은 진입하지 않고 2초 이상 길게 누르면 보호자 화면으로 정확히 한 번 진입하며, 짧은 한글 accessibility description을 유지한다.
5. 변경 전·후 동일 홈 PNG·hierarchy와 자동 그래픽 디자인·자동 QA·아이 대리 QA에서 새 P0·P1·진행 방해 P2가 0건이다.

## 완료 정의

작은 보호자 전용 시각 아이콘과 큰 터치 영역·길게 누르기·접근성이 자동 계약과 에뮬레이터 실화면에서 모두 통과할 때 완료한다. 완료 체크포인트만 `git push origin HEAD`로 일반 push한다.
