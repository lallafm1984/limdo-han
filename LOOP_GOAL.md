# 루프 목표 126 — `다음` 입력의 한국어 안내 재시작

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 한글을 읽지 못하지만 한국어를 들을 수 있는 아이가 `다음`으로 새 쓰기를 시작할 때, 새 차량과 빈 학습판뿐 아니라 처음과 같은 획순 음성 안내를 바로 듣고 행동을 연결할 수 있다.
새로운 근거: 루프 125 최종 새 APK의 정확한 2340 × 1080 `다음` 입력 화면은 경찰차에서 소방차로 바뀌고 동적 시범이 재개되지만, `INITIAL` 음성 요청은 첫 앱 시작의 `initialSpeechRequested`에 묶여 자동 재생되지 않는다.
중복 방지: `다음` 한 번마다 기존 `INITIAL` 안내가 정확히 한 번 재생되고, 중복 탭·지우기·다시 듣기·이전 성공 callback과 충돌하지 않으면 종료한다. 같은 안내 횟수를 늘리는 내구성 반복은 만들지 않는다.

## 목표

보상 완료 뒤 `다음`으로 새 입력을 준비할 때 초기 한국어 획순 안내도 정확히 한 번 다시 시작한다.

## 성공 조건

1. 활성 `다음` 한 번은 새 차량·빈 입력과 함께 `INITIAL` 음성을 정확히 한 번 요청한다.
2. 이전 `SUCCESS` 완료·취소 callback이 새 `INITIAL` 상태를 덮지 않고, 안내 중 다시 듣기·지우기는 기존 최신 요청 우선 계약을 유지한다.
3. 비활성 `다음`과 중복 탭은 추가 음성을 만들지 않는다.
4. `가` 3획 판정·3칸 보상·현재 차량 유지 지우기·다음 차량 한 단계 순환을 회귀시키지 않는다.
5. 정확한 2340 × 1080에서 보상 완료→다음→`INITIAL` 재생·완료→새 입력 화면과 hierarchy·callback을 새로 확인한다.
6. `./scripts/verify.sh`, `./scripts/check-automation.sh`, `git diff --check`를 통과한다.
7. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

`initialSpeechRequested`, `initialSpeechPending`, `onNext` 전이를 조사하고 `다음`이 기존 최신 음성 요청 계약으로 `INITIAL`을 한 번 재생하는 최소 가설을 세운다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비하고, 검증된 완료·준비 변경을 커밋 하나로 만든 뒤 `git push origin HEAD`로 일반 push한다.
