# 루프 목표 174 — 코덱스 내부 생성 홈·지우기·다음 버튼 아틀라스 적용

## 작업 가치 관문

분류: 사용자 지시
사용자 가치: 글을 읽지 못하는 다섯 살 아이가 홈·지우기·다음의 서로 다른 기능과 사용 가능 상태를 기기 글꼴 문자 없이 안정된 그림으로 구분하게 한다.
새로운 근거: 루프 172·173에서 성공과 실패 연출은 투명 아틀라스로 통일됐지만, 좌우 조작은 아직 Compose 문자·도형과 한글 레이블에 의존하며 성공·실패 그림체와 다르다.
중복 방지: 이번 루프는 홈·지우기·다음 버튼 자산과 기본·눌림·비활성 표시만 적용한다. 성공·실패 아틀라스, 버튼 callback, 글자 geometry와 판정은 보존하며 통과 뒤 전체 시각 흐름 통합 QA를 별도 다음 루프로 준비한다.

## 목표

1. 외부 API 없이 코덱스 내부 이미지 생성기로 홈·지우기·다음의 둥근 투명 버튼 그림을 생성한다.
2. 버튼 RGBA 아틀라스의 셀·슬롯·alpha·표시 크기를 한글 문서와 자동 검사로 고정한다.
3. production 좌우 조작의 문자·도형을 새 자산으로 교체하고 기본·눌림·다음 비활성 상태를 색 하나가 아닌 크기·명도·형태로 구분한다.
4. 자동 디자인·연출·QA·아이 대리 역할로 2340 × 1080의 비가림·터치 크기·callback 회귀를 독립 판정한다.

## 성공 조건

1. 최종 버튼 아틀라스는 RGBA PNG이고 모든 셀 네 모서리 alpha가 0이며 유효 픽셀이 셀 경계를 넘지 않는다.
2. 생성 prompt·원본·앱 파일·슬롯 좌표가 `docs/시각-아틀라스-루프-기획.md`에 한글로 기록된다.
3. 홈·지우기·다음은 글 없이 서로 구분되는 실루엣이며 성공·실패 자산과 같은 둥근 그림체를 사용한다.
4. 주요 버튼은 64 × 64 dp 이상, 간격은 12 dp 이상이고 눌림과 다음 비활성 상태가 모양·명도 변화로 구분된다.
5. 홈·지우기·다음 callback과 다음 비활성 차단은 회귀하지 않으며 글자 길·현재 시작점·성공·실패 연출을 가리지 않는다.
6. `./scripts/verify.sh`, `git diff --check`, 정확한 2340 × 1080 기본·눌림·비활성·활성 화면, focus·치명 오류 검사가 통과한다.
7. 자동·에뮬레이터·아이 대리 QA와 실제 아이 관찰 여부를 분리 기록한다.

## 완료 정의

모든 성공 조건을 통과하면 루프 174를 완료하고 세 메뉴·쓰기의 시각 아틀라스 통합 QA를 다음 단일 루프로 준비한다. 완료 체크포인트는 `git push origin HEAD`로 일반 push한다.
