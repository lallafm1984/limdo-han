# 루프 목표 191 — `카` 초성 `ㅋ` 가운데 가로획 연결 교정

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 다섯 살 아이가 `카`의 초성을 떠 있는 막대가 아닌 하나의 교육용 `ㅋ`로 보고 그대로 따라 쓸 수 있게 한다.
새로운 근거: 루프 189 완료 후 실화면 재검토에서 `카` 초성 `ㅋ` 가운데 가로획 끝이 주된 세로획 축에 맞닿지 않아 독립 막대처럼 보이는 구체적 결함이 확인됐다.
중복 방지: `카` 전용 초성 geometry의 가운데 가로획 연결만 교정하고, 입력 중 화살표 손가락 가림과 M2 음성 재생은 서로 다른 후속 루프로 분리한다.

## 목표

1. `카` 초성 `ㅋ`의 위 가로획과 가운데 가로획이 같은 오른쪽 세로획 축에 자연스럽게 연결되게 한다.
2. 흰 글자 길·현재 획 점선·동적 방향 표식·시작·끝·입력 판정이 하나의 production geometry를 공유하게 한다.
3. `가` 전용 곡선, `카` 중성 `ㅏ`, 다른 글자의 자형·획순·방향·판정은 변경하지 않는다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. 자동 geometry 검사에서 `카` 초성 가운데 가로획의 연결 끝점이 주된 세로획 중심선 위에 있고 guide 굵기를 포함해 빈틈·옆 이탈·의도하지 않은 겹침이 0건이다.
3. `alarmquest-qa` 물리 1080 × 2340·가로 2340 × 1080·LimDo focus에서 `카` 초기·1획 후·가운데 가로획 후·성공 PNG·hierarchy를 새로 수집하고 `ㅋ`로 즉시 읽히며 떠 있는 막대·갈고리·S자 실루엣·잘림·조작 가림이 0건이다.
4. production 정방향 4획이 순서대로 수락되고 역방향·잘못된 획순·큰 이탈은 거부되며 `가`·중성 `ㅏ`·대표 다른 글자 회귀가 없다.
5. 변경 전·후 실화면을 자동 그래픽 디자인·자동 QA·아이 대리 QA가 별도 판정해 새 P0·P1·진행 방해 P2가 0건이다.

## 완료 정의

`카` 초성 `ㅋ`의 두 가로획이 같은 세로축에 연결된 교육용 자형으로 표시·안내·판정되고, 자동·에뮤레이터·아이 대리 QA 회귀가 없을 때 완료한다. 다음에는 입력 중 방향 화살표의 손가락 가림 방지 결함 또는 M2의 쓰기 전 보호자 녹음 1회 재생 중 가장 우선인 단일 작업을 준비한다.
완료 체크포인트만 `git push origin HEAD`로 일반 push하며 실패한 반복은 커밋하거나 push하지 않는다.
