# 루프 목표 032 — 판정 경계 입력 event 간격 QA

## 목표

루프 031에서 보정한 155 px `SUCCESS`와 156 px `RETRY_DIRECTION` 경계가 빠름·보통·느림 입력 event 간격에서도 유지되는지 정확한 2340 × 1080에서 탐색한다. 문제 입증 전에 제품 로직을 바꾸지 않는다.

## 성공 조건

1. `alarmquest-qa`의 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`에서 새 debug APK를 검증한다.
2. Canvas 1962 × 775 px와 정사각형 `ㄱ` geometry를 유지한다.
3. 끝점 `(1461,742)` 도달 뒤 155 px와 156 px를 빠름·보통·느림의 통제된 서로 다른 event 간격으로 각각 최소 1회 놓는다.
4. 각 입력 좌표·명령 간격·실측 시점, 결과·버튼 화면과 음성 callback 순서를 기록한다.
5. event 간격과 무관하게 155 px는 `SUCCESS`, 156 px는 `RETRY_DIRECTION`인지 아이 대리 QA한다.
6. 문제가 입증되면 최소 보정과 단위 테스트만 적용하고, 아니면 제품 코드를 바꾸지 않는다.
7. 코드를 바꾸면 `./scripts/verify.sh`와 `git diff --check`가 통과한다.
8. `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

155 px와 156 px를 빠름·보통·느림 event 간격으로 각각 재현한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
