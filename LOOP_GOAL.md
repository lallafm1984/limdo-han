# 루프 목표 017 — 바깥 첫 접촉 재시도 뒤 두 번째 정상 입력 QA

## 목표

정확한 2340 × 1080에서 시작 tolerance 바깥 첫 접촉으로 `RETRY_START`를 받은 직후 지우기 버튼을 누르지 않고 초록 시작점에서 두 번째 정상 `ㄱ` 획을 그려 재시도 상태의 회복 일관성을 탐색한다.

루프 016 아이 대리 QA에서 제품 불편 없이 남은 미검증 흐름이다. 문제가 입증되기 전에 상태 또는 판정 로직을 바꾸지 않고, 새 글자·수업·저장·점수·서버·계정·분석·권한·의존성을 추가하지 않는다.

## 성공 조건

1. `alarmquest-qa`의 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`에서 새 debug APK를 검증한다.
2. Canvas 1962 × 775 px와 정사각형 `ㄱ` geometry를 유지한다.
3. 서로 다른 바깥 첫 접촉 실패 최소 5개 각각에서 지우기 없이 두 번째 정상 획을 실행한다.
4. 각 첫 입력과 두 번째 입력의 좌표, 판정 결과, 화면 표시, 음성 callback을 기록한다.
5. 글을 읽지 않는 아이 관점에서 시작 재시도 뒤 바로 두 번째 정상 획으로 일관되게 성공 회복하는지 아이 대리 QA한다.
6. 문제가 입증되면 가장 작은 재시도 상태 초기화 보정과 단위 테스트만 적용하고, 문제가 없으면 제품 코드를 바꾸지 않는다.
7. 코드를 바꾸면 `./scripts/verify.sh`와 `git diff --check`가 통과한다.
8. `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

바깥 첫 접촉 실패와 지우기 없이 이어질 두 번째 정상 획 좌표 세트를 정의하고 제품 코드 변경 없이 재현한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 로컬 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
