# 루프 목표 039 — 굵은 완료 획 초기화 복원 QA

## 목표

정확한 2340 × 1080에서 굵어진 74.4 px 아이 획으로 정상 성공한 뒤 지우기 버튼을 한 번 누르면 완료 획과 결과 표식이 사라지고 초기 guide·시작점·방향·끝점 및 안내 상태가 정확히 복원되는지 먼저 확인한다. 문제가 입증될 때만 초기화 동작을 최소 보정한다.

## 성공 조건

1. 정확한 에뮬레이터에서 정상 중심선 입력을 완료해 74.4 px 완료 획, `★ ✓`, `SUCCESS` callback을 새로 확인한다.
2. 성공 음성 완료 뒤 지우기 버튼을 한 번 눌러 완료 획과 결과 표식이 사라지는지 새 화면으로 확인한다.
3. 초기화 뒤 흰 guide 124 px, 초록 시작점, 두 방향 화살표, 주황 끝점과 초기 안내 상태가 복원되는지 확인한다.
4. 초기화 뒤 같은 정상 입력을 다시 수행해 `SUCCESS` callback이 한 번만 재생되는지 확인한다.
5. Canvas 1962 × 775 px, 주요 버튼 373 × 168 px와 간격 37 px를 유지한다.
6. 제품 문제가 없으면 코드를 바꾸지 않고 QA 근거만 남긴다.
7. `./scripts/verify.sh`와 `git diff --check`가 통과하고 새 아이 대리 QA 및 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

정상 성공 뒤 지우기 한 번과 재입력을 순서대로 수집해 초기화 전·후 화면과 callback을 비교한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
