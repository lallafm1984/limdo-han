# 루프 목표 130 — 현재 획 끝점만 강조

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 글을 읽지 못하는 아이가 지금 획의 초록 시작점과 주황 끝점을 겹친 신호로 오해하지 않고, 현재 획에서 어디까지 써야 하는지 바로 구분할 수 있다.
새로운 근거: 루프 129 최종 새 APK의 정확한 2340 × 1080 `captures/loop129/iteration1/current-start-flow/03-after-second-stroke.png`에서 production `가` 3획의 큰 초록 시작점과 글자 전체의 주황 최종 끝점 외곽이 겹쳐 두 신호가 경쟁한다.
중복 방지: 입력 전→1획 완료→2획 완료의 새 연속 화면에서 production `가`의 현재 획 끝점 하나만 주황색으로 보이고 현재 시작점과 겹치지 않으면 종료한다. 이미 통과한 시작점 이동이나 방향 화살표 수를 다시 늘리지 않는다.

## 목표

`가`의 주황 끝점이 글자 전체 끝에 고정되지 않고 현재 1·2·3획의 끝 위치를 일관되게 가리키게 한다.

## 성공 조건

1. 입력 전에는 production `ga` 1획 끝점에 주황 끝 표시가 보인다.
2. 1획 완료 뒤에는 2획 끝점, 2획 완료 뒤에는 3획 끝점만 주황색으로 보이며 현재 초록 시작점과 겹치지 않는다.
3. 끝점은 production `ga` geometry와 `completedStrokes`에서 파생하고 별도 좌표를 중복하지 않는다.
4. 현재 획 시작점·방향 안내·정상 3획·재시도·지우기·음성·3칸 보상·다음 차량 흐름을 회귀시키지 않는다.
5. 정확한 2340 × 1080에서 입력 전→1획 완료→2획 완료→3획 성공의 새 연속 화면·hierarchy를 확인한다.
6. `./scripts/verify.sh`, `./scripts/check-automation.sh`, `git diff --check`를 통과한다.
7. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

`WritingCanvas`의 끝점이 `glyph.strokes.flatten().last()`에 고정된 경로를 조사하고, `completedStrokes.size`로 현재 production 획 끝점을 선택하는 최소 가설을 세운다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비하고, 검증된 완료·준비 변경을 커밋 하나로 만든 뒤 `git push origin HEAD`로 일반 push한다.
