# 루프 목표 048 — 3회 재순환 뒤 회복 성공의 지우기 재복원 QA

## 목표

정확한 2340 × 1080에서 앱 데이터를 한 번만 초기화하고, 정상 중심선 입력→`SUCCESS` 시작 callback 즉시 지우기를 같은 세션에서 3회 연속 반복한 뒤 네 번째 정상 입력을 성공 완료까지 유지한다. 이어 지우기를 한 번만 눌렀을 때 이전 취소 callback의 개입 없이 초기 쓰기 상태로 다시 복원되는지 먼저 확인한다. 문제가 입증될 때만 반복 초기화 뒤 완료 상태의 지우기 전환을 최소 보정한다.

## 성공 조건

1. 정확한 에뮬레이터에서 앱 데이터를 한 번만 초기화하고 초기 안내 완료 뒤 정상 중심선을 네 번 입력한다.
2. 첫 세 입력의 `SUCCESS` 시작 callback 직후 추가 sleep 없이 지우기를 한 번씩 전달하고, 중간에 앱 데이터 재초기화나 추가 지우기를 사용하지 않는다.
3. 네 번째 입력은 지우지 않고 굵은 완료 획·`★ ✓`와 `SUCCESS` 시작→완료 callback 한 회를 확인한다.
4. 네 번째 성공 음성 완료 뒤 지우기를 정확히 한 번 누르면 완료 획·`★ ✓`가 사라지고 초기 guide·시작점·방향·끝점과 버튼 상태가 복원된다.
5. 지우기 뒤 3초 이상 이전 취소 또는 완료 callback이 늦게 나타나거나 초기 화면을 덮지 않는다.
6. Canvas 1962 × 775 px, 주요 버튼 373 × 168 px와 간격 37 px를 유지한다.
7. 제품 문제가 없으면 코드를 바꾸지 않고 QA 근거만 남긴다.
8. `./scripts/verify.sh`와 `git diff --check`가 통과하고 새 아이 대리 QA 및 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

세 번의 성공 callback 즉시 지우기와 네 번째 성공 완료를 같은 세션에서 재현한 뒤 지우기 한 번으로 초기 상태가 다시 복원되는지 확인한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
