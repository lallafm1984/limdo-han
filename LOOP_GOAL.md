# 루프 목표 016 — 시작점 바깥 첫 접촉 뒤 안쪽 진입 QA

## 목표

정확한 2340 × 1080에서 초록 시작점 tolerance 바깥에 손가락을 먼저 내린 뒤 손가락을 떼지 않고 시작점 안으로 미끄러져 들어와 정상 `ㄱ` 획으로 이어지는 한 획을 실행해 시작 판정의 일관성을 탐색한다.

루프 015 아이 대리 QA에서 제품 불편 없이 남은 미검증 흐름이다. 문제가 입증되기 전에 판정 알고리즘을 바꾸지 않고, 새 글자·수업·저장·점수·서버·계정·분석·권한·의존성을 추가하지 않는다.

## 성공 조건

1. `alarmquest-qa`의 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`에서 새 debug APK를 검증한다.
2. Canvas 1962 × 775 px와 정사각형 `ㄱ` geometry를 유지한다.
3. 정상 시작과 비교해 시작 tolerance 바깥의 첫 접촉 거리와 안쪽 진입 지점이 다른 대표 한 획을 최소 5개 실행한다.
4. 각 입력의 좌표, 첫 접촉의 경계 밖 거리, 안쪽 진입 위치, 판정 결과, 화면 표시, 음성 callback을 기록한다.
5. 글을 읽지 않는 아이 관점에서 유사한 바깥 첫 접촉 뒤 보정 입력이 일관되게 성공 또는 시작 재시도로 보이고 들리는지 아이 대리 QA한다.
6. 문제가 입증되면 가장 작은 start geometry 보정과 단위 테스트만 적용하고, 문제가 없으면 제품 코드를 바꾸지 않는다.
7. 코드를 바꾸면 `./scripts/verify.sh`와 `git diff --check`가 통과한다.
8. `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

정상 시작과 비교할 바깥 첫 접촉·안쪽 진입 좌표 세트를 정의하고 제품 코드 변경 없이 재현한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 로컬 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
