# 루프 목표 037 — 참고 화면 수준의 더 굵은 한글 획

## 목표

현재 정사각형 em의 15%인 흰색 `ㄱ` guide를 참고 화면 수준의 20%로 확대해 아이가 손가락으로 따라갈 넓은 통로로 인식하게 한다. 중심선, 정사각형 em, 균일 배율과 기존 입력 판정은 유지한다.

## 성공 조건

1. `WritingCanvasGeometry`의 guide 굵기를 em의 20%로 최소 변경하고 중심선·글자 bounds·균일 x/y 배율을 바꾸지 않는다.
2. 시각 guide만 굵히며 판정 허용 폭을 좁히지 않고 표시·판정이 같은 중심선 geometry에서 파생되게 유지한다.
3. guide em 비율과 정확한 2340 × 1080에서의 픽셀 굵기를 단위 테스트로 고정한다.
4. `alarmquest-qa`의 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`에서 초기·진행 중·재시도·성공 화면을 새로 캡처한다.
5. Canvas 1962 × 775 px, 정사각형 `ㄱ`, 화면 중앙 정렬과 시작점·방향·끝점 가독성을 실측·비교한다.
6. 정상 입력과 155 px `SUCCESS`·156 px `RETRY_DIRECTION` 경계 회귀를 새 화면·callback으로 확인한다.
7. 참고 이미지와 같은 상태를 비교해 흰색 쓰기 길이 가장 큰 과제로 보이며, 20%에서도 명확히 가늘 때만 20~22% 안에서 최소 추가 조정한다.
8. `./scripts/verify.sh`와 `git diff --check`가 통과하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

guide 굵기 상수를 em의 20%로 바꾸고 geometry·판정 회귀 테스트를 먼저 추가한 뒤 정확한 에뮬레이터 화면을 수집한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
