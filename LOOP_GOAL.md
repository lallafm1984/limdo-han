# 루프 목표 201 — 아이 보호자 목록 실행·마지막 완료

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 보호자가 구성한 글자 순서를 아이가 현재 위치부터 연속 학습하고, 마지막에서 자동 순환되지 않고 완료를 알 수 있다.
새로운 근거: 루프 200은 현재 index의 저장·표시·재시작을 완성했지만 아이 쓰기 흐름은 아직 자유 메뉴 순서만 사용하며 보호자 목록 순서·중복·마지막 완료를 소비하지 않는다.
중복 방지: 이번 루프는 저장된 목록 index의 아이 실행·정답 후 index 전진·마지막 완료 화면·홈·처음부터만 구현한다. 진도 요약·날짜·도움 단계는 후속 단일 루프로 분리한다.

## 목표

빈 목록은 기존 자유 쓰기를 유지하고, 목록이 있으면 저장된 현재 index의 lesson으로 진입해 중복과 순서를 그대로 실행하며 마지막 정답 후 완료 화면에서 홈·처음부터를 선택한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. 빈 보호자 목록에서 세 메뉴 자유 선택·이전·다음 순환이 유지되고, 목록이 있으면 저장 current index의 lesson으로 시작한다.
3. 정답 후 단일 녹음·무음·재생 실패의 기존 안전 절차 뒤 목록의 다음 저장 index로 정확히 한 번 이동하고, 같은 lesson 중복도 각 순번으로 소비한다.
4. 목록 마지막 정답 후 첫 항목으로 자동 순환하지 않고, 글을 읽지 않아도 완료와 `홈`·`처음부터` 두 192 px 이상 동작을 모양·위치·semantics로 구분한다.
5. `홈`은 현재 index를 마지막에 보존하고, `처음부터`는 index 0을 원자 저장한 뒤 첫 lesson으로 진입하며 강제 종료·재실행 후도 각각 복원된다.
6. 정확한 2340 × 1080·LimDo focus에서 첫·중간·중복·마지막·완료·빈 목록의 잘림·겹침·오동작이 0건이며 네 수동 조작·단일 녹음·성공 자동 다음에 회귀가 없다.
7. 자동 그래픽 디자인·자동 QA·아이 대리 QA에서 새 P0·P1·진행 방해 P2가 0건이다.

## 완료 정의

보호자 목록의 저장 현재 위치부터 아이 쓰기가 시작하고 순서·중복·index 저장·마지막 완료·홈·처음부터가 자동·에뮬레이터·실화면 근거로 통과할 때 완료한다. 진도 요약은 후속 제품 루프로 남기며 완료 체크포인트만 `git push origin HEAD`로 일반 push한다.
