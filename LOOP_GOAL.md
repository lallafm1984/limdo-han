# 루프 목표 166 — 1차 목표 보강 후 최종 전체 검증

## 작업 가치 관문

분류: 새 사용자 위험
사용자 가치: 교육용 자형과 `가나다` 다음 순서 교정 후에도 1차 목표 전체가 함께 정상임을 확인한다.
새로운 근거: 루프 161~165는 이전 최종 판정 후 callback과 geometry를 변경·감사했으므로 보강 변경을 포함한 새 통합 행렬이 필요하다.
중복 방지: 세 메뉴·14글자·24자모·대표 전체 흐름을 최종 상한으로 고정하고 통과 후 동일 통합 행렬 재실행을 만들지 않는다.

## 목표

루프 161~165의 보강을 포함해 `docs/1차-목표-작업-기획서.md`의 전체 완료 관문을 새 자동·에뮬레이터 행렬로 다시 판정한다.

## 성공 조건

1. 세 메뉴와 14글자 선택·쓰기·성공·다음·홈 복귀가 통과한다.
2. `가 → … → 하 → 가`, 24자모 참고 행렬, 표시·음성·판정·보상이 통과한다.
3. 2340 × 1080, LimDo focus, WritingCanvas 1962 × 954 px, 성공·거부·조작·연출 비가림이 통과한다.
4. `./scripts/verify.sh`, `git diff --check`, 자동 디자인 역할, 자동 QA 역할, `아이 대리 시뮬레이션: 8/8 통과`, 새 아이 대리 QA가 통과하고 P0·P1이 0개다.
5. `실제 아이 관찰: 실행 안 함`을 분리 기록한다.

## 완료 정의

모든 조건을 새 근거로 통과하면 큐를 `없음`, 상태를 `IDLE`로 바꾼 완료 체크포인트를 `git push origin HEAD`로 일반 push한다. 2차 목표는 시작하지 않는다.
