# 루프 목표 134 — 성공 표식의 완성 획 가림 방지

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 아이가 `가` 3획 성공 직후에도 성공 표식에 가리지 않은 전체 글자와 자기 획을 보며 결과를 확인할 수 있다.
새로운 근거: 루프 133 새 APK의 정확한 2340 × 1080 `captures/loop133/iteration1/safe-reward-path/03-after-one-step.png`~`07-final-success.png`에서 자동차는 글자와 24 dp 분리됐지만, 큰 초록 `★ ✓`가 production `가`의 두 세로획 하단과 파란 아이 획을 가린다.
중복 방지: 성공 표식의 의미·크기와 자동차 안전 경로·3칸 이동은 유지하고, 표식과 production 글자 bounds의 안전 배치만 보정한다. 같은 흐름의 반복 횟수나 캡처 수를 늘리지 않는다.

## 목표

성공 `★ ✓` 표식이 분명하게 보이면서도 production `가` guide와 아이 획을 가리지 않는 안전 영역에 배치된다.

## 성공 조건

1. 성공 표식 bounds가 production `가` guide·아이 획과 겹치지 않고 최소 24 dp 안전 여백을 유지한다.
2. 표식 위치는 현재 글자 geometry·표식 크기·화면 크기에서 파생하고 `가` 전용 절대 화면 좌표를 추가하지 않는다.
3. 성공 전에는 표식이 없고 성공 뒤에만 `★ ✓` 의미와 기존 큰 시인성을 유지한다.
4. 루프 133의 자동차 시작·1·2·3칸 24 dp 안전 경로, `ㄱ` 1칸 보상, 9종 자동차 순환·다음 차량 흐름을 회귀시키지 않는다.
5. 정확한 2340 × 1080에서 성공 전·성공 직후·3칸 완료·다음 차량 화면과 hierarchy를 확인한다.
6. `./scripts/verify.sh`, `./scripts/check-automation.sh`, `git diff --check`를 통과한다.
7. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

현재 성공 표식 bounds와 production `가` bounds를 같은 좌표계에서 측정해, 표식 의미와 시인성을 유지하면서 가림을 피하는 최소 배치 가설을 세운다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비하고, 검증된 완료·준비 변경을 커밋 하나로 만든 뒤 `git push origin HEAD`로 일반 push한다.
