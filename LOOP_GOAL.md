# 루프 목표 152 — 받침 글자 접근성 조사의 올바른 발음

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 접근성 서비스나 보호자가 쓰기판 설명을 들을 때 `갈를·감를·갑를`이라는 잘못된 조사가 아니라 자연스러운 `갈을·감을·갑을`로 듣고 글자 이름을 정확히 이해한다.
새로운 근거: 루프 151의 새 정확한 2340 × 1080 hierarchy에서 `큰 쓰기판. 초록 점에서 시작해 갈를/감를/갑를 …`가 처음 확인됐다. 새 받침 lesson의 실제 종성 유무를 반영하지 않고 모든 글자 뒤에 고정 `를`을 붙이는 production 접근성 결함이다.
중복 방지: 루프 151의 글자 geometry·판정·보상·순환을 다시 수정하거나 같은 화면을 재수집하지 않는다. lesson glyph의 마지막 음절 받침 유무에 따라 `을/를`을 고르는 공통 계약과 대표 무받침·받침 회귀 테스트를 구현하고 실제 hierarchy 발음이 맞으면 완료한다.

## 목표

쓰기판의 접근성 설명이 lesson glyph의 받침 유무에 맞춰 목적격 조사 `을/를`을 올바르게 선택하게 한다.

## 성공 조건

1. `ㄱ·가` 같은 무받침 glyph는 기존 `ㄱ를·가를`을 유지하고 `각·갈·감·갑` 같은 받침 glyph는 `각을·갈을·감을·갑을`로 설명한다.
2. 조사 선택은 표시 문자열의 마지막 한글 음절 종성 여부를 공통 함수에서 판정하며 lesson별 하드코딩을 만들지 않는다.
3. 비한글 또는 빈 glyph 입력의 동작을 테스트로 고정하고 기존 음성 cue·표시·입력 판정은 바꾸지 않는다.
4. 단위 테스트, `./scripts/verify.sh`, `git diff --check`가 통과한다.
5. 정확한 2340 × 1080 hierarchy에서 대표 무받침 `가를`과 받침 `갈을`을 확인하고 WritingCanvas 1962 × 954 px, 좌우 조작 열, 입력 흐름이 회귀하지 않는다.
6. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

현재 WritingCanvas content description 생성 지점을 찾아 glyph의 마지막 한글 음절 종성 여부를 반영하는 최소 조사 선택 함수와 회귀 테스트를 추가한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비한다. 새 불편과 미완료 제품 작업이 없으면 큐를 `없음`으로 만들고 `IDLE`로 종료한다. 완료 커밋 뒤 `git push origin HEAD`로 일반 push한다.
