# 루프 목표 038 — 굵은 guide와 대응하는 아이 획

## 목표

현재 Canvas 짧은 변의 4.5%인 약 35 px 진행 중·완료 아이 획을 124 px 흰 guide 안에서 손가락 이동을 분명히 확인할 수 있는 최소 굵기로 확대한다. guide, 중심선 geometry와 입력 판정은 바꾸지 않는다.

## 성공 조건

1. 진행 중·완료 아이 획 굵기를 guide보다 작고 시각적으로 충분히 대응되는 하나의 명시적 비율로 최소 변경한다.
2. 흰 guide 124 px, 중심선, 정사각형 em, 균일 배율과 `GieokTraceEvaluator` 판정을 바꾸지 않는다.
3. 아이 획과 guide의 픽셀 굵기·비율을 단위 테스트로 고정한다.
4. `alarmquest-qa`의 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`에서 초기·진행 중·재시도·성공 화면을 새로 캡처한다.
5. 진행 중과 완료 획이 손가락 경로를 분명히 보여 주면서 시작점·방향 화살표·끝점·결과 표식을 가리지 않는지 비교한다.
6. 정상 입력과 155 px `SUCCESS`·156 px `RETRY_DIRECTION` 경계를 새 화면·callback으로 확인한다.
7. Canvas 1962 × 775 px와 주요 버튼 373 × 168 px, 버튼 간격 37 px를 유지한다.
8. `./scripts/verify.sh`와 `git diff --check`가 통과하고 새 아이 대리 QA 및 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

아이 획 굵기 상수를 guide 대비 비율로 정리하고 화면 비교에 필요한 최소 후보를 자동 테스트한 뒤 정확한 에뮬레이터에서 검증한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
