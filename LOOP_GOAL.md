# 루프 목표 163 — `ㅅ`·`ㅈ`·`ㅊ` 교육용 사선·획순·판정 공유 교정

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 아이가 참고 교육 자료와 같은 분리·연결 획과 정방향 사선으로 `ㅅ`·`ㅈ`·`ㅊ`과 `사`·`자`·`차`를 보고 따라 쓴다.
새로운 근거: 2026-08-27 사용자 참고 자형은 `ㅅ` 둘째 획을 꼭짓점에서 오른쪽 아래로 요구하고, `ㅈ`은 윗가로와 왼쪽 아래 사선을 하나로 연결한 2획, `ㅊ`은 짧은 윗가로를 더한 3획으로 요구하지만 현재는 `ㅈ` 3획·`ㅊ` 4획으로 분리돼 있다.
중복 방지: 세 자음과 `사`·`자`·`차`의 사선 성분을 공유 geometry·방향 계약으로 고정하고 정방향·역방향·잘못된 순서·정확한 화면을 통과한 뒤 같은 흐름을 횟수만 늘려 재검증하지 않는다.

## 목표

`ㅅ`·`ㅈ`·`ㅊ`과 `사`·`자`·`차`의 표시·동적 안내·음성·입력 판정·획수 보상을 참고 자형의 사선·획 분리 계약으로 일치시킨다.

## 성공 조건

1. `ㅅ`은 첫 사선을 가운데에서 왼쪽 아래로, 둘째 사선을 같은 꼭짓점에서 오른쪽 아래로 쓴다.
2. `ㅈ`은 윗가로에서 왼쪽 아래 사선을 연결한 첫 획과 오른쪽 아래 사선 둘째 획의 2획이다.
3. `ㅊ`은 짧은 윗가로, 긴 가로에서 왼쪽 아래 사선을 연결한 획, 오른쪽 아래 사선의 3획이다.
4. `ㅅ`·`ㅈ`·`ㅊ`의 사선이 `사`·`자`·`차`에 공유되고 각 lesson의 음성·획수 보상이 일치한다.
5. 정방향 입력은 순서대로 수락되고 둘째 사선의 역방향·잘못된 획 순서는 성공으로 통과하지 않는다.
6. 정확한 2340 × 1080에서 대표 기본·조합 글자의 초기·획별·성공 화면을 직접 읽고 WritingCanvas 1962 × 954 px, LimDo focus, 조작·장식 비가림을 통과한다.
7. `./scripts/verify.sh`, `git diff --check`, 새 아이 대리 QA가 통과하고 P0·P1 미해결 결함이 없으며 `실제 아이 관찰: 실행 안 함`을 분리 기록한다.

## 완료 정의

모든 성공 조건을 새 자동·에뮬레이터 근거로 통과한 뒤 `ㅍ`·`파` 4획 순서 결함을 루프 164로 준비한다. 루프 163 완료와 다음 루프 준비를 체크포인트 커밋 하나로 만들고 `git push origin HEAD`로 일반 push한다. 루프 163의 구현은 다음 새 세션이 수행한다.
