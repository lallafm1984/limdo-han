# 루프 목표 136 — 첫 한글 교육 묶음 확장

## 작업 가치 관문

분류: 사용자 지시
사용자 가치: 아이가 이미 완성된 `가` 한 글자에 머물지 않고, 단순 자음·모음을 먼저 익힌 뒤 조합 글자로 연결되는 첫 학습 순서를 같은 큰 쓰기 경험으로 사용할 수 있다.
새로운 근거: 2026-08-26 사용자 지시에서 교육 순서에 맞춰 자음·모음과 다른 조합 글자로 범위를 확장하고 테스트·lint·에뮬레이터 검증까지 완료하라고 요청했다.
중복 방지: 전체 한글이나 설정·진도 저장을 한 번에 추가하지 않는다. 첫 묶음 `ㅏ → ㄱ → 가 → ㅓ → ㄴ → 너`의 geometry·판정·음성·다음 전환만 구현하고 각 글자 성공 뒤 종료한다.

## 목표

첫 한글 교육 묶음 `ㅏ → ㄱ → 가 → ㅓ → ㄴ → 너`가 같은 정사각형 em, 현재 획 동적 안내, 획순·방향 판정, 한국어 음성, 획수 보상과 다음 전환으로 동작한다.

기준 화면은 정확한 2340 × 1080이다.

## 성공 조건

1. 교육 순서는 `ㅏ → ㄱ → 가 → ㅓ → ㄴ → 너`이며, 모음·자음 뒤에 배운 요소를 조합한 글자가 온다.
2. 각 글자는 정사각형 em의 고유 자모 비율을 균일 배율로 표시하고 실제 판정과 같은 template을 사용한다.
3. `ㅏ`·`ㅓ`는 2획, `ㄱ`·`ㄴ`은 1획, `가`·`너`는 3획의 교육용 획순·방향을 판정한다.
4. 현재 글자와 현재 획에 맞는 시작점·끝점·동적 화살표·한국어 음성이 나오며 글자 문장을 읽지 않아도 진행 가능하다.
5. 성공 자동차 이동은 현재 글자의 획수와 일치하고 `다음` 뒤 다음 글자와 다음 차량으로 전환되며 마지막 뒤 첫 글자로 순환한다.
6. 기존 `가` 3획 판정·24 dp 자동차/성공 표식 여백·다시 듣기·지우기·재시도 흐름을 회귀시키지 않는다.
7. 새 단위 테스트, `./scripts/verify.sh`, `git diff --check`를 통과하고 정확한 2340 × 1080에서 최소 `ㅏ → ㄱ → 가 → ㅓ → ㄴ → 너` 화면 전환과 대표 입력 성공을 검증한다.
8. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

현재 `가` 하드코딩을 글자별 lesson 계약으로 좁게 일반화하고 첫 교육 묶음의 geometry·판정·음성·보상을 연결한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비한다. 새 불편이 없으면 큐를 비우고, 완료 변경을 커밋 하나로 만든 뒤 `git push origin HEAD`로 일반 push한다.
