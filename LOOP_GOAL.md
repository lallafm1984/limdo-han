# 루프 목표 175 — 세 메뉴·쓰기 시각 아틀라스 통합 QA

## 작업 가치 관문

분류: 새 사용자 위험
사용자 가치: 글을 읽지 못하는 다섯 살 아이가 세 메뉴에서 쓰기·성공·재시도·다음·홈까지 이어지는 시각 단서를 한 그림체계로 혼동 없이 이해하게 한다.
새로운 근거: 루프 172·173·174는 성공·실패·버튼 자산을 각각 단독 화면에서 검증했지만, 세 메뉴와 선택 전환을 포함한 전체 연속 흐름에서 세 아틀라스가 함께 보일 때의 시선 경쟁·상태 연결·callback은 아직 통합 판정하지 않았다.
중복 방지: 이번 루프는 기존 자산을 재생성하거나 동일 callback 반복 횟수를 늘리지 않는다. 세 메뉴의 독립 경로와 성공·실패·조작 자산이 한 연속 흐름에서 충돌하는지 고정 행렬로 한 번만 판정하고, 통과 뒤에는 시각 통합 QA 횟수를 늘리지 않는다.

## 목표

1. `자음·모음·가나다` 세 경로에서 홈·선택·쓰기·성공·실패·다음·홈 복귀를 한 고정 통합 행렬로 검증한다.
2. 성공·실패·버튼 RGBA 자산의 그림체·시각 위계·상태 전환·비가림을 자동 디자인·연출·QA 역할로 분리 판정한다.
3. 통합 근거에서 발견한 새 P0·P1 결함이 있으면 가장 중요한 하나만 다음 루프로 준비하고, 없으면 대기 중인 독창적 영웅 안내 음성 제품 루프를 준비한다.

## 성공 조건

1. `./scripts/verify.sh`와 `git diff --check`가 통과한다.
2. 정확한 2340 × 1080에서 세 메뉴의 홈→선택→쓰기 진입과 홈 복귀가 각각 통과한다.
3. 대표 한 경로의 실패→재시도→성공→다음 전환에서 상태와 callback이 엇갈리지 않는다.
4. 성공·실패·버튼 자산이 글자 길·현재 획·시작점·주요 조작을 가리지 않고 각 연출은 기존 유한 시간 안에 끝난다.
5. WritingCanvas 1962 × 954 px 이상, 주요 버튼 64 × 64 dp 이상, 간격 12 dp 이상과 글자 geometry·판정 회귀가 없다.
6. 자동 디자인·자동 연출·자동 QA·아이 대리 QA와 실제 아이 관찰 여부를 분리 기록한다.

## 완료 정의

고정 통합 행렬의 모든 조건을 새 자동·에뮬레이터·화면 근거로 통과하면 루프 175를 완료한다. 새 P0·P1 결함이 없으면 `.loop/user-directives.md`에 대기 중으로 기록된 독창적 영웅 안내 음성 생성·적용을 다음 단일 제품 루프로 준비한다. 완료 체크포인트는 `git push origin HEAD`로 일반 push한다.
