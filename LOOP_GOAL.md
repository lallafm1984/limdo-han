# 루프 목표 137 — 다음 기본 자음 묶음 확장

## 작업 가치 관문

분류: 사용자 지시
사용자 가치: 첫 `가나다` 묶음을 마친 아이가 교육부의 자음 단계와 국립국어원의 기본 자모 순서를 따라 다음 자음을 같은 큰 쓰기 경험으로 계속 익힐 수 있다.
새로운 근거: 2026-08-26 사용자는 현재 첫 묶음에서 끝내지 않고 교육부의 한글 교육 흐름으로 이어질 수 있게 구성하라고 요청했다. 루프 136은 첫 묶음과 단계 계약을 완료했으며 다음 기본 자음은 아직 제품에 없다.
중복 방지: 전체 자음이나 다음 모음·받침을 한꺼번에 추가하지 않는다. 공식 자모 순서에서 `ㄱ·ㄴ·ㄷ` 다음의 좁은 묶음 `ㄹ → ㅁ → ㅂ`만 조사·구현·검증한다.

## 목표

다음 기본 자음 묶음 `ㄹ → ㅁ → ㅂ`을 교육용 획 template, 획순·방향 판정, 현재 획 동적 안내, 한국어 음성, 획수 자동차 보상과 다음 전환에 연결한다.

기준 화면은 정확한 2340 × 1080이다.

## 성공 조건

1. `ㄹ·ㅁ·ㅂ`의 획 구분과 방향은 신뢰 가능한 한글 교육 근거를 먼저 확인하고, 공식 근거와 LimDo 제품 template 결정을 문서에서 구분한다.
2. 기본 자음 순서는 기존 `ㄱ → ㄴ → ㄷ` 다음 `ㄹ → ㅁ → ㅂ`으로 이어지고, 다음 모음이나 조합 글자를 이 루프에 추가하지 않는다.
3. 각 글자는 같은 정사각형 em 안에서 고유 모양과 비율을 보존하고 보이는 guide와 입력 판정이 같은 template을 사용한다.
4. 현재 획의 시작점·끝점·움직이는 방향과 짧은 한국어 음성이 글자별 교육용 획수 전체를 빠짐없이 안내한다.
5. 정상 입력은 획순과 첫 방향을 판정하고 성공 자동차가 선언된 획수만큼 이동한다. 4획을 넘는 글자도 자동차 전체가 화면 안에 있고 글자에서 24 dp 이상 떨어진다.
6. 기존 `ㄱ → ㄴ → ㄷ → ㅏ → 가 → 나 → 다`의 geometry·판정·음성·보상·다음 흐름을 회귀시키지 않는다.
7. 새 단위 테스트, `./scripts/verify.sh`, `git diff --check`를 통과하고 정확한 2340 × 1080에서 세 글자의 초기 화면·대표 정상 입력·성공 보상·다음 전환을 검증한다.
8. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

공식 또는 교육 기관 자료에서 `ㄹ·ㅁ·ㅂ`의 획 구분·방향 근거를 확인한 뒤, 기존 lesson 계약에 세 글자를 추가하는 최소 구현을 시작한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비한다. 새 불편이 없으면 교육부 흐름의 다음 미완료 단계 하나만 준비하고, 완료 변경을 커밋 하나로 만든 뒤 `git push origin HEAD`로 일반 push한다.
