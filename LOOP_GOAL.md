# 루프 목표 119 — `가` 3획 동적 안내 연결

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 아이가 글을 읽지 않아도 화면에 보이는 `가`의 세 획을 실제 획순과 방향대로 시작할 수 있게 한다.
새로운 근거: 루프 118의 정확한 2340 × 1080 화면에서 `가` 3획 표시는 연결됐지만 초록 시작점·정적 화살표·움직이는 표식은 이전 `ㄱ` 경로를 계속 가리켜 표시와 안내가 불일치한다.
중복 방지: production `ga` geometry에서 파생한 세 획의 시작·방향·끝 동적 안내를 연속 프레임으로 확인하면 이 안내 연결은 종료한다. 이 루프에서 3획 입력 판정이나 음성 문구까지 묶지 않는다.

## 목표

`WritingCanvasGeometry.ga`의 획 순서와 좌표에서 `가` 입력 전 동적 안내를 파생해, `ㄱ` 가로→세로, `ㅏ` 세로, `ㅏ` 짧은 가로 순서와 각 진행 방향을 화면에 보여 준다.

## 성공 조건

1. 입력 전 동적 안내가 production `ga(width, height)`의 세 획 좌표에서 파생되고 별도 좌표를 중복하지 않는다.
2. 안내 순서는 `ㄱ` 가로→세로, `ㅏ` 위→아래, `ㅏ` 왼쪽→오른쪽이며 각 획의 시작과 끝을 지나간다.
3. 움직이는 표식에 현재 구간 방향의 화살촉이 있고 획 전환 때 방향이 올바르게 바뀐다.
4. 정확한 2340 × 1080 연속 프레임에서 안내가 `가` guide·자동차·버튼을 가리지 않고 Canvas 입력을 가로채지 않음을 확인한다.
5. 기존 `ㄱ` geometry·입력 판정·보상·음성·초기화 테스트를 회귀시키지 않는다.
6. `./scripts/verify.sh`, `./scripts/check-automation.sh`, `git diff --check`를 통과한다.
7. 아이 대리 QA에서 동적 안내만 입증하고 3획 입력 판정을 완료했다고 주장하지 않으며 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

production `ga`의 각 선분 길이를 이용한 전체 진행률→획·선분 매핑을 geometry API로 추가하고, 기존 시범 원을 방향 화살표가 있는 `가` 동적 안내로 교체한다.

## 완료 정의

모든 조건을 통과하면 다음 제품 단계인 `가` 3획 입력 판정 연결을 새 아이 대리 QA 근거로 준비한다. 완료 루프와 준비된 다음 루프를 체크포인트 커밋 하나로 만들고 `git push origin HEAD`로 일반 push한다.
