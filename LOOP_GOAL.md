# 루프 목표 198 — 보호자 자유 학습 목록 중복 추가·로컬 보존 기반

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 보호자가 아이에게 보여 줄 lesson을 원하는 수와 반복으로 고르고, 앱을 다시 열어도 선택한 목록을 잃지 않는다.
새로운 근거: 현재 보호자 화면은 38개 lesson의 단일 녹음만 관리하며 학습 목록을 추가·보존하는 production model과 UI가 없다. 2차 M3의 첫 미구현 사용자 흐름이다.
중복 방지: 이번 루프는 lesson 중복 추가와 app-private 로컬 저장·재실행 복원까지만 구현한다. 재정렬·삭제·현재 위치·아이 목록 실행·완료 화면·진도 요약은 후속 단일 루프로 분리한다.

## 목표

보호자 전용 lesson 그리드에서 각 lesson을 자유 학습 목록에 추가하고 같은 lesson을 반복 추가할 수 있게 하며, 목록의 lessonId 순서를 app-private 로컬 저장소에 저장해 앱 강제 종료·재실행 뒤 그대로 복원한다. 기존 단일 녹음 관리 진입과 자유 쓰기 세 메뉴는 유지한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. 목록 model은 38개 production lessonId만 저장하고 중복·추가 순서를 보존하며, 손상·미지원 ID는 안전하게 제외하고 빈 목록으로 자유 쓰기를 막지 않는다.
3. 보호자 화면에서 lesson의 녹음 관리와 목록 추가가 글자·상태·동작으로 구분되고, 추가 1회마다 목록 항목이 정확히 1개 늘며 같은 lesson의 반복 추가가 가능하다.
4. 목록은 app-private·no-backup 범위에 원자적으로 저장되고 앱 강제 종료·재실행 뒤 ID·중복·순서가 그대로 복원되며, cloud backup·외부 저장·네트워크·민감 정보 저장은 0건이다.
5. 2340 × 1080·LimDo focus의 자음·모음·글자 보호자 화면에서 첫·중간·마지막 lesson, 목록 빈 상태·1개·중복 상태의 잘림·겹침·192 px 미만 주요 터치·내부 스크롤이 0건이고 기존 단일 녹음 callback이 유지된다.
6. 기존 세 메뉴 자유 쓰기·네 조작·단일 녹음·성공 자동 다음에 회귀가 없고 자동 그래픽 디자인·자동 QA·아이 대리 QA에서 새 P0·P1·진행 방해 P2가 0건이다.

## 완료 정의

중복 lesson 목록 추가와 로컬 재실행 복원이 자동·에뮬레이터·실화면 근거로 통과할 때 완료한다. 재정렬·삭제·목록 실행은 다음 제품 루프로 남기며 완료 체크포인트만 `git push origin HEAD`로 일반 push한다.
