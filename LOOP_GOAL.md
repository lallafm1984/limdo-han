# 루프 목표 018 — 재시도 음성 중 두 번째 정상 입력 QA

## 목표

정확한 2340 × 1080에서 시작 tolerance 바깥 첫 접촉으로 `RETRY_START` 음성이 재생 중일 때 지우기 버튼을 누르지 않고 초록 시작점에서 두 번째 정상 `ㄱ` 획을 그려 입력·화면·음성 전환 일관성을 탐색한다.

루프 017 아이 대리 QA에서 제품 불편 없이 남은 미검증 흐름이다. 문제가 입증되기 전에 상태·판정·음성 로직을 바꾸지 않고, 새 글자·수업·저장·점수·서버·계정·분석·권한·의존성을 추가하지 않는다.

## 성공 조건

1. `alarmquest-qa`의 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`에서 새 debug APK를 검증한다.
2. Canvas 1962 × 775 px와 정사각형 `ㄱ` geometry를 유지한다.
3. `RETRY_START` 재생 시작 뒤 서로 다른 시점 최소 5개에서 지우기 없이 두 번째 정상 획을 실행한다.
4. 각 첫 입력과 두 번째 입력의 좌표, 두 번째 입력 시작 시점, 판정 결과, 화면 표시, 음성 callback 순서를 기록한다.
5. 글을 읽지 않는 아이 관점에서 재시도 음성 중 바로 다시 쓸 때 입력이 일관되게 성공하고 음성이 겹치지 않는지 아이 대리 QA한다.
6. 문제가 입증되면 가장 작은 입력 또는 음성 상태 전환 보정과 단위 테스트만 적용하고, 문제가 없으면 제품 코드를 바꾸지 않는다.
7. 코드를 바꾸면 `./scripts/verify.sh`와 `git diff --check`가 통과한다.
8. `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

바깥 첫 접촉 실패 뒤 `RETRY_START` 재생 시작을 기준으로 서로 다른 두 번째 입력 시작 시점과 정상 획 좌표 세트를 정의하고 제품 코드 변경 없이 재현한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 로컬 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
