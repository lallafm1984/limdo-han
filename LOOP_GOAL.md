# 루프 목표 230 — `ㄷ + ㅡ → 드` 셋째 초성 평행 가로 모음 확장

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 다섯 살 아이가 `듀`의 아래쪽 짧은 획과 `드`의 평행 가로획을 모양으로 구분해 조립한다.
새로운 근거: 루프 229는 `ㄷ + ㅠ → 듀`만 연결했으며, 짧은 세로획이 없는 `ㅡ`와 production `드` 쓰기 흐름은 아직 구현하지 않았다.
중복 방지: 이번 루프는 `ㄷ + ㅡ → 드` 한 조립 흐름과 production 쓰기 연결만 다룬다. 다음 모음은 함께 늘리지 않는다.

## 목표

`ㄷ`을 위쪽 칸과 `ㅡ`를 아래쪽 칸에 놓아 `드`를 완성하고 production `드` 쓰기 lesson으로 연결한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. `듀`와 `드`를 아래쪽 짧은 세로획 유무로 구분해 선택하고 홈·뒤로 복귀가 동작한다.
3. `ㄷ`·`ㅡ` 조각과 위·아래 칸, 조립 전·선택·완성 상태가 글을 읽지 않아도 구분된다.
4. 오순서·빈 칸은 완성으로 처리하지 않는다.
5. 조립·production 표시·안내·판정이 같은 정사각형 em·균일 배율·자모 배치를 쓴다.
6. WritingCanvas 1962 × 954 px·네 조작·정방향 입력·성공·다음·홈 회귀가 없다.
7. 정확한 2340 × 1080 전·후 PNG·hierarchy·focus·APK SHA와 핵심 상태에서 잘림·겹침·가림이 0건이다.
8. 새 P0·P1·진행 방해 P2가 0건이고 자동 그래픽 디자인·자동 QA·에뮬레이터·아이 대리 QA가 통과한다.

## 완료 정의

`ㄷ + ㅡ → 드` 한 연결만 통과하면 완료하고 다음 모음은 별도 루프로 나눈다.
완료 체크포인트만 `git push origin HEAD`로 일반 push한다.
