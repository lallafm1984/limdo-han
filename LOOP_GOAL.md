# 루프 목표 061 — 복원된 `IDLE`의 `INITIAL` 재생 중 프로세스 복원 탐색

## 목표

새 PID로 복원된 소방차 `IDLE`에서 다시 듣기 `INITIAL`을 재생하는 중 상태를 저장하고 다시 새 PID로 복원했을 때 음성 pending·차량·빈 획·입력 가능 상태가 일관되는지 좁게 검증하고, 실제 실패가 확인될 때만 원인에 맞는 최소 보정을 적용한다.

## 성공 조건

1. 소방차 `IDLE` 새 PID 복원 뒤 다시 듣기 `INITIAL` 재생 중 상태를 `mHaveState=true` Bundle로 저장하고 다시 새 PID 복원을 확정한다.
2. 복원 전후 소방차 종류·시작 bounds·빈 획·결과 표식 없음·입력 가능 상태가 같다.
3. 끊긴 `INITIAL`이 새 PID에서 중복 없이 한 번만 마무리되거나 제품의 명시된 대체 동작으로 일관되게 끝난다.
4. 복원 뒤 첫 정상 입력이 `SUCCESS`를 한 번만 만들고 소방차를 126 px 한 칸만 이동시킨다.
5. 다시 듣기·지우기·비활성 다음·차량 비가림·비상호작과 음성 callback이 회귀하지 않는다.
6. 제품 변경이 있으면 자동 상태 회귀를 추가하고 `./scripts/verify.sh`, `git diff --check`를 통과한다.
7. 정확한 2340 × 1080 화면·hierarchy·PID·saved-state·callback과 아이 대리 QA, `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

제품 코드를 먼저 바꾸지 않고 독립 앱 데이터에서 소방차 `IDLE` 복원→다시 듣기 `INITIAL` 시작→HOME 저장→PID 종료→기존 task 복원→음성 완료→첫 정상 입력을 수집한다. 실제 불일치가 확인될 때만 최소 보정을 적용한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
