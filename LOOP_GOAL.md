# 루프 목표 206 — 세로 모음 조립 두 번째 좁은 묶음

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 다섯 살 아이가 `ㅏ`와 반대 방향으로 짧은 획이 놓이는 `ㅓ`를 초성 오른쪽에 조립해 `거`가 되는 위치 관계를 직접 비교하고 같은 글자 쓰기로 연결할 수 있다.
새로운 근거: 루프 205는 `ㄱ + ㅏ → 가`의 오른쪽 짧은 획 한 흐름만 구현했으며, 왼쪽 짧은 획을 가진 세로 모음 `ㅓ`의 조립 모양·production `거` 연결은 실질적으로 다른 미구현 교육 흐름이다.
중복 방지: 이번 루프는 `ㄱ + ㅓ → 거` 한 조립 화면과 쓰기 연결만 다룬다. 다른 모음·초성·받침을 함께 늘리지 않으며 통과 뒤 다음 모음은 새 루프로 분리한다.

## 목표

루프 205의 검증된 조립 상호작용과 production token을 재사용해 `ㄱ`과 세로 모음 `ㅓ`를 좌우 칸에 놓아 `거`를 완성하고 기존 `거` 쓰기 lesson으로 연결한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. 기존 조립 진입에서 `가`와 `거`를 글자 모양으로 구분해 선택하고 홈·뒤로 복귀가 동작한다.
3. `ㄱ`과 `ㅓ` 조각, 초성 왼쪽 칸과 세로 모음 오른쪽 칸, 조립 전·선택·완성 상태가 글을 읽지 않아도 구분된다.
4. 잘못된 칸·순서·빈 칸은 완성으로 처리하지 않고 처벌 없이 다시 안내한다.
5. 완성 `거`와 production `거` 표시·안내·판정이 같은 정사각형 em·균일 배율·자모 상대 위치를 사용한다.
6. 완성 뒤 기존 `거` WritingCanvas 1962 × 954 px로 진입하고 네 그림 조작·정방향 입력·성공·다음·홈 회귀가 없다.
7. 정확한 2340 × 1080 변경 전·후 PNG·hierarchy·focus·APK SHA와 시작·오순서·완성·쓰기 근거에서 잘림·겹침·가림이 0건이다.
8. 새 P0·P1·진행 방해 P2가 0건이고 자동 그래픽 디자인·자동 QA·에뮬레이터·아이 대리 QA가 통과한다.

## 완료 정의

`ㄱ + ㅓ → 거` 한 조립 흐름과 기존 `거` 쓰기 연결만 새 자동·실화면 근거로 통과하면 완료한다. 다른 모음·초성·받침은 별도 단일 루프로 나누며 완료 체크포인트만 `git push origin HEAD`로 일반 push한다.
