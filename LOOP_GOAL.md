# 루프 목표 052 — 입력 중 다음 방향 동적 안내

## 목표

정확한 2340 × 1080의 입력 중 상태에서 아이 손가락보다 조금 앞쪽에 현재 다음 방향만 짧게 움직이는 안내를 추가한다. 입력 전 반복 시범·정사각형 학습판과 기존 입력·판정·음성은 유지하며 자동차 자산·보상은 이번 루프에 구현하지 않는다.

## 성공 조건

1. 정상 입력 중 안내가 현재 진행 위치보다 앞선 같은 `ㄱ` 중심선 위에서 다음 방향을 짧게 움직인다.
2. 완료한 구간의 안내는 사라지거나 약해지고, 꼭지 전후에는 가로 안내에서 세로 안내로 순서대로 전환된다.
3. 안내는 guide·시작점·끝점·아이 획을 가리지 않고 터치 입력을 가로채지 않는다.
4. 입력 전 시범과 입력 중 안내가 동시에 나타나지 않으며 재시도·성공·지우기·다시 듣기·음성 상태와 충돌하지 않는다.
5. 정확한 2340 × 1080 연속 프레임과 단위 테스트로 진행 위치·다음 방향·상태 전환을 새로 검증한다.
6. `./scripts/verify.sh`와 `git diff --check`가 통과하고 새 아이 대리 QA 및 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

현재 입력 진행률과 판정 geometry를 확인하고, 입력 중 다음 방향 안내만 추가하는 최소 상태·애니메이션 변경을 적용한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 경찰차 기본 적용과 동물 안내 교체를 별도 루프로 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
