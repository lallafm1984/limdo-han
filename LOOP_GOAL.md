# 루프 목표 150 — 받침 있는 글자 `각 → 간 → 갇` 첫 묶음

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 아이가 이미 익힌 `가`와 기본 자음 `ㄱ·ㄴ·ㄷ`을 아래 받침으로 결합해 받침 있는 글자의 짜임을 처음 배운다.
새로운 근거: 루프 149 최종 아이 대리 QA에서 `파·하`까지의 받침 없는 `ㅏ` 조합이 통과했고 새 미해결 불편은 없었다. 사용자 지시의 교육 단계 계약에서 다음 미구현 단계는 `받침 있는 글자`이며, 현재 production에는 받침 geometry·판정·안내가 하나도 없다.
중복 방지: 루프 149의 `파·하`나 좌우 레이아웃을 다시 수정하지 않는다. 첫 받침 묶음 `각·간·갇`의 production template·획순·판정·음성·보상·순환을 한 번 일치시켜 검증하면 완료하고, 전체 받침 글자를 한꺼번에 추가하거나 동일 흐름을 다시 검증하지 않는다.

## 목표

기존 `하` 뒤에 `각 → 간 → 갇`을 추가하고, `가`의 초성 `ㄱ`·중성 `ㅏ` 아래에 종성 `ㄱ·ㄴ·ㄷ`을 정사각형 em의 고유 비율로 배치한다.

## 성공 조건

1. production 교육 순서가 `… → 파 → 하 → 각 → 간 → 갇 → ㄱ`로 이어지고 세 lesson의 stage가 `FINAL_CONSONANTS`이다.
2. `각·간·갇`은 초성 `ㄱ`, 중성 `ㅏ`, 종성 `ㄱ·ㄴ·ㄷ`의 표준 상하·좌우 상대 위치를 정사각형 em 안에서 보존하며 비균일 확대하지 않는다.
3. 표시·입력 판정·획별 동적 안내가 각 lesson의 같은 template과 균일 x/y 배율을 사용한다.
4. 정상 획순·방향은 성공하고 반대 방향·잘못된 시작·미완성 입력은 기존 판정 계약으로 구분된다.
5. 초기·획별·성공 한국어 음성과 자동차 보상 칸이 `각·간·갇`의 `strokeCount`와 일치한다.
6. 기존 39개 lesson, 1962 × 954 px WritingCanvas, 좌우 조작 열과 음성 callback이 회귀하지 않는다.
7. 새 curriculum 회귀 테스트, `./scripts/verify.sh`, `git diff --check`와 정확한 2340 × 1080의 세 lesson 초기·입력·성공·`갇 → ㄱ` 전환 화면을 통과한다.
8. 아이 대리 QA와 실제 아이 관찰을 구분하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

기존 `가·ㄱ·ㄴ·ㄷ` production template을 대조해 종성 공간을 포함한 `각·간·갇`의 최소 lesson 계약과 회귀 테스트를 구현한다.

## 완료 정의

모든 조건을 통과하면 새 아이 대리 QA에서 가장 중요한 미해결 불편 하나만 다음 루프로 준비한다. 새 불편과 미완료 사용자 제품 지시가 없으면 큐를 `없음`으로 만들고 `IDLE`로 종료한다. 완료 커밋 뒤 `git push origin HEAD`로 일반 push한다.
