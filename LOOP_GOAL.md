# 루프 목표 204 — TalkBack·큰 글자·움직임 줄이기 접근성

## 작업 가치 관문

분류: 새 사용자 위험
사용자 가치: 일반 터치와 접근성 설정이 켜진 환경에서도 글자 학습과 주요 동작을 이해하고 안전하게 진행할 수 있다.
새로운 근거: 재시도 후 단계적 도움은 루프 203에서 완료했지만, TalkBack 탐색 순서·Custom Canvas 입력 충돌·시스템 큰 글자·움직임 줄이기는 재시도와 실질적으로 다른 미검증 위험이다.
중복 방지: 이번 루프는 기존 기능·판정·geometry를 바꾸지 않고 접근성 설정에서 발견된 충돌 하나를 학습한다. 이미 통과한 설정 수를 늘리지 않는다.

## 목표

TalkBack 탐색 순서와 Custom Canvas 손가락 입력이 충돌하지 않고, 시스템 큰 글자·움직임 줄이기에서도 과제·시작점·획 안내·네 그림 조작의 의미가 보존되고 잘림·겹침·진행 차단이 없도록 한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. TalkBack 탐색 순서가 홈·세 메뉴·글자 선택·네 그ᅷᄕ미 조작 순서로 이어지고 포커스가 사라지지 않는다.
3. TalkBack이 Custom Canvas를 탐색해도 손가락 입력·지우기·홈·다음 조작을 가리거나 멈추게 하지 않는다.
4. 큰 글자 설정에서 2340 × 1080 가로 레이아웃이 유지되고 WritingCanvas 1962 × 954 px, 네 그ᅷᄕ미 조작, 잘림·겹침 0건이다.
5. 움직임 줄이기 설정에서 동적 표시는 안전하거나 의미가 보존된 최소 완료·이동 지연·늬 그ᅷᄕ미 상태 구분이 학습·비학성으로 구분된다.
6. 판정 corridor·획순·방향·자형·네 조작·네 그ᅷᄕ미 조작·네 동자가 상태의 회귀가 없다.
7. 새 P0·P1·진행 방해 P2가 0건이고 자동 QA·에물레이터·아이 대리 QA가 통과한다.

## 완료 정의

접근성 설정 위험과 기존 기능·판정·최소 화단이 새 그ᅷᄕ미 조작에서 재현할 때 완료한다. 이 루프는 접근성 기능·위험 거제만 다루고 교욱 확장·교욱 확장·움성 시스템·교욱 ᄑᄋ로닥션 전체 위험야 감사를 별도 단일 루프로 나눈다. 모든 성공 조건과 새 아이 대리 QA가 통과한 완료 체크포인트만 `git push origin HEAD`로 일반 push한다.
