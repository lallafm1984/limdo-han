# 루프 목표 009 — 교육용 한글 비율과 큰 쓰기 면적

## 목표

정확한 2340 × 1080에서 넓은 입력 Canvas를 유지하면서도 현재 `ㄱ`을 가로로 늘인 모양이 아니라 교육용 한글 글자꼴의 비율과 굵기로 보이고 판정되게 한다.

이번 루프는 현재 `ㄱ` 화면의 geometry를 고치고 이후 `가` 같은 조합 글자에도 재사용할 정규화 template 계약을 준비한다. 새 글자 수업, 저장, 점수, 서버, 계정, 분석, 권한, 의존성은 추가하지 않는다.

## 성공 조건

1. `alarmquest-qa`의 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`에서 새 debug APK를 검증한다.
2. 실제 손가락 입력 Canvas는 최소 1872 × 648 px이며 화면에서 가장 큰 아이 상호작용 영역을 유지한다.
3. 글자 geometry는 정사각형 em 안의 글자별 정규화 template을 사용하고 x축·y축에 동일한 균일 배율을 적용한다. Canvas를 채우기 위한 독립 가로·세로 늘이기는 금지한다.
4. 현재 `ㄱ`의 실제 글자 bounds는 가로와 세로가 각각 최소 580 px이고, 높이는 Canvas 높이의 75% 이상이다. template의 가로/세로 비율과 화면 실측 비율 차이는 5% 이내다.
5. 목표 획 굵기는 em 크기의 12% 이상 18% 이하이며, 굵은 교육용 글자처럼 손가락이 지나갈 넓은 입력 통로를 제공한다.
6. 보이는 목표 길, 시작점·방향·도착점, 입력 허용 범위와 `GieokTraceEvaluator` 판정은 같은 template과 균일 배율에서 파생한다.
7. geometry API는 특정 Canvas 비율에 묶인 `ㄱ` 전용 x/y 계산을 피한다. 테스트용 `가` fixture가 하나의 정사각형 em에서 자음과 모음의 상대 위치·비율을 유지하며 균일 배율로 변환되는지 검증한다. `가`를 새 화면에 노출하지 않는다.
8. 초기·재시도·성공·지우기 화면에서 글자 비율, 굵기, 시작·방향·도착 단서, 진한 아이 획과 음성·조작 회귀를 새로 확인한다.
9. `./scripts/verify.sh`의 자동화 계약, 단위 테스트, Android lint, debug build와 `git diff --check`가 통과한다.
10. 아이 대리 QA에서 글을 읽지 않고도 실제 한글과 비슷한 큰 글자 형태, 넓은 입력 통로, 시작·방향·결과를 구분할 수 있는지 판정하고 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

현재 1491 × 620 px로 늘어난 `ㄱ` geometry를 정사각형 em 기반 template과 균일 배율로 바꾸는 최소 변경부터 적용하고, 표시와 판정이 같은 좌표를 쓰는 단위 테스트를 추가한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 로컬 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다. push가 실패하면 정확한 오류를 기록하고 force push하지 않는다.
