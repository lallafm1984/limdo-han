# 루프 목표 045 — 회복 성공 callback 즉시 재지우기 재순환 QA

## 목표

정확한 2340 × 1080에서 첫 정상 입력의 `SUCCESS` 시작 callback 직후 지연 없이 지워 초기 상태로 수렴하고 정상 중심선을 다시 입력한 다음, 회복된 두 번째 `SUCCESS` 시작 callback 직후에도 다시 지연 없이 지워 초기 상태로 재순환하는지 먼저 확인한다. 문제가 입증될 때만 반복 초기화 뒤 입력·음성 상태를 최소 보정한다.

## 성공 조건

1. 정확한 에뮬레이터에서 앱 데이터를 한 번 초기화하고 첫 정상 입력의 `SUCCESS` 시작 callback 직후 추가 지연 없이 지우기 한 번으로 초기 상태 수렴을 새로 확인한다.
2. 앱 데이터 재초기화·추가 지우기 없이 정상 중심선을 다시 입력하고 두 번째 `SUCCESS` 시작 callback을 확인한다.
3. 두 번째 `SUCCESS` 시작 callback 직후 추가 지연 없이 지우기를 한 번 더 전달해 완료 획과 `★ ✓`가 사라지고 초기 guide·표식·버튼 상태로 수렴하는지 확인한다.
4. 두 취소된 `SUCCESS`의 늦은 완료 callback이나 성공 화면이 두 번째 지우기 뒤 되살아나지 않는지 3초 이상 확인한다.
5. Canvas 1962 × 775 px, 주요 버튼 373 × 168 px와 간격 37 px를 유지한다.
6. 제품 문제가 없으면 코드를 바꾸지 않고 QA 근거만 남긴다.
7. `./scripts/verify.sh`와 `git diff --check`가 통과하고 새 아이 대리 QA 및 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

앱 데이터 초기화와 초기 안내 완료 뒤 첫 성공 즉시 지우기→정상 재입력→회복된 두 번째 성공 즉시 지우기를 같은 세션에서 연속 재현한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
