# 루프 목표 199 — 보호자 자유 학습 목록 순서 변경·삭제

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 보호자가 추가한 lesson의 실제 진행 순서를 확인하고 한 항목씩 안전하게 옮기거나 지워 아이에게 원하는 순서를 정확히 준비한다.
새로운 근거: 루프 198은 중복 추가·로컬 복원 기반을 만들었지만 추가된 항목 목록 자체와 순서 변경·삭제 동작은 아직 production UI·model에 없다. M3의 다음 독립 사용자 흐름이다.
중복 방지: 이번 루프는 저장된 항목의 전량 표시·한 칸 위/아래 이동·단일 삭제와 재실행 보존까지만 구현한다. 현재 위치·이어하기·처음부터·아이 목록 실행·완료 화면·진도 요약은 후속 단일 루프로 분리한다.

## 목표

보호자 화면에서 중복을 포함한 자유 학습 목록을 저장 순서대로 전부 보고, 각 항목을 한 칸 위·아래로 이동하거나 해당 항목 하나만 삭제하며 변경된 ID·중복·순서를 no-backup app-private 저장소에 원자 저장해 재실행 뒤 복원한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. 중복 항목을 ID가 아니라 index로 구분해 위·아래 이동과 단일 삭제가 정확히 한 항목에만 적용되고 첫 항목의 위·마지막 항목의 아래는 글을 읽지 않아도 비활성임이 구분된다.
3. 임의 길이 목록의 모든 항목이 저장 순서대로 표시되며, 목록이 길 때도 보호자만 사용하는 명시적 스크롤·위치 단서로 첫·중간·마지막에 접근할 수 있고 주요 동작은 192 px 이상이다.
4. 순서 변경·삭제 결과는 기존 38개 allowlist·중복을 보존해 같은 no-backup 파일에 원자 저장되고 앱 강제 종료·재실행 뒤 그대로 복원된다.
5. 정확한 2340 × 1080·LimDo focus에서 빈 목록·중복 목록·첫·중간·마지막 항목, 이동 전후·삭제 전후의 잘림·겹침·오동작이 0건이고 녹음 관리·lesson 추가 callback이 유지된다.
6. 기존 세 메뉴 자유 쓰기·네 조작·단일 녹음·성공 자동 다음에 회귀가 없고 자동 그래픽 디자인·자동 QA·아이 대리 QA에서 새 P0·P1·진행 방해 P2가 0건이다.

## 완료 정의

보호자 목록의 저장 순서 표시·한 칸 이동·단일 삭제·재실행 복원이 자동·에뮬레이터·실화면 근거로 통과할 때 완료한다. 현재 위치·아이 목록 실행은 다음 제품 루프로 남기며 완료 체크포인트만 `git push origin HEAD`로 일반 push한다.
