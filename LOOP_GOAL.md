# 루프 목표 192 — 입력 중 방향 화살표 손가락 가림 방지

## 작업 가치 관문

분류: 결함 수정
사용자 가치: 다섯 살 아이가 손가락으로 쓰는 중에도 다음 진행 방향을 계속 볼 수 있게 한다.
새로운 근거: 현재 `inputDirectionGuide`는 마지막 입력점보다 획 전체 길이의 6~12%만 앞에 화살표를 두어 짧은 획과 끝부분에서 손가락 가림 범위 안으로 들어온다.
중복 방지: 모든 production 글자의 공통 입력 중 화살표 배치만 교정하고, `카` geometry·초기 동적 시범·입력 판정은 별도 후속 작업으로 넣지 않는다.

## 목표

1. 긴 직선·곡선·꺼임·짧은 가로·세로획에서 입력 중 화살표를 마지막 입력점의 손가락 가림 범위 밖으로 유지한다.
2. 남은 획이 짧으면 끝점에 붙이지 않고 진행 방향을 보존한 경로 옆 안전 위치로 이동한다.
3. 쓰기 길·시작·끝·다른 획·조작과 혼동하지 않고 쓰기판 안에 유지한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. 자동 검사에서 화살표 중심과 마지막 입력점 사이 최소 가시 거리, 짧은 잔여 구간의 옆 배치, 쓰기판 내부, 다음 경로 방향 일치가 통과한다.
3. `alarmquest-qa` 물리 1080 × 2340·가로 2340 × 1080·LimDo focus에서 대표 긴 직선·곡선·꺼임·짧은 가로·세로획의 입력 중 PNG·hierarchy를 새로 수집한다.
4. 각 마지막 입력점에 다섯 살 아이 손가락 가림 범위의 검수용 반투명 원을 캡처 사본에만 겹쳐 화살표 핵심 glyph와 방향이 원 밖에서 보인다.
5. 자형·획순·방향·입력 판정·네 조작·성공·자동 재시작 흐름에 회귀가 없고 자동 그래픽 디자인·자동 QA·아이 대리 QA의 새 P0·P1·진행 방해 P2가 0건이다.

## 완료 정의

입력 중 화살표가 모든 production 획에서 손가락 가림 밖에 보이고 다음 판정 경로 방향과 일치하며 자동·에뮬레이터·아이 대리 QA 회귀가 없을 때 완료한다. 완료 체크포인트만 `git push origin HEAD`로 일반 push하며 실패한 반복은 커밋하지 않는다.
