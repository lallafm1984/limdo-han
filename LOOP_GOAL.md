# 루프 목표 031 — 155 px·156 px 판정 경계 반복 안정성 QA

## 목표

루프 029·030에서 각각 한 번 확인한 155 px `SUCCESS`와 156 px `RETRY_DIRECTION`을 정확한 2340 × 1080에서 교차 반복해 판정 경계의 안정성을 탐색한다. 문제 입증 전에 제품 로직을 바꾸지 않는다.

## 성공 조건

1. `alarmquest-qa`의 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`에서 새 debug APK를 검증한다.
2. Canvas 1962 × 775 px와 정사각형 `ㄱ` geometry를 유지한다.
3. 끝점 `(1461,742)` 도달 뒤 155 px와 156 px를 각각 최소 3회, 순서를 교차해 되돌아가 놓는다.
4. 각 입력 좌표·시점, 결과·버튼 화면과 음성 callback 순서를 기록한다.
5. 같은 거리의 반복 결과가 일치하고 155 px와 156 px 사이에서만 결과가 바뀌는지 아이 대리 QA한다.
6. 문제가 입증되면 최소 보정과 단위 테스트만 적용하고, 아니면 제품 코드를 바꾸지 않는다.
7. 코드를 바꾸면 `./scripts/verify.sh`와 `git diff --check`가 통과한다.
8. `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

155 px와 156 px를 각각 최소 3회 교차 반복해 제품 변경 없이 재현한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
