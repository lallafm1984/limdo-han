# 루프 목표 043 — 성공 callback 즉시 지우기 반복 안정성 QA

## 목표

정확한 2340 × 1080에서 정상 획의 `SUCCESS` 재생 시작 callback을 에뮬레이터 내부에서 감시하고 추가 지연 없이 지우기를 예약하는 흐름을 앱 데이터 초기화부터 최소 3회 독립 반복해, 직접 경합 결과가 매번 초기 상태 하나로 수렴하는지 먼저 확인한다. 문제가 입증될 때만 성공 판정과 초기화 순서를 최소 보정한다.

## 성공 조건

1. 정확한 에뮬레이터에서 앱 데이터를 초기화한 독립 사례 최소 3개의 정상 중심선 끝점 `MOVE`와 `SUCCESS` 재생 시작을 새로 확인한다.
2. 각 사례에서 `SUCCESS` 시작 callback 감시가 끝나는 즉시 추가 지연 0 ms로 지우기 버튼 한 번을 전달하고 실제 callback·전달 시점을 기록한다.
3. 모든 최종 화면에서 완료 획과 결과 표식이 사라지고 흰 guide 124 px, 초록 시작점, 두 방향 화살표, 주황 끝점과 초기 안내 상태가 복원되는지 확인한다.
4. 취소된 `SUCCESS`의 늦은 완료 callback이나 성공 화면이 초기 상태를 덮지 않는지 각 사례에서 3초 이상 확인한다.
5. Canvas 1962 × 775 px, 주요 버튼 373 × 168 px와 간격 37 px를 유지한다.
6. 제품 문제가 없으면 코드를 바꾸지 않고 QA 근거만 남긴다.
7. `./scripts/verify.sh`와 `git diff --check`가 통과하고 새 아이 대리 QA 및 `실제 아이 관찰: 실행 안 함`을 기록한다.

## 다음 반복

각 사례에서 앱 데이터를 초기화하고 초기 안내 완료를 확인한 뒤, 에뮬레이터 내부 `SUCCESS` 시작 감시와 지연 없는 지우기 탭을 최소 3회 독립 재현해 결과를 비교한다.

## 완료 정의

모든 성공 조건을 새 근거로 통과하면 아이 대리 QA로 다음 루프 하나를 준비하고 체크포인트 커밋 후 `git push origin HEAD`로 일반 push한다.
