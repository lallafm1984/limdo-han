# 루프 목표 213 — 세로 모음 조립 네 번째 좁은 묶음

## 작업 가치 관문

분류: 제품 변경
사용자 가치: 다섯 살 아이가 초성 오른쪽의 짧은 가로획이 있는 `가·거·겨`와 가로획 없이 세로획 하나만 있는 `기`의 모양 차이를 조립과 쓰기로 연결할 수 있다.
새로운 근거: 루프 212는 `ㄱ + ㅡ → 그`를 구현했지만, 초성 오른쪽에 세로획 하나를 놓는 `ㄱ + ㅣ → 기`는 아직 구현하지 않은 다른 교육 과제다.
중복 방지: 이번 루프는 `ㄱ + ㅣ → 기` 한 조립 화면과 쓰기 연결만 다룬다. `ㄴ` 초성·받침을 함께 늘리지 않으며 통과 뒤 다음 교육 묶음은 새 루프로 분리한다.

## 목표

루프 212의 검증된 복수 목표 선택·세로 모음 좌우 조립 상호작과 production token을 재사용하되, `ㄱ`을 왼칸과 `ㅣ`를 오른칸에 놓아 `기`를 완성하고 기존 `기` 쓰기 lesson으로 연결한다.

## 성공 조건

1. `./scripts/verify.sh`, `git diff --check`, `scripts/check-visual-loop.sh`가 통과한다.
2. 기존 조립 진입에서 `가`·`거`·`겨`·`고`·`교`·`구`·`규`·`그`·`기`를 글자 모양으로 구분해 선택하고 홈·뒤로 복귀가 동작한다.
3. `ㄱ`과 `ㅣ` 조각, 초성 왼칸과 세로 모음 오른칸, 조립 전·선택·완성 상태가 글을 읽지 않아도 구분된다.
4. 잘못된 칸·순서·빈 칸은 완성으로 처리하지 않고 처벌 없이 다시 안내한다.
5. 완성 `기`와 production `기` 표시·안내·판정이 같은 정사각형 em·균일 배율·자모 상대 위치를 사용한다.
6. 완성 뒤 기존 `기` WritingCanvas 1962 × 954 px로 진입하고 네 그림 조작·정방향 입력·성공·다음·홈 회귀가 없다.
7. 정확한 2340 × 1080 변경 전·후 PNG·hierarchy·focus·APK SHA와 시작·오순서·완성·쓰기 근거에서 잘림·겹침·가림이 0건이다.
8. 새 P0·P1·진행 방해 P2가 0건이고 자동 그래픽 디자인·자동 QA·에뮬레이터·아이 대리 QA가 통과한다.

## 완료 정의

`ㄱ + ㅣ → 기` 한 조립 흐름과 기존 `기` 쓰기 연결만 새 자동·실화면 근거로 통과하면 완료한다. 다른 모음·초성·받침은 별도 단일 루프로 나누며 완료 체크포인트만 `git push origin HEAD`로 일반 push한다.
