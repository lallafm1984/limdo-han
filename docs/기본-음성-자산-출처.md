# 기본 음성 자산 출처

## 2026-08-29 루프 236 현재 여자아이 후보 — 보호자 38 lesson

- 생성 서비스: TTSFree.com
- 음성: 한국어 여성 `Standard-A`, pitch +20, speed +4
- 생성 문구: 자음 이름 14개, 기본 모음 소리 10개, `가·나·다·라·마·바·사·아·자·차·카·타·파·하` 14글자를 세 묶음으로 같은 음색에서 생성했다.
- 음색 경계: 특정 실제 아동·성우·캐릭터를 복제하지 않은 합성 여성 음색이며 밝은 여자아이 느낌을 사용한다.
- 후처리: 자연 무음 경계에서 38개로 분리하고 각 항목에 80 ms lead·140 ms tail을 남겼다. peak를 -2 dB로 정규화한 뒤 44.1 kHz AAC/M4A로 변환하고 다시 PCM으로 decode해 검사했다.
- 자동 검사: production 파일 38개, 서로 다른 SHA-256 38개, 길이 0.403~0.719초, RMS 0.095~0.255로 무음 0건이다. 자음 원본은 `기역·니은·디귿·리을·미음·비읍·시옷·이응·지읒·치읓·키읔·티읕·피읖·히읗`, 모음·글자 원본은 입력 순서와 검출된 자연 무음 구간이 각각 10개·14개로 일치했다.
- 제품 경계: 앱은 고정 M4A만 로컬 재생한다. runtime TTS·서버 호출·네트워크 권한을 추가하지 않으며, 자연스러움과 극단 단구간 음성인식의 한계는 사용자의 실기기 청취로 최종 판정한다.

| 분류 | production 파일 |
| --- | --- |
| 자음 14개 | `gieok`, `nieun`, `digeut`, `rieul`, `mieum`, `bieup`, `siot`, `ieung`, `jieut`, `chieut`, `kieuk`, `tieut`, `pieup`, `hieuh` |
| 모음 10개 | `a`, `ya`, `eo`, `yeo`, `o`, `yo`, `u`, `yu`, `eu`, `i` |
| 글자 14개 | `ga`, `na`, `da`, `ra`, `ma`, `ba`, `sa`, `ah`, `ja`, `cha`, `ka`, `ta`, `pa`, `ha` |

모든 파일명은 위 식별자 앞에 `limdo_voice_`, 뒤에 `.m4a`를 붙인다. `a`는 모음 `ㅏ`의 소리 `아`, `ah`는 글자 `아`를 위한 서로 다른 자산이다.

## 2026-08-29 루프 236 남성 후보 — 최신 지시로 기각

- 생성 서비스: TTSFree.com
- 확인한 이용 조건: 무료 사용자가 생성한 음성도 개인·교육·상업 용도로 사용할 수 있다는 서비스 약관을 생성 당일 확인했다.
- 음성: 한국어 신경망 `InJoon`
- 생성 문구: `기역`, `니은`, `디귿`, `리을`
- 생성 방식: 가입·API 키 없이 웹 생성 후 각 결과를 브라우저의 `Download Mp3`로 개별 내려받았다. 특정 인물·성우·캐릭터의 목소리를 복제하거나 모사하지 않았다.
- 후처리: 긴 무음만 줄이고 peak를 -2 dB로 정규화한 뒤 AAC/M4A로 변환했다. 앱은 이 고정 파일만 로컬 재생하며 runtime TTS·서버·네트워크를 호출하지 않는다.
- 발음 검사: 최종 AAC 네 파일을 순서대로 연결해 큰 한국어 음성인식 모델로 다시 읽었고 `기역·니은·디귿·리을`을 확인했다. 자연스러움은 사용자의 실제 청취 승인을 별도로 요구한다.

| production 파일 | 길이 | SHA-256 |
| --- | ---: | --- |
| `limdo_voice_gieok.m4a` | 0.608초 | `de26c1b8fd1d5745a2ae33005472197586abe1347f2b32603d8d7365185657e3` |
| `limdo_voice_nieun.m4a` | 0.681초 | `98c6b848a8347afd5a233e2f85c536143241aef4cdaed8ec8da7f1c562cbdadc` |
| `limdo_voice_digeut.m4a` | 0.629초 | `85670bb91f5ced1bb521af46bce55eb394da10e95804f7cc73701750aa5776a2` |
| `limdo_voice_rieul.m4a` | 0.692초 | `217500b32b6b96a6f9a6b258603ce00f597ff9a500b00075db49b9b4311da54b` |

서비스 약관: <https://ttsfree.com/terms>
