# LimDo 회귀 규칙

## 목적

같은 사용자 지적 또는 같은 근본 원인이 두 번 나타났을 때 말로만 주의하지 않고 제품 규칙과 기계 검사로 승격한다. 이 문서는 이력 요약이 아니라 앞으로 모든 새 세션이 지켜야 할 재발 방지 계약이다.

## 승격 절차

1. `.loop/history.md`와 `.loop/user-directives.md`에서 같은 현상의 독립 근거가 두 번인지 확인한다.
2. 아이에게 미치는 영향과 공통 근본 원인을 한 문장씩 적는다.
3. 피해야 할 구현이 아니라 지켜야 할 제품 규칙을 적고 `왜`를 함께 기록한다.
4. 좌표·크기·순서·상태처럼 측정 가능하면 단위 테스트 또는 자동화 검사로 만든다.
5. 화면 품질이면 정확한 2340 × 1080 새 이미지·hierarchy의 판정 방법을 고정한다.
6. 검사 이름과 처음 통과한 루프를 아래 표에 연결한다.

## 현재 규칙

| 반복된 문제 | 제품 규칙과 이유 | 기계·시각 검사 |
| --- | --- | --- |
| 의미 없는 동일 QA 반복 | 이미 통과한 화면·callback의 횟수만 늘리지 않는다. 제품 판단이 달라지지 않는데 비용과 시간이 계속 들기 때문이다. | `scripts/check-work-value.sh`, `LOOP_GOAL.md` 작업 가치 관문 |
| 한글 자형·비율 불일치 | 표시·안내·판정은 같은 정사각형 em과 균일 배율을 쓴다. 다른 경로를 쓰면 아이가 본 모양과 입력 성공 조건이 달라지기 때문이다. | `KoreanCurriculumTest`, guide 외곽·비율·방향 계약, 정확한 화면 |
| 쓰기 영역이 좁거나 주변 UI가 공간을 차지함 | 실제 WritingCanvas는 2340 × 1080에서 최소 1872 × 648 px이고 가장 큰 상호작용 영역이어야 한다. 다섯 살 아이의 손가락 오차를 받아야 하기 때문이다. | `QA_CHECKLIST.md`, hierarchy bounds 측정 |
| 다른 앱이 공용 에뮬레이터 전면을 가져감 | 매 입력·캡처 전에 현재 focus와 glyph 관문을 확인하고 다른 프로젝트가 사용 중이면 조작을 중지한다. 다른 앱에 입력한 화면은 LimDo 근거가 아니기 때문이다. | `mCurrentFocus`·`mFocusedApp`, 현재 glyph·성공 hierarchy 관문 |
| `ㅅ·ㅈ·ㅊ` 자형과 동적 안내 경로 불일치 | 여섯 글자 `ㅅ·ㅈ·ㅊ·사·자·차`는 첨부 교육 기준의 초성 template을 공유하며 현재 획의 동일 크기 원형 점선·동적 표식·시작·끝점·입력 판정은 production geometry 하나에서 계산한다. `ㅅ`은 위 중앙에서 내려와 왼쪽으로 휘는 1획과 중앙 접합부에서 오른쪽으로 갈라지는 2획, `ㅈ·ㅊ` 몸체는 가로 뒤 중앙 접합부를 지나 왼쪽으로 휘는 연결 획과 접합부에서 갈라지는 오른쪽 획을 쓴다. 처음 배우는 아이에게 보이는 모양과 성공 조건이 다르면 잘못된 자형과 획순을 학습하기 때문이다. | `KoreanCurriculumTest` 공유 곡선 좌표·접합부 포함·현재 획 노출·원 크기와 간격·정역방향 판정, 첨부 이미지와 정확한 2340 × 1080 대표 화면 비교 |
| `ㅈ·ㅊ` 몸체의 반복된 과도한 굴곡 | `ㅈ·자` 몸체 첫 획은 윗가로 시작·끝, 중앙 접합부, 왼쪽 아래 끝의 네 점만 사용하고 오른쪽 획은 접합부·오른쪽 아래 끝의 두 점만 사용한다. `ㅊ·차`는 짧은 윗가로 뒤 같은 몸체를 공유한다. 교육용 중심선에 시각 효과용 중간 점을 여러 개 넣으면 두꺼운 선에서 굴곡이 증폭돼 아이가 다른 자형으로 인식할 수 있기 때문이다. | `KoreanCurriculumTest.siotJieutChieutFamiliesShareConnectedStrokeDirectionAndJudgmentContract`, 실제 2340 × 1080 `ㅈ·ㅊ` 화면 비교 |
| 기본 자음의 광학 크기·끝점 불일치 | 기본 자음 14개는 같은 정사각형 em·안내 굵기·광학 중심 계약을 사용한다. `ㄹ`의 위·아래 좌우 끝과 `ㅈ·ㅊ` 몸체의 윗가로·아랫다리 좌우 끝은 중심선뿐 아니라 둥근 cap을 포함한 보이는 외곽도 맞춘다. 굵은 교육용 선에서는 같은 좌표라도 열린 끝과 꺾인 끝의 렌더링 차이가 크게 보여 아이가 길이와 비율을 다르게 배울 수 있기 때문이다. | `KoreanCurriculumTest.basicConsonantsShareOneOpticalFrame`, `KoreanCurriculumTest.rieulJieutAndChieutKeepBalancedVisibleExtents`, 실제 2340 × 1080 밝은 외곽 측정 |
| 동적 획 표식이 크거나 여러 도형으로 뭉침 | 동적 표식은 현재 획 점선 중심 위의 작은 단일 원과 내부 방향표만 사용하고, 지름은 안내 획의 28% 이하이며 이동은 획의 8~92%로 제한한다. 시작·끝 원, 외곽 고리, 긴 화살표가 겹치면 글을 읽지 못하는 아이가 이동 물체와 목표점을 구분하기 어렵기 때문이다. | `KoreanCurriculumTest.haPreservesHieuhProportionsAndMarkersStayInsideTheGuide`, 정확한 2340 × 1080 시작·중간·끝 프레임 비교 |
| 단순 자음의 둘레 크기가 서로 다름 | 기본 직각 자음 `ㄱ·ㄴ·ㄷ`은 같은 정사각형 em 안에서 중심선 76% × 76%, 동일 굵기와 중앙 정렬을 사용하며 둥근 cap을 포함한 보이는 외곽도 같아야 한다. 넓은 허용 범위만 검사하면 94%인 `ㄱ`도 통과해 처음 배우는 아이가 글자마다 기준 칸 크기가 다른 것으로 받아들일 수 있기 때문이다. | `KoreanCurriculumTest.rightAngleConsonantsShareTheSameVisibleOuterFrame`, 정확한 2340 × 1080 `ㄱ·ㄴ·ㄷ` 전후 화면 비교 |
| 기본 모음 순서·획순이 참고표와 다름 | 기본 모음 메뉴와 성공 뒤 순환은 `ㅏ·ㅑ·ㅓ·ㅕ·ㅗ·ㅛ·ㅜ·ㅠ·ㅡ·ㅣ`만 이 순서로 사용한다. 10개 모음의 표시·현재 획 점선·동적 표식·시작끝 표식·입력 판정은 같은 production 획 목록을 공유한다. 처음 배우는 아이에게 메뉴 순서와 손 움직임 순서가 달라지면 잘못된 모음 체계와 획순을 반복 학습시키기 때문이다. | `LearningNavigationTest` 기본 10모음 순환 oracle, `KoreanCurriculumTest.basicJamoMatchTheTwentyFourEntryReferenceStrokeMatrix`·`correctedVowelsMatchTheReferenceStrokeOrderAndJunctions`, 정확한 2340 × 1080 선택·대표 모음 화면 비교 |
| 보호자 상태 카드의 하단 버튼·문구 반복 잘림 | 가로 2340 × 1080에서 보호자 상태 카드의 각 필수 동작은 의도한 높이를 부모 제약에서 강제 축소하지 않고 실측 64 dp 이상을 유지하며 라벨 픽셌이 카드 범위 안에 완전히 보여야 한다. Composable에 `size` 수치만 지정해도 상위 Column의 가용 높이 부족하면 실제 버튼이 몇 px로 압축되어 보호자가 상태와 동작을 오판할 수 있기 때문이다. | 녹음 없음·녹음 중·완료·재생 중 2340 × 1080 hierarchy에서 각 callback bounds 높이 최소 192 px(64 dp), 라벨 bounds가 카드 bounds 내부임을 검사. 루프 185 반복 6에서 규칙 승격, 기계 검사는 다음 배치 수정 반복에서 고정 |
| `사` 초성 `ㅅ` 중심선의 반복된 꾸불거림·각짐 | `사` 초성은 위에서 거의 수직으로 내려오다 공유 접합부 주변에서 연속 곡률로 좌우 아래로 벌어지고, 두 획 모두 아래쪽과 각자의 바깥쪽으로 단조 진행해야 한다. 불필요한 방향 반전은 물결·갈고리로, 점을 지나치게 줄인 직선은 굵은 guide에서 각진 V자로 증폭돼 아이가 첨부 교육 기준과 다른 `ㅅ`을 익힐 수 있기 때문이다. | `KoreanCurriculumTest.saSiotKeepsContinuousEducationalCurvesWithoutDirectionReversal`, 정확한 2340 × 1080 첨부 기준·`사` 변경 전·후 및 획별 화면 비교. 루프 193 반복 2에서 고정 |
| 보호자 목록을 손가락 스크롤로 탐색하는 UI의 반복 기각 | 보호자 lesson 선택은 분류별 전체 고정 그리드로, 임의 길이 자유 학습 편집은 명시적 이전·다음 버튼과 현재/전체 위치가 있는 고정 카드 페이지로 제공한다. 가로 고정 앱에서 숨은 첫·마지막 항목을 drag로 찾아야 하면 목록 범위와 현재 위치를 한눈에 확인하기 어렵고 작은 행 동작까지 겹치기 때문이다. | production 보호자 목록에 `LazyColumn`·`LazyRow`·`verticalScroll`·`horizontalScroll`·swipe pager 0개, 2340 × 1080 hierarchy에서 페이지 이동 192 px 이상·현재/전체 페이지 표시·첫/마지막 비활성·첫/중간/마지막 페이지 실화면 검사. 루프 199에서 고정 |
| 실기기 connected test 중복 사고 | 자동 루프의 Android 대상은 `emulator-5554`/`alarmquest-qa` 하나로 고정하고, 실기기가 ADB에 보이면 새 세션을 시작하지 않는다. Gradle connected test가 연결된 모든 기기에서 대상 패키지를 설치·제거해 보호자 녹음과 설정을 소실시킨 같은 근본 원인이 루프 236과 237에서 반복됐기 때문이다. | 감독자가 `ANDROID_SERIAL=emulator-5554`·`ADB_MDNS_AUTO_CONNECT=0`을 강제하고, `scripts/check-emulator-only.sh`를 세션 전·후에 실행하며, `scripts/emulator-only/adb`로 실기기 serial·`-d`·transport id·연결 명령을 차단한다. `scripts/check-automation.sh`가 이 세 관문의 존재와 실행 권한을 검사한다. 루프 237 반복 3 후 고정 |
| 작업자 종료와 함께 에뮬레이터가 사라져 다음 세션이 중지됨 | cold boot는 작업자 `codex exec` 내부가 아니라 감독자 수명에서 실행하고 세션 전·후 부재·uptime·AVD·물리 기기 0건을 확인한다. 작업자 process에 묶인 에뮬레이터는 성공 보고 직후 사라져 postcheck와 다음 반복을 끊을 수 있기 때문이다. | `scripts/ensure-emulator-ready.sh`, `scripts/run-cli-loop.sh` 세션 전·후 호출, `scripts/check-automation.sh` 호출 횟수·실행 권한·한글 조사 변수 경계 검사. 루프 237 반복 6 후 고정 |
| cold boot 직후 회전값이 다시 0으로 덮어써짐 | 새 부팅 직후에는 `accelerometer_rotation=0`·`user_rotation=1`을 한 번 쓴 결과만 믿지 않고 1초 뒤 읽기까지 최대 15회 안정화한다. Android 부팅 서비스의 늦은 설정 복원이 첫 준비 관문 뒤 값을 0으로 되돌리면 다음 세션 전 검사에서 자동화가 중단되기 때문이다. | `scripts/ensure-emulator-ready.sh`의 `stabilize_display`, `scripts/check-automation.sh`의 함수·15회 상한·호출 검사. 루프 239 자동화 재설정에서 고정 |

새 규칙은 기존 행을 삭제하지 않고 추가한다. 근거가 한 번뿐이거나 원인이 다른 문제를 억지로 같은 규칙으로 묶지 않는다.
