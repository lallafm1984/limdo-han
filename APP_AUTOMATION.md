# LimDo Codex 앱 인계 기록

## 실행 단계

단계: `CLI`

초기 Codex 앱 Goal 단계는 검증된 루프 004 완료 후 종료되었다. 현재 기준 계약은 새 세션 CLI 자동화이며 `CLI_AUTOMATION.md`를 따른다.

Codex 앱은 상태를 확인하거나 사용자가 요청한 설정을 바꿀 수 있지만 `.loop/runtime/supervisor.lock`이 살아 있는 동안 두 번째 감독자를 실행하면 안 된다.

CLI 단계는 로컬 우선이다. 사용자가 승인한 완료 커밋의 `git push origin HEAD`만 일반 원격 변경으로 허용하며 출시, 배포, 외부 메시지, 파괴적 작업은 별도 승인이 필요하다.

사용자는 앞으로 수정 지시를 Codex 앱으로 전달한다. 앱 작업자는 최신 지시를 `.loop/user-directives.md`와 현재 루프 목표에 반영한 뒤 CLI 자동화를 재개한다.

사람 검토 관문 대신 각 완료 화면을 정확한 2340 × 1080에서 `아이 대리 QA`하고, `.loop/qa-findings.md`의 가장 중요한 불편 하나로 다음 루프를 자동 생성한다. 새 불편이 없으면 최신 사용자 지시의 미완료 제품 작업으로 전환하며, 같은 검증의 숫자만 늘린 반복은 만들지 않는다. 실제 아이 관찰을 수행하지 않았다면 그렇게 주장하지 않는다.

현재 지속 제품 목표는 `docs/1차-목표-작업-기획서.md`다. 루프 154 이후 `자음·모음·가나다` 세 메뉴 기능, 통일된 유아용 디자인, 안전한 화려한 연출, 자동 QA 역할, 다섯 살 아이 대리 시뮬레이션을 순서대로 자동 진행한다. 모든 완료 관문 통과 전에는 큐를 비우지 않고, 통과 뒤에는 `IDLE`로 종료해 2차 목표를 임의로 시작하지 않는다.

새 세션·환경 설정·날짜별 로그·STOP·launchd 준비와 제어 방법은 `docs/루프-엔지니어링-적용.md`와 `README.md`를 따른다. launchd는 저장소에 준비만 하며 사용자의 명시적 재개 전에는 켜지 않는다.
