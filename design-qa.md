# 한글 획 굵기 시각 비교 QA

- 원본 시각 기준: `/Users/lim/Desktop/스크린샷 2026-08-24 오후 2.40.44.png`
- 구현 화면: `/Users/lim/Desktop/프로젝트/LimDo/captures/loop037/iteration2/01-initial/screen.png`
- 상태: 입력 전 초기 쓰기 화면
- 화면: 원본 1257 × 696 px, 구현 2340 × 1080 px
- 정규화: 서로 다른 앱 화면이므로 전체 픽셀을 강제 확대하지 않고, 중앙 `ㄱ`의 em과 획 굵기 비율 및 화면 내 시각 위계를 비교했다. 구현은 실제 앱 기준 2340 × 1080, 밀도 변환 없는 원본 에뮬레이터 캡처다.

## 전체 화면 비교

- 두 화면 모두 밝은 노란 배경에서 중앙의 흰색 쓰기 길이 가장 강한 학습 과제다.
- LimDo 구현은 참고 화면의 상표·캐릭터·과일·배치를 복제하지 않고 기존 리모 캐릭터, 시작점, 방향 화살표, 끝점과 조작 버튼을 유지한다. 이는 의도한 제품 제약이며 이번 굵기 변경의 불일치가 아니다.
- 쓰기판 1962 × 775 px와 큰 하단 조작 버튼이 유지되어 굵어진 글자가 버튼이나 시스템 영역을 침범하지 않는다.

## 집중 영역 비교

- 비교 영역은 두 화면 중앙의 흰색 `ㄱ` 쓰기 길이다.
- 변경 전 구현은 em 620 px의 15%인 약 93 px였고 참고 화면보다 가늘었다.
- 변경 후 구현은 em의 20%인 124 px로 약 33% 두꺼워졌다. 가로·세로 중심선은 각각 582.8 px로 같고, x/y 배율과 글자 비율은 변하지 않았다.
- 참고 화면은 각진 블록형 끝을 쓰지만 LimDo는 아이가 손가락으로 자연스럽게 따라가도록 기존 둥근 cap을 유지한다. 사용자가 요청한 굵기 범위에서는 허용 가능한 제품 차이다.
- 굵어진 획 위에서도 초록 시작점, 두 방향 화살표, 주황 끝점이 충분히 분리되어 보인다.

## 필수 시각 표면

- 글꼴·문자: 보호자용 하단 문구의 서체·크기·줄바꿈은 변경하지 않았고 잘림이 없다.
- 간격·배치: 중앙 정렬, 정사각형 em, 버튼 간격과 안전 여백이 유지됐다.
- 색상: 노란 배경과 흰색 guide의 강한 명도 대비, 초록 시작·방향 및 주황 끝점의 의미 대비가 유지됐다.
- 이미지 품질: 기존 리모와 꽃 배경 원본 자산은 선명하며 새 이미지 대체나 임시 자산이 없다.
- 문구·내용: 이번 굵기 변경으로 문구나 접근성 설명을 바꾸지 않았다.

## 발견 사항

- P0/P1/P2 없음.
- P3: 참고 화면보다 구현의 획 끝이 더 둥글지만, 손가락 경로와 기존 LimDo 디자인 언어를 보존하기 위한 의도된 차이다.

## 비교 이력

1. 변경 전: em 15%, 약 93 px로 참고 화면의 굵은 쓰기 길보다 가늘었다.
2. 수정: guide를 em 20%, 124 px로 확대했다.
3. 수정 후: 정사각형 글자 비율과 중심선, 표식 가독성을 유지하면서 요청한 굵기 증가가 화면에서 명확해졌다.

## 구현 점검표

- [x] guide 굵기 확대
- [x] 가로·세로 동일 배율과 중심선 유지
- [x] 시작점·방향·끝점 가독성 유지
- [x] 정확한 2340 × 1080 초기 화면 비교
- [ ] 실제 아이 관찰: 실행 안 함

final result: passed
