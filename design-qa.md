# 루프 180 참고 이미지형 `가` 초성 곡선 디자인 QA

- 원본 시각 기준: `/tmp/codex-remote-attachments/01a04311-87ad-7093-9906-fc24d61ecb9a/766e3a07-bc18-42fc-8d77-3da32b872362/1-Photo-1.jpg`
- 구현 화면: `/Users/lim/Desktop/프로젝트/LimDo/captures/loop180/iteration1/ga-curve-initial.png`
- 검증 상태: `가`를 선택한 입력 전 쓰기 화면
- 원본 크기: 1280 × 929 px
- 구현 화면: 물리 1080 × 2340 에뮬레이터, `user_rotation=1`, 앱 2340 × 1080 px
- 정규화: 원본의 중앙 `가`와 구현의 중앙 쓰기 길을 각각 잘라 1600 × 840 한 장에 나란히 배치했다. 전체 앱 배치를 원본 사진처럼 복제하지 않고 사용자가 지정한 초성 `ㄱ`의 중심선 실루엣과 중성 `ㅏ`에 대한 상대 위치를 집중 비교했다.

## 전체 화면 근거

- 원본 전체: `/tmp/codex-remote-attachments/01a04311-87ad-7093-9906-fc24d61ecb9a/766e3a07-bc18-42fc-8d77-3da32b872362/1-Photo-1.jpg`
- 구현 전체: `captures/loop180/iteration1/ga-curve-initial.png`
- 집중 비교: `captures/loop180/iteration1/design-qa-after.png`
- 실제 첫 획 입력: `captures/loop180/iteration1/ga-curve-after-stroke.png`
- 3획 성공: `captures/loop180/iteration1/ga-curve-success.png`

## 집중 형태 비교

- 원본 초성은 위 가로 끝에서 거의 수직으로 내려오기 시작한 뒤, 아래로 갈수록 왼쪽으로 휘며 끝점이 위 가로 시작점 부근으로 돌아온다.
- 구현은 위 가로 `(0.05, 0.12)→(0.48, 0.12)` 뒤, 제어점 `(0.48, 0.46)`·`(0.27, 0.75)`를 거쳐 `(0.10, 0.88)`에 닿는 cubic Bézier를 12구간으로 표본화했다.
- 이 형태는 꺾임 직후 x 변화가 거의 없는 수직 시작, 아래쪽에서 점진적으로 커지는 왼쪽 휨, 위 가로 시작점과 가까운 아래 끝 위치를 모두 재현한다.
- 같은 곡선 좌표를 큰 흰 글자 길, 현재 획 점선, 동적 방향 표식, 입력 판정이 함께 사용한다. 실제 곡선 중심선을 한 획으로 입력했을 때 다음 `ㅏ` 안내로 전환됐다.

## 필수 시각 표면

- 글꼴·문자: 원본의 흐린 검정 글꼴 래스터를 복제하지 않고, LimDo의 교육용 굵은 안내선과 둥근 끝 처리를 유지했다. 이번 목표인 초성 중심선의 곡률과 상대 위치는 일치한다.
- 간격·배치: 초성은 중성 왼쪽에 분리되어 있고 위 가로·곡선 내림·`ㅏ`의 상대 비율이 원본과 같은 읽기 구조를 만든다. 기존 1962 × 954 px 쓰기판과 좌우 조작 열은 변하지 않았다.
- 색상·토큰: 원본의 회색 카드와 검정 글자 대신 기존 LimDo의 노란 배경, 흰 안내선, 초록 시작·방향, 주황 끝 표식을 유지한다. 이는 아이 입력 안내를 위한 의도된 제품 색 체계다.
- 이미지 품질·자산: 흐린 사진을 글자 자산으로 가져오지 않고 Compose geometry로 정확한 2340 × 1080에서 선명하게 그렸다. 새 래스터 글자 자산이나 임시 대체 자산은 없다.
- 문구·내용: 사용자가 지정한 `가` 형태 외의 보호자용 접근성 문구와 버튼 내용은 변경하지 않았다.

## 비교 이력

1. 반복 1 변경 전: `design-qa-before.png`에서 내림 획이 꺾임부터 끝까지 단일 직선 대각선이어서, 원본의 거의 수직인 시작과 점진적 곡률이 사라지는 P1 형태 불일치가 있었다.
2. 수정: 단일 직선을 12구간 cubic Bézier 표본 곡선으로 교체하고 표시·안내·판정의 공통 geometry를 유지했다.
3. 반복 1 수정 후: `design-qa-after.png`에서 꺾임 직후 수직 시작, 아래로 갈수록 왼쪽으로 휘는 윤곽, 위 가로 시작점 부근의 끝 위치가 확인됐다. 선택한 초성 실루엣 범위에 P0·P1·P2 불일치가 남지 않았다.

## 발견 사항

- P0/P1/P2 없음.
- P3: 원본은 각지고 비스듬한 아래 끝을 쓰지만 구현은 다른 LimDo 글자 길과 같은 둥근 `StrokeCap.Round`를 쓴다. 아이가 손가락으로 따라 쓰는 연속 경로와 기존 디자인 언어를 보존하기 위한 의도된 차이다.
- 실제 아이 관찰: 실행 안 함. 자동 디자인 QA와 에뮬레이터 아이 대리 QA만 수행했다.

final result: passed
