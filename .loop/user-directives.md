# Codex 앱 사용자 수정 지시

이 문서는 사용자가 Codex 앱에서 전달한 최신 제품 수정 지시를 새 CLI 세션에 인계한다. 최신 미완료 지시가 현재 루프 범위 안에서 가장 높은 우선순위다. 지시를 반영한 뒤에도 기존 검증, 로컬 우선, 안전 경계와 실제 아이 관찰 근거 구분을 유지한다.

## 2026-08-27 — `ㄱ·ㄴ` 기본 자음 외곽 크기 통일

- 상태: 반영 완료 (루프 170)
- 사용자 지시: `ㄱ·ㄴ`이 다른 기본 자음보다 커 보이므로 전체 자음의 둘레 크기가 통일되어 보이게 한다.
- 측정 근거: standalone `ㄱ` 중심선은 em의 94% × 94%를 차지하지만 `ㄷ·ㅁ`은 76% × 76%다. `ㄴ`은 76% × 80%로 세로가 대표 직각 자음보다 길다.
- 제품 기준: `ㄱ·ㄴ·ㄷ`은 같은 em·굵기·중심을 유지하면서 중심선 외곽을 76% × 76%로 맞추고, 둥근 cap을 포함한 보이는 외곽도 정확히 같아야 한다.
- 공유 geometry: 표시·원형 점선·동적 표식·시작끝 표식·입력 판정은 교정된 production template 하나를 사용한다.
- 검증 기기: 최신 지시에 따라 `emulator-5554`의 정확한 2340 × 1080 화면으로 비교한다.

## 2026-08-24 — 어린이 학습 화면 시각 방향

- 상태: 활성
- 사용자 지시: `design/reference-kids-tracing-ui.png` 같은 UI·UX로 아이들이 쉽게 학습할 수 있는 화면을 만든다.
- 적용 화면: 현재 `ㄱ` 따라 쓰기 핵심 화면.
- 반드시 유지: 정확한 손글씨 입력과 판정, 한국어 음성 안내와 다시 듣기, 지우기, 오프라인 동작, 2340 × 1080 기준, 큰 실제 입력 영역.
- 시각 기준: 밝고 따뜻한 노란 배경, 장식은 낮은 대비, 화면 중앙의 아주 큰 흰색 `ㄱ` 쓰기 길, 시작점의 독창적인 동물 안내 캐릭터, 끝점 목표, 가장자리의 크고 단순한 조작 버튼, 최소한의 읽기 의존성.
- 원본 자산: `app/src/main/res/drawable-nodpi/limdo_gecko_guide.png`, `limdo_sunny_flower_background.png`를 사용한다. 참고 화면의 캐릭터와 장식을 직접 복사하지 않는다.
- 접근성: 글을 읽지 않아도 시작점, 진행 방향, 성공·재시도, 다시 듣기, 지우기, 비활성 동작을 알 수 있어야 한다. 보호자와 접근성 서비스를 위한 짧은 한글 설명은 유지한다.
- 범위 제한: 이번 루프에서 새 글자, 새 수업, 저장, 점수, 보상, 서버, 로그인, 분석, 광고, 민감 권한을 추가하지 않는다.
- 전달 방식: 앞으로 새 수정 지시는 Codex 앱에서 전달하며 앱 작업자가 이 문서와 현재 루프 목표에 한글로 반영한다.

## 참고 이미지의 역할과 제한

- 참고 이미지는 밝은 색감, 중앙 집중형 쓰기 과제, 캐릭터가 획 방향을 보여 주는 구조와 큰 가장자리 버튼의 시각 기준이다.
- 참고 이미지의 상표, 로고, 정확한 캐릭터, 과일 그림, 배치를 그대로 복제하지 않는다.
- 실제 Android 구현은 LimDo의 기존 기능과 접근성 계약에 맞게 재구성한다.

## 2026-08-24 — 실제 글자 쓰기 폭 확대

- 상태: 활성, 이전 시각 방향보다 우선
- 사용자 지시: 입력 넓이가 아이들에게 너무 좁으므로 참고 이미지처럼 넓게 만들어 쉽게 글자를 쓰게 한다.
- 측정 기준: 2340 × 1080에서 실제 손가락 입력 내부는 최소 1872 × 648 px이다.
- 핵심 보정: 빈 Canvas만 넓히지 않는다. 실제 보이는 동시에 판정에 사용하는 `ㄱ` 가로 획은 최소 1170 px, 세로 획은 최소 540 px로 확대한다.
- 구현 경계: 화면에 보이는 목표 길과 `GieokTraceEvaluator`가 판정하는 경로는 같은 geometry를 사용해야 한다.
- 배치: 다시 듣기, 지우기, 비활성 다음 동작은 쓰기 길을 침범하지 않도록 가장자리 또는 하단 안전 영역에 둔다.
- 검증: 새 APK의 초기·재시도·성공 화면에서 입력 내부 bounds와 목표 가로·세로 획을 각각 실측한다.

## 2026-08-24 — 교육용 한글 비율과 큰 양방향 입력 면적

- 상태: 활성, 이전 고정 `ㄱ` 가로 1170 px 기준을 대체하는 최우선 지시
- 사용자 지시: 입력 영역만 넓히거나 글자를 가로로 늘이지 않는다. 교육 목적에 맞게 실제 한글과 같은 비율·모양을 유지하고, 굵은 폰트처럼 가로와 세로 모두 넓은 입력 통로를 제공한다.
- 현재 문제: 2340 × 1080 화면의 `ㄱ`은 약 1491 × 620 px로 가로/세로 비가 약 2.41이라 자연스러운 한글 글자꼴보다 지나치게 가로로 늘어났다.
- 유지 기준: 실제 손가락 입력 Canvas는 최소 1872 × 648 px이고 화면에서 가장 큰 아이 상호작용 영역으로 유지한다.
- 글자 기준: 글자별 정규화된 교육용 template을 정사각형 em 안에 정의하고 x축·y축 동일 배율로 확대한다. 현재 `ㄱ`의 실제 bounds는 가로·세로 각각 최소 580 px이고 Canvas 높이의 75% 이상을 사용한다.
- 굵기 기준: 목표 획은 em의 12~18% 굵기로 보여 손가락이 지나갈 수 있는 넓은 통로가 되며, 입력 허용 폭과 시각 guide가 같은 geometry에서 파생된다.
- 조합 글자 기준: 이후 `가`는 자음 `ㄱ`과 모음 `ㅏ`의 상대 위치와 비율을 하나의 정사각형 em 안에서 보존한다. 이번 루프는 새 수업 화면을 추가하지 않고 geometry 계약과 테스트 fixture까지만 준비할 수 있다.
- 검증: 표시 경로와 판정 경로의 균일 배율, 글자 bounds, 획 굵기, 중심 정렬을 단위 테스트와 정확한 2340 × 1080 초기·재시도·성공 화면에서 실측한다.

## 2026-08-24 — 참고 화면 수준의 더 굵은 한글 획

- 상태: 활성, 기존 굵기 기준을 대체하는 최신 최우선 지시
- 사용자 지시: 첨부 참고 이미지처럼 화면 중앙 한글의 획 굵기를 현재보다 더 크게 만든다.
- 참고 이미지: `/Users/lim/Desktop/스크린샷 2026-08-24 오후 2.40.44.png`. 이미지는 굵고 넓은 흰색 쓰기 길의 시각 밀도만 참고하며 캐릭터·상표·배치는 복제하지 않는다.
- 현재 기준과 목표: 현재 `WritingCanvasGeometry`의 guide는 정사각형 em의 15%로, 2340 × 1080의 실제 Canvas에서 약 93 px이다. 우선 em의 20%인 약 124 px로 확대해 기존보다 약 33% 굵게 보이게 한다. 실제 화면 비교에서 참고 이미지의 굵은 쓰기 길보다 여전히 명확히 가늘면 20~22% 안에서 최소 추가 조정할 수 있다.
- 반드시 유지: `ㄱ` 중심선, 가로·세로 길이, 정사각형 em, 균일 x/y 배율, 화면 중앙 정렬, 시작점·방향 화살표·끝점의 의미를 바꾸지 않는다. 글자를 가로 또는 세로 한쪽으로 늘려 굵게 보이게 하지 않는다.
- 입력 판정: 시각 guide만 굵히되 아이의 기존 입력 허용 폭을 좁히지 않는다. 표시와 판정은 계속 같은 `WritingCanvasGeometry` 중심선에서 파생하며, 굵기 변경 때문에 기존 정상 입력·155/156 px 되돌림 경계가 역전되지 않도록 회귀 검증한다.
- 아이 관점: 흰색 쓰기 길이 노란 배경에서 한눈에 가장 큰 과제로 보여야 하고, 손가락으로 따라갈 수 있는 넓은 통로로 인식되어야 한다. 시작점·방향·끝점 표시는 굵어진 획에 묻히지 않아야 한다.
- 검증: 정확한 앱 2340 × 1080에서 초기·진행 중·재시도·성공 화면을 새로 캡처하고 guide 굵기를 픽셀과 em 비율로 실측한다. 참고 이미지와 현재 화면을 같은 상태로 비교하고, `./scripts/verify.sh`, `git diff --check`, 정상 입력 및 경계 회귀를 통과한다.
- 실제 아이 관찰: 자동·에뮬레이터·아이 대리 QA와 구분하며 실제 아이가 사용하지 않았다면 수행했다고 기록하지 않는다.

## 2026-08-24 — 정사각형 학습판·동적 획 안내·자동차 맞춤 보상

- 상태: 활성, 이전 동물 안내와 정적인 화살표 지시를 대체하는 최신 최우선 지시
- 사용자 지시: 쓰기 방향 화살표를 아이가 움직임만 보고 이해할 수 있는 동적 guide로 바꾸고, 화면의 보이는 학습판은 직사각형이 아닌 정사각형으로 실제 한글 쓰기 규격과 일치시킨다. 동물보다 자동차를 좋아하는 아이에 맞춰 경찰차·소방차·구급차·스포츠카와 다양한 자동차를 같은 그림체로 생성·적용하고, 정답이면 자동차가 해당 글자의 실제 획수만큼 이동하는 보상을 보여 준다.
- 정사각형 학습판: 손가락 입력을 받는 전체 Canvas는 기존 최소 1872 × 648 px와 화면 최대 상호작용 영역 계약을 유지한다. 다만 아이가 눈으로 보는 점선 중심축·학습판은 Canvas 짧은 변을 기준으로 한 정사각형으로 중앙 정렬한다. 정사각형을 만들기 위해 글자나 판정 경로를 비균일 배율로 늘이거나 줄이지 않는다.
- geometry 단일 진실: 보이는 정사각형 학습판, 교육용 글자 template, 동적 안내 위치, 아이 획 진행률, `GieokTraceEvaluator` 판정은 같은 정사각형 em과 균일 x/y 배율에서 파생한다. 현재 `ㄱ`과 향후 `가`의 자모 배치·횡종 비율을 각각 새 픽셀 실측과 단위 테스트로 고정한다.
- 동적 guide: 입력 전에는 첫 획의 시작→방향→끝을 큰 움직임으로 반복 시범하고, 입력 중에는 현재 획의 손가락보다 조금 앞쪽에서 다음 방향만 짧게 움직인다. 완료한 구간의 화살표는 사라지거나 약해지고 다음 획 안내만 활성화한다. 아이가 화면 문장을 읽지 않아도 시범만 보고 시작점과 진행 방향을 알아야 한다.
- 동적 화살표 명확성: 위치만 보여 주는 원·점 하나를 최종 동적 화살표로 간주하지 않는다. 움직이는 표식에는 현재 구간을 가리키는 오른쪽 또는 아래쪽 화살촉·방향성이 있어야 하고, 꼭지를 지나면 방향이 오른쪽에서 아래로 바뀌는 장면을 새 연속 프레임으로 확인한다. 원·링을 보조 강조로 함께 쓸 수는 있지만 방향 화살표를 대체할 수 없다.
- 동작 안전: 동적 안내는 시작점·글자 guide·아이 획·끝점·자동차를 가리지 않고, 입력 좌표를 가로채지 않으며, 앱이 배경으로 가거나 화면이 초기화되면 중단·복원된다. 성공·재시도·지우기·다시 듣기 상태에서는 서로 모순되는 애니메이션이나 음성이 동시에 나오지 않는다.
- 자동차 자산: 경찰차, 소방차, 구급차, 스포츠카, 택시, 버스, 쓰레기차, 화물트럭, 지게차의 9종을 우선 적용한다. 모든 차량은 오른쪽을 향한 둥근 유아용 2D 그림체, 굵고 일정한 외곽선, 밝은 고대비 색, 큰 표정, 글자·번호·브랜드 없는 투명 PNG로 통일한다. 동물 안내 이미지는 자동차 적용 뒤 기본 화면에서 제거하되 삭제하지 않고 회귀 근거를 보존한다.
- 자동차 선택·순환: 새 설정 화면이나 읽기 텍스트에 의존하지 않는다. 첫 적용은 경찰차를 기본 안내차로 사용하고, 이후 수업 또는 성공 흐름에서 9종이 예측 가능한 순서로 순환하도록 한다. 같은 세션의 지우기·재시도에서는 선택 차량이 임의로 바뀌지 않는다.
- 획수 이동 보상: 글자 lesson data에 실제 교육용 `strokeCount`를 명시하고, 정답 한 번마다 활성 자동차가 정확히 `strokeCount`칸 이동한다. 현재 `ㄱ`은 1획이므로 성공 시 1칸만 이동한다. 향후 다획 글자는 한 번에 건너뛰지 않고 획수만큼 짧고 분명한 이동을 순서대로 보여 준다. 실패·지우기·다시 듣기는 이동시키지 않고, 같은 성공 callback의 중복 이동을 금지한다.
- 보상 배치: 이동 경로와 자동차는 쓰기 정사각형 밖의 안전 영역에 두거나 guide를 가리지 않는 별도 레이어로 둔다. 아이가 결과를 바로 연결하도록 성공 `★ ✓`와 음성 시작 뒤 자동차가 움직이며, 이동 완료 전 새 입력을 받아 상태가 꼬이지 않게 한다.
- 구현 순서: 현재 활성 루프를 정상 완료한 다음 새 번호 루프를 만들어 ① 정사각형 학습판과 geometry 실측 ② 동적 획 안내 ③ 경찰차 기본 적용과 동물 교체 ④ 9종 자동차 순환 ⑤ `strokeCount` 기반 이동 보상을 각각 하나의 좁은 가설로 구현·검증한다. 한 반복에 여러 제품 변경을 묶지 않는다.
- 검증: 매 루프에서 정확한 앱 2340 × 1080 화면·hierarchy와 필요한 연속 영상 또는 프레임을 새로 수집한다. 정사각형 학습판의 가로·세로 동일 픽셀, 전체 입력 Canvas 최소 크기, 균일 em, 자동차 가림 없음, 동적 안내의 올바른 획 순서, `ㄱ` 성공 1칸·실패 0칸·중복 callback 0칸을 자동 테스트와 에뮬레이터 근거로 확인한다.
- QA 경계: 글을 읽지 못하지만 한국어를 들을 수 있는 다섯 살 아이의 대리 관점에서 시범·모양·위치·움직임·검증된 음성만으로 이해 가능성을 평가한다. 실제 아이가 직접 사용하지 않았다면 실제 아이 관찰로 표현하지 않는다.
- 이미지 생성 방식: Codex 내장 이미지 생성으로 만든 원본을 사용하며, 앱에 넣는 최종본은 실제 알파 채널과 투명 모서리를 검사하고 Android `drawable-nodpi`에 저장한다. 흰색·회색 체크무늬가 픽셀에 포함된 결과, 어두운 배경, 글자·상표·워터마크가 있는 결과는 사용하지 않는다.

## 2026-08-25 — 무의미한 작업 금지와 제품 진전 우선

- 상태: 활성, 모든 자동 전환보다 우선
- 사용자 지시: 의미없는 작업은 하지 않도록 자동화를 설정한다.
- 금지: 같은 화면·callback·동작에서 반복 횟수나 상한만 `N+1`로 바꾸는 루프, 이미 통과한 근거의 재수집, 제품 코드·판정·아이 경험에 새 판단을 주지 않는 작업을 만들거나 실행하지 않는다.
- 현재 판단: 루프 116에서 동일 안내 흐름을 53/52회까지 확인했으므로 내구성 근거는 충분하다. 쉰네번째 재생만 추가하는 기존 루프 117은 중복으로 종료한다.
- 전환 순서: 근거 있는 새 불편 → 최신 미완료 사용자 제품 지시 → 장기 한글 학습 목표의 다음 제품 단계 순으로 하나를 선택한다. 모두 없으면 큐를 비우고 `IDLE`로 종료한다.
- 다음 제품 단계: 테스트 fixture에 머문 `가` geometry를 production 계약으로 전환하고, 이후 표시·동적 획 안내·판정을 각각 검증 가능한 좁은 루프로 연결한다.
- 기계 차단: 모든 목표는 작업 가치 관문을 작성하고 감독자가 매 새 세션 직전에 검사한다. 문구만 꾸며 통과시키지 않으며 중복 목표는 실행하지 않는다.

## 2026-08-26 — 실제 아이 관찰 준비와 한글 학습 범위 확장

- 상태: 활성, 최신 제품 지시
- 사용자 지시 1: 실제 5세 아이 관찰을 수행했다고 주장하지 말고, 현재 가능한 범위에서 보호자 동의·중단 기준·관찰 절차·기록 양식·검증 기준을 먼저 준비한다.
- 사용자 지시 2: 교육 순서에 맞춰 자음·모음과 조합 글자로 학습 범위를 확장하고 단위 테스트·Android lint·debug build·정확한 2340 × 1080 에뮬레이터 검증까지 완료한다.
- 교육 순서: 한 번에 전체 한글을 추가하지 않는다. 단순 모음과 자음을 먼저 익힌 뒤 바로 조합해 보는 좁은 첫 묶음으로 `ㅏ → ㄱ → 가 → ㅓ → ㄴ → 너`를 사용한다.
- 공통 경계: 글을 읽지 못하지만 한국어를 들을 수 있는 아이가 음성·시범·모양·위치·움직임으로 진행할 수 있어야 하며, 기존 큰 쓰기판·교육용 정사각형 em·획순·방향 판정·9종 자동차·획수 보상을 유지한다.

## 2026-08-26 — 한글 교육 순서 정정

- 상태: 활성, 바로 앞 교육 순서를 대체하는 최신 지시
- 사용자 지시: 자음 다음 모음이며, 조합 글자는 `가 → 나 → 다` 순서가 기본이다. 한글 교육을 공식 자료로 조사하고 이후 작업이 참고할 수 있게 문서화한다.
- 첫 묶음: `ㄱ → ㄴ → ㄷ → ㅏ → 가 → 나 → 다`.
- 근거 문서: `docs/한글-교육-순서-근거.md`에서 교육부·국립국어원 근거와 LimDo 제품 결정을 구분한다.
- 폐기: 앞 절의 `ㅏ → ㄱ → 가 → ㅓ → ㄴ → 너`는 최신 지시와 맞지 않으므로 구현·완료·QA 근거로 사용하지 않는다.

## 2026-08-26 — 교육부 흐름으로 이어지는 단계 구성

- 상태: 활성, 최신 제품 지시
- 사용자 지시: 현재 첫 묶음에서 끝내지 않고 교육부의 한글 교육 흐름으로 계속 이어질 수 있게 구성한다.
- 단계 계약: `연필잡기·선 긋기 → 자음 → 모음 → 글자의 짜임 → 받침 없는 글자 → 받침 있는 글자 → 겹받침`.
- 현재 범위: 손가락 준비는 별도 관찰 과제로 유지하고, 이번 루프는 `자음 ㄱㄴㄷ → 모음 ㅏ → 글자의 짜임 가나다`까지만 구현한다. 이후 단계의 글자를 미리 한꺼번에 추가하지 않는다.
- 코드·문서: `KoreanCurriculum.educationFlow`와 `docs/한글-교육-순서-근거.md`에서 이후 확장 순서를 고정한다.

## 2026-08-26 — `ㅊ·ㅌ` 교육용 모양과 쓰기 방법 교정

- 상태: 활성, 기존 `파·하` 확장보다 우선하는 최신 결함 지시
- 사용자 지시: 현재 화면의 `ㅊ·ㅌ` 모양과 쓰는 방법이 평소 쓰는 것과 다르므로 바로잡는다.
- 원인 경계: 화면 글꼴 문제가 아니라 `WritingCanvasGeometry`의 Custom Canvas template과 lesson 획 계약 문제로 취급한다.
- 교정 범위: `ㅊ·ㅌ`뿐 아니라 같은 자형과 획 흐름을 공유해야 하는 `ㅈ·자·차·타`의 표시·판정·동적 안내·음성·획수 보상을 한꺼번에 일치시킨다.
- 검증: 정확한 2340 × 1080에서 교정 전후 모양, 연결 획 입력, 보상 칸과 순환을 새 근거로 확인한다. 실제 아이가 사용하지 않았다면 실제 아이 관찰로 표현하지 않는다.

## 2026-08-26 — 시스템 고딕 자형과 좌우 세로 UI

- 상태: 활성, 바로 앞의 `ㅈ·ㅊ·ㅌ` 연결형 선택을 대체하는 최신 지시
- 자형 지시: `ㅂ·ㅅ·ㅐ·ㅈ·ㅊ·ㅌ·ㅍ·ㅎ`의 모양을 Codex에서 보이는 시스템 고딕 한글 자형처럼 교정한다. 폰트 외곽선만 보여 주는 것이 아니라, 보이는 모양과 입력 판정 중심선을 같게 유지한다.
- 교육 정확성: 글자를 처음 배우는 아이이므로 편의상 평가 통과를 위해 글자 고유 모양을 다른 형태로 바꾸지 않는다.
- 순서 보정: `ㅐ`를 누락하지 않고 표준 자모 순서에 맞게 `ㅏ → ㅐ → ㅑ`로 둔다.
- UI 지시: 하단에 모인 조작 UI를 좌우 가장자리의 세로 배치로 재구성해 가운데 쓰기 공간의 가로·세로 비율을 높인다.
- 루프 경계: 자형 결함은 현재 루프 147에서 완료하고, UI 재배치는 정확한 화면 측정과 입력 영역 회귀를 따로 검증하기 위해 루프 148로 준비한다.

## 2026-08-26 — `ㄷ·ㄹ·ㅇ` 교육용 자형 재검토

- 상태: 활성, 준비 상태였던 받침 확장 루프 153보다 우선하는 최신 결함 지시
- 사용자 지시: 실제 화면에서 `ㄷ·ㄹ` 같은 글자가 끊겨 보이고 `ㄹ`의 쓰기 방향이 잘못된 것 같으므로 다시 검토한다. 다른 자모도 확인하고, 특히 `ㅇ`은 원형이어야 하는데 각져 있으므로 고친다.
- 새 확인 근거: `/tmp/limdo-glyph-audit-20260826/`의 정확한 2340 × 1080 단일 입력 캡처에서 `ㄷ·ㄹ`의 획별 흐름을 확인했다. `/tmp/limdo-ieung-audit-20260826-154/`의 새 `ㅇ` 초기·입력·성공 화면에서는 8개 직선 구간으로 된 팔각형이 실제로 표시되고 입력되는 것을 확인했다. 동시에 실행돼 입력이 겹친 첫 `ㄷ·ㄹ` 캡처는 별도 폴더로 옮겨 판정에서 제외했다.
- 교정 기준: `ㄷ`은 2획 방향을 유지하되 왼쪽 위 접점이 연결되어 보여야 한다. `ㄹ`은 3획을 `위 가로→오른쪽 아래 / 가운데 왼쪽→왼쪽 아래 / 아래 오른쪽`으로 나눈다. `ㅇ`은 다각형 근사선이 아니라 원형 곡선 중심선으로 표시·시범·판정한다.
- 파급 범위: 기본 자모만 고치지 않고 같은 획 계약을 사용하는 `다·라·아·갇·갈`까지 동기화한다. 다른 기본 자모도 새 화면과 template으로 확인하되 구체적인 결함 근거가 없으면 임의로 다시 그리지 않는다. 새 `갓·강·갖` 확장은 이 결함을 검증한 뒤로 보류한다.
- 검증 경계: 자동 테스트와 새 APK의 정확한 2340 × 1080 초기·획별·성공 캡처를 확보한다. 실제 아이가 사용하지 않았으므로 실제 아이 관찰로 표현하지 않는다.

## 2026-08-26 — `가나다` 다음 기본 모음 행과 교육 단계 선택 메뉴

- 상태: 활성, 현재 자형 결함 완료 뒤 최우선 제품 지시
- 사용자 지시: 교육 순서를 다시 확인해 `가 → 나 → 다` 다음에는 받침 없는 글자에서 초성은 유지하고 모음만 교육 순서대로 바뀌도록 구성한다.
- 확인한 큰 흐름: 교육부 자료의 `자음 → 모음 → 글자의 짜임 → 받침 없는 글자 → 받침 있는 글자 → 겹받침`을 유지한다. 교육부 자료가 모든 세부 음절 행의 유일한 순서를 지정한다고 과장하지 않는다.
- LimDo 세부 순서: `가나다`로 결합 원리를 먼저 익힌 뒤 받침 없는 기본 모음 행을 `가·갸·거·겨·고·교·구·규·그·기`, 다음 `나·냐·너·녀·노·뇨·누·뉴·느·니`, 다음 `다·댜·더·뎌·도·됴·두·듀·드·디`처럼 초성을 고정하고 `ㅏ·ㅑ·ㅓ·ㅕ·ㅗ·ㅛ·ㅜ·ㅠ·ㅡ·ㅣ` 순서로 확장한다.
- `ㅐ` 경계: 맞춤법 자모 나열 순서와 초기 읽기 교육 순서를 구분한다. `ㅐ`는 기본 모음 행 뒤 복잡한 모음 단계에서 조합 글자로 확장하며, 기존 `ㅏ → ㅐ → ㅑ` 순서를 초기 교육의 근거로 사용하지 않는다.
- 메뉴 지시: 자음·모음·글자의 짜임·받침 없는 글자·받침 있는 글자를 교육 순서대로 큰 단계 카드에서 고르고, 다음 화면에서 실제 글자 또는 좁은 묶음을 골라 수업을 시작할 수 있게 한다.
- 아이 사용성: 글을 읽지 못하는 다섯 살 아이가 글자 모양, 단계 아이콘, 위치, 선택 움직임과 검증된 한국어 음성으로 메뉴를 사용할 수 있어야 한다. 보호자·접근성용 짧은 한글 문구는 유지한다.
- 구현 순서: 현재 자형·표시 결함을 완료한 뒤 ① 기본 모음 행 curriculum과 첫 `ㄱ` 행 ② 교육 단계·글자 선택 메뉴 순서의 별도 제품 루프로 진행한다.

## 2026-08-26 — `하` 상단 비율과 시작·끝 표시 크기

- 상태: 활성, 루프 153 완료를 보류시키는 최신 결함 지시
- 사용자 지시 1: `하`에서 `ㅎ`의 상단 모양 비율이 아래 원형에 비해 너무 작으므로 교육용 글자 비율을 다시 맞춘다.
- 사용자 지시 2: 시작점과 끝점의 원이 너무 크므로 흰색 쓰기 길 영역 안에서 표시되게 줄인다.
- 새 화면 근거: `captures/loop153/iteration8/shared-geometry/ha-initial.png`의 정확한 2340 × 1080 화면에서 두 결함을 확인했다.
- 변경 경계: `하` 초성의 상단 두 가로획과 공통 시작·끝 표시 반지름만 최소 변경한다. 이미 통과한 원형·획순·방향·1962 × 954 px 쓰기판·좌우 조작 열은 유지한다.
- 검증: `하` 초기·획별·성공과 대표 짧은 획의 시작·끝 표시를 정확한 2340 × 1080에서 새로 확인하고, 실제 아이 관찰은 수행하지 않았다고 구분한다.

## 2026-08-26 — `갈` 받침 `ㄹ` 겹침 교정

- 상태: 활성, 루프 153 완료 전 추가된 최신 결함 지시
- 사용자 지시: `갈`의 받침이 너무 겹쳐 보이므로 교육용 글자 구조에 맞게 다시 교정한다.
- 새 화면 근거: `captures/loop153/iteration8/shared-geometry/gal-initial.png`의 정확한 2340 × 1080 화면에서 종성 `ㄹ`의 위·가운데·아래 가로 층과 세로 연결이 굵은 흰색 덩어리처럼 합쳐져 보인다.
- 원인: 현재 종성 `ㄹ`은 0.70~0.94em의 높이 0.24em 안에 배치되지만 공통 guide 굵기는 0.20em이다. 획 중심 간격이 guide 지름보다 작아 별도 층이 겹친다.
- 교정 기준: 초·중성 아래에 받침 전용 세로 공간을 확보하고 `ㄹ`의 세 가로 층을 구분한다. 의도된 꺾임·공유 접점은 연결하되 비접점 획은 합쳐지지 않게 한다.
- 보존: `갈` 6획 순서·방향·음성·보상·판정, 정사각형 em, 큰 쓰기판과 좌우 조작 열은 유지한다. `갇·감·갑`은 받침 칸 회귀만 확인한다.
- 검증: 자동 좌표·간격 계약과 정확한 2340 × 1080 `갈` 초기·획별·성공 화면을 모두 통과한 뒤에만 완료한다. 실제 아이 관찰은 실행하지 않았다.

## 2026-08-26 — 받침 음절의 초·중·종성 전체 비율 재구성

- 상태: 활성, 앞의 `갈` 받침 간격 교정보다 구체적인 최신 교육 정확성 지시
- 사용자 지시: `갈`의 상단 `가` 비율도 잘못됐다. 받침이 들어가는 글자는 받침 없는 글자와 초·중성 비율 자체가 달라지므로 한글을 충분히 조사한 뒤 교육 앱에 맞게 교정한다.
- 공식 근거: 국립국어원의 초성·중성·종성 음절 구성, 국립한글박물관의 위치별 자모 크기·모양 변화, 국립국어원 글꼴 연구의 `좌우`와 `좌우하` 조합 구분, 교육청 `찬찬한글`의 받침 전용 자리 연습을 확인했다.
- 구현 원칙: 받침 6글자가 같은 상단 `가` 좌표를 공유하지 않는다. 종성 높이에 따라 보통 받침·낮은 받침·높이를 많이 쓰는 받침 조합으로 나누고, 초성·중성·종성 공간을 함께 재배분한다.
- `갈` 기준: 초성 `ㄱ`은 `0.06~0.50em`, 중성 `ㅏ`는 `0.04~0.50em`, 종성 `ㄹ`은 `0.64~0.94em`에 두며 세 층과 윗칸 여백을 보존한다.
- 검증: 공식 원리와 LimDo의 수치 결정을 문서에서 구분하고, 자동 비율·간격·입력 판정과 정확한 2340 × 1080 실제 화면을 모두 확인한다. 실제 아이 관찰은 실행하지 않았다고 명시한다.

## 2026-08-26 — `갈` 정사각형 외곽·`파` 겹침·`하` 원형 방향 재교정

- 상태: 활성, 반복 13 실화면을 사용자가 직접 보고 내린 최신 결함 지시
- `갈`: 중심선만 정사각형 범위에 있는 것으로 통과하지 않는다. guide 굵기를 포함한 실제 흰색 외곽이 정사각형 em 안에 여백을 두고 들어오며 완성 글자의 가로·세로 외곽이 정방형에 가까워야 한다.
- `파`: 왼쪽 초성 `ㅍ`의 두 세로획이 겹쳐 한 덩어리로 보이지 않게 중심 간격을 guide 굵기보다 크게 벌린다.
- `하`: 원형 획은 현재와 반대인 위에서 왼쪽으로 진행하도록 바꾼다. 같은 원을 쓰는 `ㅇ·아·ㅎ·하`의 geometry·방향 metadata·음성·판정을 함께 동기화한다.
- 검증: `갈` 실제 외곽 inset과 정방형 비율, `파` 두 세로획의 외곽 간격, 네 글자의 반시계 원형 첫 진행 방향을 자동 테스트와 정확한 2340 × 1080 새 화면·입력으로 확인한다.

## 2026-08-26 — 1차 목표: 세 기본 메뉴·전체 디자인·연출·QA

- 상태: 활성, 루프 154 완료 뒤 최우선 지속 목표
- 사용자 지시: 작업 기획서에 메뉴 화면의 기본 세 가지 `자음·모음·가나다`를 먼저 만들고 실제 학습 진입 기능까지 구현하는 것을 1차 목표로 둔다.
- 디자인 지시: 자동 루프 안에 디자인 팀 역할을 두고 다섯 살 아이가 좋아할 자동차 중심의 밝고 통일된 화면을 전체 흐름에 적용한다. 진입·선택·성공·재시도에 화려하지만 학습을 가리지 않는 연출을 포함한다.
- QA 지시: 자동 QA 팀 역할의 기능·회귀·정확한 2340 × 1080 검증과 다섯 살 아이 대리 시뮬레이션을 모두 통과할 때까지 새 루프를 이어 간다.
- 정직성 경계: 자동 디자인·QA 역할과 아이 대리 시뮬레이션은 실제 사람 디자이너·QA 담당자·실제 아이 관찰이 아니다. 참여하지 않은 사람의 승인을 받았다고 표현하지 않는다.
- 완료 경계: `docs/1차-목표-작업-기획서.md`의 완료 관문을 모두 통과하고 체크포인트 커밋을 일반 push한 뒤 큐를 `없음`으로 만들고 `IDLE`로 종료한다. 2차 목표는 사용자 지시 없이 시작하지 않는다.

## 2026-08-26 — 외부 루프 엔지니어링 사용법의 선별 적용

- 상태: 활성, 자동화 기반 지시
- 사용자 지시: 제공한 자율 개발 루프 사용법을 참고해 LimDo 개발에 도움이 되는 부분을 현재 자동화에 반영한다.
- 적용: 새 `codex exec` 세션, 파일 기억, 설정 분리, 날짜별 로그, 현재 바퀴 뒤 STOP, 6절 작업자 지시, 같은 지적 두 번의 규칙·검사 승격, macOS 로그인 자동 실행 준비와 제어 명령을 현재 구조에 맞게 사용한다.
- 중복 방지: 기존 `.loop/state.md`, `.loop/user-directives.md`, `scripts/run-cli-loop.sh`와 같은 단일 진실을 새 이름으로 복제하지 않는다.
- 품질 경계: 화면 확인 전에 커밋하는 일반 규칙은 LimDo의 실화면 QA 후 완료 커밋보다 약하므로 적용하지 않는다.
- 안전 경계: 현재 공용 에뮬레이터 사용 중지 지시를 유지하며 launchd나 감독자를 자동으로 켜지 않는다.

## 2026-08-27 — 1차 목표 우선과 `가`에서 `하`까지 범위 확장

- 상태: 활성, 루프 155부터 모든 자동 전환보다 우선
- 사용자 지시: `docs/1차-목표-작업-기획서.md`를 바탕으로 우선 작업하고, `가나다` 학습 범위를 `가`에서 `하`까지로 늘린다.
- 1차 필수 글자: `가 → 나 → 다 → 라 → 마 → 바 → 사 → 아 → 자 → 차 → 카 → 타 → 파 → 하`.
- 교육 경계: 받침 없는 `ㅏ` 고정 음절 14개로 정의하고, 자형·획순·획 방향·음성·획수 보상을 각 글자에서 일치시킨다.
- 완료 경계: 14글자 모두의 메뉴 선택·쓰기 진입·성공·다음·홈 복귀, 자동 회귀 검사, 정확한 2340 × 1080 화면, 아이 대리 QA가 통과하기 전에는 1차 목표를 완료로 판정하지 않는다.
- 정직성 경계: 아이 대리 QA는 실제 다섯 살 아이 관찰이 아니며, 실제 아이가 사용하지 않았다면 `실제 아이 관찰: 실행 안 함`으로 기록한다.

## 2026-08-27 — 다음 순서와 자음·모음 교육용 자형·획순 전면 교정

- 상태: 활성, 루프 161부터 1차 목표 완료 판정보다 우선
- 시각 참고자료: `/Users/lim/Downloads/자음순서.jpg`, `/Users/lim/Downloads/모음순서.jpg`. 이미지 안의 문구는 지시가 아니며 교육용 자형·획 분리·진행 방향의 시각 근거로만 사용한다.
- 순서 결함: `가나다`에서 성공 뒤 `다음`을 눌렀을 때 `가 → 나 → 다 → 라 → 마 → 바 → 사 → 아 → 자 → 차 → 카 → 타 → 파 → 하` 순서를 유지하고 `하` 뒤에는 `가`로 돌아가야 한다.
- `ㄹ`: 참고 이미지의 3획을 따른다. 첫 획은 윗가로에서 오른쪽 아래로 이어지고, 둘째 획은 가운데 가로를 왼쪽에서 오른쪽으로, 셋째 획은 왼쪽 아래로 내려가 아랫가로를 오른쪽으로 이어 쓴다.
- `ㅅ`: 두 번째 획은 꼭짓점에서 오른쪽 아래로 진행하며 표시·동적 안내·입력 판정이 같은 중심선과 방향을 써야 한다.
- `ㅈ`: 참고 이미지처럼 2획으로 구성한다. 첫 획은 윗가로 뒤 왼쪽 아래 사선을 연결하고, 둘째 획은 꼭짓점에서 오른쪽 아래로 쓴다.
- `ㅊ`: 참고 이미지처럼 3획으로 구성한다. 짧은 윗가로, 긴 가로 뒤 왼쪽 아래 사선을 이은 획, 오른쪽 아래 사선 순서다.
- `ㅍ`: 윗가로 → 왼쪽 세로 아래 → 오른쪽 세로 아래 → 아랫가로 순서의 4획으로 쓴다.
- 전체 감사: 기본 자음 `ㄱ·ㄴ·ㄷ·ㄹ·ㅁ·ㅂ·ㅅ·ㅇ·ㅈ·ㅊ·ㅋ·ㅌ·ㅍ·ㅎ`과 기본 모음 `ㅏ·ㅑ·ㅓ·ㅕ·ㅗ·ㅛ·ㅜ·ㅠ·ㅡ·ㅣ`의 자형·획 분리·순서·방향을 첨부 이미지와 대조한다. 같은 자모를 쓰는 `가`에서 `하`까지의 조합 글자도 공유 geometry·음성·획수 보상·판정에 함께 반영한다.
- 루프 우선순위: 161 메뉴 내부 다음 순서 → 162 `ㄹ` → 163 `ㅅ·ㅈ·ㅊ`의 공유 사선·둘째 획 판정 → 164 `ㅍ` → 이후 전체 자음·모음 감사. 한 루프에서 서로 다른 결함을 무리하게 묶지 않는다.
- 완료 경계: 자동 테스트만 맞춰 통과하지 않고 정확한 2340 × 1080 화면에서 자형·획별 안내·정방향 입력 수락·역방향 또는 잘못된 획 거부를 새 근거로 확인한다. 실제 아이 관찰을 수행하지 않았다면 수행했다고 표현하지 않는다.

## 2026-08-27 — 루프 166 다시 실행

- 상태: 활성, 외부 에뮬레이터 점유 차단 해제
- 사용자 지시: 중단된 LimDo 루프를 다시 실행한다.
- 재개 근거: `com.nullplaying`만 안전하게 종료한 뒤 LimDo를 실행해 `topResumedActivity`, `mCurrentFocus`, `mFocusedApp`이 모두 `com.example.limdo/.MainActivity`임을 확인했다. 화면은 정확한 2340 × 1080이고 `user_rotation=1`이다.
- 실행 경계: 기존 반복 7과 미커밋 한글 인계를 보존하고 루프 166의 고정 최종 통합 행렬만 재개한다. 같은 차단 확인 반복을 추가하지 않는다.

## 2026-08-27 — 실제 플레이의 획 안내와 `ㅅ·ㅈ·ㅊ` 결함

- 상태: 활성, 완료된 1차 목표 뒤 새로 들어온 최우선 결함 지시
- 사용자 관찰: 쓰기 안내 오브젝트와 안내선이 일치하지 않고, 오브젝트가 커서 쓰기 배경을 가린다.
- 대비 지시: 원 표식의 흰 테두리가 쓰기 배경과 헷갈리므로 배경과 명확히 구분되는 색으로 바꾼다.
- 안내 지시: 아이가 경로를 쉽게 인식하도록 쓰기 안내를 점선 중심선으로 개선하고, 움직이는 안내도 같은 실제 판정 경로 위에 둔다.
- 자형 지시: `ㅅ·ㅈ·ㅊ`의 자형·획순·방향·판정을 첨부 자음 순서 이미지 기준으로 다시 교정하고 `사·자·차`에도 같은 초성 계약을 적용한다.
- 근거 구분: 이는 사용자의 실제 플레이 지적이며 실제 5세 아이 관찰로 표현하지 않는다.

## 2026-08-27 — 현재 획 전용 원형 점선과 `ㅅ` 2획 별도 시작점

- 상태: 활성, 바로 앞 루프 167 안내 표현과 `ㅅ` 시작점 기준을 구체적으로 대체한다.
- 점선 형태: 선분형 dash를 사용하지 않는다. 같은 지름의 작은 원을 촘촘하고 일정한 간격으로 production 중심선 위에 배치한다.
- 노출 범위: 현재 아이가 써야 하는 획의 원만 표시하고, 완료한 획과 이후 획의 안내 원은 숨긴다.
- `ㅅ` 시작점: 2획은 1획 시작점과 같지 않다. 1획 시작점보다 오른쪽 위의 별도 시작점에서 오른쪽 아래로 내려간다. `사`도 같은 초성 geometry·시범·입력 판정을 공유한다.
- 회귀 경계: `ㅅ·사`에서 두 획의 시작점 좌표가 서로 다르고, 2획을 1획 시작점에서 시작한 입력은 성공하지 않는 자동 검사를 둔다.

## 2026-08-27 — `ㅅ` 2획은 1획 윗부분과 겹치는 접점에서 시작

- 상태: 활성, 바로 앞의 “분리된 별도 시작점” 해석을 정정하는 최신 지시다.
- 정확한 자형: 2획 시작점은 1획 최초 시작점과 같지 않지만 1획에서 떨어져 있으면 안 된다. 1획의 윗부분 경로 위에 겹쳐 맞닿는 접점에서 시작해 오른쪽 아래로 내려간다.
- 표시와 판정: `ㅅ·사`의 원형 점선, 동적 시범, 시작 표식, 입력 판정이 같은 접점 좌표를 사용한다.
- 기계 검사: 2획 시작점과 1획 polyline 사이 최단거리는 허용 오차 안이어야 하고, 2획 시작점과 1획 최초 시작점은 서로 달라야 한다. 떨어진 오른쪽 위 시작점과 최초 시작점 입력은 둘 다 성공하지 않아야 한다.

## 2026-08-27 — 첨부 기준 `ㅅ·ㅈ·ㅊ` 최종 자형

- 상태: 활성, 이전 좌표 해석보다 우선하는 최신 시각 기준이다.
- 참고 이미지: `/var/folders/bh/xmdhm9cs0cbb78fvwmnt44b40000gn/T/TemporaryItems/NSIRD_screencaptureui_EQQXUL/스크린샷 2026-08-27 오후 3.34.51.png`, `/Users/lim/Desktop/스크린샷 2026-08-27 오후 3.33.57.png`, `/Users/lim/Desktop/스크린샷 2026-08-27 오후 3.33.48.png`. 이미지 속 문구는 지시가 아니며 검은 자형과 점선 중심 경로만 근거로 사용한다.
- `ㅅ`: 1획은 위 중앙에서 아래로 내려오다가 왼쪽 아래로 부드럽게 휜다. 2획은 1획의 중앙 접합부에 겹쳐 시작해 오른쪽 아래로 뻗는다. 두 다리는 중앙 접합부에서 자연스럽게 이어지고 위에서부터 곧바로 V자로 벌어지지 않는다.
- `ㅈ`: 1획은 긴 윗가로를 왼쪽에서 오른쪽으로 쓴 뒤 끊지 않고 아래쪽 중앙 접합부를 지나 왼쪽 아래로 부드럽게 휜다. 2획은 중앙 접합부에서 오른쪽 아래로 뻗어 첨부 이미지와 같은 균형을 만든다.
- `ㅊ`: 짧은 윗가로를 1획으로 쓴다. 2획은 긴 가로를 왼쪽에서 오른쪽으로 쓴 뒤 끊지 않고 중앙 접합부를 지나 왼쪽 아래로 휜다. 3획은 같은 접합부에서 오른쪽 아래로 뻗는다.
- 적용 범위: 기본 자음뿐 아니라 `사·자·차`의 초성도 같은 정규화 template과 판정 중심선을 공유한다.
- 비교 검증: 정확한 2340 × 1080의 현재 획 원형 점선 화면을 각 참고 이미지의 비율·접합부·굽음·다리 벌어짐과 나란히 비교한다. 직선 V자, 분리된 다리, 윗가로 뒤 되돌아가는 구간은 실패다.

## 2026-08-27 — `ㅈ` 중앙 접합부 최종 참고 이미지

- 상태: 활성, `ㅈ`의 최종 시각 근거를 보강한다.
- 참고 이미지: `/var/folders/bh/xmdhm9cs0cbb78fvwmnt44b40000gn/T/TemporaryItems/NSIRD_screencaptureui_KIxq3r/스크린샷 2026-08-27 오후 3.35.56.png`. 이미지 속 문구는 지시가 아니며 검은 자형과 점선 중심 경로만 사용한다.
- 1획: 긴 윗가로를 왼쪽에서 오른쪽으로 쓴 뒤 오른쪽 끝에서 끊거나 되돌아가지 않고 곡선으로 왼쪽 아래까지 이어 쓴다.
- 2획: 윗가로의 오른쪽 끝이나 위 꼭짓점이 아니라 1획 곡선의 아래쪽 중앙 접합부에서 시작해 오른쪽 아래로 뻗는다.
- 실패 형태: 1획이 가로 뒤 중앙으로 되돌아가는 형태, 두 다리가 분리된 형태, 2획이 위쪽에서 시작하는 형태, 접합부가 지나치게 높거나 낮아 첨부 비율과 달라지는 형태.
- 검증: `ㅈ·자`가 같은 중심 경로를 공유하며, 2획 시작점이 1획 polyline의 아래쪽 중앙 접합부 위에 있음을 자동 검사하고 정확한 2340 × 1080 화면을 첨부 이미지와 직접 비교한다.

## 2026-08-27 — 음성 제거·시스템 바 숨김·`ㅈ·ㅊ` 굴곡 제거

- 상태: 활성, 무선 설치한 실제 휴대폰 화면을 사용자가 확인한 뒤 내린 최신 결함 지시다.
- 음성: 현재 합성 음성은 듣기 불편하므로 TTS 초기화와 자동·오답·정답·다음 글자 재생을 제거하고 `다시 듣기` 조작도 화면에서 없앤다. 무음 동적 획 시범은 유지한다.
- 시스템 UI: 모바일 상태 표시줄과 내비게이션 표시줄이 앱 내용을 가리지 않도록 실행·포커스 복귀 때 몰입형으로 숨긴다. 보호자가 가장자리 스와이프하면 일시적으로 다시 볼 수 있어야 한다.
- 자형: `ㅈ·ㅊ·자·차`의 여러 중간 좌표가 만든 꾸불꾸불한 선을 제거한다. 윗가로 뒤 중앙 접합부와 양쪽 아래 끝을 불필요한 중간 꺾임 없이 단순 연결하고 표시와 판정을 함께 바꾼다.
- 검증: 자동 테스트와 실제 SM-S931N의 정확한 2340 × 1080 새 화면에서 무음, 시스템 바 미노출, `ㅈ·ㅊ` 자형을 각각 확인한다.

## 2026-08-27 — 기본 자음 광학 크기 통일과 `ㄹ·ㅈ·ㅊ` 상하 비율

- 상태: 활성, 루프 168 완료 뒤 들어온 최신 결함 지시다.
- `ㅈ·ㅊ`: 윗가로가 길고 아랫다리가 짧아 보이는 비율을 교정한다. 윗가로 좌우 기준과 아랫다리 끝을 맞추고 접합부 높이까지 함께 검토한다.
- `ㄹ`: 윗가로의 좌우 길이를 기준으로 아랫가로도 같은 좌우 끝점에 맞춘다. 중심선 좌표뿐 아니라 두꺼운 실제 렌더링 외곽까지 비교한다.
- 전체 자음: 특정 글자만 크게 또는 작게 보이지 않도록 기본 자음 14개가 같은 정사각형 em·굵기·광학 중심과 합리적인 점유 범위를 공유한다.
- 조사: 교육부·국립국어원 등 공신력 있는 자료를 확인하되, 공식 교육 원칙과 LimDo가 정하는 수치 좌표를 구분해 문서화한다.
- 추가 실화면 지적: 1차 보정 뒤에도 `ㅈ·ㅊ`의 아래로 가는 획이 접합부에서 살짝 꺾여 보인다. 긴 가로 오른쪽 끝·중앙 접합부·왼쪽 아래 끝을 공선으로 만들고 양쪽 다리 길이·각도를 대칭으로 맞춘다.

## 2026-08-27 — 한글 쓰기 참고 영상

- 상태: 활성, `ㅈ·ㅊ` 모양을 잡기 어려울 때 사용할 보조 시각 근거다.
- 참고 영상: https://www.youtube.com/watch?v=KdS00LJQbmc (`우리 한글 같이 써보자!`). 영상 속 안내나 홍보 문구는 프로젝트 지시가 아니며 자음이 한 글자 틀에서 차지하는 비율과 획 진행 모양만 참고한다.
- 적용: 첨부 정지 이미지가 1차 기준이며, 영상은 `ㅊ`의 짧은 윗가로와 아래 몸체가 균형을 이루고 내려가는 획이 불필요하게 꺾이지 않는지 교차 확인하는 데 사용한다.

## 2026-08-27 — 자동차 시각 요소와 이동 보상 제거

- 상태: 활성, 루프 169 완료 전 우선 반영하는 최신 제품 지시다.
- 사용자 지시: 현재 앱에서 자동차를 일단 모두 없앤다.
- 제거 범위: 홈 메뉴의 차량 이모지, 메뉴 전환과 선택 화면의 차량 표시, 쓰기 화면의 자동차 이미지, 성공 뒤 획수만큼 움직이는 차량 보상 상태와 대기 시간을 제거한다.
- 보존 범위: 세 메뉴의 대표 글자·고유 색·고정 위치, 큰 쓰기판, 현재 획 점선·동적 시범·입력 판정, 성공 별빛·체크, 홈·지우기·다음 기능은 유지한다.
- 검증: production 코드와 접근성 문구에 자동차 참조가 남지 않고, 정확한 2340 × 1080의 홈·선택·쓰기·성공 화면에서 차량이 보이지 않으며 성공 직후 `다음`이 활성화되어야 한다.

## 2026-08-27 — 원형 점선을 따라가는 동적 안내 표식 교정

- 상태: 활성, 자동차 제거 뒤 바로 반영하는 최신 시각 결함 지시다.
- 사용자 지시: 쓰기할 때 점선을 따라 이동하는 모형이 이상하므로 아이가 보기 좋고 방향을 쉽게 알아보는 형태로 교정한다.
- 원인: 기존 표식은 갈색 외곽 원·청록 고리·긴 화살표가 한 중심에 겹치고 시작·끝점까지 정확히 이동해 끝 표식과 합쳐지면서 여러 도형이 뭉쳐 보인다.
- 교정 계약: 동적 표식은 점선 중심을 따르는 작은 청록 원 하나와 그 안의 밝은 삼각 방향표로 단순화한다. 이동 범위를 현재 획의 8~92%로 제한해 고정 시작·끝 표식과 겹치지 않으며, 외곽 지름은 안내 획 굵기의 28% 이하로 둔다.
- 보존: 현재 획만의 원형 점선, production geometry·입력 판정·동적 진행 방향, 시작·끝 표식은 바꾸지 않는다.
## 2026-08-27 — 동적 안내 표식 검증 기기

- 이번 동적 안내 표식 수정은 실제 휴대폰이 아니라 `emulator-5554`에서 검증한다.
- 에뮬레이터 물리 화면은 1080 × 2340, `user_rotation=1`로 두고 앱의 실제 가로 화면 2340 × 1080을 확인한다.
- 실제 휴대폰에서 LimDo를 더 실행하거나 입력·캡처하지 않는다.

## 2026-08-27 — 기본 모음 노출 순서와 획순 교정

- 기본 모음 중 화면에 나오는 순서와 실제 쓰는 순서가 다른 항목을 전부 확인한다.
- 첨부된 모음 순서표의 `ㅏ·ㅑ·ㅓ·ㅕ·ㅗ·ㅛ·ㅜ·ㅠ·ㅡ·ㅣ` 순서와 획 화살표를 제품 기준으로 삼는다.
- 선택 메뉴, 성공 뒤 다음 전환, 현재 획 점선, 동적 방향 표식과 입력 판정이 같은 순서·geometry를 사용하게 한다.
- 실제 아이 관찰을 하지 않았다면 했다고 표현하지 않는다.

## 2026-08-27 — 코덱스 내부 생성 투명 이미지 아틀라스 자동 루프

- 성공·실패 파티클과 이펙트, 버튼 UI를 투명 이미지 아틀라스로 개선한다.
- 자동 디자인 역할과 연출 역할이 시각 자산을 생성·적용하고, 자동 QA 역할이 실제 플레이를 확인해 통과하지 못하면 같은 결함을 최소 수정해 반복한다.
- 외부 이미지 API나 API 키를 사용하지 않고 코덱스 내부 이미지 생성기를 사용한다.
- 한 반복에 성공·실패·버튼을 모두 바꾸지 않는다. 성공 연출부터 적용·검증한 뒤 실패 연출, 버튼 순서로 새 루프를 만든다.
- 실제 사람 디자이너·연출팀·QA팀이나 실제 아이가 참여하지 않았다면 참여하거나 승인했다고 표현하지 않는다.

## 2026-08-27 — 현재 작업 전체 완료 뒤 독창적 영웅 안내 음성 생성·적용

- 상태: 대기. 현재 활성 루프 174뿐 아니라 이미 예정된 시각 아틀라스 전체 통합 QA와 그 QA에서 발견한 관련 결함 수정까지 모두 끝나기 전에는 시작하지 않는다.
- 시작 경계: 진행 중인 제품 작업 사이에 끼워 넣거나 현재 루프의 미커밋 변경과 섞지 않는다. 현재 시각 자산 작업 계열의 큐가 완료된 뒤 별도의 단일 제품 루프로 준비해 생성부터 앱 적용과 검증까지 수행한다.
- 음성 정체성: `헬로 카봇` 주인공 또는 해당 성우의 식별 가능한 목소리를 복제·모사하지 않는다. 대신 밝고 씩씩하며 친근한 어린이 로봇 영웅 분위기의 독창적인 한국어 목소리를 사용한다.
- 생성 범위: 실제 제품에서 필요한 짧은 메뉴 안내·쓰기 시작·재시도·성공 대사를 먼저 확정하고, 로컬 고정 음성 파일로 생성한다. 외부 서버 호출이나 런타임 TTS에 의존하지 않는다.
- 앱 적용: 생성 파일을 Android 로컬 리소스에 포함하고 production 화면의 실제 상태와 연결한다. 중복 재생·화면 이탈 뒤 재생·자원 누수를 막고, 재생 실패나 음소거 상태에서는 기존 무음 동적 획 시범이 계속 동작하게 한다.
- 검증: 음성 파일 형식·길이·음량·무음 구간과 대사 매핑을 자동 검사하고, 정확한 2340 × 1080 에뮬레이터에서 메뉴·초기 안내·오답·정답·다음 글자 전환의 재생과 실패 대체 동작을 확인한다. 실제 아이 관찰을 하지 않았다면 했다고 표현하지 않는다.
- 완료 경계: 음성 생성만 하거나 파일만 저장한 상태는 완료가 아니다. 앱 번들 포함, production 연결, 자동 검증, 에뮬레이터 재생 확인, 한글 이력·상태 갱신까지 모두 통과해야 완료로 판정한다.

## 2026-08-27 — 감독자 시각 품질 보강 지시

- 감독자는 UI·UX 품질을 높이는 데 필요한 전용 이미지가 발견되면 자동 디자인 역할이 직접 생성해 production 화면에 적용하도록 지시한다.
- 이미지는 외부 API가 아니라 `imagegen` 스킬의 코덱스 내부 이미지 생성기로 만든다.
- 생성한 사실만으로 통과시키지 않는다. 자동 디자인 역할은 그림체·형태·색·다섯 살 시인성을, 연출 역할은 움직임·비가림을, QA 역할은 투명도·셀 경계·실제 2340 × 1080 플레이 화면과 callback을 각각 판정한다.
- 하나라도 통과하지 못하면 같은 결함에 필요한 최소 재생성 또는 표시 조정을 다음 반복에서 수행한다.
- 교육 과제와 다음 행동을 더 분명하게 하지 않는 장식은 추가하지 않는다. 화면의 화려함보다 아이의 집중과 전체 UI·UX 일관성을 우선한다.
- 실제 사람 디자이너나 감독관이 참여하지 않았다면 사람의 생성·검토·승인으로 표현하지 않는다.

## 2026-08-27 — 적용 음성 제거와 무선 재설치

- 상태: 활성. 루프 176에서 적용한 안내 음성이 마음에 들지 않으므로 제품에서 제거한다.
- 제거 범위: `hero_*.wav` 로컬 음성 파일, 음성 재생기·매핑, 메뉴·쓰기 시작·재시도·성공·다음·홈 전환의 음성 호출과 음성 전용 검사를 모두 제거한다.
- 보존 범위: 무음 동적 획 시범, 현재 획 원형 점선, 시작·끝 표식, 성공·실패·버튼 아틀라스, 글자 geometry와 입력 판정을 유지한다.
- 전달: 전체 자동 검증 뒤 최신 debug APK를 실제 `SM-S931N`에 무선 덮어쓰기 설치하고 실행·포커스·2340 × 1080 화면·무음 흐름을 확인한다.

## 2026-08-27 — 쓰기 화면 홈·다시쓰기·이전·다음 상시 활성

- 상태: 활성. 쓰기 화면에 홈, 다시쓰기, 이전, 다음 네 버튼을 둔다.
- 사용 가능 상태: 네 버튼은 학습 성공 여부와 관계없이 항상 누를 수 있어야 하며 회색 잠금·자물쇠·`사용 불가` 상태를 표시하지 않는다.
- 순환: 이전과 다음은 현재 메뉴의 교육 순서 안에서 각각 직전·다음 글자로 이동하고 첫 글자에서 이전은 마지막으로, 마지막 글자에서 다음은 첫 글자로 순환한다.
- 초기화: 이전·다음으로 이동하면 현재 입력·성공·실패 연출을 지우고 새 글자의 첫 획 안내부터 시작한다. 다시쓰기는 현재 글자만 초기화한다.
- 배치·보존: 왼쪽은 홈·다시쓰기, 오른쪽은 이전·다음으로 배치하고 기존 64 dp 터치 크기·12 dp 간격·아틀라스 그림체·무음 동적 시범·글자 geometry·입력 판정을 유지한다.

## 2026-08-28 — 쓰기 화면 전체 크기 성공·재도전 이미지

- 성공 판정은 글자 우측의 작은 성공·실패 표시가 아니라 쓰기 화면을 가득 채우는 큰 원형 그림으로 바꾼다.
- 성공 원형은 코덱스 내부 이미지 생성기로 예쁘고 아이 친화적인 투명 이미지로 생성해 production에 적용한다.
- 실패 판정에는 상실감을 줄 수 있는 `X`를 쓰지 않는다. 미소·부드러운 색·되돌림 동작으로 다시 해보자는 뜻을 전하는 귀여운 투명 이미지를 별도로 생성한다.
- 두 판정 이미지를 크게 보여도 홈·다시쓰기·이전·다음은 항상 보이고 누를 수 있게 유지한다.

## 2026-08-28 — 실패 이미지 제거·부드러운 자동 재시도·아이 판정 완화

- 실패했을 때 실패 이미지를 띄우지 않는다. 루프 178에서 만든 재도전 이미지는 production과 프로젝트 리소스에서 제거한다.
- 대신 화면 전체가 짧게 좌우로 흔들리거나 번쩍이는 정도의 부드러운 효과만 주고, 효과 뒤 입력을 자동으로 비워 아이가 바로 다시 시작할 수 있게 한다.
- 다섯 살 아이의 손 움직임을 고려해 모든 production 글자의 시작점·경로·끝점과 전체 이탈 허용치를 전반적으로 완화한다.
- 판정을 완화해도 역방향·잘못된 획순·가이드에서 크게 벗어난 입력까지 성공으로 처리하지 않는다.

## 2026-08-28 — `가` 초성 `ㄱ` 대각선 내림

- `가` 글자의 초성 `ㄱ`은 완전한 직각 모양으로 내리지 않고, 꺾인 뒤 왼쪽 아래 대각선으로 내려가게 한다.
- 이번 지시는 `가`에 한정한다. 독립 `ㄱ`과 다른 글자 자형은 함께 바꾸지 않는다.
- 화면의 흰 글자 길·현재 획 점선·동적 방향 표식과 실제 쓰기 판정은 같은 대각선 geometry를 사용한다.

## 2026-08-28 — 첨부 이미지형 `가` 초성 곡선 보강

- 첨부한 1280 × 929 JPEG의 `가` 모양을 최종 시각 기준으로 삼는다.
- 초성 `ㄱ`은 위 가로 끝에서 단일 직선 대각선으로 내려가지 않고, 처음에는 아래로 내려오다가 아래로 갈수록 왼쪽으로 완만하게 휘어 끝나는 모양이어야 한다.
- 기존 LimDo의 굵은 흰 글자 길·원형 점선·시작·끝·동적 표식은 유지하고 중심선 geometry만 참고 형태에 맞춘다.

## 2026-08-28 — `카` 하단 기울기와 `하` 자모 분리

- 상태: 활성. 현재 진행 중인 루프의 완료 후 다음 새 CLI 세션에서 다른 목표보다 먼저 다룬다.
- 사용자 지시 1: `카`의 모양도 `가`와 비슷하게 아래쪽이 기울어져 보이게 한다. 표시 흰 글자 길·현재 획 점선·동적 방향 표식·입력 판정은 같은 production geometry를 사용한다.
- 사용자 지시 2: `하`에서 초성 `ㅎ`과 중성 `ㅏ`가 붙어 보이지 않도록 교육용 자형의 분리 간격을 확보한다. 비접점 획의 실제 guide 외곽이 맞닿거나 겹치지 않아야 한다.
- 보존: `가` 전용 초성 `ㄱ` 곡선 지시, 다른 글자의 이미 통과한 획순·방향·정사각형 em·큰 쓰기판·무음 흐름은 바꾸지 않는다.
- 검증: `카`와 `하` 초기·획별·성공의 정확한 2340 × 1080 새 화면에서 중심선과 guide 외곽을 함께 확인하고, 자동 geometry·입력 판정·역방향 거부를 통과한다. 실제 아이 관찰을 수행하지 않았다면 실행 안 함으로 구분한다.

## 2026-08-28 — `카` 자형 재검토와 에뮬레이터 우선 시각 검수

- 상태: 활성. 현재 진행 중인 루프 190을 마친 뒤, 다음 새 CLI 세션에서 다른 목표보다 먼저 `카` 결함을 수정·검증한다.
- 새 실화면 근거: 실제 `SM-S931N`의 `captures/loop189/iteration1/after/ka-initial.png`와 획별 PNG를 다시 확인했다. 첫 획의 하단 곡선이 지나치게 크게 부풀어 `ㄱ`의 자연스러운 꺾임이 아니라 장식적인 갈고리·S자처럼 보인다. 루프 189의 `카` 자형 시각 통과 판정은 이 결함에 한해 유효하지 않다.
- `카` 기준: 초성 `ㅋ`의 주된 가로·세로 구조와 두 보조 가로획이 한눈에 읽혀야 한다. 첫 획의 하단은 `가`의 완만한 왼쪽 내림과 같은 방향성을 갖되, guide 외곽이 되돌아가거나 큰 고리·S자·갈고리 실루엣을 만들면 실패다. production 표시·점선·방향 표식·입력 판정은 하나의 geometry를 사용한다.
- 에뮬레이터 우선: 자형, 쓰기판, 조작, UI·UX, 그래픽 디자인을 바꾸는 모든 후속 루프는 `alarmquest-qa` 에뮬레이터의 물리 1080 × 2340·가로 앱 2340 × 1080에서 먼저 실제 화면을 직접 검토한다. 실제 기기 검증은 별도 보강 근거이며 에뮬레이터 검토를 대체하지 않는다.
- 의무 시각 관문: 초기·현재 획별·성공/재시도 상태의 PNG와 hierarchy·focus를 새로 수집하고, 글자 외곽·획 접점·자모 간격·잘림·겹침·대비·조작 위치·터치 영역·상태 전환을 자동 그래픽 디자인 역할과 자동 QA 역할이 각각 판정한다. 화면에서 어색함·P0·P1·진행 방해 P2가 하나라도 있으면 완료·커밋·push하지 않고 가장 중요한 한 결함만 다음 반복에서 교정한다.
- 검증 경계: 자동 좌표·입력 수락만으로 자형을 통과시키지 않는다. 에뮬레이터 PNG를 직접 확인해 교육용 글자로 자연스럽게 읽히는지와 실제 아이 관찰 미실행을 구분해 기록한다. 여러 ADB 대상이 함께 있으면 각 명령에 대상 serial을 명시해 다른 검증을 방해하지 않는다.

## 2026-08-28 — `ㅋ` 가운데 가로획 연결 정정

- 상태: 활성. 바로 앞 `카` 재검토 지시의 해석을 정정하며, 다음 새 CLI 세션에서 최우선으로 적용한다.
- 사용자 지적: 문제의 핵심은 `카` 초성 `ㅋ`의 가운데 가로획이 기본 `ㄱ`의 세로획에서 벗어나 따로 떠 보이는 것이다. 하단 곡선의 크기만 줄여서는 해결이 아니다.
- 교육용 자형 기준: `ㅋ`의 위 가로와 가운데 가로는 같은 오른쪽 세로획에서 각각 정확히 시작하거나 끝나야 한다. 가운데 가로의 중심선 끝점은 기본 `ㄱ` 세로획 중심선 위에 놓이며, guide 굵기를 포함해 빈틈·옆으로 빗나감·의도되지 않은 겹침이 없어야 한다. 가운데 가로만 독립된 둥근 막대처럼 보이면 실패다.
- 수정 범위: `카`의 초성 `ㅋ` production template과 그 표시·점선·동적 방향 표식·입력 판정만 함께 교정한다. `가`의 초성 `ㄱ` 곡선, 중성 `ㅏ`, 다른 글자 자형은 회귀 검증만 하고 변경하지 않는다.
- 에뮬레이터 수용 기준: `alarmquest-qa`의 정확한 가로 2340 × 1080에서 `카` 초기·첫 획·가운데 가로획 뒤·성공 PNG를 나란히 직접 확인한다. 두 가로획이 같은 세로축에서 자연스럽게 만나는지, `ㅋ`로 즉시 읽히는지, UI·UX·조작·글자 외곽에 새 결함이 없는지 각각 판정한 뒤에만 완료·커밋·push한다.

## 2026-08-28 — 입력 중 안내 화살표의 손가락 가림 방지

- 상태: 활성. `카` 자형 결함과 함께 다음 최우선 시각·입력 결함으로 처리하되, 서로 다른 원인이므로 검증과 이력 판정은 분리한다.
- 사용자 지적: 아이가 손가락으로 글씨를 쓰는 동안 입력 중 안내 화살표가 손가락 아래에 가려져 진행 방향을 볼 수 없다.
- 확인한 원인: 현재 `inputDirectionGuide`는 마지막 입력점보다 현재 획 전체 길이의 6~12%만 앞에 화살표를 둔다. 짧은 획이나 끝부분에서는 이 거리가 실제 손가락 가림 범위보다 작아 화살표 중심과 glyph가 손가락에 가려진다.
- 제품 기준: 입력 중 안내 화살표는 손가락이 닿은 마지막 좌표와 겹치지 않고 진행 방향 앞쪽에서 계속 보여야 한다. 긴 획은 경로 앞쪽에 충분한 최소 화면 간격을 유지하고, 남은 길이가 짧으면 획 끝에 눌러 붙이지 말고 방향을 보존한 채 경로 옆 안전 위치로 이동한다. 화살표가 쓰기 길·시작점·끝점·다른 획·조작 버튼과 혼동되면 실패다.
- 공유 동작: 모든 production 글자와 모든 획에서 같은 가림 방지 계산을 사용한다. 표시 화살표와 실제 판정 경로는 서로 다른 좌표일 수 있지만, 화살표가 가리키는 방향과 다음 판정 경로 방향은 일치해야 한다.
- 에뮬레이터 검수: `alarmquest-qa`의 정확한 가로 2340 × 1080에서 긴 직선·곡선·꺾임·짧은 가로/세로 획의 입력 중 PNG를 새로 수집한다. 각 마지막 입력 좌표에 다섯 살 아이 손가락 가림 범위를 나타내는 검수용 반투명 원을 캡처 사본에 겹쳐, 안내 화살표의 핵심 glyph와 진행 방향이 원 밖에서 보이는지 직접 판정한다. 이 검수용 원은 production UI에 넣지 않는다.
- 자동 회귀: 화살표 중심과 입력점 사이 최소 가시 거리, 짧은 잔여 구간의 옆 배치, 화면·쓰기판 내부 유지, 다음 경로 방향 일치를 기계 검사로 고정한다. 초기 자동 시범 표식과 입력 중 화살표를 구분하고, 자동 검사만으로 시각 통과를 주장하지 않는다.
- 완료 경계: 에뮬레이터 입력 중 화면에서 손가락 가림 뒤에도 방향을 읽을 수 있고 자형·UI·UX·디자인 회귀가 없을 때만 완료·커밋·push한다. 실제 아이 관찰을 하지 않았다면 별도로 `실행 안 함`을 기록한다.

## 2026-08-28 — 모든 테스트 확인은 에뮬레이터 기준

- 상태: 활성. 이후 CLI 자동화의 테스트·화면·입력·UI·UX·디자인 완료 판정에 항상 적용한다.
- 사용자 지시: 테스트 확인은 실기기가 아니라 `alarmquest-qa` 에뮬레이터에서 수행한다.
- 기준 환경: 에뮬레이터 물리 해상도 1080 × 2340, `user_rotation=1`, LimDo 앱 화면 정확한 2340 × 1080을 사용한다. 모든 `adb` 명령은 에뮬레이터 serial을 명시해 다른 연결 기기와 섞이지 않게 한다.
- 완료 근거: 새 debug APK를 에뮬레이터에 설치한 뒤 실제 사용자 입력으로 대상 화면·중요 상태를 방문하고 PNG·hierarchy·focus·callback·터치 영역을 확인한다. 자형·획순·방향·입력 판정과 UI·UX·그래픽 디자인은 에뮬레이터 실화면을 직접 검토해야 한다.
- 차단 기준 정정: 실기기 미연결·인증 잠금·무선 ADB 실패는 이후 테스트 완료의 차단 사유가 아니다. 실기기 검증은 사용자가 별도로 요청한 경우의 보강 근거일 뿐이며 에뮬레이터 완료 판정을 대체하거나 지연시키지 않는다.
- 품질 경계: unit test·lint·build 성공이나 좌표 검사만으로 화면을 통과시키지 않는다. 에뮬레이터에서 글자 모양, 획 접점, 자모 간격, 손가락 가림, 잘림·겹침, 대비, 조작 위치, 상태 전환을 확인하고 하나라도 어색하면 완료·커밋·push하지 않는다.

## 2026-08-28 — 실기기 확인 후 발견한 세 결함

- 상태: 활성. 아래 세 항목은 최신 사용자 지시이며 이전 입력 중 화살표 가림 방지 지시보다 우선한다.
- 입력 중 화살표 제거: 아이가 쓰는 동안 화살표가 앞뒤로 움직여 방해되므로 입력이 시작된 뒤에는 따라다니는 방향 화살표를 표시하지 않는다. 입력 전의 획순 시범·시작점·끝점·점선과 아이가 그린 선은 유지한다. 이 항목은 현재 루프 192에서 먼저 처리한다.
- `사` 자형 교정: `사`의 초성 `ㅅ`이 꾸불거리거나 물결처럼 보이지 않게 교육용 `ㅅ`의 두 다리를 매끈하고 단정한 중심선으로 교정한다. 표시·점선·입력 판정은 같은 geometry를 사용하고 에뮬레이터 초기·획별·성공 화면에서 직접 판정한다. 화살표 제거 완료 뒤 다음 독립 루프로 처리한다.
- 성공 후 자동 다음: 현재 성공 화면이 멈춘 채 남는 결함을 수정한다. 정답 후 녹음이 있으면 한 번 재생을 마친 뒤, 없으면 기존 성공 연출의 최소 노출 시간 뒤 현재 메뉴의 다음 lesson으로 자동 이동한다. 홈·다시쓰기·이전·다음 수동 조작은 예약된 이동을 취소하고 우선해야 한다. `사` 자형 교정 뒤 다음 독립 루프로 처리한다.
- 검증 환경: 세 항목 모두 `alarmquest-qa` 물리 1080 × 2340·가로 2340 × 1080에서 실제 입력·PNG·hierarchy·focus·상태 전환을 확인한다. 실기기 무선 설치는 보강 근거일 뿐 완료 판정을 대체하지 않는다.

## 2026-08-28 — 기획팀 통합 2차 제품 기획

- 상태: 기획 완료, 구현 시작 안 함
- 사용자 지시: 기획자팀과 함께 LimDo를 제대로 된 앱으로 만들기 위한 기획을 수립한다.
- 통합 역할: 교육과정, 다섯 살 아이 UX·접근성, 제품 로드맵의 세 자동 기획 역할이 현재 구현·문서·QA 근거를 독립적으로 검토했다. 실제 사람 기획팀 승인으로 표현하지 않는다.
- 기획 결론: 글자 수 확대보다 `하루 세 글자·약 5분`, 로컬 진도·이어하기, 단계적 도움, 보호자 로컬 요약, 실제 아이 관찰을 2차 핵심으로 둔다.
- 제품 기본값: 무음, 모든 글자와 네 조작 잠금 없음, 점수·실패 횟수·스트릭 없음, 서버·로그인·광고·분석 없음, 손글씨 원시 좌표 저장 없음.
- 기획 문서: `docs/2차-목표-제품-기획서.md`
- 구현 전 결정: 정식 `applicationId`와 `android:allowBackup` 정책. 기본 백업안은 `false`다.
- 권장 첫 제품 루프: `로컬 완료 기록과 마지막 학습 이어가기`. 사용자가 구현 시작을 지시하기 전까지 큐는 `없음`과 `IDLE`을 유지한다.

## 2026-08-28 — 정식 package·무백업·보호자 녹음·자유 세션

- 상태: 루프 181에서 package·백업 정책 실제 적용과 2차 기획 교정
- 앱 식별자: 개발용 `com.example.limdo`를 프로젝트에 맞는 정식 package로 변경한다. 구현 선택은 `com.nullplaying.limdo`다.
- 백업: 진도와 보호자 녹음을 포함한 앱 데이터의 cloud backup·device transfer를 사용하지 않는다.
- 기존 음성 기획: 합성·내장 음성 안내와 조건부 음성 생성 기획을 제거한다.
- 보호자 녹음: 보호자 전용 메뉴에서 각 자음·모음·글자 lesson마다 `쓰기 전`과 `정답 후` 음성을 직접 녹음하고 앱 내부에만 저장한다. 미리 듣기·다시 녹음·삭제를 포함한다.
- 재생: 아이가 새 글자를 쓰기 전에 시작 음성을 한 번, 정답 뒤 결과 음성을 한 번 재생한다. 녹음이 없거나 재생에 실패하면 무음 시각 흐름을 유지한다.
- 자동 진행: 정답 후 결과 재생이 끝나면 보호자가 구성한 다음 lesson으로 자동 진행한다. 자유 쓰기는 현재 메뉴의 다음 순서를 따른다. 네 조작은 재생·대기 중에도 계속 사용할 수 있고 수동 조작은 예약된 자동 진행을 취소한다.
- 학습량: 하루 세 글자·5분 또는 2·3·5개 같은 고정 제약을 두지 않는다. 보호자가 원하는 수의 lesson을 원하는 순서로 구성하며, 구성하지 않으면 모든 글자를 자유롭게 선택한다.
- 구현 경계: 이번 루프는 package·백업을 실제 변경하고 기획을 확정한다. 마이크 권한·녹음·재생·보호자 UI·자동 다음 구현은 후속 제품 루프로 분리한다.

## 2026-08-28 — 독립 package·자동 그래픽 디자인·최종 전체 플레이 루프

- package 교정: `nullplaying`은 다른 프로젝트 이름이므로 LimDo의 package·namespace·applicationId에서 제거한다. 새 독립 식별자는 `com.limdo.hangul`로 한다.
- 자동 그래픽 디자인 역할: UI를 만들거나 바꾸는 모든 루프에서 독립 자동 그래픽 디자인 역할이 현재 production 화면과 기존 디자인 언어를 먼저 검토한다. 실제 사람 그래픽 디자이너가 참여하지 않았다면 사람의 검토·승인으로 표현하지 않는다.
- 자산 필요 판정: 각 시각 루프는 전용 그림·아이콘·배경·파티클이 `필요`한지 `불필요`한지 구체적 근거와 함께 먼저 기록한다.
- 자체 생성·적용: 필요한 자산은 `imagegen` 스킬의 Codex 내장 이미지 생성기로 자체 생성해 production 리소스에 넣고 실제 소비 코드에 연결한다. 생성 파일만 있거나 임시 화면에만 놓인 상태는 완료가 아니다.
- 반복 검토: 정확한 2340 × 1080의 같은 상태에서 변경 전·후 앱 화면과 hierarchy를 비교한다. 자동 그래픽 디자인·자동 QA·아이 대리 QA 중 하나라도 실패하거나 새 P0·P1 또는 진행 방해 P2가 있으면 가장 중요한 결함 하나만 최소 재생성·표시 수정하고 새 화면으로 다시 검토한다.
- 종료 경계: `만족`은 체크리스트 전부 통과, 새 P0·P1 0개, 진행 방해 P2 0개로 측정한다. 단순 회차 증가나 장식 추가를 위해 반복하지 않는다.
- 최종 실제 플레이: 그래픽과 2차 시스템 구현이 모두 끝나면 별도 최종 전체 플레이 루프를 실행한다. 새 최종 APK에서 홈, 세 선택 화면, 노출된 모든 lesson의 쓰기·성공, 공통 실패·재시도·네 조작, 보호자 녹음·학습 목록의 정상·권한 거부·빈 상태·완료 상태를 실제 탭·그리기·뒤로가기 등 사용자 입력으로 방문한다.
- 전 화면 캡처: 최종 전체 플레이의 모든 고유 화면과 중요 상태는 새 PNG·hierarchy·focus·APK SHA와 1:1로 기록하고, 누락 0건과 종합 디자인·기능 판정 통과 전에는 큐를 `없음` 또는 `IDLE`로 바꾸지 않는다.

## 2026-08-28 — 정답 성공 이미지를 더 귀엽고 직관적으로 교체

- 상태: 활성. 현재 `사` 자형 교정과 성공 후 자동 다음 이동 뒤의 독립 시각 루프로 처리한다.
- 사용자 지시: 정답을 맞혔을 때의 이미지를 귀엽고, 글을 읽지 못하는 아이도 누가 봐도 정답이라고 즉시 이해할 수 있는 이미지로 변경한다.
- 기존 화면 판단: 현재 `limdo_success_fullscreen_feedback.png`는 별·리본 원형이라 축하 분위기는 있지만 정답을 직접 뜻하는 표식이 없다.
- 생성 후보: Codex 내장 `imagegen`으로 실제 투명 RGBA 후보 `.loop/runtime/generated/limdo_success_correct_feedback_v2.png`를 생성했다. 원본은 1254 × 1254이며 큰 투명 중앙, 상단의 큼직한 초록 원형·흰 체크 하나, 웃는 초록 도마뱀의 엄지 표시, 가장자리 별·색종이로 구성한다.
- production 적용: 후보를 1024 × 1024 RGBA로 고품질 규격화해 기존 `app/src/main/res/drawable-nodpi/limdo_success_fullscreen_feedback.png`를 명시적으로 교체하고 `SuccessFeedbackOverlay`의 실제 소비·APK 포함·접근성 설명을 함께 확인한다. 생성 후보를 임시 경로에만 둔 상태는 완료가 아니다.
- 의미 기준: 초록 체크가 가장 큰 의미 표식이어야 하며 체크는 정확히 하나만 둔다. 글자·문구·숫자·X·물음표·왕관·트로피·버튼처럼 혼동 가능한 요소는 넣지 않는다.
- 비가림 기준: 이미지 중앙의 투명 구멍으로 완성한 한글 글자가 계속 읽혀야 하고 홈·다시쓰기·이전·다음 네 조작을 가리거나 pointer 입력을 소비하면 실패다.
- 에뮬레이터 검수: `alarmquest-qa` 물리 1080 × 2340·가로 2340 × 1080에서 변경 전·후 동일 글자 성공 화면 PNG·hierarchy·focus를 수집한다. 체크가 첫눈에 정답으로 읽히는지, 완성 글자·조작 비가림, 투명 halo·잘림·검은 배경·색 번짐·대비, 성공 후 자동 다음 전환까지 직접 판정한다.

## 2026-08-28 — 첨부 이미지 기준 `사` 초성 곡선형 재교정

- 상태: 최우선 활성. 루프 193의 직선 3점형 완료 판정을 즉시 기각하고 같은 루프에서 다시 교정한다.
- 사용자 지시: `사`의 `ㅅ`은 각지면 안 된다. `/Users/lim/Desktop/스크린샷 2026-08-28 오후 8.28.17.png`의 검은 `사` 모양을 시각 기준으로 삼아 똑같은 인상으로 만든다.
- 자형 기준: 초성 `ㅅ`은 위 중앙 접합부에서 양쪽 아래로 벌어질 때 직선 세로 뒤 갑자기 꺾이지 않는다. 양쪽 다리는 첨부 글꼴처럼 접합부 주변에서 연속 곡률로 부드럽게 휘어야 하며, 특히 왼쪽 다리는 위에서 내려오며 자연스럽게 왼쪽 아래로 돌아나가고 오른쪽 다리는 같은 접합부에서 매끈하게 오른쪽 아래로 뻗는다.
- 실패 기준: 3점 polyline의 뚜렷한 각, 세로 막대 뒤 급격한 대각선, V자 직선, S자·물결·갈고리, 접합 틈, 비정상 겹침 중 하나라도 보이면 실패다. 중간점을 무조건 0개로 줄이는 것이 목표가 아니며 첨부 실루엣을 만드는 데 필요한 최소 곡선 샘플은 허용한다.
- 공유 경로: 흰 글자 길·현재 획 점선·입력 전 시범·시작·끝·아이 입력 판정은 같은 곡선 production geometry를 사용한다. 입력 중 추종 화살표는 0개를 유지한다.
- 검증: `alarmquest-qa`의 정확한 2340 × 1080에서 첨부 이미지와 변경 후 `사` 초기·초성 각 획·성공 화면을 원본 크기로 직접 비교한다. 각진 인상이 남으면 완료·커밋·push·루프 194 전환을 금지한다.

### 루프 193 반복 2 에뮬레이터 근거 주의

- `captures/loop193/iteration2/after/sa-initial.png`와 `sa-initial-v2.png`는 접근성 문구가 `바를 6획으로 그려요`였던 잘못된 화면이므로 `사` 초기 근거로 사용하지 않는다.
- 접근성 문구가 `사를 4획으로 그려요`인 상태에서 새 초기 PNG·hierarchy를 다시 수집한다. 파일명만 보고 현재 글자를 판정하지 않는다.

### 루프 193 반복 3 시각 실패 후 교정 기준

- `captures/loop193/iteration3/after/sa-initial.png`의 최종 APK는 정확한 `사` 화면이지만, 왼쪽 획이 긴 수직 막대 뒤 대각선으로 꺾여 첨부 글꼴의 연속 곡률과 다르므로 실패다.
- 단조 진행이나 중간점 개수만 맞추지 말고, `saJunction` 전후의 진행 방향이 급변하지 않는 곡선이어야 한다. 기존 `cubicCurve` 같은 실제 곡선 표본 또는 동등한 연속 접선 구조를 사용하고, 인접 구간 최대 회전각도 자동 검사로 급격한 꺾임을 고정한다.
- 새 APK의 실제 `사` 초기 화면에서 수직 구간과 대각선 구간 사이의 모서리가 눈에 띄지 않을 때만 통과한다.

## 2026-08-28 — 보호자 녹음 화면 잘림 수정·lesson당 녹음 하나로 통합

- 상태: 루프 193 완료 직후 최우선. 아직 구현하지 않은 루프 194의 기존 자동 다음 이동 목표보다 먼저 반영한다.
- 실화면 결함: 보호자 화면의 `자음·모음·글자` 각 목록에서 lesson 아이템이 잘리거나 온전히 표시되지 않는다. `alarmquest-qa`의 정확한 2340 × 1080에서 세 분류를 실제 탭해 첫·중간·마지막 항목, 스크롤 끝, 버튼·문구 bounds를 PNG·hierarchy로 확인하고 잘림 0건으로 고친다.
- 녹음 모델 변경: lesson마다 `쓰기 전`과 `정답 후` 두 칸을 나누지 않고 녹음 하나만 둔다. 보호자가 한 번 녹음한 같은 파일을 아이가 쓰기 전과 정답을 맞힌 뒤 각각 재생한다.
- UI 기준: lesson 카드에는 녹음·재생·다시 녹음·삭제 상태가 한 벌만 보이고, 두 event 칸이나 중복 문구·중복 파일은 남지 않는다. 기존 두 파일이 있으면 데이터 손실을 피하는 명시적 로컬 마이그레이션·선택 규칙을 세우고 자동 검사로 고정한다.
- 자동 다음 연계: 통합 녹음이 있으면 정답 후 같은 녹음을 1회 재생한 뒤 다음 lesson으로 이동하고, 없거나 실패하면 최소 성공 노출 뒤 이동한다. 네 수동 조작은 예약과 재생을 취소한다.
- 루프 순서: 현재 준비된 루프 194를 먼저 `보호자 목록 잘림·단일 녹음 통합`으로 재정의하고, 성공 후 자동 다음 이동은 그 완료 뒤 별도 다음 루프로 이어 간다.

## 2026-08-28 — 생성된 직관적 정답 이펙트 production 반영

- 상태: 보호자 화면·단일 녹음 통합과 자동 다음 이동 뒤의 독립 시각 루프로 반드시 처리한다.
- 사용자 지시: 이미 생성해 둔 귀엽고 직관적인 정답 이미지를 실제 성공 이펙트에 반영한다.
- 후보 원본: `.loop/runtime/generated/limdo_success_correct_feedback_v2.png`. 큰 초록 체크 하나와 웃는 캐릭터로 누가 봐도 정답임을 알 수 있는 방향이다.
- production 적용: 후보를 규격화해 `app/src/main/res/drawable-nodpi/limdo_success_fullscreen_feedback.png`의 실제 소비 경로에 적용하고 APK 포함·투명 중앙·완성 글자·네 조작 비가림·pointer 비소비·접근성 설명을 확인한다.
- 에뮬레이터 검수: `alarmquest-qa` 2340 × 1080에서 변경 전·후 동일 lesson 성공 PNG·hierarchy·focus를 비교하고, 체크가 즉시 정답으로 읽히며 halo·잘림·검은 배경·색 번짐·조작 가림이 각각 0건일 때만 완료한다.

## 2026-08-28 — 항목별 단일 루프 자율 감독·보호자 접근 아이콘 재배치

- 자율 감독: 최신 사용자 지시를 감독자가 우선순위대로 하나씩 단일 루프로 구성한다. 각 루프는 최소 구현·자동 검사·정확한 2340 × 1080 에뮬레이터 실화면 QA·기록·커밋·일반 push를 끝낸 뒤에만 다음 항목으로 전환한다. 대기 항목을 한 루프에 섹어 단일 결함의 판정 근거를 흐리지 않는다.
- 보호자 접근 버튼: 현재 홈 화면 가운데 하단의 보호자 접근 버튼을 왼쪽 상단으로 옮기고, 아이의 세 메뉴와 겹치지 않도록 시각 크기를 작은 아이콘으로 줄인다.
- 터치·접근성 기준: 시각 아이콘은 작게 하되 터치 영역은 Android 최소 목표를 유지하고, system bar·화면 외곽·제목·자음·모음·글자 메뉴와의 겹침·잘림이 0건이어야 한다. 짧은 한글 accessibility description은 유지한다.
- 루프 배치: 사용자가 `하나하나`를 요청했으므로 194는 lesson당 단일 녹음 model·호환 규칙에만 집중한다. 그 다음 루프는 보호자 목록 잘림과 홈의 보호자 접근 아이콘 재배치를 같은 화면 layout 결함으로 처리한다. 그 뒤 성공 후 자동 다음과 생성된 정답 이펙트 반영을 각각 독립 루프로 이어 간다.
- 단일 녹음 문구: 통합 후 시각 UI와 accessibility semantics에 `쓰기 전 녹음 열기`나 `정답 후`가 단독 event처럼 남지 않게 한다. lesson 목록의 접근성 설명은 `lesson <글자>, 녹음 열기`로 통일하고, 같은 녹음을 쓰기 전·정답 후 재생 lifecycle에서만 구분한다.

## 2026-08-28 — 보호자 lesson 목록 세로 스크롤안 기각·분류 전환형 전체 그리드

- 상태: 루프 194 반복 2의 세로 스크롤 카드 UI를 사용자 검토에서 기각했다. 해당 형태로 완료·커밋·push하지 않는다.
- 사용자 지시: 보호자 화면의 작업자 목록을 스크롤해서 찾는 방식이 마음에 들지 않으므로 다른 UI로 만든다.
- 새 UI 방향: `자음·모음·글자`를 상단의 큰 분류 전환 버튼으로 두고, 선택한 분류 하나의 lesson을 아래 고정 영역에 비스크롤 그리드로 전부 표시한다. 자음·글자 14개는 7열×2행, 모음 10개는 5열×2행을 기본안으로 삼아 정확한 2340×1080에서 전 항목이 동시에 보여야 한다.
- 항목 표현: 각 셀은 글자를 중심으로 크고 또렷하게 표시하고 녹음 상태는 작은 보조 아이콘·색으로 구분한다. 셀을 누르면 lesson당 단일 녹음 관리 화면으로 들어가며 접근성 설명은 `lesson <글자>, 녹음 열기`를 유지한다.
- 실패 기준: lesson 목록 내부 스크롤, 첫·마지막 항목 미동시 표시, 글자 잘림·찌그러짐, 192px 미만 터치 영역, 분류 버튼·닫기·제목과의 겹침 중 하나라도 있으면 실패다.
- 검증: `alarmquest-qa` 2340×1080에서 자음·모음·글자를 각각 선택한 새 전체 화면 PNG·hierarchy·focus를 수집하고 38개 셀의 bounds·문구·터치 크기를 자동 측정한다.

## 2026-08-28 — 단일 녹음 재생 실패 보존·수동 조작 취소 관문

- 상태: 루프 194 반복 4의 첫 구현은 감독 코드 검토에서 기각한다. 그대로 완료·커밋·push하지 않는다.
- 녹음 보존: `MediaPlayer.prepare`·재생 중 오류·완료 callback 실패가 발생해도 보호자가 만든 단일 최종 녹음 파일을 자동 삭제하거나 덮어쓰지 않는다. 재생만 안전하게 종료하고 시각 학습 흐름을 계속한다. 명시적인 보호자 `삭제` 동작만 최종 녹음을 삭제할 수 있다.
- 네 수동 조작: 홈·다시쓰기·이전·다음을 누르면 성공 재생과 시작 재생을 즉시 중지한 뒤 사용자가 고른 동작을 수행한다. 특히 같은 lesson에 머무는 다시쓰기도 반드시 현재 player를 정리한다.
- 자동 검사: production에서 재생 실패 경로에 `finalFile.delete()`가 0개인지, 명시적 `delete()`만 최종 파일을 지우는지, 네 조작 callback 모두 현재 재생 취소를 호출하는지 고정한다.
- 에뮬레이터: 유효 녹음이 있는 대표 lesson에서 시작 재생·성공 재생과 네 조작 중 최소 다시쓰기·다음의 취소 동작을 `alarmquest-qa` 2340×1080에서 확인한다. 실패·무녹음에서도 입력과 화면 전환이 멈추지 않아야 한다.

## 2026-08-28 — 성공 후 자동 다음의 실제 최소 노출 지연 관문

- 상태: 루프 196 반복 1의 첫 후보는 무녹음 성공 직후 새 성공 화면 PNG를 수집하기 전에 이미 다음 lesson으로 이동했다. 그대로 완료·커밋·push하지 않는다.
- 지연 계약: `SuccessCelebrationSpec.DURATION_MS`의 `animateTo` 완료만 최소 노출 시간으로 간주하지 않는다. Android animator duration scale이 0이거나 움직임 줄이기가 적용돼도 성공 화면이 실제 wall-clock 기준으로 잠깐 보이도록 별도의 명시적 지연을 둔다.
- 동작 순서: 성공 직후 명시적 최소 노출 지연을 먼저 보장하고, 그 뒤 단일 녹음이 있으면 1회 재생 완료까지 기다린 후 다음 lesson으로 이동한다. 무녹음·prepare/재생 실패도 같은 최소 노출 지연 이전에는 이동하지 않는다.
- 수동 취소: 최소 노출 지연 중이거나 녹음 재생 중일 때 홈·다시쓰기·이전·다음을 누르면 예약과 재생을 취소하고 사용자 동작만 한 번 수행한다.
- 검증: `alarmquest-qa`의 animator duration scale을 0으로 둔 상태에서도 무녹음 성공 PNG가 실제로 먼저 캡처되고, 고정한 최소 시간 전에는 현재 lesson 성공 상태, 이후에는 다음 lesson임을 시간 로그·PNG·hierarchy로 입증한다.

## 2026-08-28 — 자유 학습 편집 목록 스크롤안 기각·버튼식 페이지 UI

- 상태: 루프 199의 보호자 자유 학습 목록 편집에 즉시 적용한다. 작업자가 기록한 세로 스크롤 행 가설은 사용자 검토에서 기각됐으며 해당 형태로 구현·완료·커밋하지 않는다.
- 사용자 지시: 자유 학습 작업 목록을 스크롤해서 찾고 편집하는 방식이 마음에 들지 않으므로 다른 UI로 만든다.
- 새 UI 방향: 고정 개수의 큰 항목 카드를 한 페이지에 배치하고, swipe가 아닌 명시적인 `이전 페이지`·`다음 페이지` 버튼으로만 페이지를 바꾼다. `현재/전체 페이지` 표시를 항상 보여 보호자가 목록 내 위치를 잃지 않게 한다.
- 편집 방향: 페이지 카드 하나를 선택하고 별도의 큰 `앞으로`·`뒤로`·`삭제` 동작을 사용하거나, 동일한 192 px 이상 동작을 확보하는 동등한 구조를 쓴다. 중복 글자는 저장 index·순번으로 구분하며 첫 항목의 `앞으로`와 마지막 항목의 `뒤로`는 모양과 semantics 모두 비활성으로 보인다.
- 금지: 목록 탐색에 `LazyColumn`·`LazyRow`·`verticalScroll`·`horizontalScroll`·swipe pager 또는 손가락 drag 기반 스크롤을 사용하지 않는다. 항목마다 작은 세 동작을 욱여넣어 터치 목표가 192 px 미만이 되는 안도 금지한다.
- 회귀 고정: 보호자 lesson 선택 목록과 자유 학습 편집 목록에서 스크롤안이 각각 기각됐으므로 `docs/회귀-규칙.md`에 공통 비스크롤 탐색 규칙을 승격하고 production 소스·hierarchy 검사로 고정한다.
- 에뮬레이터 검수: `alarmquest-qa` 2340 × 1080에서 빈 목록·중복 목록·1페이지·중간 페이지·마지막 페이지, 페이지 이동·첫/마지막 비활성·중복 하나 이동·삭제·재실행 복원을 실제 탭과 새 PNG·hierarchy·focus로 판정한다.

## 2026-08-28 — 에뮬레이터 2시간 사용 후 의무 재시작

- 상태: 즉시 적용하는 영구 루프 운영 규칙이다.
- 사용자 지시: 루프 엔지니어링 중 에뮬레이터를 오래 켜 두면 메모리가 누적돼 버벅이고 느려지므로, 테스트 사용 시간이 2시간 이상이면 에뮬레이터를 종료하고 다시 실행한다.
- 측정: 각 에뮬레이터 QA 전에 `adb -s emulator-5554 shell cat /proc/uptime` 첫 값을 읽고 7,200초 이상인지 확인한다.
- 조치: 7,200초 이상이면 새 테스트 입력·캡처를 중지하고 `adb -s emulator-5554 emu kill`→serial 소멸 확인→`alarmquest-qa` cold boot를 수행한다. snapshot memory를 복원하지 않도록 `-no-snapshot-load -no-snapshot-save`를 사용한다.
- 재개 관문: `sys.boot_completed=1`, 물리 1080 × 2340, `user_rotation=1`, 최신 APK 재설치, LimDo 2340 × 1080·focus를 다시 확인한 뒤에만 테스트를 이어 간다.
- 증거: 재시작 전·후 uptime을 현재 반복 기록에 남기고, 한계 도달 뒤 재시작 전에 수집한 화면은 완료 근거로 사용하지 않는다.

## 2026-08-29 — `가나다`는 조립 선택이 아닌 기존 쓰기 흐름으로 복원

- 상태: 반영 완료 (루프 235).
- 사용자 지시: `가나다`는 전처럼 글자 쓰기로 진입해야 하며, `가`를 누른 뒤 조립 대상 선택 화면으로 보내지 않는다.
- 정확한 복원 경계: 홈 `가나다` → 기존 14글자 화면 → `가`를 누르면 `LearningDestination.Writing(GANADA, GA, ...)`로 즉시 진입한다. `GANADA + GA`를 `LearningDestination.GaAssembly`로 보내는 예외 분기를 제거한다.
- 보존 범위: 자음·모음·나머지 글자 쓰기, 보호자 목록 학습, 녹음 재생, 성공 후 자동 다음, 네 조작의 기존 동작은 바꾸지 않는다. 조립 관련 코드를 제거하는 대규모 정리는 이 회귀 수정에 섞지 않는다.
- 회귀 검사: `가나다` 화면에서 `가`를 누른 직후 `가를 3획으로` 쓰기판이 나타나고 `가 조립 선택`이 나타나지 않음을 자동 테스트로 고정한다.
- 에뮬레이터 검수: `alarmquest-qa` 2340 × 1080에서 홈 → `가나다` → `가` → 쓰기판을 실제 탭하고 PNG·hierarchy·LimDo focus를 수집한다. 조립 선택 화면이 삽입되면 실패다.

## 2026-08-29 — 네 자음 기본 음성팩과 사용자 녹음 사용 온·오프

- 상태: 최신 최우선순위 반영 중 (루프 236).
- 기본 음성: `ㄱ·ㄴ·ㄷ·ㄹ`에 각각 `기역·니은·디귿·리을`을 말하는 자연스러운 테스트 음성을 생성해 production APK에 포함한다.
- 로컬 우선: 앱 실행 중 TTS·서버·네트워크를 호출하지 않고 미리 생성한 음성 파일만 사용한다.
- 사용자 녹음 설정: 보호자 음성 화면에서 `사용자 녹음 사용`을 온·오프한다. 켜짐이면 유효한 사용자 녹음을 기본 음성보다 우선하고, 꺼짐이면 녹음 파일을 삭제하지 않은 채 기본 음성만 사용한다.
- fallback: 켜짐이지만 해당 lesson의 사용자 녹음이 없거나 재생할 수 없으면 기본 음성을 사용한다. 기본 음성도 없는 lesson은 기존 무음 시각 흐름을 유지한다.
- 보존: 설정을 껐다 켜도 사용자 녹음 파일은 보존하고, 녹음·미리 듣기·다시 녹음·삭제 UI는 기존처럼 사용할 수 있다.
- 검증: 에뮬레이터와 SM-S931N에서 네 음성의 lesson 매핑·재생 완료, 설정 지속성, 켜짐·꺼짐 실제 재생 출처와 보호자 화면을 확인한다.

## 2026-08-29 — Yuna 음성 기각·무료 자연음성 직접 생성

- 상태: 루프 236에서 즉시 반영한다. 기존 macOS `Yuna` 테스트 음성은 지나치게 인위적이라는 사용자 판정으로 기각하며 완료 근거로 사용하지 않는다.
- 생성 요구: 가입·결제 없이 무료 생성 가능한 경로를 확인하고, `기역·니은·디귿·리을` 네 음성을 직접 생성해 production 자산으로 교체한다.
- 음성 기준: 특정 인물·성우·캐릭터를 복제하거나 모사하지 않고, 한국어 자음 이름을 자연스럽고 또렷하게 짧게 말하는 독립적인 신경망 음색을 사용한다.
- 검증 기준: 네 파일은 서로 달라야 하고 M4A/AAC 형식·무음 아님·길이·음량을 자동 검사한다. 큰 한국어 음성인식 모델에서 네 단어를 이어 붙인 결과가 `기역·니은·디귿·리을`로 확인돼야 하며, 최종 자연스러움은 실기기 사용자 청취 전까지 완료하지 않는다.
- 제품 경계: 생성에 웹 서비스를 사용하더라도 앱에는 서버 호출·runtime TTS·네트워크 권한을 넣지 않고 검증된 고정 음성 파일만 포함한다.

## 2026-08-29 — 기본 음성을 여자아이 목소리로 재적용

- 상태: 루프 236에서 즉시 반영한다. 남성 `InJoon` 후보는 이번 최신 지시로 교체하며 완료 근거로 사용하지 않는다.
- 사용자 지시: `기역·니은·디귿·리을` 기본 음성을 자연스러운 여자아이 목소리로 다시 생성하고 앱과 무선 설치본에 적용한다.
- 음색 경계: 특정 실제 아동·성우·캐릭터를 복제하거나 모사하지 않는 독립적인 한국어 여자아이 느낌의 합성 음색을 사용한다.
- 검증: 네 파일의 발음·서로 다른 SHA-256·duration·무음 아님과 여자아이 범위의 음높이를 자동 검사하고, 최종 AAC 연결본의 `기역·니은·디귿·리을` 인식을 확인한다. 실기기에는 connected test를 실행하지 않고 덮어쓰기 설치·실행·버전·APK 동일성만 확인한다.
- 완료 경계: 사용자가 SM-S931N에서 새 여자아이 후보를 직접 듣고 자연스러움·발음을 승인하기 전에는 완료·커밋·push하지 않는다.

## 2026-08-29 — 여자아이 기본 음성을 보호자 38 lesson으로 확대·쓰기 시작 1회 재생

- 상태: 루프 236에서 즉시 반영한다. 앞선 네 자음 테스트 범위를 대체하는 최신 지시다.
- 적용 범위: 같은 여자아이 느낌의 음색으로 자음 14개, 기본 모음 10개, `가·나·다·라·마·바·사·아·자·차·카·타·파·하` 14글자, 총 38개 고정 음성을 생성해 production APK에 연결한다.
- 재생 시점: 선택된 기본 음성 또는 사용자 녹음은 각 쓰기 lesson 진입 때 한 번만 재생한다. 정답 뒤에는 같은 음성을 다시 재생하지 않고 성공 연출의 짧은 지연 뒤 다음 lesson으로 자동 이동한다.
- 로컬·음색 경계: 앱에는 runtime TTS·네트워크 권한을 추가하지 않고 고정 M4A만 포함한다. 특정 실제 아동·성우·캐릭터 음성을 복제하거나 모사하지 않는다.
- 검증: 38개 파일의 서로 다른 SHA-256·M4A/AAC·duration·무음 아님·lesson 매핑과 에뮬레이터 실제 재생 완료·수동 정지를 확인한다. SM-S931N에는 데이터 보존 덮어쓰기 설치만 하고 connected test는 실행하지 않는다.
- 완료 경계: 사용자가 설치본의 확대된 음성을 직접 듣고 승인하기 전에는 루프 완료·커밋·push하지 않는다.

## 2026-08-30 — 전 씬 프리미엄 UI·UX·자산·파티클·연출 디자인 에픽

- 상태: 최신 최우선 디자인 지시. 루프 236의 미커밋 음성·조립 변경을 덮어쓰지 않고 보존하며, 사용자 청취 승인 또는 명시적 중단 뒤 다음 번호 루프에서 시작한다.
- 사용자 판단: 현재 LimDo는 기능이 동작하더라도 디자인만 보면 상품성이 부족하다. 홈부터 보호자·완료까지 실제 사용자가 방문하는 모든 씬을 더 풍족하고 프리미엄한 앱으로 다시 꾸민다.
- 디자인 범위: 공통 art direction·design token, 홈, 메뉴 전환, 자음·모음·가나다 선택, 쓰기 초기·음성·도움·재시도·성공·자동 다음, 보호자 overview·분류·진도·녹음·권한·오류·자유 목록·완료까지 포함한다.
- UI/UX 우선: 장식보다 먼저 과제·주 행동·오류 복구·destructive action·정보 위계·터치·대비·font scale·TalkBack을 정돈한다. WritingCanvas 1962 × 954 px와 네 조작 168 × 168 px, 교육 geometry, 비스크롤 보호자 계약은 축소·훼손하지 않는다.
- 자동 역할: 자동 아트 디렉션, UI/UX, 그래픽 자산, 모션·VFX, Android 성능, 접근성·아이 대리 QA, 최종 QA가 서로 독립 판정한다. 실제 사람 팀의 검토·승인이라고 표현하지 않는다.
- 자산: 필요한 전용 배경·캐릭터 상태·scene frame·아이콘·particle atlas·완료 illustration은 슬롯을 먼저 측정하고 `imagegen`의 Codex 내장 생성기로 만든 뒤 production에 연결한다. 외부 이미지 API·emoji·시스템 glyph·가짜 코드 illustration·AI 생성 한글을 금지한다.
- 파티클·연출: single Canvas 기반의 제한된 seeded particle, scene 진입·menu tap·음성·재시도·성공·완료 연출을 추가하되 글자·시작점·입력·네 조작을 가리지 않는다. animator scale 0에서는 ambient·이동·overshoot를 제거하고 같은 의미의 정적 상태를 보존한다.
- 화풍 경계: 기존 성공 도마뱀과 action atlas의 부드러운 3D toy/clay를 품질 기준으로 쓰고, 단색·반복 카드·화풍 단절·보호자 화면의 과밀 또는 과도한 빈 공간을 해소한다. 과거 제거한 차량은 production 참조가 없으므로 새 디자인에 재도입하지 않는다.
- 조립 경계: `GaAssembly`는 현재 production 진입점이 없으므로 사용자가 재노출하라고 지시하기 전에는 도달 가능한 씬으로 계산하거나 디자인 비용을 쓰지 않는다.
- 첫 루프: 같은 APK의 전 씬 inventory, 보호자 녹음 삭제 안전 UX, 성능 baseline, 공통 art bible을 만들고 현재 화면을 기준으로 full-screen mock 방향 정확히 세 가지를 생성한다. 사용자가 한 방향을 선택하기 전에는 production 전면 리디자인을 시작하지 않는다.
- 직렬 전환: 홈·공통 shell → 세 선택 화면 → 쓰기 외곽·음성·도움 → 재시도·성공·완료 → 보호자 overview → 녹음·권한·오류 → 자유 목록·편집 → 공통 motion·particle → 자산·성능 정리 → 최종 전체 플레이 순서로 한 화면군씩 진행한다.
- 완료 관문: 모든 고유 씬·중요 상태를 같은 최종 commit·APK에서 새 PNG·hierarchy·focus·callback으로 수집하고, 화면 누락 0건, 새 P0·P1 0건, 진행 방해 P2 0건, 자동 역할 전부 통과 전에는 `프리미엄 디자인 완료` 또는 `IDLE`을 선언하지 않는다.
- 전체 계약: `docs/전-씬-프리미엄-디자인-루프-작업지시.md`를 이번 디자인 에픽의 지속 작업 지시로 사용한다.

## 2026-08-30 — 38 lesson 기본 음성 승인·프리미엄 디자인 시작

- 상태: 즉시 적용.
- 사용자 승인: 사용자는 SM-S931N 설치본의 자음 14개·기본 모음 10개·`가~하` 14글자, 총 38개 여자아이 느낌 기본 음성의 자연스러움과 발음을 승인했다. 루프 236의 남은 사용자 청취 관문은 충족됐다.
- 루프 236 완료: 기존 검증 근거를 새로 확인하고 문제가 없으면 상태·이력·큐를 완료 처리하며, 완료 체크포인트를 `git push origin HEAD`로 일반 push한다. 승인된 음성 자산·매핑·쓰기 진입 1회 재생·사용자 녹음 온·오프를 임의로 다시 만들지 않는다.
- 다음 루프 시작: 루프 236 완료 직후 다음 번호 루프에서 `docs/전-씬-프리미엄-디자인-루프-작업지시.md`의 D0를 활성화하고 자동 감독을 계속 실행한다.
- D0 첫 결과: 도달 가능한 전 씬 inventory·보호자 녹음 삭제 안전 UX·성능 baseline·art bible과 현재 기준 화면을 이용한 full-screen mock 방향 정확히 세 가지를 만든다. 세 방향이 준비되면 production 전면 구현은 사용자 선택 전까지 멈춘다.

## 2026-08-30 — 프리미엄 디자인 루프는 에뮬레이터 우선·최종 1회 무선 설치

- 상태: 즉시 적용하는 최신 사용자 지시다.
- 작업 기기 경계: D0부터 최종 전체 플레이 통합 검증이 완료될 때까지 디자인·연출·입력·성능 QA는 `alarmquest-qa` 에뮬레이터에서만 수행한다.
- 실기기 금지: 중간 루프·화면군 완료·mock 선택·회귀 검증을 위해 SM-S931N에 설치하거나 connected test를 실행하지 않는다.
- 무선 설치 시점: 모든 고유 씬·중요 상태·콜백·자산·파티클·성능·접근성·아이 대리 QA와 최종 전체 플레이가 같은 최종 commit·APK에서 통과한 뒤에만 실행한다.
- 최종 전달: 위 통합 관문을 통과한 최종 APK를 SM-S931N에 데이터 보존 덮어쓰기 무선 설치하고, 설치·실행/focus·버전·기기 APK 동일성을 분리해 확인한다. 중간 결과를 최종본으로 설치하지 않는다.

## 2026-08-30 — 에뮬레이터 강제 안전장치 후 루프 237 재개 승인

- 상태: 즉시 적용.
- 사용자 승인: 실기기 계측 사고 후 자동 감독을 중지한 상태에서, 에뮬레이터 serial 강제 안전장치를 먼저 고정하고 검증한 뒤 루프 237을 재개한다.
- 강제 경계: 작업자 전체 환경에 `ANDROID_SERIAL=emulator-5554`를 강제하고, 실기기가 ADB 목록에 보이면 새 세션을 시작하지 않는다. 실기기 serial·transport id·재연결·절대 경로 adb 우회를 금지한다.
- 실기기 복구 경계: SM-S931N의 LimDo 패키지를 중간에 재설치하지 않는다. 기존 지시대로 모든 프리미엄 디자인·최종 전체 플레이 관문을 통과한 최종 APK만 무선 설치한다.

## 2026-08-30 — 감독자 에뮬레이터 지속 실행 보강 뒤 진행 승인

- 상태: 즉시 적용.
- 사용자 승인: 루프 237 반복 6 종료 후 postcheck가 에뮬레이터 소멸과 셸 변수 경계 오류로 중지된 상태에서, 안전장치를 보강하고 반복 7부터 계속 진행한다.
- 감독자 책임: 에뮬레이터 부재 또는 uptime 7,200초 이상을 새 작업자 세션 전에 감지하고 `alarmquest-qa`를 `-no-snapshot-load -no-snapshot-save`로 cold boot한다. 작업자 세션 종료 뒤에도 process가 유지되어야 한다.
- 안전 경계: ADB에 실기기 후보가 나타나거나 다른 프로젝트 앱이 전면을 사용하는 경우 기기를 연결·설치·입력·계측·종료하지 않고 감독을 중지한다. SM-S931N 무선 설치는 최종 전체 플레이 이후 한 번만 수행한다.

## 2026-08-30 — 프리미엄 디자인 1번 시안 선택

- 상태: 즉시 적용하는 최신 디자인 선택이다.
- 사용자 선택: 사용자는 D0에서 표시된 세 시안 중 첫 번째, A `햇살 정원 글자 공방`을 production 아트 방향으로 선택했다.
- 정확한 시각 목표: `captures/loop237/iteration10/mocks/direction-a-sunny-garden-workshop-2340x1080.png`, SHA-256 `dd4f1cddd0a24f034d852a973c82772529f32e9231b8d58bb5bfd581f1647c60`을 선택 기준으로 사용한다. 두 번째 felt 하늘과 세 번째 paper 공방은 production 방향으로 섞지 않는다.
- 구현 순서: 먼저 루프 237의 preview-only·production 자산 자동화 계약 불일치를 해결해 D0 전체 관문과 체크포인트를 완료한다. 이어 D1 홈·공통 scene shell을 단일 다음 루프로 준비하고 자동 감독을 재개한다.
- 적용 경계: full-screen preview를 통째로 production UI로 붙이지 않는다. 정원 배경·clay 카드 재질·가장자리 공방 소품을 실제 슬롯별 자산으로 측정·생성·분해하고, Compose의 실제 글자·접근성 semantics·touch callback을 보존한다.
- 계속 보존: 중앙 카드의 실제 한글, 세 메뉴의 큰 touch 영역, 왼쪽 위 보호자 진입, WritingCanvas 1962 × 954 px, 네 조작 168 × 168 px, 교육 geometry와 38개 음성을 유지한다. 최종 전체 플레이 전 SM-S931N 설치·입력·계측은 하지 않는다.
