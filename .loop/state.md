# 현재 루프 상태

루프: 130 — 현재 획 끝점만 강조

상태: 준비

반복: 0

마지막 검증: 루프 129 반복 1의 새 APK에서 `./scripts/verify.sh`의 73개 단위 테스트·Android lint·debug build를 통과했다. 정확한 2340 × 1080 입력 전→1획→2획→3획 화면에서 초록 시작점이 production `가`의 현재 획 시작 좌표로 이동했고, 성공 뒤 경찰차 3칸·다음 소방차·새 `INITIAL`·다시 듣기·지우기 준비 상태가 유지됐다.

완료한 조건: 루프 129 성공 조건 1~7 전체. 루프 130은 아직 구현하지 않았다.

현재 실패: 2획 완료 뒤 큰 초록 3획 시작점과 글자 전체의 주황 최종 끝점 외곽이 겹쳐 시작과 끝 신호가 경쟁한다.

현재 가설: `completedStrokes.size`로 production `ga`의 현재 획 끝점을 선택하면 시작점과 겹치지 않으면서 방향 안내와 같은 획의 목표를 가리킬 수 있다.

다음 작업: `WritingCanvas`의 고정 끝점 렌더링을 조사하고 현재 획 끝점 하나만 강조하는 최소 상태를 연결한다.

남은 조건: 루프 130 성공 조건 1~7 전체. `아이 대리 점검: 최종 수행 안 함`, `실제 아이 관찰: 실행 안 함`.
