# 현재 루프 상태

루프: 028 — 끝점 도달 뒤 큰 되돌림의 판정 경계 QA

상태: 준비

반복: 0

마지막 검증: 2026-08-24 루프 027 반복 1에서 정상 중심선 `ㄱ`으로 끝점 `(1461,742)`에 도달한 뒤 같은 세로 경로를 10·25·50·75·100 px 위로 되돌아가 놓았다. 다섯 경우 모두 `★ ✓`·`SUCCESS`로 전환됐다.

완료한 조건: 루프 027 성공 조건 1~8 전체. 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`, Canvas 1962 × 775 px와 정사각형 `ㄱ` geometry를 유지했다. 제품 코드는 바꾸지 않았고 `./scripts/verify.sh`의 전체 단위 테스트 33개·lint·debug build와 `git diff --check`가 통과했다. `아이 대리 점검: 통과`, `실제 아이 관찰: 실행 안 함`.

현재 실패: 루프 027에서 100 px까지의 짧은 되돌림은 모두 일관되게 성공했다. 100 px보다 큰 되돌림에서 성공과 재시도 판정 경계가 일관되는지는 아직 검증하지 않았다.

현재 가설: 정상 획으로 끝점에 도달한 뒤 100 px보다 큰 되돌림 거리를 단계적으로 늘리면 성공에서 재시도로 바뀌는 판정 경계가 한 번 나타나고, 경계 이후에는 일관된 재시도 결과가 유지될 것이다.

다음 작업: 정상 중심선 획으로 끝점에 도달한 뒤 같은 세로 경로를 100 px보다 큰 서로 다른 거리 최소 5개만큼 위로 되돌아가 놓는 사례를 정확한 `alarmquest-qa`에서 재현한다.

남은 조건: 루프 028 성공 조건 1~8 전체.
