# 현재 루프 상태

루프: 059 — `COMPLETE` 복원 후 재입력의 추가 보상 이동

상태: 준비

반복: 0

마지막 검증: 루프 058 반복 4의 정확한 2340 × 1080, `mHaveState=true`, PID `15199→15355` 복원에서 `MOVING` 실제 포인터가 잠기고 같은 경찰차가 126 px에 한 번만 수렴했다. `./scripts/verify.sh`, `git diff --check`, `./scripts/check-automation.sh`가 통과했다.

완료한 조건: 루프 058 성공 조건 1~7 전체. 최종 아이 대리 QA를 통과했고 `실제 아이 관찰: 실행 안 함`이다.

현재 실패: 루프 058의 제외 탐색에서 `COMPLETE` 새 PID 복원 후 지우기 전 재입력이 같은 경찰차를 126 px 추가 이동시켜 독립 재현과 원인 구분이 필요하다.

현재 가설: 아직 세우지 않음. 다음 새 세션이 루프 059 반복 1의 반증 가능한 가설을 기록한다.

다음 작업: 코드를 먼저 바꾸지 않고 1배·지연 이동에서 `COMPLETE` PID 복원 후 재입력을 재현해 새 성공 소비와 저장 offset 중복 합산을 구분한다.

남은 조건: 루프 059 성공 조건 1~6 전체.
