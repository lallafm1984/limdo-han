# 현재 루프 상태

루프: 036 — 밀집 분포의 이벤트 간격 지터 QA

상태: 준비

반복: 0

마지막 검증: 루프 035 반복 1에서 정확한 2340 × 1080, MOVE 18개의 꼭지·끝점 밀집 분포를 빠름·느림 실측 총 입력 시간으로 각각 재현했다. 두 분포와 두 시간 구간 모두 155 px는 `SUCCESS`, 156 px는 `RETRY_DIRECTION`으로 일관되어 제품 코드를 바꾸지 않았다.

완료한 조건: 루프 035 성공 조건 1~8 전체. 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`, Canvas 1962 × 775 px와 정사각형 `ㄱ`을 유지했다. `./scripts/verify.sh`의 35개 테스트·lint·debug build와 `git diff --check`가 통과했다. `아이 대리 점검: 통과`, `실제 아이 관찰: 실행 안 함`.

현재 실패: 비슷한 총 입력 시간 안에서 명령 간격이 불규칙하게 짧고 길게 섞이는 지터 패턴에서도 155 px와 156 px 경계가 유지되는지는 미검증이다.

현재 가설: 같은 시작·끝·되돌림 좌표와 MOVE 18개라면 꼭지·끝점 밀집 분포의 명령 간격이 불규칙해도 155 px는 `SUCCESS`, 156 px는 `RETRY_DIRECTION`을 유지할 것이다.

다음 작업: MOVE 18개의 꼭지 밀집·끝점 밀집 분포에서 총 시간을 비슷하게 맞춘 서로 다른 명령 간격 지터 패턴으로 155 px와 156 px를 각각 재현한다.

남은 조건: 루프 036 성공 조건 1~8 전체.
