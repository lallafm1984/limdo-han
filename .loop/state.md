# 현재 루프 상태

루프: 052 — 입력 중 다음 방향 동적 안내

상태: 준비

반복: 0

마지막 검증: 루프 051 반복 2에서 `./scripts/verify.sh`의 39개 단위 테스트·Android lint·debug build와 `git diff --check`가 통과했다. 물리 1080 × 2340, 앱 2340 × 1080, `user_rotation=1`에서 입력 전 시범, 실제 다시 듣기, 정상 성공·성공 음성 완료, 성공 후 지우기 복원을 `captures/loop051/iteration2/`의 새 연속 화면·hierarchy·callback으로 확인했다.

완료한 조건: 루프 051 성공 조건 1~6 전체. 입력 전 링의 시작→가로→꼭지→세로→끝 반복 이동, 입력 시작 중단, 성공·재시도 비활성, 지우기 복원, 다시 듣기 중 지속과 음성 상태 비충돌을 자동·에뮬레이터·아이 대리 QA로 확인했다. `실제 아이 관찰: 실행 안 함`.

현재 실패: 입력 중에는 입력 전 시범이 즉시 사라지지만, 아이 손가락보다 조금 앞에서 다음 방향을 짧게 움직이는 동적 안내는 아직 없다.

현재 가설: 기존 `ㄱ` 중심선과 아이 획의 투영 진행률에서 조금 앞선 위치를 계산해 입력 중에만 짧은 안내를 표시하면, 입력 전 시범과 충돌하지 않고 가로에서 꼭지·세로 순서로 다음 방향을 움직임으로 보여 줄 수 있다.

다음 작업: 현재 입력 진행률과 판정 geometry를 확인하고 입력 중 다음 방향 안내만 추가하는 최소 상태·애니메이션 변경과 테스트를 적용한다.

남은 조건: 루프 052 성공 조건 1~6 전체.
