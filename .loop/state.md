# 현재 루프 상태

루프: 032 — 판정 경계 입력 event 간격 QA

상태: 준비

반복: 0

마지막 검증: 루프 031 반복 1에서 변경 전 155 px 세 번 중 한 번이 `RETRY_DIRECTION`으로 흔들리는 문제를 입증했다. backtrack `Float` 비교에 0.0001 비율 허용오차와 회귀 테스트를 적용한 뒤 정확한 2340 × 1080에서 155 px 세 번 모두 `SUCCESS`, 156 px 세 번 모두 `RETRY_DIRECTION`으로 안정화됐다.

완료한 조건: 루프 031 성공 조건 1~8 전체. 물리 1080 × 2340, 앱 2340 × 1080, `ROTATION_90`, Canvas 1962 × 775 px와 정사각형 `ㄱ`을 유지했다. `./scripts/verify.sh`의 35개 테스트·lint·debug build와 `git diff --check`가 통과했다. `아이 대리 점검: 통과`, `실제 아이 관찰: 실행 안 함`.

현재 실패: 155 px와 156 px 판정이 의도적으로 통제한 빠름·보통·느림 입력 event 간격에서도 유지되는지는 미검증이다.

현재 가설: event 간격과 무관하게 155 px는 `SUCCESS`, 156 px는 `RETRY_DIRECTION`을 유지할 것이다.

다음 작업: 155 px와 156 px를 빠름·보통·느림 event 간격으로 각각 재현한다.

남은 조건: 루프 032 성공 조건 1~8 전체.
