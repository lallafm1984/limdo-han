# 현재 루프 상태

루프: 149 — 받침 없는 글자 `파 → 하` 확장
상태: 준비
반복: 0

마지막 검증: 루프 148에서 `./scripts/verify.sh`의 전체 89개 단위 테스트, Android lint, debug build와 `git diff --check`가 통과했다. SHA-256 `6a35da9e3a6ac1d2880a63644ab63ecd00b4e69b781759d7f75d8dea0b1f7f5f` APK를 물리 1080 × 2340·`user_rotation=1`의 정확한 앱 2340 × 1080 `emulator-5554`에서 확인했다. WritingCanvas는 `[189,63][2151,1017]` = 1962 × 954 px이고 좌·우 64 dp 조작 열, 입력 중 안내, `ㄱ` 성공 보상, `ㄱ → ㄴ` 전환이 일치했다.

완료한 조건: 루프 148 성공 조건 1~8을 모두 통과했다. 하단 `ActionShelf`를 제거했고 왼쪽 듣기·지우기와 오른쪽 다음의 시각·접근성·활성 상태 계약을 유지했다. `실제 아이 관찰: 실행 안 함`.
현재 실패: 없음. 루프 149 구현은 아직 시작하지 않았다.
현재 가설: 기존 교정된 `ㅍ·ㅎ·ㅏ` template을 정사각형 em 안에서 조합해 `파·하`를 `타` 뒤에 추가하면, 표시·판정·획순·음성·보상·순환이 하나의 lesson 계약으로 일치하고 기존 37개 lesson과 루프 148 레이아웃을 유지할 수 있다.
다음 작업: `KoreanCurriculum`의 조합 글자 template과 `ㅍ·ㅎ·ㅏ` geometry를 대조해 `파·하` 두 lesson을 추가하는 최소 제품 변경과 회귀 테스트를 구현한다.
남은 조건: 루프 149 성공 조건 1~8 전체.
실제 아이 관찰: 실행 안 함.
