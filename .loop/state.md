# 현재 루프 상태

루프: 136 — 첫 한글 교육 묶음 확장

상태: 준비

반복: 0

마지막 검증: 루프 135 반복 1에서 `./scripts/check-automation.sh`와 `git diff --check`를 통과했고 실제 관찰 계획·빈 기록 양식·판정 기준을 준비했다.

완료한 조건: 루프 135 성공 조건 1~7 전체. 루프 136은 아직 구현하지 않았다.

현재 실패: production 앱은 `가` geometry·판정·음성에 고정돼 자음·모음과 다른 조합 글자를 순서대로 학습할 수 없다.

현재 가설: 첫 교육 묶음을 lesson 계약으로 일반화하면 기존 큰 쓰기 경험을 유지하면서 `ㅏ → ㄱ → 가 → ㅓ → ㄴ → 너`를 순서대로 독립 학습할 수 있다.

다음 작업: 글자별 lesson·template·획 방향·음성 계약을 추가하고 Canvas·판정·보상·다음 전환을 현재 lesson에 연결한다.

남은 조건: 루프 136 성공 조건 1~8 전체. `실제 아이 관찰: 실행 안 함`.
