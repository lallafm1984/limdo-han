# 현재 루프 상태

루프: 124 — `가` 3획 음성·시각 시범 동기화

상태: 준비

반복: 0

마지막 검증: 루프 123 반복 1에서 production `가` 3획 초기·다시 듣기·획별 재시도 음성을 연결했다. `./scripts/verify.sh`가 통과했고 정확한 2340 × 1080에서 `INITIAL`, 2·3획 방향 재시도, `SUCCESS`, 3칸 보상·지우기를 확인했다.

완료한 조건: 루프 123 성공 조건 1~8 전체.

현재 실패: 새 APK `INITIAL`은 약 13.37초인데 시각 시범은 3초 주기라 음성 한 번에 약 4.5회 반복되어 말하는 획과 보이는 획이 일치하지 않는다.

현재 가설: 음성을 1·2·3획 단위로 순차 재생하고 각 callback 동안 해당 production `ga` 획의 시범만 재생하면 음성·화살표 순서가 일치할 것이다.

다음 작업: `LocalKoreanSpeech`의 utterance callback과 `WritingCanvas` 시범 progress 전달 경계를 조사해 최소 상태 모델·테스트를 추가한다.

남은 조건: 루프 124 성공 조건 1~7 전체.
