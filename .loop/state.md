# 현재 루프 상태

루프: 123 — `가` 3획 음성 안내 일치

상태: 준비

반복: 0

마지막 검증: 루프 122 반복 1에서 `GaLesson(glyph = "가", strokeCount = 3)`과 칸별 이동을 연결했다. 정확한 2340 × 1080에서 126 px씩 세 구간, 총 378 px 이동과 다시 듣기 0칸·지우기 뒤 시작 위치를 확인했고 `./scripts/verify.sh`가 통과했다.

완료한 조건: 루프 122 성공 조건 1~8 전체.

현재 실패: production `가` 3획과 달리 `SpokenCue.INITIAL`은 첫 획의 `오른쪽, 아래`만 말하고 2획 아래쪽·3획 오른쪽을 누락한다.

현재 가설: production `ga` 획 index와 음성 cue를 연결하면 초기·다시 듣기·재시도에서 같은 3획 순서를 짧게 안내할 것이다.

다음 작업: `SpokenCueModel`과 재시도 callback의 획 정보 전달 경계를 조사하고 음성 모델 테스트부터 추가한다.

남은 조건: 루프 123 성공 조건 1~8 전체.
